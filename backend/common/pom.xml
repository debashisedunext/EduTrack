<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>com.edunext.edutrack</groupId>
    <artifactId>edutrack-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>
  <artifactId>edutrack-common</artifactId>
  <name>EduTrack — common</name>
  <description>Shared DTOs, exceptions, hashing and the canonical JSON serialiser. Owned by Stream A.</description>

  <dependencies>
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-validation</artifactId></dependency>
    <dependency><groupId>com.fasterxml.jackson.core</groupId><artifactId>jackson-databind</artifactId></dependency>
    <dependency><groupId>com.fasterxml.jackson.datatype</groupId><artifactId>jackson-datatype-jsr310</artifactId></dependency>
    <dependency><groupId>org.mapstruct</groupId><artifactId>mapstruct</artifactId></dependency>
    <!-- A-020 · Argon2id. Hashing lives in `common` per TEAM-PLAN.md §6 because
         A-026/A-027/A-028 and Stream B's Resource Master all set passwords: a
         second encoder configured with different parameters would silently
         write hashes the login path cannot verify. -->
    <dependency><groupId>org.springframework.security</groupId><artifactId>spring-security-crypto</artifactId></dependency>
    <!-- Argon2PasswordEncoder delegates to BouncyCastle's Argon2BytesGenerator.
         Compile scope, not runtime: `common` instantiates the encoder itself. -->
    <dependency><groupId>org.bouncycastle</groupId><artifactId>bcprov-jdk18on</artifactId></dependency>
  </dependencies>
</project>
