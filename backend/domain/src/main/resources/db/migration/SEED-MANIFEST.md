# Seed manifest — the fixed load order
**B-008.** Authored by Stream B; **owned by Stream A** alongside the rest of this
directory (TEAM-PLAN.md §6). Every stream adds a row to it.

TEAM-PLAN.md §7.3 sets the rule this file implements:

> **Rule: one seed file per stream** (`seed_masters.sql`, `seed_tickets.sql`, …),
> never one shared file. Loaded in a fixed order by a manifest A owns.

Flyway already *executes* in a fixed order — ascending version, which for us is
the `V<YYYYMMDD>_<HHMM>` timestamp. What it does not do is make that order
**reviewable**. Ordering defects do not live inside any one file; they live
between two files, and nobody reads the whole directory end to end to find them.
This is the one page where the whole queue is visible at once.

That is not hypothetical. `V20260808_1400` exists because laying this register
out revealed that a correction migration was silently undone by the seed file
queued immediately behind it — see [Correction history](#correction-history).

---

## 1. The load order

Ascending by version — top to bottom is the order Flyway runs them, on every
environment, always.

<!-- load-order:begin -->

| # | File | Stream | Task | Kind | Loads |
|---|---|---|---|---|---|
| 1 | `V20260805_1024__baseline_identity.sql` | A | A-003 | schema | `users`, `roles`, `permissions`, `role_permissions`, `user_roles`, `projects`, `project_members` |
| 2 | `V20260805_1041__baseline_tickets.sql` | A | A-004 | schema | `tickets`, `ticket_cycles`, `ticket_history`, `ticket_effort_logs`, `ticket_watchers`, `ticket_links` |
| 3 | `V20260805_1042__baseline_workflow.sql` | A | A-005 | schema | `workflow_templates`, `workflow_stages`, `ticket_stage_transitions` |
| 4 | `V20260805_1106__baseline_masters_ops.sql` | A | A-007 | schema | `task_types`, `priorities`, `statuses`, `workflow_transitions`, `sla_policies`, `holidays`, `resource_leaves`, `notification_templates`, `notifications`, `chat_threads`, `chat_participants`, `chat_messages`, `audit_logs` |
| 5 | `V20260805_1107__immutability_triggers.sql` | A | A-008 | guard | The append-only triggers: no-UPDATE / no-DELETE on history and effort logs, seal-only on stage transitions |
| 6 | `V20260805_1108__generated_columns_and_indexes.sql` | A | A-009 | schema | `pcd_open`, `current_ticket_id`, the `FULLTEXT` index |
| 7 | `V20260805_1530__baseline_clients_content.sql` | A | A-006 | schema | `clients`, `client_contacts`, `client_projects`, `ticket_comments`, `ticket_attachments`, `email_log`, `import_batches` |
| 8 | `V20260806_0900__seed_roles_permissions.sql` | B | B-001 | **seed** | 6 roles · 18 permissions · the §2 grant matrix |
| 9 | `V20260807_1000__seed_task_types_priorities.sql` | B | B-002 | **seed** | 11 task types · 4 priorities |
| 10 | `V20260807_1030__fix_support_role_code.sql` | B | B-001 | correction | `roles`: `SUPPORT_DESK` → `SUPPORT` |
| 11 | `V20260807_1100__seed_statuses_transitions.sql` | B | B-003 | **seed** | 8 statuses · 72 transition rows |
| 12 | `V20260807_1500__email_suppressions.sql` | D | D-034 | schema | `email_suppressions` |
| 13 | `V20260807_1700__seed_workflow_templates_stages.sql` | B | B-004 | **seed** | 3 templates · 18 stages |
| 14 | `V20260808_1000__chat_project_channels_and_message_lifecycle.sql` | D | D-050 | schema | Project channels; the message edit window and tombstones |
| 15 | `V20260808_1200__chat_message_search.sql` | D | D-053 | schema | `FULLTEXT` on `chat_messages.body` |
| 16 | `V20260808_1400__fix_workflow_transitions_support_role_code.sql` | B | B-003 | correction | `workflow_transitions`: 13 rows, `SUPPORT_DESK` → `SUPPORT` |
| 17 | `V20260808_1630__working_calendar.sql` | B | B-023 | schema + **seed** | `working_calendar` — the weekly-off pattern and working-day bounds, plus the one row it is constrained to hold |
| 18 | `V20260810_0930__holidays_unique_org_wide.sql` | B | B-023 | correction | `holidays`: `uq_holidays` rebuilt over a NULL-free `project_scope`, so two org-wide holidays cannot share a date |
| 19 | `V20260810_1015__notification_delivery_log.sql` | D | D-046 | schema | `notifications.delivered_at` + `ix_notifications_undelivered` — what was shown, as distinct from what was read |
| 20 | `V20260810_1120__password_reset_tokens.sql` | A | A-027 | schema | `password_reset_tokens` — hashed at rest, 30-minute TTL, single use via `used_at` |
| 21 | `V20260810_1420__notification_preferences.sql` | D | D-042 | schema | `notification_preferences` — sparse overrides only; the default lives in code |
| 22 | `V20260810_1620__stage_sla_alerts.sql` | D | D-023 | schema | `stage_sla_alerts` — the once-per-segment claim, kept outside the append-only chain |
| 23 | `V20260810_1810__sla_prebreach_alerts.sql` | D | D-021 | schema | `sla_prebreach_alerts` — once per ticket **per cycle**, so a reopen re-arms the warning |
| 24 | `V20260810_1930__stale_ticket_nudges.sql` | D | D-022 | schema | `stale_ticket_nudges` — one row per ticket, updated in place; a nudge repeats where a warning does not |
| 25 | `V20260810_2010__import_batch_status_vocabulary.sql` | B | B-030 | correction | `import_batches.status`: the column's private vocabulary replaced by the contract's `QUEUED\|RUNNING\|COMPLETED\|FAILED`, with a `CHECK` so they cannot diverge again |
| 26 | `V20260810_2040__l2_escalations.sql` | D | D-024 | schema | `l2_escalations` — the second-level claim; L1 needs none, `tickets.is_delayed` already records it |
| 27 | `V20260811_1030__password_policy.sql` | A | A-028 | schema | `password_history` + `users.password_changed_at` — the no-reuse rule and the optional expiry clock |
| 28 | `V20260811_1100__ping_pong_flags.sql` | D | D-025 | schema | `ping_pong_flags` — one row **per cycle**, holding the highest iteration already announced, so a further bounce is new information and a reopen re-arms |
| 29 | `V20260811_1400__unassigned_ticket_alerts.sql` | D | D-026 | schema | `unassigned_ticket_alerts` — one row per ticket, cleared the moment somebody picks it up, so a later reassignment away alerts immediately |
| 30 | `V20260811_1420__two_factor_totp.sql` | A | A-029 | schema | `users.totp_secret`/`totp_enabled`/`totp_confirmed_at` + `totp_recovery_codes` — the enrol-then-confirm states and the way back in |
| 31 | `V20260811_1520__resource_profile_fields.sql` | B | B-011 | schema | `users`: `date_of_joining`, `avatar_url`, `location`, `weekly_off`, `skills` — the five S-08 fields A-003 had no reader for. Plus `ck_project_members_role`, which fixes the vocabulary of `role_in_project` at its first writer |
| 32 | `V20260811_1600__push_subscriptions.sql` | D | D-045 | schema | `push_subscriptions` — one row per browser that granted permission; unique on the **endpoint alone**, so a second user on a shared machine takes the subscription over rather than inheriting somebody else's alerts |
| 33 | `V20260813_1030__ticket_status_requests.sql` | D | D-055 | schema | `ticket_status_requests` — one open ask per manager per ticket (a generated column plus a unique key, A-009's `pcd_open` idiom), and the answered row carries the wait in **working** minutes, stamped once so a holiday added later cannot restate it |
| 34 | `V20260813_1420__project_master_fields.sql` | B | B-016 | schema | `projects`: `description` and `auto_assign_rule`, the two S-10 fields with no column; `ck_projects_status` fixes the vocabulary at `ACTIVE\|ON_HOLD\|CLOSED` at its first writer, before the javadoc's fourth and fifth values could be written by anything |
| 35 | `V20260813_1810__project_member_allocation.sql` | B | B-017 | schema | `project_members.allocation_pct` — the S-10 Team tab field the contract has promised since D-001 with no column behind it. **Nullable, and the contract's `default: 100` dropped**: a backfill would read 300% for every fixture resource on three projects, and B-061 could not tell that from a stated figure |
| 36 | `V20260814_1120__project_settings.sql` | B | B-019 | schema | `project_task_types` + `projects.mandatory_fields` — the two S-10 Settings-tab fields B-016 deliberately left out. **No rows in the join table means every active task type is allowed, not none**: every project predates the table, so the other reading would have stopped ticket creation everywhere the migration ran, and a backfill would have frozen each project's list at the eleven types that existed today. `ck_projects_mandatory_fields` constrains **shape and not vocabulary**, unlike `ck_projects_status`, because the codes track Stream C's create form |
| 37 | `V20260814_1530__one_correction_per_entry.sql` | A | A-043 | schema | `UNIQUE` on `corrects_entry_id` for `ticket_history` and `ticket_effort_logs`, replacing the plain index. MySQL allows any number of NULLs in a unique index, so this constrains exactly the correction rows: a second reversal of one entry collides, while every ordinary append is untouched. Two reversals of a `+8` net to `-8` and the §4A.4 grid reports negative hours for a stage somebody worked |
| 38 | `V20260814_1900__chain_anchors.sql` | A | A-044 | schema | `chain_anchors` — row count and head hash per ticket per chain, so tail truncation becomes a comparison. Append-only makes the count monotonic; `trg_chain_anchor_monotonic` refuses one that decreases and `trg_chain_anchor_no_delete` refuses removal, so laundering a deletion means defeating a second trigger on a second table. Named in `apply-app-grants.sql` to withhold `DELETE`, which the default branch would grant |
| 39 | `V20260814_2130__summary_tables.sql` | A | A-050 | schema | `daily_ticket_stats` and `resource_daily_stats` — the pre-aggregated rows the dashboard reads instead of counting, keyed `(date, project)` and `(date, user)`. Each row carries both **flow** (what happened that day) and **stock** (what was true at end of day), because stock does not aggregate over days and widgets 2, 5, 12 and 16 would otherwise need the live `COUNT(*)` CLAUDE.md forbids. `wip_by_stage` is declared NULL now rather than added with A-058: a point-in-time column cannot be backfilled |
| 40 | `V20260815_1100__notification_template_recipients.sql` | B | B-022 | schema + seed | `notification_templates.recipients`, and the first rows the table has ever held — one per (event, channel) pair blueprint §11 ticks, for every event `NotificationEvent` declares. `recipients` is a `NotificationRecipient` vocabulary rather than a join onto `roles`, because eight of §11's ten "To" values are positions relative to a ticket. **The channel vocabulary is `IN_APP\|EMAIL\|PUSH` and supersedes A-007's `POPUP\|BELL\|EMAIL` column comment** — everything that runs keys on `NotificationChannel`, and the bell is not a channel. `MAIL_DELIVERY_FAILED` has no email row on purpose |
| 41 | `V20260815_1140__attachment_settings.sql` | C | C-027 | schema + **seed** | `attachment_settings` — §4B.4's three attachment caps, administrator-editable at runtime instead of only at deploy. Exactly one row, `id = 1`, enforced by a `CHECK`; seeded with the blueprint's own 10 MB / 50 MB / 20 so the enforced values are unchanged the moment it runs. Creates and seeds in one file for row 17's reason — a singleton config row is the table's initial state, not a separate load step |
| 42 | `V20260816_1030__client_master_s33_fields.sql` | B | B-026 | schema | `clients`: `logo_url`, `billing_reference`, `billing_email`, `tags` — the four blueprint §4B.2 fields S-33's form needs and the table had no column for. Plus `ck_clients_status`, which lands at the column's **first writer of the third value**: §4B.2's Identity group names Active / Inactive / **Prospect** and the column has carried two of them unconstrained. `tags` is JSON constrained for **shape and not vocabulary**, `users.skills` and `projects.mandatory_fields` one table over. **`clients` is A-006's table — flagged for Stream A** |
| 43 | `V20260816_1230__type_counts.sql` | A | A-056 | schema | `daily_ticket_stats.type_counts` — the task-type breakdown §S-05's donut needs and A-050 did not build. **JSON rather than a column per type**, for exactly the argument A-050 made for `wip_by_stage`: `task_types` is a master an Admin extends (B-020), and a column set would need a migration every time somebody adds one. Shaped `{"3": 41}` — task_type_id to **open** count, matching `open_total` and the level columns beside it rather than counting creations, and keyed by id so renaming a type does not rewrite history. Existing rows stay NULL rather than backfilling to `{}`: an empty object would claim no tickets of any type were open that day, which is false for every historical row |
| 44 | `V20260816_1615__resource_in_progress.sql` | A | A-056 | schema | `resource_daily_stats.assigned_in_progress` — the middle segment of §S-05's widget 10, which A-050 left without a column. **Disjoint from `assigned_delayed`, which `assigned_critical` is not**: critical is a lens over the open set and the overlap is the point, but widget 10 *stacks* its three segments, so they must partition `assigned_open` or the bar's length double-counts every ticket that is both delayed and being worked. In progress is `status IN ('IN_PROGRESS','REWORK')` — not `ON_HOLD` or `AWAITING_INFO`, which are open and stopped. Backfills to 0 rather than NULL because this table is deleted and rewritten per day, so no row outlives its default |
| 45 | `V20260816_2020__sla_compliance.sql` | A | A-057 | schema | `daily_ticket_stats.sla_closed` / `sla_met` — §S-05 widget 14's denominator and numerator, neither of which A-050 built. **Flow, not stock, and deliberately not derived from `open_delayed`**: that counts what is late *now*, so a gauge fed from it would improve every time somebody closed an overdue ticket — late delivery registering as compliance rising. Two columns because a ticket with no `planned_close_date` has made no commitment and can neither meet nor breach one, so it is excluded from both halves rather than inflating the denominator. NULL until A-051 recomputes, like `type_counts` and unlike `assigned_in_progress`: this table is upserted, so `0` on a historical row would claim nothing with an SLA closed that day |
| 46 | `V20260817_1130__resource_variant_columns.sql` | A | A-062 | schema | `resource_daily_stats`: the four `assigned_aging_*` buckets and `assigned_due_today` / `assigned_due_next_7` — §S-05's developer dashboard is "widgets 1–6, 9, 12 scoped to `assignee = me`, plus My due today / this week", and A-056 refused widget 12 for a delivery role because this table had no aging columns, naming this task as the one that owed them. **The bucket edges are A-050's 0–2 / 3–7 / 8–30 / 31+, deliberately repeated rather than corrected towards §S-05's 0–2 / 3–5 / 6–10 / >10**: a Developer and their PM read two charts with the same four labels and the same drill-down links, and edges differing by a day between them produce figures that never reconcile with no way to see why. **Due is bounded below by the start of the day, so it cannot overlap `assigned_delayed`** — overdue work appearing under "due today" would overstate the one tile somebody plans their day around; `due_next_7` contains `due_today` because nesting is what the two labels claim. Seven rolling days rather than the calendar week, which would make the tile mean six days on a Monday and one on a Friday. Backfills to 0 rather than NULL for row 44's reason — this table is deleted and rewritten per day, so no row outlives its default |
| 47 | `V20260817_1615__import_mapping_presets.sql` | B | B-033 | schema | `import_mapping_presets` — §4B.3 step 3's "presets can be saved and reused for the next import". A new table, touching nothing. **Org-wide rather than per user, and a table rather than `localStorage`**, because "the *next* import" is next month, quite possibly from a reimaged laptop and by a different Admin: a preset records how another system's export is shaped, which is organisation knowledge and not a display preference. `mapping` is JSON constrained for **shape and not vocabulary** — `clients.tags` (row 42) and `users.skills`' argument, and which target fields are legal is a property of a Java class the API refuses against with a 422, not something a `CHECK` should hold a second copy of. `schema_key` is **not** a foreign key because there is no table of import schemas to point at: a schema is a Spring `@Component`, which is B-030's whole design and what makes B-038 one file. `UNIQUE (schema_key, name)` is what makes Save an upsert rather than an accumulator, and `created_by` is `ON DELETE SET NULL` — an Admin leaving takes their attribution, not the mapping the team still imports with. **Renamed from `V20260817_1130` on the rebase**: A-062's row 46 above took that exact version while this branch was open, and Flyway refuses two migrations at one version — the timestamp scheme makes a same-minute collision unlikely rather than impossible, and this is the first time it has happened |
| 48 | `V20260818_1030__ticket_pct_complete.sql` | C | C-036 | schema | `tickets.pct_complete` — S-21 Quick Update's progress slider, declared on the contract's `Ticket`, `QuickUpdateRequest` and `TicketPatchRequest` schemas since D-001 with no column behind any of them. `SMALLINT NOT NULL DEFAULT 0`, `CHECK (pct_complete BETWEEN 0 AND 100)` — zero backfills honestly, since nothing already in the table has been reported done through this field. **Touches `tickets` — flagged for Stream A**, row 42's precedent |
| 49 | `V20260818_1210__import_batch_reversal.sql` | B | B-037 | schema | `import_batches`: `reversed_at`, `reversed_by`, `reversed_rows`, `retained_rows` — blueprint §4B.3's closing validation rule and §17's mitigation for "Client Excel import silently corrupts the master": *every import writes an `import_batch` row so a bad import can be identified and reversed as a set*. B-035 wrote the row and stamped `clients.import_batch_id`; nothing had ever read either. **Four columns rather than a fifth `status` value**, which was the obvious shape and is wrong: `status` records how the *run* ended — `ImportBatchStatus` is explicit that COMPLETED is not a synonym for no rejections — and a reversal is a later fact about a run that already ended. Overwriting it collapses "completed with 6 rejections, later reversed" and "failed at row 314, later reversed" into one word, and leaves B-036's `error_report_key` belonging to a run whose status no longer explains why it has one. It also avoids dropping and recreating `ck_import_batches_status`, which B-030 added (row 25) precisely so the column's vocabulary and the contract's could not drift a second time — a constraint that must be rebuilt every time the lifecycle grows is a constraint on its way to being deleted. **`reversed_at` NULL is the whole state machine**: a boolean beside a timestamp would be two columns that can disagree, and "has this already been reversed?" is answered by the presence of the fact rather than by a flag about it. **`retained_rows` is stored rather than derived** because neither counter survives the operation it describes: `tickets.client_id` is RESTRICT, so a client the import created and which has since been named on a ticket is kept rather than destroyed — and once the others are deleted the `import_batch_id` pointing at this run goes with them, so an unreversed batch and a fully reversed one both `COUNT` zero. **No before-image table**, deliberately: an undo of the undo is a bigger promise than the blueprint makes and one nothing here could keep, since the delete cascades to `client_projects` and takes `client_contacts` with it. Restoring deleted master data is a backup's job. **`import_batches` is A-006's table — flagged for Stream A**, like V20260810_2010 (row 25) before it, though CLAUDE.md asks for no review since it is none of the three append-only tables |
| 50 | `V20260818_1500__audit_log_immutability.sql` | A | A-071 | schema | `trg_audit_no_update` / `trg_audit_no_delete` on `audit_logs`, plus `entity_ref` and `ix_audit_logs_recent`. **The decision V20260805_1106 deferred by name** — it created the table with a note saying immutability triggers were "a separate decision and a separate migration", and S-16's "export only, never editable" is what settles it. `audit_logs` now has four of the five layers `ticket_history` has and pointedly not the fifth: no hash chain, so tampering that first defeats the triggers is undetectable and a truncated tail has no `chain_anchors` to contradict it — A-075's external anchoring, not this. **The DELETE trigger means the table only ever grows**, and that is accepted rather than overlooked: there is no retention policy in the blueprint, and acquiring one by leaving DELETE available is the wrong order, so pruning becomes a DBA operation. `entity_ref VARCHAR(40)` because half the subjects in this product are ticket *codes* (`CRM-26-00347`) and `entity_id BIGINT` has nowhere to put one — the contract already types `AuditLogEntry.entityId` as a string. Not one of the four tables in CLAUDE.md's review rule; `audit_logs` is Stream A's own |
| 51 | `V20260818_1530__ticket_cycles_close_fields.sql` | C | C-040 | schema | `ticket_cycles`: `root_cause_category`, `client_verification_requested` — S-23's close dialog fields with nowhere to land. `resolution_summary` already existed (row 2); these two are the same per-cycle shape. Both nullable/defaulted and additive — no existing row changes. **Touches a ticket-lifecycle table — flagged for Stream A**, row 42/48's precedent |
| 52 | `V20260818_1720__status_categories.sql` | B | B-039 | schema | `statuses.category` — blueprint §7.4's S-13 tab 1 asks for "status list, **categories (To-do / In progress / Done)**, allowed-transition matrix per role", and the column did not exist. **It cannot be derived from `is_open` and `is_terminal`, which is the reason it is a column at all**: NEW and REOPENED are To-do while ON_HOLD, AWAITING_INFO and REWORK are In progress, and all five carry `is_open = 1, is_terminal = 0` — identical on both columns, three categories apart. Deriving it would have to hard-code the eight seeded codes in Java, putting the master's own vocabulary back into the application that S-13 exists to take it out of. `VARCHAR(20)` + `CHECK` rather than `ENUM`, PLAN.md §3.1's standing substitution — a MySQL `ENUM` compares by ordinal and renumbers its set on every `ALTER`, so a fourth category added later would silently renumber the three already stored. **NOT NULL with no `DEFAULT`**, added nullable → backfilled → constrained in one file, because a `DEFAULT` would outlive this migration and let a ninth status be inserted carrying a category nobody chose. The eight backfills are one statement each rather than a `CASE`, so each judgement is reviewable on its own line; the mapping is reasoned rather than cited, since §3.2's lifecycle diagram names no categories. **RESOLVED is DONE while `is_open` stays 1** — the category is about the work, not about the ticket record, which is the distinction that stops this column being `is_open` renamed. `statuses` is A-007's table and none of the four protected ones, so no Stream A review is required |
| 53 | `V20260818_1745__resource_import_batch.sql` | B | B-038 | schema | `users.import_batch_id` — the second registration's half of §4B.3's closing rule, *every import writes an `import_batch` row so a bad import can be identified and reversed as a set*. `clients` has carried this column since A-006 laid it down in row 7, whose own comment says `import_batches` is created first "because `clients.import_batch_id` points at it"; `users` never did, because until B-038 nothing imported them. **Without it B-038 is not a registration.** B-037 put `reverse` on the SPI precisely so this task would get reversal without writing any of it, and what only a registration can supply is *which rows this run created* — a question whose only durable answer is a stamp on the row. A Reverse button offered on a resource import that could delete nothing is worse than no button. **Stamped on INSERT only**, held by `ResourceImportSchema.upsert` and its tests rather than by DDL: nothing here can express "insert only", and a trigger refusing an UPDATE of one column on the identity table is an immutability guard Stream A should own if it is ever wanted. The rule is B-035's and B-037 depends on it — a run that merely corrected a department must not re-attribute that person to itself, and with no before image an update is not a thing a reversal could undo anyway. **RESTRICT rather than ON DELETE SET NULL**, matching `fk_clients_import_batch`: the batch row is the audit trail and is never deleted, not even by the reversal that fills in four of its columns, so a rule for that event would be a rule for something that does not happen. **Touches `users` — flagged for Stream A on B-011's precedent (row 31)**, though CLAUDE.md asks for no review since it is neither `tickets` nor one of the three append-only tables |
| 54 | `V20260818_2140__workflow_stage_deprecation.sql` | B | B-042 | schema | `workflow_stages`: `is_deprecated`, `deprecated_at` — blueprint §7.4's *"Stages used by live tickets can only be deprecated, never deleted, otherwise historical ribbons would break"*, which §17's risk table states a second time as a mitigation. B-040 built the whole of S-13 tab 2 with **no removal at all** rather than ship a delete this file would have had to take away; this is the column that lets the tab retire a stage. **A DELETE here breaks nothing and is still destructive, which is the reason the rule is a column rather than a constraint**: A-005 made `ticket_stage_transitions.to_stage` and `tickets.current_stage` plain `VARCHAR` holding the code with no foreign key onto this table, so removing a row cascades nothing, fails nowhere, and leaves every historical ribbon segment pointing at a definition that is gone while Stream D's §4A.7 stuck-in-stage scan quietly stops matching them — the same silent pair B-040 froze `stage_code` against, one verb further along. **Two columns, not one**: "when did we stop using this?" is not derivable, because the last hop into a stage records when it was last *used* rather than when it was *retired*, and those differ by exactly the interval that makes the question worth asking. `ck_workflow_stages_deprecation` keeps the flag and the timestamp from disagreeing, so no reader has to defend against a row no code path can produce. **NOT NULL with a `DEFAULT`, which is the opposite of row 51's call and deliberately so**: a category is a judgement with no correct initial value, while this is a state whose initial value is correct for every row — all 18 of B-004's stages are live, and so is every stage created after this file. `workflow_stages` is A-005's table and none of the four protected ones, so no Stream A review is required |
| 55 | `V20260819_0443__client_daily_stats.sql` | A | A-059 | schema | `client_daily_stats` — §S-05's widget 20 is "client-wise volume" and nothing summarised could answer it: A-050's two tables are keyed by project and by person, and a client is neither. **Keyed by `(stat_date, project_id, client_id)`, and the project column is the one it would be wrong without** — §2's row rule scopes every read by project, so a table keyed by client alone can express no scope filter at all and every PM would be served organisation-wide figures for work they cannot open a single ticket from. With both, a client's bar is the sum over the projects that caller can see, and two callers with different scopes get different bars for one client — each the honest answer to "this client's volume, within the work you can open". **Summing stock across projects is sound; across days it is not** — A-050's header states the second half, and the first is worth stating beside it, because a ticket belongs to exactly one project but exists on many days. `created` is what widget 20 reads; `closed` is a free extra SUM over rows the recompute already visits; **`open_total` is here now because it is stock and stock cannot be backfilled** — the wip_by_stage argument, and A-068's client report is a trend report, which is precisely the thing a hole ruins. Deliberately absent: SLA and resolution-time columns that report will also want, both flow, both derivable from immutable close timestamps whenever it lands. Tickets with `client_id IS NULL` are summarised nowhere — §4B.7 allows an internally-raised ticket, and there is no client whose volume it is — so this is a breakdown of client-attributed work rather than of all work, which is what "client-wise" asks for. **Rows are earned, so the refresh clears the day and rewrites it**, `resource_daily_stats`' argument reaching a mutable column instead of a missing history table: `tickets.client_id` is editable, and an upsert cannot retract the row it wrote for the old client, so one re-attributed ticket would be drawn twice under two names. None of the four protected tables is touched |
| 56 | `V20260819_0555__report_schedules.sql` | A | A-065 | schema + seed | `report_schedules`, `report_schedule_runs`, and the `SCHEDULED_REPORT` email template — §7.8's "All reports schedulable by email (daily/weekly/monthly)". **Two tables, because a standing instruction and one firing of it are different facts with different lifetimes**: the schedule can be edited and cancelled, while a run is what happened at 06:00 on a Tuesday and is what an auditor asks about — folding the last outcome onto the schedule row would keep exactly one answer and overwrite the rest. 🔴 **The scope is deliberately NOT stored, and that is the whole security design.** A report is scoped to whoever runs it (§2), and a schedule runs with nobody logged in, so the obvious move is to freeze the creator's role and projects onto the row. That is wrong in a way that never announces itself: roles change, and a PM moved to Developer would go on receiving project-wide figures every Monday from a row recording what they used to be — with the mail arriving on time being exactly what stops anybody noticing. Only `created_by` is kept, and the runner re-reads that user's *current* role and *current* project memberships on every run, so a demotion narrows the next email and deactivating a leaver stops it. **Recipients are stored as text rather than user ids** although they must resolve to active users: the report was addressed to a person at an address, and if that user is later deleted the history of who received last quarter's audit extract must not be erased by a foreign key — `email_log.to_email` makes the same argument one table over. **No date range is stored**, because the period is derived from the cadence — a stored window would freeze it and the second run would email a copy of the first, a failure indistinguishable from a working schedule until two files are compared. `report_schedule_runs` is append-only **by intent and not by trigger**, and says so rather than claiming a guarantee it does not have: it is none of CLAUDE.md's four hash-chained tables. Its `ck_report_schedule_runs_outcome` is the constraint worth naming — a SUCCEEDED row without a `storage_key` is a 500 waiting for the first person to click download, so the two are tied where they cannot drift. **The file is not in the email**: `storage_key` points at the object store and the mail carries a link to an authenticated download, `ImportReportStore`'s argument arriving somewhere it matters more — an attachment is an uncontrolled copy that outlives every permission change made afterwards, sitting in an inbox and in every relay between here and there. The seeded template uses `{{portal_url}}`, the merge tag this task adds, because until now a tag could only be filled from a ticket and **no mail that is not about a ticket could carry a working link at all** — the daily digest has the same gap and can now close it. None of the four protected tables is touched |
| 57 | `V20260819_1336__product_modules_and_where_it_happened.sql` | D | C-065 | schema + seed | `product_modules` (8 rows) and §7.5's four columns on `tickets`: `module_id`, `screen_name`, `feature`, `steps_to_generate`, plus `ix_tickets_module`. **ALTERs `tickets`, so it needs Stream A's review** (TEAM-PLAN §7.1) — nothing here implies mutation of an append-only table, and `tickets` itself is ordinarily mutable. **The eight rows are seed data and never a Java enum**, which PLAN.md §3.9 states outright: the ninth module is a row somebody inserts, not a migration and a deployment, and that is the whole reason this is a master rather than a `CHECK`. **`steps_to_generate` is `MEDIUMTEXT` rather than `TEXT`** on the same section's instruction — `TEXT` is 64 KB and a few screenshots pasted as data URIs exceed it, truncating silently into invalid markup; the 20 000-character bound belongs on the DTO where springdoc emits it into the contract, not on the column. **All four are nullable with no default**: §7.5 is explicit that a change request may span three modules and that a draft is saved before anybody knows the answer, so a `NOT NULL` would force a wrong value at the moment the truth is least known. **The foreign key carries no `ON DELETE` action, deliberately** — modules are deactivated rather than deleted, because a ticket raised against a since-retired module must still render its name, and the default `RESTRICT` is what makes deleting one impossible while a ticket still points at it. Carried on a task reassigned from Stream C to Stream D on 19 Aug, which is why the owner column reads D against a `C-` id |
| 58 | `V20260819_1545__fix_scheduled_report_subject.sql` | A | A-065 | correction | `notification_templates`: the `SCHEDULED_REPORT` email row's `subject_template`, seeded `NULL` by row 56. **The master's own rule is that an EMAIL row carries a subject** — `NotificationTemplateService.requireSubjectWhenEmail` refuses one without on create *and* on update — and row 56's `INSERT` went in as raw SQL, so it bypassed the check the API enforces. **The symptom is not a blank subject on the wire**: `ScheduledReportRunner` computes its own subject and passes it to the outbox, so the mail reads correctly. The symptom is that the row cannot be *saved* — an Admin who opens S-15 and changes one word gets a 400, because the update path re-checks the stored `NULL`. That is the outcome row 56's own header says it exists to avoid, *"a body compiled into the worker would be the one mail they cannot change"*, reached by another route. It also turned `develop` red: `NotificationTemplateMasterIT.subjectsMatchTheirChannel` (B-022) reads the same invariant from the other side. **Owed:** the runner does not render this column at all, so an edit to it still changes nothing — closing that needs two `MergeTag` entries (report title, period) in Stream D's enum and a change to `ScheduledReportRunner`, which is a task and not an unblock. Flagged for Stream A against A-065. Written by Stream B during B-062, which the red `develop` was blocking; **`db/migration/` is Stream A's and this needs their sign-off**. None of the four protected tables is touched |
| 59 | `V20260819_1700__ticket_comment_author_role.sql` | C | C-032 | schema | `ticket_comments.author_role` — the role half of §4B.5's stamp, the column `CommentDto.authorRole`'s own javadoc has named as C-032's since C-029: the author's role AT THE TIME OF WRITING, where the field has read their current role as a stand-in until now. `VARCHAR(20)` with no FK, `workflow_transitions.role_code`'s own precedent for a role held as a plain string. Nullable and never backfilled — no honest value exists for a comment posted before the column did, so `CommentDto.of` falls back to the author's current role on those rows exactly as it always has. **Touches a table another stream reads — flagged for Stream A**, row 51/72's precedent |
| 60 | `V20260820_0445__ticket_list_sort_indexes.sql` | A | A-073 | schema | `tickets`: `ix_tickets_created (created_at, id)`, `ix_tickets_assignee_created (assigned_to, created_at, id)`, `ix_tickets_reported (date_reported, id)`. **ALTERs `tickets`, so it needs Stream A's review** (TEAM-PLAN §7.1) — this is Stream A's own task and the review is the measurements in `tools/perf/README.md`, re-runnable via `tools/perf/explain.sql`. Indexes only: no column added, no data written, nothing about an append-only table implied. **What was wrong**: `TicketListSpecs.SORTABLE` claims its sortable columns are *"all of which are indexed or the primary key"* and three of five were not, one of them `created_at` — which `DEFAULT_SORT` is (`-createdAt`), so the unfiltered ticket list was a full scan plus a filesort for every caller. At B-007's 200 rows that is invisible; at A-073's 50,000 it is work proportional to the **table** rather than the **page**, and A-053's keyset cursor made it worse rather than better because each deep page repeated the whole sort to discard everything before the cursor. **The third index is a counterweight, not a third improvement, and this is the sentence to find before anyone drops it as redundant**: every §S-05 widget drill-down filters a reported-date window (A-060) under the default `created_at` sort, and given only `ix_tickets_created` the optimiser takes it to avoid the filesort, then walks 46,090 index entries to return 51 — 66.3 ms, against 19.5 ms for having *no* index at all. `ix_tickets_reported` is what makes that plan lose on cost. Shipping the first two alone would have made the list 20× faster and every drill-down 3× slower, surfacing on the dashboard a long way from this file. **`(project_id, created_at, id)` was measured and refused**: 0.56 ms in the single-project case, ignored by the optimiser in the multi-project one, and beaten by `ix_tickets_project_status` on every filtered variant — not worth a fourth index on the most heavily written table in the product. **Write cost +9.9 %** (49,800 bulk inserts, 4.891 s → 5.376 s), and none of the three indexed columns is touched by the hot update path, so a ticket changing status, stage, iteration or effort moves no entry in any of them. Pinned by `TicketScopeIT.theTicketListSortIndexesExist`, which asserts the column lists rather than a plan — at fixture scale MySQL correctly ignores all three, so a plan assertion would fail while the schema was right |
| 61 | `V20260820_1720__chat_attachments.sql` | D | D-053 | schema | `chat_attachments` — §7.6's file and image share, the half of D-053 that has been blocked since it was written. **A new table rather than making `ticket_attachments.ticket_id` nullable**, which was the obvious fix and the wrong one: that NOT NULL is load-bearing for C-025's scope check, C-028's delete window and C-060's cycle grouping, and a chat file has no cycle, no stage, no client-visibility flag and no delete window of its own. Weakening a constraint four features lean on, to serve a fifth that shares none of them, buys a `CHECK` that exactly one of two FKs is set — and the query that forgets it reads a chat file into a ticket's attachments tab. **Nothing about the safety pipeline is duplicated**: `AttachmentTypePolicy`, `ImageMetadataStripper` and `AttachmentScanner` are injectable beans already, so a chat upload gets the same verdict from the same three, called from a second place. Only the storage key was ticket-shaped, and `StorageKey` now covers both namespaces. `message_id` is nullable on purpose — the upload and the send are separate requests so the file is sniffed, stripped and queued for AV while the author is still typing, and `ix_chat_attachments_orphans` is what a future sweeper claims on. **No `is_client_visible`**: §7.6's three surfaces are all internal and a flag that is always 0 is a flag somebody eventually sets to 1. **No delete-window column**: D-057 already governs chat immutability and an attachment follows its message's tombstone rather than inventing a second, differently-sized window over the same evidence. Touches no ticket-lifecycle table |
| 62 | `V20260821_1015__workflow_template_mappings.sql` | B | B-041 | schema + seed | `workflow_template_mappings` — blueprint §4A.9's *"lets an Admin define a template per project and per task type"* and §7.4 tab 3's *"mapped to project x task type"*, which had no table to be mapped in. **A table and not two columns on `workflow_templates`, which is what the contract has implied since D-001**: `WorkflowTemplateWriteRequest` carries scalar `projectId`/`taskTypeId`, and §4A.9's own examples refute the shape — Standard Dev Flow is named against Production Bug, Change Request *and* Future Release. B-040 removed both fields from the response rather than serve two nulls and named this task as the one owing the mapping; a scalar pair could not have held it. **Both columns are nullable and NULL means "any"**, which is what makes the four-rung resolution ladder expressible in rows instead of code: (project, task type) → (project, any) → (any, task type) → `is_default`. Without rung 2 an organisation running one flow across a project would need one row per task type and a new one every time somebody added a task type. 🔴 **The two STORED generated columns are the point of the file, and the obvious version of this table is silently broken without them**: `UNIQUE (project_id, task_type_id)` reads as "one template per pair" and does not enforce it, because MySQL treats every NULL inside a unique index as distinct — `(5, NULL)` inserts twice, under two different templates, and neither insert fails. Rung 2 then has two answers and the resolver returns whichever the optimiser reached first, so a ticket created on Monday and one created on Tuesday get different ribbons with nothing recording that the configuration was ambiguous. `IFNULL(x, 0)` collapsed into `project_key`/`task_type_key` restores it, since 0 is not distinct from 0 and neither referenced table can hold an id of 0. STORED rather than VIRTUAL because a unique index over a virtual column recomputes on every read and this one is read on every ticket creation; generated columns rather than a functional index (also legal on 8.0.13+) because they show up in `DESCRIBE` and in an `EXPLAIN`, and a constraint whose mechanism is invisible is one somebody removes while tidying. **The three foreign keys deliberately do not agree**: `template_id` is `ON DELETE CASCADE`, since a mapping is a fact *about* a template and means nothing without it — A-005's call for `workflow_stages`; `project_id` and `task_type_id` are `RESTRICT`, because a mapping is not a fact about the project but a routing rule an Admin authored, and cascading it away with the project would silently re-route every task type that rule covered to the default template. **Seeds §4A.9's seven pairs and nothing beyond them**, all `(NULL, task_type)` — the blueprint's examples are task-type rules that hold across projects, and a project-scoped seed row would invent a policy for a project this file knows nothing about. Matched by task-type *code* rather than by id, so a code that does not exist contributes no row instead of failing the migration. Everything else falls through to rung 4, which §4A.9 calls the flow every other template is a reduction of. `workflow_templates`, `projects` and `task_types` are read, none of the four protected tables is touched, and the new table has no other reader — so no Stream A review is required, the same call B-039 (row 52) and B-042 (row 54) made |
| 63 | `V20260821_1245__stage_flow_stats.sql` | A | A-058 | schema | `stage_daily_stats`, plus `daily_ticket_stats.rework_open` and `.pingpong_open` — §S-05's widgets 16–19, the four that had to wait on Stream C because all four read `ticket_stage_transitions`. **None of them may read it live**: CLAUDE.md forbids a live `COUNT(*)` behind a dashboard, and the transitions table is the largest in the schema — one row per hop, so several per ticket, where every other dashboard source is one row per ticket or fewer. 🔴 **Widget 16 is deliberately not in this file, and the split is the decision to find before anyone “tidies” it into one place.** A-050 declared `daily_ticket_stats.wip_by_stage` for it and left it NULL naming this task as the one that fills it; nothing about that has gone stale, so the worker gains a `refreshWipByStage` pass and no DDL. The two shapes answer different questions: `wip_by_stage` is a **snapshot** read at one day — “how many sit in each stage right now” — which a JSON document serves in one read with no arithmetic between rows, while widgets 18 and 19 are **averages across the window**, and an average over thirty days is the sum of numerators over the sum of denominators, arithmetic that has to happen in SQL across every day in range. Summing a JSON document per row needs `JSON_TABLE` and a lateral join per day standing in for a `GROUP BY` anybody can read. So the point-in-time fact keeps the column A-050 shaped for it, the summable facts get rows, and **`sitting` is deliberately not duplicated into the new table** — each figure lives in exactly one place. **Durations are minutes, not hours**: `ticket_stage_transitions.duration_mins` is working minutes from the calendar service and this table sums it unchanged, where converting at storage time would round once per row and widget 18 divides these sums by a count afterwards. `elapsed_mins` against `active_mins` is what makes widget 18 able to say a stage averaging four days held three hours of work — the split between waiting and working, which is the figure the funnel exists to expose. Keyed `(stat_date, project_id, stage_code)` for §2's row rule, `daily_ticket_stats`' own precedent: a table keyed by stage alone could express no scope filter and would serve every PM organisation-wide figures. The two new columns on `daily_ticket_stats` are stock rather than flow and carry `NOT NULL DEFAULT 0`, which is correct for every existing row — nothing was reworked in a day nobody recorded. **ALTERs `daily_ticket_stats` and reads `ticket_stage_transitions`; it writes to neither that table nor any of the other three append-only ones**, so nothing here implies mutation of a hash-chained table. Stream A's own task, so the §7.1 review is this file and the widget ITs rather than a second pair of eyes |
| 64 | `V20260822_1200__timesheet_approvals.sql` | B | B-065 | schema | `timesheet_approvals` — §21's second half, split out of B-063: "an approval step for the manager." **A table, not a flag on `ticket_effort_logs`**: that table is one of CLAUDE.md's three append-only, hash-chained ones, so an "approved" column on it cannot exist — the review is its own row instead, `ticket_stage_transitions`' sealing exception and `ticket_status_requests` (row 33)'s own precedent for a fact *about* an append-only stream living beside it, never inside it. **Grain is one row per resource per week**, matching `TimesheetService`'s own document shape (Monday–Sunday), and `uq_timesheet_approvals_week` makes a second approval of the same week a 409 the service reports rather than a silent second row. **Who may approve — Admin, or the resource's own direct reporting manager via `users.reporting_manager_id`** — is enforced in `TimesheetApprovalService`, not a `CHECK`, since a constraint cannot see a caller's identity or walk the reporting chain. **Insert-only with no hash chain of its own**: there is no UPDATE or DELETE anywhere in this feature and un-reviewing a week is a policy nobody has asked for, so unlike the three protected tables it sits beside, there is nothing yet to correct. New table, foreign keys into `users` only, alters none of the three protected tables or `ticket_effort_logs` itself — flagged for Stream A's review per the file's own note, though none of the four protected tables is touched |
| 65 | `V20260826_1520__closed_stage_owned_by_support.sql` | C | — | correction | `workflow_stages.owner_role` for `CLOSED`, moved from PM to SUPPORT on every template. **B-004's seed data — flagged for Stream B**, not one of the four append-only tables. V20260807_1700 seeded Closed as "whichever role signed off", which put it on the same role that owns Sign-off; `TransitionService.resolveAssignee` then kept the outgoing owner, so a PM signing off handed the ticket to themselves and closed it in one click, and the desk that raised it never saw it again. §4A.1 gives Sign-off as "PM / Support Desk" precisely because those are two hands. Idempotent by predicate — Support Fast-Track was already SUPPORT and is untouched. **No task id: an ad-hoc fix requested 26 Aug 2026**, outside the plan's backlog |
| 66 | `V20260826_1815__closed_stage_can_return_to.sql` | C | — | correction | `workflow_stages.can_return_to` for `CLOSED`, seeded `['DEV']` (or `['TRIAGE']` on a template with no Development stage). **B-004's seed data — flagged for Stream B.** The other half of row 65: a desk handed a ticket to close needs to be able to refuse it, and §4A.1's mechanism for refusing is a backward move to a `can_return_to` target. Closed was seeded NULL — correct while the stage was terminal-and-nobody's, wrong once somebody stands in it — so `ReworkService.requireReturnTargetAllowed` answered 422 for every backward move out of Closed and the only exit was closure. Resolved by what the template actually contains rather than by name, and only rows with no target yet are touched, so B-043's designer keeps anything it authored. **No task id — ad-hoc, 26 Aug 2026** |
| 67 | `V20260826_1930__all_task_types_use_standard_dev_flow.sql` | C | — | correction | `workflow_template_mappings.template_id`: all seven task-type rules repointed at Standard Dev Flow. **B-041's table, seeded by row 62 — flagged for Stream B.** 🔴 **A deliberate departure from §4A.9's routing table, not a defect fix** — Support Fast-Track (5 stages) and Infra Flow (5) were doing exactly what they were seeded to do, so a Network Issue drew a ribbon with no Development, QA or Sign-off segment. Requested 26 Aug 2026: every new ticket shows all eight stages. `workflow_templates` and `workflow_stages` are untouched, so both five-stage templates keep their stages and stay selectable in the designer — they are simply no longer any task type's automatic answer. Project-scoped rules (ladder rungs 1–2) are left alone. To revert, put §4A.9's routing back **in a new migration**. **No task id — ad-hoc** |
| 68 | `V20260826_2015__open_tickets_use_standard_dev_flow.sql` | C | — | correction | ⚠ **ALTERs `tickets` (`workflow_template_id`), so it needs Stream A's review** (TEAM-PLAN §7.1) — one column, open tickets only, none of the three append-only tables read or written. The companion to row 67, which left existing tickets alone and so had a ticket created twelve minutes earlier still drawing five segments. 🔴 **Safe only because Standard Dev Flow's stage codes are a strict superset of the other two templates'** — verified against the data as well as the seed, every distinct `to_stage` in `ticket_stage_transitions` is one of its eight — so no hop can be orphaned. Excludes CLOSED tickets (a sealed cycle's ribbon is a record, not a view that should gain two Pending segments a month later) and tickets with no template (`RibbonAssembler` answers those with an empty segment list on purpose). **Known and accepted:** a reopened ticket's earlier sealed cycles move too, because the column is per-ticket rather than per-cycle. **No task id — ad-hoc** |
| 69 | `V20260826_2130__resolved_may_reopen.sql` | C | — | seed | `workflow_transitions`: `RESOLVED -> REOPENED` for the same three roles row 13 already gives `CLOSED -> REOPENED`. **B-003's seed data — flagged for Stream B.** The desk's Reopen fires on a ticket that is RESOLVED — the status a sign-off handoff leaves behind once row 65 put the terminal stage in Support's hands — so this is the whitelist entry that makes the table and `ReopenService` agree. Row 13's LOCKED note governs **which roles** may reopen and is kept exactly (Admin, PM, Support Desk); only the `from_status` widens. `SUPPORT_DESK`, matched off the existing rows rather than typed, since V20260808_1400 settled this table's spelling apart from `roles.code`. **Superseded in practice by row 70 — see there.** **No task id — ad-hoc** |
| 70 | `V20260826_2145__resolved_may_reopen.sql` | C | — | seed | 🔴 **A duplicate of row 69 — the same `RESOLVED -> REOPENED` rows, written twice fifteen minutes apart.** Both applied successfully (`flyway_schema_history`, 26 Aug 10:28), and because both are insert-if-absent this one inserted nothing: it is a no-op against any database that ran row 69 first. **Kept rather than deleted, and that is the rule not an oversight** — Flyway has recorded it as applied, so removing the file would fail validation on every database that has it. The only correction to an applied migration is a new one. The difference between the two is the anti-1093 wrapping: row 69 references `workflow_transitions` directly in its `NOT EXISTS`, this one wraps it in a derived table. **No task id — ad-hoc** |
| 71 | `V20260831_1400__ticket_cycle_start_finish_stamps.sql` | A | — | schema | `ticket_cycles.started_at` / `.finished_at`, `DATETIME(6) NULL` plus one index each. Dashboard Rework Dev 1, PR 3 — the dashboard's "Started Today" / "Finished Today" sections need the first moment a cycle's status entered `IN_PROGRESS` and the first moment it entered `DONE` (`statuses.category`, row 56), which neither `start_date` (cycle open) nor `actual_close_date` (formal close, a later and separate step) answers. **Per cycle, not per ticket**, so a reopened ticket counts again in its new cycle rather than staying permanently "finished" — the same reasoning `effort_hrs` already follows. **Backfilled in the same file** from existing `ticket_history` `STATUS_CHANGED` rows, one `UPDATE … JOIN` per column, keyed on `(ticket_id, cycle_no)` (history's `cycle_no` is stamped from `tickets.current_cycle_no` at append time, so it lines up exactly) and taking the *earliest* qualifying row per cycle — a cycle bounced back for rework and resolved twice does not un-start or un-finish. `is_correction = 0` excludes reversal rows from being read as a genuine transition. **Schema and backfill only** — stamping the columns at transition time going forward is a paired Stream C PR (Divyansh), scoped separately so this file does not duplicate `TransitionService`/`QuickUpdateService` work. Cycles with no matching history stay `NULL` rather than guess, which is most of the fixture corpus: it was seeded with tickets inserted directly at a target status, so only 13 real `STATUS_CHANGED` rows exist to backfill from. `ticket_cycles` sits beside the three protected tables in the ticket lifecycle without being one of them — flagged for Stream A's review per CLAUDE.md and the `V20260818_1530` precedent for this same table, even though this is Stream A's own task |
| 72 | `V20260831_1600__today_stats_daily_ticket_columns.sql` | A | — | schema | `daily_ticket_stats`: fourteen `INT NULL` counters (`ns_total/overdue/due_today`, `wip_total/updated_today/near_delay/delayed`, `started_today`, `finished_early/on_time/late`, `blocked_on_hold/awaiting_info`, `pending_review`) plus `open_by_role JSON`. Dashboard Rework Dev 1, PR 4 — the Today's Progress tab's seven summary cards and the Open Issues card, per `docs/Dashboard-Rework-Plan.md`'s "Data" §2. **`NULL`, not `NOT NULL DEFAULT 0` like every existing column on this table**, because two of the fourteen cannot promise a value for a day that predates them: `wip_updated_today` reads `tickets.updated_at`, which carries no history, so a day other than the one the worker is actually running on gets `NULL` rather than a wrong count of today's edits attributed to the past; `pending_review` resolves through row 73's `is_review_stage`, which did not exist for any day summarised before that migration — landed one PR later than this file (PR 5, ticket-list-today-params, merged first) but read here regardless, since only the worker's Java code depends on the column existing at *run* time, not at this file's own migration time. The other twelve are computed on every pass including backfill, and are declared `NULL` anyway for the `wip_by_stage` / `type_counts` reason: a table row can predate the column that fills it, and only `NULL` can say so. **`open_by_role` is JSON, keyed by `roles.code` plus the literal `"UNASSIGNED"`**, the same masters-table argument as `type_counts` and `wip_by_stage` rather than six columns for a role vocabulary an Admin's Role Master can in principle extend. None of the four protected tables is touched |
| 73 | `V20260831_1615__workflow_stages_review_flag.sql` | A | — | schema | `workflow_stages.is_review_stage`, `TINYINT(1) NOT NULL DEFAULT 0`, backfilled `1` for `VERIFY`/`SIGNOFF`. Dashboard Rework Dev 1, PR 5 — `GET /tickets`' new `pendingReview` param needs to resolve review stages from the stage master rather than a hardcoded `VERIFY`/`SIGNOFF` list in Java, which is exactly what the plan rules out and what B-039's `statuses.category` (row 56) already argued against for the identical reason: a renamed or added review stage would silently stop counting. Matched by `stage_code` rather than owner role or name, since the three seeded templates disagree on both — Infra Flow has no `SIGNOFF` stage at all. `NOT NULL DEFAULT 0` follows B-042's `is_deprecated` precedent (row 68): every seeded stage already has a correct answer, so this is a known state, not a judgement to phase in nullable. No index — eighteen rows, the same call row 68 made; the predicate that runs per ticket is against `tickets.current_stage`, a different table's question. **Row 72's `daily_ticket_stats.pending_review` reads this column too**, landed by a parallel PR (4) that surfaced the identical need independently — see that row for why the two timestamps do not agree with dependency order. **`workflow_stages` is Stream B's table — flagged for Stream B's sign-off**, the same precedent rows 65–70 set for a blocking cross-stream correction |
| 74 | `V20260831_1620__today_stats_resource_daily_columns.sql` | A | — | schema | `resource_daily_stats`: the same fourteen counters row 72 gave the project table, as `INT NOT NULL DEFAULT 0` — the Assignee MIS table's columns. Dashboard Rework Dev 1, PR 4, `docs/Dashboard-Rework-Plan.md`'s "Data" §3. **`NOT NULL DEFAULT 0` here, where row 72 just went `NULL`, because the two tables answer "not computed" two different ways**: `daily_ticket_stats` gives every project a row on every day by construction, so `NULL` is the only way to mark a row that predates a column; `resource_daily_stats` gives a user a row only when they earn one that day and rewrites the whole day by DELETE + INSERT (row 50's own argument), so a row that exists at all was always freshly computed by the pass that wrote it — there is no older-row case to distinguish. **Same column names as the project table, not `assigned_`-prefixed like every existing column here**, deliberately: the two tables' figures are meant to reconcile on the same MIS row (the prototype's own header, "figures reconcile with the MIS below"), and giving one fact two names in the two tables it lives in is the kind of drift a future reader has to notice is not a bug. `open_by_role` stays project-only — a row already scoped to one `user_id` has nothing left to group by role. None of the four protected tables is touched |
| 75 | `V20260901_1120__module_daily_stats.sql` | A | — | schema | `module_daily_stats` — the fourth summary table, on row 55's precedent and for the same reason: CLAUDE.md forbids a live `COUNT(*)` behind a dashboard, and nothing already summarised can answer "how much open work per module". `daily_ticket_stats` is keyed by project, `resource_daily_stats` by person, `client_daily_stats` by client; `tickets.module_id` is orthogonal to all three. Dashboard Rework Dev 2, PR 14 — §S-05's fifteenth widget, `module-open`. **Keyed by `(stat_date, project_id, module_id)`, and the project column is the one it would be wrong without** — row 55's argument exactly, and it is a disclosure rather than a rounding error: a table keyed by module alone can express no scope filter, so every non-Admin would be served counts covering projects they cannot open a single ticket from. Unlike clients, modules are a small fixed master (§7.3 lists eight), so cardinality is projects × 8 × days rather than anything unbounded. **All three columns are stock, which is the case where the backfill test answers "no" for every one of them** — row 55 could at least backfill `closed`; "how many of this module's tickets were WIP at the end of 3 August" is unrecoverable once that day passes, since status is a current value and reconstructing it per day per module is the live computation this table exists to avoid. So the table starts empty and fills forward from the day it lands, stated here rather than discovered: a chart blank for its first days is correct, not broken. **The three segments partition the population, and overdue takes precedence** — a stacked bar makes an arithmetic claim, so `open_overdue` / `open_wip` / `open_not_started` are disjoint by construction, written by one `CASE` per ticket in `DailyStatsRepository.refreshModuleStats` with overdue tested first. Three independent `SUM`s would count an overdue in-progress ticket twice, every module's bar would overstate its load in proportion to how late that module is running, and each segment would stay individually plausible. **Category `DONE` is excluded**, which drops `RESOLVED`-not-`CLOSED`: finished work whose record is still open, counted on S-05's Today tab as Pending Review. Including it would put finished work in a chart titled "open tickets" and in none of the three segments, so the bar would stop summing to its own total. Tickets with `module_id IS NULL` are summarised nowhere — §7.5's fields postdate the tickets raised before them — so this is a breakdown of module-attributed work rather than of all work, exactly as row 55 is of client-attributed work. **Rows are earned, so the refresh clears the day and rewrites it**, rows 55 and 70's argument reaching another mutable column: `tickets.module_id` is editable, and an upsert cannot retract the row it wrote for the old module, so one re-pointed ticket would stand in two modules' bars at once. `module_id` is `INT` to match `product_modules.id` (row 57) rather than the `BIGINT` the other summary tables use for their own keys. None of the four protected tables is touched |
| 76 | `V20260901_1330__weekly_stats_columns.sql` | A | — | schema | `daily_ticket_stats`: `open_pct_sum`, `delay_days_sum`, `open_due_next_7` (`INT NULL`, row 72's precedent). `resource_daily_stats`: `pct_sum`, `delay_days_sum` (`INT NOT NULL DEFAULT 0`, row 74's precedent). Dashboard Rework Dev 2, PR 11 — Weekly Progress's avg-progress, due-this-week and avg-delay cards, per `docs/Dashboard-Rework-Plan.md`'s "Data" §4. **Sums, not averages** — the average is computed on read as sum ÷ open total, so it stays correct against whichever open total is current rather than the one true when the row was written; storing a pre-divided percentage would go stale the moment the denominator moved without a fresh worker pass. **`open_pct_sum` is `NULL` outside the actual current day, `delay_days_sum` and `open_due_next_7` are not** — `tickets.pct_complete` carries no history, row 72's `wip_updated_today` argument exactly; the other two derive purely from `planned_close_date` compared against the day being summarised, which does not depend on when the pass runs, so both backfill honestly like `ns_overdue` and `ns_due_today` beside them. **`resource_daily_stats`' two columns are `NOT NULL DEFAULT 0` including `pct_sum`**, unlike the project table — row 74's argument: this table is deleted and rewritten per day, so there is no pre-migration row for `NULL` to distinguish, and the worker writes a real `0` on a backfill day instead. Written by `DailyStatsRepository.refreshWeeklyStats` (worker module) as a second pass after `refreshTodayStats`/`refreshResourceStats`, for `refreshTodayStats`' own reason: a correlated read of `tickets` folded into either table's `INSERT` would contend with the shared locks it already holds. 🔴 **`worker/` is Stream D's directory (TEAM-PLAN §6) — flagged for Debashis's sign-off**, the precedent the class's own header already establishes six times over for this exact file. None of the four protected tables is touched |
| 77 | `V20260903_1210__ob_client_capture.sql` | A | A-101 | schema | `ob_products`, `ob_clients`, `ob_client_contacts`, `ob_client_applications`, `ob_client_requirements` — the Client Onboarding module's client capture (`docs/Onboarding-Module-Plan.md` §4). **The first `ob_`-prefixed migration, and the prefix is the module boundary rather than a naming habit**: plan §2 makes separability a requirement ("separable by `grep ob_` alone"), and verified from `information_schema` on a full 77-migration run, the only cross-module foreign-key target from any `ob_` table is `users` — which plan §2 lists as shared platform. `ob_clients` is **not** `clients`: different companies' data, different lifecycles, ids that do not correspond, joined only by A-125's audited identity-layer link. **PAN is two columns, and the one-column version is a constraint that cannot fire.** PHASE-2-BUILD-PLAN.md decision 3 is closed as application-level AES-GCM, which is randomised — the same PAN encrypts to different bytes every call — so a `UNIQUE` index on the ciphertext would apply cleanly, look like a working guard and never collide, while the thing it exists to prevent is two rows for one legal entity (plan §1.1 item 6, and the contract's `ob-client-pan-duplicate`). So `pan_ciphertext VARBINARY(255)` carries no constraint, and a deterministic `pan_blind_index BINARY(32)` HMAC carries `uq_ob_clients_pan_blind`; a duplicate PAN with *different* ciphertext is refused, which is exactly the row a ciphertext index lets through. The HMAC key is not the AES key and has a different lifecycle — AES may rotate freely, rotating the HMAC invalidates every stored index and the uniqueness guarantee with it. **Both columns are `NULL` and nothing writes them until A-113** (the vault they need, A-075, is not up until 5 Nov); created now so no later migration ever reads, encrypts and rewrites live identity data in place. **One primary SPOC per client is enforced here, not only in the service** — `is_primary_key` is a generated column, `1` when primary *and* active and `NULL` otherwise, with `UNIQUE (ob_client_id, is_primary_key)` ignoring the `NULL`s: the MySQL idiom for a partial unique index, which 8.4 does not have and PLAN.md §3.1 does not cover because phase 1 never needed it. In the database because the primary SPOC receives the kickoff mail, the one-time portal password and every sign-off request — two primaries sends the credential to both, none sends it to nobody, silently. Deactivating a primary releases the slot. **`ob_products` is A-124's table in the backlog and is created here anyway**, because `ob_client_applications.product_id` needs its foreign-key target and A-101 cannot apply without it; A-124 keeps the master screen and its service layer, only the DDL moves. **No financial columns anywhere** (plan §1.2) — applications record what was bought and for how long, never for how much; `license_end` is captured for the renewals module and read by nothing here. **No `rag` column**, though plan §4 lists one: it is derived worst-wins from the open journeys at read time and no journey table exists until A-104, and a stored colour nothing recomputes is a colour that disagrees with its own steps. None of the four protected tables is touched |
| 78 | `V20260903_1420__ob_journey_templates.sql` | A | A-103 | schema | `ob_journey_templates`, `ob_journey_template_steps`, `ob_journey_template_step_items`, `ob_journey_template_step_docs` — the Module Service page's definitions (`docs/Onboarding-Module-Plan.md` §4, §5.1, §5.5, §5.6, §9 OB-07). **One row is one *version*, not one service**: editing publishes a new version and in-flight journeys keep theirs (§5.1), so a product accumulates rows and `ob_journeys` pins the one it was instantiated from (A-104) — that pinning is the whole mechanism by which an admin's edit cannot corrupt a running journey. **"One active version per product" is the generated-column partial-index idiom, second use after row 77's `is_primary_key`**: `active_key` is `1` when active and `NULL` otherwise, and the unique index ignores the `NULL`s, so retired versions accumulate while a second *active* one is refused. **`sequence` is deliberately not unique, where `workflow_stages.seq` next door is** — it orders services (§5.5, OB-07's ↑/↓), every version of one product's service shares that position, and a unique index over a table holding all history would refuse the second version of anything. **The two dependency kinds have opposite guarantees, and the plan calls both "cycle-free"**: the *step* dependency is cycle-free by construction and the database holds half of it — `uq_ob_journey_template_steps_scope (template_id, id)` exists solely so the composite FK `(template_id, depends_on_step_id)` can resolve only within the same template, verified refusing a cross-template edge — while the "earlier step" half compares another row's `sequence` and is C-119's. The *service* dependency (`depends_on_template_id`) is **not** cycle-free by construction: cross-product by design (§5.5's Biometric-after-ERP) and unbounded in depth, so detection is a transitive graph walk that no CHECK or trigger can perform — C-123 owns it entirely, including the one-hop self-reference, because **MySQL 8.4 rejects `CHECK (depends_on_x <> id)` with ERROR 3818, "cannot refer to an auto-increment column"**. A reader who assumes the database holds that line will not write the check. **`template → steps` is RESTRICT rather than CASCADE, and that was measured**: under CASCADE a template whose steps form a chain fails with ERROR 1451 naming a constraint on a *different* table, while one whose steps are all parallel deletes cleanly — deletion working unless the admin used the feature the page is built around. RESTRICT fails identically every time and the service deletes steps in reverse dependency order, which is what the designer already does on a single delete. `ON DELETE SET NULL` is not available: the composite key would have to null `template_id`, which is `NOT NULL`. Deletion is the rare path regardless — a published version is pinned by its journeys and is retired with `is_active = 0`, the argument `ob_products` makes one table over. **TATs are working days** (`tat_days`), a v1.2 change of unit and not only of label. None of the four protected tables is touched |
| 79 | `V20260903_1600__ob_journey_instances.sql` | A | A-104 | schema | `ob_journeys`, `ob_journey_steps`, `ob_journey_step_items` — what a running journey *is* (§4, §5.2, §5.3, §5.5, §5.6, §5.8). Deliberately three tables: `ob_step_clock_events` is A-105 and the append-only pair is A-106, both listed beside these in §4 and both somebody's own task. **The rows are a snapshot, which is the point** — `name`, `tat_days`, `requires_signoff` and the task-list labels are copied from the template at instantiation rather than read through the template at display time; that looks like denormalisation and is the opposite, because §1.1 item 2 makes snapshotting architectural so an admin's edit cannot mutate a running journey. The template FK is provenance, never the display source. **Two independent holds, and modelling them as one field is the mistake this table invites**: `gate_status` is the client's *prerequisites* (§5.3) and flips for every journey at once, while `held_by_journey_id` is this journey's *service-level dependency* (§5.5) and clears per journey — a journey can be past the gate and still held, and one flag would silently start it the moment prerequisites cleared. Verified accepting exactly that combination. **There is no `application_id`, because (ob_client_id, product_id) *is* the foreign key to the purchase**: `ob_client_applications` is already unique on that pair (row 77), so a third column beside them could only ever disagree with them — verified refusing a journey for a product the client never bought. `uq_ob_journeys_client_product` is "one per client per product among non-archived" (§4) and the **third** use of the generated-column partial-index idiom, after rows 77 and 78. **`journey → steps` is RESTRICT for row 78's measured reason** — cascade collides with the step self-FK the moment any step depends on another; journeys are archived, not deleted. The step self-FK repeats row 78's composite-key trick scoped to the journey, and here it earns more than tidiness: without it a snapshot bug could point one client's step at another client's, leaking the existence of another client's journey through a blocked-by message, and no test that builds a single journey would catch it. **Two §1.1 rules the database holds rather than the service**: BLOCKED requires `blocked_reason_code` (item 5 — a block with no reason is invisible in the report that exists to find it) and SKIPPED requires `skip_reason`. **§5.8's task-list gate is split**, and the split is the same one row 78 made: "False requires a remark" is a same-row rule and is a CHECK here, while "every item answered" compares rows and stays with C-106's completion gate. None of the four protected tables is touched |
| 80 | `V20260903_1615__ob_client_escalations.sql` | A | A-128 | schema | `ob_client_escalations` — the v1.2 addition (§4, §7, §9 CP-03 and OB-02): an escalation a **client** raises from the portal against one service. **A-128 is "service dependency & escalation schema" and the service-dependency half already landed** — `depends_on_template_id` in row 78, `held_by_journey_id` in row 79 — because a column belongs in the migration that creates its table; adding either a file later would have been an ALTER on a table nobody had used yet. **This is the one table in the module a client writes to directly**, and the columns say so: `raised_by_contact_id` points at `ob_client_contacts` because the author is an external principal (§2.3's `principal_type: CLIENT`) with no staff user to attribute it to, while `resolved_by` points at `users` because only staff resolve. Two people-columns pointing at two tables is the shape of the fact, not an inconsistency. **"One open escalation per service" is the fourth use of the partial-index idiom** (after rows 77, 78 and 79): `open_key` is 1 while `resolved_at` is NULL, so resolved escalations accumulate and a second open one is refused — which matters rather than being tidy, because §7 makes the notification **immediate and unmutable**, so a client double-tapping Escalate on a slow connection would otherwise send two unmutable notifications for one complaint and count one client twice on OB-02's card. `comment` is `NOT NULL` and checked non-empty: an escalation with no text is a red chip that fires a notification and gives the manager nothing to read. `resolved_at` and `resolved_by` are checked to move together, or the OB-02 count and the client's own view disagree about who is waiting. `ob_client_id` and `journey_id` are denormalised from the step so the dashboard can count and group without joining three tables per row — CLAUDE.md forbids a live `COUNT(*)` behind a dashboard. **No FK to `ob_step_communications`**: §4's "mirrored into the timeline as an ESCALATION entry" is a one-directional service insert, and a key between them would invite the timeline (A-106, append-only) to be treated as the source of truth for something a client can still resolve. None of the four protected tables is touched |
| 81 | `V20260903_1730__ob_step_clock_events.sql` | A | A-105 | guard | `ob_step_clock_events` — the step clock (§5.7, §5.10, §1.1 item 1, §14), **plus its own immutability triggers**. **Events, not intervals, and the name is the design**: an interval row with a nullable `ended_at` sealed on close — what `ticket_stage_transitions` does (row 5) — would make the roll-up a single `SUM`, but sealing is a *mutation*, and a mutation needs PLAN.md §3.6's seal-only trigger: forty lines of NULL-safe column comparison to get right and keep right. Append-only events have no update path to guard, so none to get wrong; the cost is that "utilized so far" is a fold over paired events rather than a SUM, which is C-120's query to write once against a table that cannot lie to it. 🔴 **DELIBERATE TIGHTENING BEYOND THE PLAN, flagged rather than silent**: §4 annotates only `ob_step_communications` and `ob_step_history` as append-only, not this one — this file protects it anyway. The argument is that this table *is* the TAT record: §5.10 derives utilized hours from it at read time precisely so no stored aggregate can disagree with its parts, but that moves the trust rather than creating it, and if a row here is editable then every breach, escalation and waiting-on-client attribution is editable too — while §14 names TAT disputes as a live risk whose *mitigation is this clock*. The asymmetry favours it too: dropping a trigger later is one migration, whereas retrofitting immutability onto rows already edited is a forensics exercise with no correct answer. Same reasoning PLAN.md §3.6 used to tighten blueprint §4A.5; raised for review rather than presented as settled. **Not hash-chained**, and not by oversight — the chain costs a pessimistic parent lock per append (§3.7), the scanner writes across many steps at once, and every clock event worth disputing writes a history row beside it inside the chain. `attributed_to` is stored per event rather than derived from `pause_reason` at report time, so the attribution is fixed by whoever made it and cannot be re-derived differently six months later by a changed mapping. `occurred_at` is separate from `created_at`: identical for a live transition, and not for a scanner catching up after an outage — the gap is itself informative. Two CHECKs keep `pause_reason` meaning one thing: mandatory on PAUSED, forbidden elsewhere. None of the four pre-existing protected tables is touched |
| 82 | `V20260903_1745__ob_step_append_only_pair.sql` | A | A-106 | guard | `ob_step_communications`, `ob_step_history` — the onboarding module's append-only pair (§4, §6, §1.1 item 10), **plus their four triggers**. This is the file CLAUDE.md warns about: "the guarantee that erodes first and is hardest to restore". Nothing here has an update path and none may be added; a correction is a compensating row (`is_correction`, `corrects_entry_id`), exactly as A-043 does for `ticket_effort_logs`. Layer 4 of A-008's own four, the other three being the service (no update/delete method), the routes (none registered) and the grants (A-109: INSERT and SELECT only). **Only one of the two is chained, and the asymmetry is §4's rather than an omission**: the chain costs a pessimistic parent lock per append (§3.7) and buys tamper-evidence for the record of *what happened and who did it* — the record an escalation, a TAT dispute or a sign-off argument turns on. Communications are the correspondence alongside it, already attributable, frequently written by the client through the portal, and a forged comment is a far less useful lie than a forged transition; chaining both would double lock traffic on the same parent for no additional guarantee. **The chain is per JOURNEY, not per step and not global**, and the granularity is forced: §5.6 runs dependency-free steps in parallel and completing one re-evaluates the whole journey, so one action writes several steps' history — per-step chains would take N locks in an order nothing guarantees, which is a deadlock against ourselves, and §3.7's closing note makes exactly this argument for tickets ("one lock, not three"). `prev_hash IS NULL` anchors each journey's chain; the verifier walks per journey and a break is localised to one client. `canonical_json` must be the one in `common` — a second serialiser with a different key order produces hashes the verifier cannot reproduce, and *that failure is indistinguishable from real tampering*. Hash columns are `ascii_bin` so comparison is byte-exact: a case-insensitive collation would make two different hashes compare equal. Both tables carry a three-way author CHECK (STAFF names a user, CLIENT names a contact, SYSTEM names neither) because the portal writes here too, and `ob_step_communications.is_client_visible` **defaults to 0** — §9 CP-03 and §11 put internal comms on the never-visible list, and defaulting the other way leaks a staff note the moment somebody forgets a flag, which cannot be undone once read. 🔴 **Unverified in the sense A-008 was until A-013**: the triggers are exercised by hand here but nothing re-checks them per CI run until A-123 covers these tables. Worth recording what that hand-exercise found — `SIGNAL … SET MESSAGE_TEXT` is capped at **128 characters**, and over it MySQL replaces the whole signal with ERROR 1648 "Data too long for condition item": the write is still refused so the guarantee holds, but the caller learns nothing about immutability and the SQLSTATE is 22001 rather than the declared 45000. Three of the six messages were 139–162 characters on the first draft; A-008's are all under 105, which is why it never surfaced there. None of the four pre-existing protected tables is touched |
| 83 | `V20260903_1900__ob_dashboard_summary.sql` | A | A-108 | schema | `ob_dashboard_summary`, `ob_implementor_daily_stats` — the onboarding dashboard's pre-aggregates (§4, §9 OB-02, §10). CLAUDE.md forbids a live `COUNT(*)` behind a dashboard, and §4 says the roll-ups gain a product dimension. **Two tables where §4 names one**, and it is the ticketing precedent rather than a new idea: OB-02 asks for cards journey-counted per product *and* an implementor grid with one row per person, and those cannot share a primary key — a row keyed by product cannot say how many clients Ravi carries, one keyed by person cannot say how many ERP journeys are amber. Forcing both into one table means a nullable dimension and a `WHERE user_id IS NULL` on every card query, which is how a summary table starts double-counting the moment someone forgets the predicate; rows 51, 50, 55 and 75 are four tables for exactly this reason. The alternative was letting B-128 add the second table, which would mean a Stream B task needing a Stream A migration mid-sprint. **The three RAG columns partition `journeys_open_running`** — an arithmetic contract on whoever writes the refresh, not a hint: three independent counts would double-count a journey that is both overdue and blocked, every product would overstate in proportion to how badly it is running, and each number would stay individually plausible. One `CASE` per journey, worst-wins, as row 75 does it. **`journeys_locked` and `journeys_held` are not RAG states**: a journey whose gate has not cleared has no colour at all (row 79's nullable `rag`) and neither does one held behind a sibling (§5.5) — folding either into green would report a client who has not started as on track. Cards count **clients, not items**, where §9 says clients: one client late on three services is one overdue client, and with one open escalation per service (row 80) a client with three unhappy services would otherwise triple its own card. **The performance score is deliberately not a column** — §9 computes it from on-time and early completions weighted against delays and blocks, a weighting that will be tuned, and a score stored under last month's weights cannot be recomputed under this month's, so every historical row would silently mean something different from the ones beside it; the four completion counters are stored and the score is derived on read, the same call row 76 made for sums rather than pre-divided averages. **Both tables start empty and fill forward**: RAG, gate status and step status are current values with no history, so "how many ERP journeys were amber on 12 August" is unrecoverable once that day passes — a chart blank for its first days is correct, not broken (row 75's own note). `delayed` is backticked because `DELAYED` is still a MySQL reserved word in 8.4; the name is kept because §9 lists these six as the grid's own columns and B-128 maps them straight to its headers. **A row must be written for an implementor with zero clients** (§9, explicitly) — a refresh-job obligation no schema can enforce, and the one most likely to be lost, because the natural GROUP BY over open steps produces no row for somebody who has just finished everything and the grid then shows a fully-delivered implementor as absent rather than clear. None of the protected tables is touched |

<!-- load-order:end -->

`SeedManifestTest` parses the block between those two markers and fails the
build if it does not match this directory exactly — every file listed, nothing
listed that does not exist, and in ascending version order. **A new migration
without a row here is a build failure, not a review comment.**

---

## 2. Why the order is what it is

Flyway will happily run a seed file before the data it depends on exists. These
are the edges that hold today — break one and the failure is a silently empty
table, not an error.

- **Schema before seed.** Rows 1–7 create every table rows 8–17 write into,
  except row 17, which creates and seeds its own in one file — a singleton
  config row is the table's initial state, not a separate load step.
  Row 7 carries a later timestamp than rows 5–6 on purpose: `clients` and
  `email_log` have FKs into `sla_policies` and `notification_templates`, both
  created by row 4.
- **Row 8 before rows 9, 11, 13.** Roles come first because the three later
  seeds all name role codes. Rows 11 and 13 store them as *plain strings* with
  no FK, so the dependency is real but unenforced by the database.
- **Row 9 before row 13.** Templates map to task types.
- **Row 11 before row 13.** Stages and statuses are different axes (§3 — a
  ticket can be `IN_PROGRESS` while sitting in the QA stage), but the ribbon's
  stage seed reads the status vocabulary, so statuses land first.
- **Corrections run after what they correct**, which is what the timestamp
  gives for free — and is also the trap in §3.

Seeds are **idempotent by position, not by statement**. They are plain
`INSERT`s, not upserts; Flyway runs each exactly once and that is the only
thing stopping a duplicate. Never re-run one by hand against a database that
already has it.

---

## 3. Correction history

A correction is a **new** migration. Applied files are checksummed — editing
one breaks every other developer's database, and CI's migration-guard job
rejects it.

The trap this register exists to expose:

> A correction fixes a value. A seed file queued **behind** it writes the old
> value straight back. Both files are individually correct. Only the order is
> wrong, and only a list like the one above shows it.

| Correction | Undone by | Repaired by |
|---|---|---|
| Row 10 — `roles.code` → `SUPPORT` | Row 11, which hardcoded `'SUPPORT_DESK'` into 13 `workflow_transitions` rows | Row 16 |
| Row 57 — `SCHEDULED_REPORT`'s `subject_template` | nothing behind it | — |

Row 57 is a third kind again: **an invariant the application enforces and a
migration wrote around.** `notification_templates` is B-022's master and its
service refuses an EMAIL template with no subject; row 56 inserted one directly
in SQL. Nothing in the database objected, because the rule has never been a
`CHECK` — it lives in `NotificationTemplateService`, where it can carry a
sentence explaining itself to whoever hits it.

Worth stating as a general hazard rather than one file's slip: **every seed in
this directory writes past the service layer that owns the table it writes to.**
Where a rule is enforced in Java and not in DDL, a migration is the one caller
that can break it, and the failure surfaces on the next *edit* rather than on
the insert. The standing guard is `NotificationTemplateMasterIT`, which reads
this particular invariant back out of a real database; it did its job here, one
merge later than would have been ideal.

Row 18 is a different kind of correction — not a value written back by a later
file, but a constraint that never held what it appeared to. `uq_holidays
(holiday_date, project_id)` reads as "one holiday per date per scope", and for
project-scoped rows it is. For **org-wide** rows, where `project_id IS NULL`,
MySQL compares NULLs as distinct and the index permits unlimited duplicates.
Nothing failed; the second insert simply succeeded. Found by calling the
endpoint against a real database rather than by reading the DDL, which is the
only way this class of defect surfaces.

Row 13, written later and aware of the rename, seeded `owner_role` correctly —
which is why the defect sat in exactly one table.

Neither column has an FK to `roles.code`, so neither would have failed loudly.
`SeedDataIT` now asserts referential integrity across those unenforced string
references; that is the standing guard, not this paragraph.

---

## 4. Adding a migration

1. Name it `V<YYYYMMDD>_<HHMM>__<snake_case>.sql`. Timestamps make collisions
   impossible; sequential numbering does not.
2. **Add a row to the table in §1.** Append it in version order.
3. If it seeds data, keep it to *your* stream's tables. One seed file per
   stream — never a shared one, or four developers conflict on every merge.
4. If a correction: state in the file header what it corrects and why the
   original is not being edited, then add a row to §3.
5. Migrations touching `tickets`, `ticket_history`, `ticket_effort_logs` or
   `ticket_stage_transitions` need **Stream A's review** — the append-only
   guarantees live there.

Before adding a seed that references another stream's data, read §2. If your
reference is a plain string rather than an FK, the database will not catch you
getting it wrong — add an assertion to `SeedDataIT` instead.

---

## 5. Seed data is not fixture data

Everything in §1 is **seed data**: reference rows the product cannot boot
without — roles, statuses, task types, workflow templates. It belongs in every
environment, production included.

**B-007's 200-ticket fixture corpus is not that.** It is dev and test data, and
it must never reach production.

`spring.flyway.locations` is a single `classpath:db/migration` in both `api` and
`worker`, so **nothing currently separates the two** — a fixture migration
dropped into this directory would ship. B-007 needs its own location
(`db/fixtures`) enabled per profile rather than another row above.

Flagged here rather than solved here: B-007 is the task that has to choose, and
this is the file where the choice becomes visible.

---

## 6. Related

- [`README.md`](README.md) — directory rules (Stream A)
- `docs/TEAM-PLAN.md` §7.1, §7.3 — migration and seed conflict rules
- `docs/streams/STREAM-B-MASTERS.md` — B-001 … B-008
- `SeedManifestTest` / `SeedDataIT` — `backend/domain/src/test/java/…/seed/`
