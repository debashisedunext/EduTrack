# Stream A — Platform & Security · Task Backlog

**Milestones:** M0 (foundation) · M1 (auth + scope guard) · M2 (immutability core) · M6 (dashboard + reports)
**Owner:** Shivendra · `shivendra.edunext@gmail.com` · @shivendraedunext-18
**Branch prefix:** `feat/platform/…`
**Owns:** `backend/common/`, `backend/domain/db/migration/`, `backend/api/security/`, `backend/api/feature/{auth,dashboard,reports}/`, `docker-compose.yml`, `.github/`, `frontend/src/features/{auth,dashboard,reports}/`

> **You are the critical path.** Streams B, C and D are blocked on A-012 (`dev-noauth` profile, due day 10) and on the baseline schema. Ship those before anything else on this list.

> Cross-stream sequencing — who is waiting on you and what to do if you are blocked — is in [`../DEPENDENCIES.md`](../DEPENDENCIES.md).

---

## Sprint 0 — weeks 1–2

- [x] **A-001** Maven multi-module skeleton: `common`, `domain`, `api`, `worker`. `api` and `worker` both depend on `domain`; neither depends on the other.
- [x] **A-002** `docker-compose.yml` — MySQL 8.4, Redis 7, MinIO, Mailpit. `utf8mb4`, `connectionTimeZone=UTC`. *(verified 2026-08-07 — `docker compose ps` shows all four services healthy, `minio-init` ran to completion)*
- [x] **A-003** Flyway baseline 1/5 — identity: `users`, `roles`, `permissions`, `role_permissions`, `user_roles`, `projects`, `project_members`. *(blueprint §8.2)* — *`V20260805_1024__baseline_identity.sql`; confirmed applied (`flyway_schema_history.success = 1`) against the live MySQL container*
- [x] **A-004** Flyway baseline 2/5 — tickets: `tickets`, `ticket_cycles`, `ticket_history`, `ticket_effort_logs`, `ticket_watchers`, `ticket_links`. *(`V20260805_1041__baseline_tickets.sql`; confirmed applied)*
- [x] **A-005** Flyway baseline 3/5 — workflow: `workflow_templates`, `workflow_stages`, `ticket_stage_transitions`. *(§4A.5 — note `can_return_to` becomes `JSON`)* — *(`V20260805_1042__baseline_workflow.sql`; confirmed applied)*
- [x] **A-006** Flyway baseline 4/5 — clients & content: `clients`, `client_contacts`, `client_projects`, `ticket_comments`, `ticket_attachments`, `email_log`, `import_batches`. *(§4B.7)* — *`V20260805_1530__baseline_clients_content.sql`; confirmed applied (runs last by version, as designed)*

  > **Runs fifth, not fourth.** `clients.sla_policy_id` and `email_log.template_id` reference `sla_policies` and `notification_templates`, both created by A-007 (`V20260805_1106`). The timestamp is therefore later than A-007's, which is what Flyway orders on — the task number is not the run order.
  > **`email_log` carries a `next_attempt_at` column that blueprint §4B.7 does not.** PLAN.md §2.2 makes this table the outbox queue in place of BullMQ, and backoff needs somewhere to live. Without it D-010 has a log, not a queue.
  > **No immutability trigger on `ticket_comments`, deliberately.** The 5-minute edit window (C-033) and the tombstone flag both need `UPDATE`. `original_body` preserves the pre-edit text and the service layer enforces the rest. The trigger-protected set stays exactly `ticket_history`, `ticket_effort_logs`, `ticket_stage_transitions`.
- [x] **A-007** Flyway baseline 5/5 — masters & ops: `task_types`, `priorities`, `statuses`, `workflow_transitions`, `holidays`, `resource_leaves`, `notification_templates`, `notifications`, `chat_threads`, `chat_participants`, `chat_messages`, `audit_logs`. *(`V20260805_1106__baseline_masters_ops.sql`; confirmed applied)*
- [x] **A-008** Immutability triggers — two per table (MySQL needs separate UPDATE and DELETE triggers) on `ticket_history` and `ticket_effort_logs`; the seal-only trigger on `ticket_stage_transitions`. *(PLAN.md §3.5, §3.6)*
  > **Verified working on MySQL 8.4** — a `BEFORE UPDATE`/`BEFORE DELETE` trigger raising `SIGNAL SQLSTATE '45000'` rejects both, and the row survives intact.
  > **Re-verified 2026-08-07** directly against the live container: UPDATE and DELETE on `ticket_history` both rejected; the seal-only trigger on `ticket_stage_transitions` let `exited_at` NULL→timestamp through once, then rejected a second write. All probes run inside a transaction that was never committed, so no test data was left behind.
  > **Delimiter gotcha:** a `BEGIN … END` body contains `;`, which terminates the statement early in `mysql -e` and in some Flyway configurations. The one-line form takes no delimiter handling at all and is preferred wherever the body is a single `SIGNAL`:
  > ```sql
  > CREATE TRIGGER trg_hist_no_update BEFORE UPDATE ON ticket_history
  >   FOR EACH ROW SIGNAL SQLSTATE '45000' SET MESSAGE_TEXT = 'Immutable: cannot update';
  > ```
  > The seal-only trigger on `ticket_stage_transitions` **does** need `BEGIN … END` (it has conditional logic), so that migration needs Flyway's `SET statement_delimiter` or a dedicated `.sql` with explicit `DELIMITER`.
- [x] **A-009** Generated columns + indexes replacing PostgreSQL partial indexes: `pcd_open`, `current_ticket_id`. Plus `FULLTEXT` on `tickets(title, description)`. *(PLAN.md §3.3, §3.8)* — *confirmed present via `SHOW CREATE TABLE` on the live schema, 2026-08-07*
- [x] **A-010** Two DB users: `edutrack_app` (no DDL; **`INSERT, SELECT` only** on the three append-only tables) and `edutrack_migrate` (DDL, deploy step only). — *`docker/mysql-init/01-users.sql` + `docker/grants/apply-app-grants.sql`; confirmed live 2026-08-07: `edutrack_app` holds no DB-level grant (fail-closed), table-level grants are `Select,Insert` on `ticket_history`/`ticket_effort_logs`, `Select,Insert,Update` on `ticket_stage_transitions`; `edutrack_migrate` has full DDL on `edutrack.*` with no GRANT OPTION*
- [x] **A-011** CI pipeline — build, test, Testcontainers integration tests, OpenAPI staleness check, frontend build. — *`.github/workflows/ci.yml` covers migration-guard, backend (`mvn verify`, runs `SchemaIntegrationIT`), frontend, OpenAPI contract staleness, and a packaged-jar smoke test*
- [x] **A-012** 🔴 **`dev-noauth` Spring profile** — injects a configurable fake principal (role, projects, reportees). **Rejects startup outside `local`**; disabled in CI. *Due day 10 — B, C and D are blocked without it.* — *`api/security/dev/`, activate as `local,dev-noauth`; refusal + injection + dormancy all covered by tests*
- [x] **A-013** Negative tests proving triggers reject `UPDATE` and `DELETE` on each protected table. — *`SchemaIntegrationIT.java` covers update/delete rejection on `ticket_history` and `ticket_effort_logs`, delete rejection and non-seal-column rejection on `ticket_stage_transitions`, and the exactly-once seal; recently hardened by Debashis's `9de2ed1` fix so its fixtures don't collide with Stream B's upcoming role seed data. Not re-run here (no local `mvn`), but nothing in this session contradicts it.*

**Exit:** `docker compose up` yields a migrated DB; `mvn verify` green including A-013.

---

## M1 — Authentication & the scope guard · weeks 3–7

### Authentication
- [x] **A-020** Login endpoint — Argon2id (64 MB, 3 iterations), constant-time, **generic error messages** that never reveal which field failed. *(§10.1)*
  > `POST /api/v1/auth/login` in `api/feature/auth/`; hashing in `common/security/PasswordHashing` per TEAM-PLAN §6. Argon2id parameters are constants, not configuration — they are baked into every stored hash, so changing them is a re-hash migration rather than a config edit, and a tunable invites someone to lower it.
  > **Uniform timing.** An unknown username is verified against a decoy hash built once at startup, so every outcome costs one full KDF. Without it the endpoint answers "does this account exist?" in microseconds versus ~100 ms and becomes a staff directory. Asserted structurally (the encoder is invoked on both paths) rather than with a stopwatch, which would be flaky on a loaded runner and get muted.
  > **One failure mode.** Unknown user, wrong password and deactivated account all raise `InvalidCredentialsException`, which carries no field, and map to one `https://edutrack/errors/invalid-credentials` problem. `AuthLoginIT` asserts the bodies are byte-identical.
  > **No JPA entity.** B-005 (Ayush) owns the object model and has not started; auth reads a flat projection through `JdbcClient` instead of defining `User` first and making Stream A the de-facto owner of Stream B's mapping.
  > **Deliberately not included:** no token is issued (A-022) — the response omits `accessToken`/`expiresIn` rather than sending null, so A-022 is purely additive. No refresh cookie (A-023), no `failed_attempts`/lockout or per-IP rate limit (A-021), no forced-change redirect (A-026), no TOTP (A-029), no `last_login_at` or `LOGIN_SUCCESS` audit row (A-021/A-071). **Until A-021 lands there is no cap on guess volume — Argon2id's cost is the only brake.** S-01 (A-030) cannot complete a login against this endpoint yet.
  > **Two changes outside Stream A, both flagged for sign-off rather than made quietly:** springdoc 2.6.0 → 2.8.9 in `backend/pom.xml` (2.6.0 calls a `ControllerAdviceBean` constructor Spring 6.2 removed, so building the OpenAPI document throws `NoSuchMethodError` as soon as the app contains any `@ControllerAdvice` — latent for everyone, not just here), and the datasource exclusions dropped from `ContractConformanceTest` (Stream D) so the context can build a bean that reads the database. Both need Debashis.
- [x] **A-021** `failed_attempts` counter, 15-minute lockout at 5, email to Admin on lockout.
  > **The lock is reported only to someone who already knows the password.** The contract is explicit — lockout is "reported as `account-locked` only *after* correct credentials, for the same reason". So the password is verified first; a wrong password against a locked account still gets the generic 401. Checking the lock earlier would confirm the account exists to a stranger, and would answer faster than an unlocked one, leaking the same fact through timing.
  > **`locked_until` is an expiry, not a flag.** The lock lapses on its own with nothing scheduled to clear it. Applying a lock also zeroes the counter, so the window after it lifts is a fresh five rather than one mistake from re-locking.
  > **The writes live in `LoginAttemptRecorder`, a separate bean, with `REQUIRES_NEW`.** Recording a failure ends in a thrown exception and Spring rolls back on any unchecked throw — increment and throw in one transaction and the increment is undone, so no account would ever lock. The obvious fix (annotate a method on the same class) silently does nothing, because `@Transactional` is proxy-based and a self-call bypasses the proxy. Both failure modes leave every mocked unit test green, which is why `AuthLoginIT` proves the counter against a real database instead.
  > **Admin mail goes through Stream D's outbox** (`OutboxEnqueuer`), so it commits with the lock — an announced lockout that rolled back, or a lock with no announcement, are both worse than either alone. Two stated limits: D-035 throttles to one mail per recipient per minute and these carry no ticket id, so a burst of lockouts inside a minute reports the first only; and there is no ADMIN role to find until B-001 seeds it, so today this logs a warning and sends nothing. **The lock still applies — announcing it is best-effort, holding it is not.**
  > **Still open after this: the per-IP throttle, and the cost of a rejected attempt.** A per-IP limiter was built and then **deliberately removed** — keying on IP puts an entire office behind one NAT address into a single shared budget, so one colleague's typos can lock out everyone around them. Shipping that felt worse than the gap it closed. Two consequences to carry forward into **A-074**:
  > • A locked account still runs the full Argon2id verify before the lock is checked (~175 ms, 64 MB per attempt — measured), so the login endpoint remains the cheapest DoS target in the system. Short-circuiting on the lock is **not** the fix: it would make locked accounts answer faster than unlocked ones and leak which accounts exist.
  > • Nothing caps guess *volume*. The per-user counter stops repeated guessing at one account; it does nothing about one password sprayed across a thousand usernames, where no account ever reaches five failures.
  > A per-IP limit is the wrong shape for both. The design that actually fits is two-dimensional — cap attempts per `(IP, username)` **and** cap the number of *distinct* usernames tried per IP — so a clumsy colleague only ever affects their own account while a spray attack trips on its real signature. Recorded here rather than built, because it is A-074's scope and wants its own review.
  > **Fixed `RealtimeRelayIT` (Stream D) — flagged for Debashis.** D-012 merged before A-020 and excludes `DataSourceAutoConfiguration`; A-020's repository needs a `JdbcClient`, so `develop` has been red since that merge. Neither PR could have seen it — the test did not exist when A-020 branched. Same three-line fix as `ApplicationSmokeTest` and `ContractConformanceTest`.
- [x] **A-022** JWT access token, 15 min, claims `sub`, `role`, `permissions[]`, `projects[]`, `reportees[]`, `iat`, `exp`, `jti`.
  > `AccessTokenIssuer` (`api/feature/auth/`) mints; `JwtEncoderConfig` (`api/security/jwt/`) wires the single `JwtEncoder` bean, mirroring `PasswordEncoderConfig`. `AuthController` composes `AuthenticationService` (identity) with `AccessTokenIssuer` (token) rather than the two merging into one class — credential verification keeps its own tight timing/failure-shape contract, untouched by this task.
  > **HS256, symmetric secret — confirmed over RS256.** One process signs and, from A-032, verifies; nothing else needs to check a signature without holding the secret, and there is no key-rotation/vault story until A-075. `edutrack.jwt.secret` follows the `DB_PASSWORD`/`EMAIL_WEBHOOK_SECRET` pattern: a committed placeholder for `local`/CI, `JWT_SIGNING_SECRET` required everywhere else. Revisit if a separate verifier service ever appears.
  > **`sub` is the numeric user id, not the username.** Every other claim (`projects[]`, `reportees[]`) is already id-based, and A-034's `ScopeResolver` wants an id to compare against `assigned_to`, not a name to look up.
  > **Scope stays exactly what A-020 already resolved.** No new query — `AccessTokenIssuer` reads straight off `AuthenticatedUser`, which was shaped for this from the start.
  > **Not in this task:** the refresh cookie (A-023), rotation/family revocation (A-024), logout/blacklist (A-025), and verification itself — no route rejects a missing or expired token yet, that is A-032's filter chain.
- [x] **A-023** Opaque refresh token, 7 days, HttpOnly + Secure + SameSite=Strict cookie, `jti` in Redis, device-bound.
  > `RefreshTokenIssuer` mints, `RefreshTokenStore` persists, `AuthController` sets the cookie. All in `api/feature/auth/` rather than split into `api/security/` the way A-022 was — the `JwtEncoder` went to `security/jwt/` because A-032's filter chain will need a decoder from the same key, whereas every future caller of this store (A-024, A-025) is inside `feature/auth`.
  > **Redis holds `SHA-256(token)`, never the token.** The raw value exists in exactly two places: the `Set-Cookie` header and the browser's cookie jar. Redis persists to disk and gets backed up, so keying on the raw value would mean an RDB file — or one `KEYS *` during an incident — is seven days of live sessions. SHA-256 and not Argon2id: the input is 256 bits from `SecureRandom`, so there is no dictionary to slow down, and a password-grade KDF would put ~175 ms and 64 MB on every refresh for a threat that cannot exist. A-027's reset tokens want the same treatment.
  > **Redis and not a `refresh_tokens` table.** The key carries its own TTL, so expiry is the storage layer's job. A table needs a scheduled delete, and the day that job fails is the day expired sessions quietly keep working — invisible until audited.
  > **Device binding is User-Agent, deliberately not IP.** An IP-bound token logs honest users out several times a day — a phone moving wifi→cellular, a NAT pool rotating, a VPN reconnect — and the fix everyone reaches for is to stop enforcing it. A User-Agent is stable for as long as the browser is and changes precisely when the cookie has been replayed elsewhere. Stored hashed: equality is the only operation, and a plaintext UA is a fingerprinting datum sitting in a cache for a week. It is a weak binding and is not claimed to be more — an attacker with the cookie can copy the header too.
  > **`familyId` is stamped here, at login, though A-024 uses it.** A family is the chain of rotations descending from one login, so it is born at login; a token issued without one could never be joined to its siblings afterwards. One family per login, so revoking a compromised family ends *that* session rather than logging the user out everywhere.
  > **`Path=/api/v1/auth`, narrower than the contract's illustrative `Set-Cookie` line.** Only `/auth/refresh` and `/auth/logout` ever read it; scoping to `/` attaches a seven-day credential to every ticket list and attachment download, and to every proxy log along the way. Invisible to code — the cookie is `HttpOnly` and browser-managed — but **flagged for Debashis** as a deviation from `contracts/openapi.yaml`'s worked example, which shows no `Path`.
  > **An unreachable Redis shortens the session; it does not fail the login.** Same trade `RealtimeRelay` already made for the same dependency — failing would take the whole product down over a degradation confined to session length. It fails towards *less* access (15 minutes, then re-login), so nothing is weakened, only shortened. Logged at ERROR because a fleet in this state presents to users as "the app keeps logging me out", which is a support ticket rather than an alert.
  > **`secure-cookie: false` refuses to boot outside `local`** (`RefreshTokenConfig`, same posture as `DevNoAuthConfig`). The knob exists because Safari on `http://localhost` and any plain-http LAN address used to test on a real phone reject a `Secure` cookie outright, so those developers could not hold a session at all. Without the guard, someone eventually ships `false` — a defect with no error, no log line and no failing request.
  > **`SameSite=Strict` has a real cost, accepted.** A user arriving from a link in a notification mail cannot refresh on that first navigation and looks logged out until they navigate once inside the app. `Lax` avoids it and is what most sites pick; Strict is what makes CSRF against `/auth/refresh` structurally impossible rather than a thing a token has to defend against, which is worth one extra click on an internal tool.
  > **Not in this task:** `POST /auth/refresh` and rotation with family revocation (A-024), logout and the access blacklist (A-025), and *enforcement* of the device binding — the fingerprint is recorded but the only place it can be checked is the endpoint that does not exist yet. `StoredRefreshToken#matchesDevice` is that hook, present with its own test so A-024 does not have to reinvent it. Same shape as A-022, which minted a token nothing verified.
  > **`AuthLoginIT` gained a Redis container.** Without one the issuer degrades to no cookie and every A-023 assertion fails against a login that is working perfectly.
- [x] **A-024** 🔴 **Refresh rotation with family revocation** — reuse of a consumed token means theft: revoke the whole family, force re-login.
  > `RefreshRotationService` holds the sequence, `AuthController#refresh` is the route, and `RefreshTokenStore` grew the three primitives it needs. All in `api/feature/auth/`, following A-023.
  > **Deleting the old token on rotation is what makes reuse undetectable, and is the mistake the whole task turns on.** A deleted key and a key that never existed are the same absence, so a stolen token replayed *after* the victim's browser has rotated looks exactly like a typo — and §10.1's reuse rule becomes unenforceable with no symptom. Rotation therefore writes a `refresh-consumed:<sha256>` marker holding the family id, TTL'd to what remained of the token's own life. The marker is the memory that the token was real and spent.
  > **Family revocation is a tombstone, not a sweep.** When reuse is detected, the *other* members of the family are still live in Redis. Finding and deleting them needs a family→members index kept in step with every rotation, and the day it drifts a revoked token keeps working. `refresh-family-revoked:<familyId>` inverts that — nothing has to be found, and the check is positive: a family is usable only while nothing says it isn't. Its TTL is deliberately the full 7-day maximum rather than the family's real remaining life; a tombstone that expires *early* resurrects a family revoked for theft, and carrying the exact deadline would mean a second copy of an expiry that can disagree with the record.
  > **The claim is an atomic `DEL`, and that is the load-bearing line.** Two requests can carry one cookie — two tabs waking, or a retry of a refresh whose response was lost. Read-then-delete lets both through and forks one session into two. `DEL` reports whether it removed anything, so exactly one caller is ever told it won.
  > **The loser of that race is treated as reuse, and the honest double-submit pays for it.** Server-side a second presentation of a consumed token is indistinguishable from a replay, and §10.1 carves out no exception, so the family dies and the user re-logs in. The usual softener is a grace window — re-issue the successor if the reuse is within N seconds, same device — and it was **considered and not built**: it weakens the one guarantee this task exists to provide, and loosening later with evidence is far easier than tightening. **The frontend must single-flight its refresh** (one in-flight call, other callers await it) or a two-tab app will log itself out; that is the mitigation, and it belongs to whoever builds the interceptor.
  > **A device mismatch refuses the token but does *not* revoke the family — the deliberate asymmetry.** Chrome rewrites its `User-Agent` on every auto-update, which inside a seven-day window is close to routine; revoking on it would sign people out after a browser update and file it as theft. A control that fires on benign events gets switched off by whoever is on support that week. Refusing one token costs one re-login, the right price for a binding `StoredRefreshToken` openly calls weak — and a real thief copies the header anyway, so revoking here buys almost nothing against the actual threat. The presented token *is* discarded though, and that part matters: otherwise an attacker retries with one `User-Agent` after another until one matches and the fingerprint becomes a guessing game they win. Discarding caps it at one attempt, and costs the benign case nothing — a token whose recorded UA no longer matches was already permanently unusable.
  > **A-021's lockout is not consulted on refresh.** Refreshing is not a login. Honouring the lock would let any outsider end an employee's active session by typing a wrong password five times at their username — the protection becomes the attack. The lock governs the login form, where guessing happens.
  > **Expiry does not slide.** The successor inherits `expiresAt`, not a fresh seven days. Sliding expiry makes the session unbounded — a token used weekly never expires, and neither does a stolen chain kept warm — so the only thing that would ever end it is a logout, which an attacker has no reason to perform. The family gets a hard deadline set at login; the cost, accepted, is that an actively working user is signed out at day 7 rather than carried past it. **A-025's "absolute 12 h" tightens this and supersedes the 7-day bound.**
  > **Identity is re-read from the database on every refresh, not replayed from the stored record** (`AuthenticationService#resolveActiveUser`, `AuthUserRepository#findById`). A deactivation, a role change or a project reassignment lands at the next refresh — at most 15 minutes stale instead of seven days.
  > **Reuse is the one auth failure with its own `type` URI** (`refresh-token-reuse` vs `invalid-refresh-token`). Every other refusal in this package is uniform to deny an outsider information; this path is reachable only by someone holding a token we genuinely issued *and* consumed, so they already know it was real and the response tells them nothing. The gain is on the other side — the victim learns they were compromised, instead of seeing "session expired" and shrugging. Everything else (missing, expired, unknown, revoked family, wrong device, deactivated) shares one body.
  > **It fails closed in both directions.** Every write happens after the claim, so a failed marker or successor write ends the session rather than issuing anything. Read failures are **not** converted to 401: a 401 is a statement about the credential, and saying it because Redis blinked signs out the fleet and tells each of them, falsely, that their session was invalid. Those propagate as 5xx, which is retryable and leaves the cookie alone. Note this is the opposite of A-023's login degradation, and correctly so — there was a session to preserve there, here there is not.
  > **Every refusal clears the cookie.** Without it a browser holding a revoked-family token replays it forever: a 401 loop the user cannot break without clearing cookies by hand.
  > **Two contract deviations flagged for Debashis** rather than edited into `contracts/openapi.yaml` (Stream D's file), following A-023's precedent. The `/auth/refresh` `200` has no `Set-Cookie` header documented though the endpoint sets one, and the `401` does not mention that reuse carries a distinct `type`. Both are documented on the springdoc side; `ContractConformanceTest` compares path+verb only, so neither breaks the build.
  > **Not in this task:** logout and the access blacklist (A-025). A revoked family still leaves the last-issued *access* token valid for up to its remaining 15 minutes — nothing reads a blacklist yet and no filter chain exists (A-032). Until then "force re-login" means "issue nothing further", not "cut the current call off mid-flight". Worth stating plainly because it is the natural assumption on reading "revoke the whole family".
  > **Fixed a ~1%-flaky A-023 test** — `RefreshTokenIssuerTest.tokenValueIsOpaque` asserted a random 43-char base64url token `doesNotContain("42")`, which happens by chance about one run in a hundred and did so during this task. It tested the RNG, not the code; replaced with a decoded-length check that establishes the same property deterministically.
- [x] **A-025** Logout — delete refresh `jti`, blacklist access `jti` in Redis until natural expiry. Idle timeout 30 min, absolute 12 h.
  > `LogoutService` + `AccessTokenBlacklist` + `AuthController#logout` in `api/feature/auth/`; `JwtDecoderConfig` in `api/security/jwt/`; the two session bounds in `SessionProperties`, enforced in A-024's `RefreshRotationService`.
  > **Both halves of logout are required and each alone is a bug.** Deleting only the refresh token leaves the access token working for up to fifteen more minutes — on a shared machine that is fifteen minutes of someone else's session *after* they pressed Sign out, which is exactly the window the user believes they just closed. Blacklisting only the access token leaves the cookie able to mint a new one on the next tick, so the logout undoes itself.
  > **`JwtDecoderConfig` is new, and A-022 predicted the wrong first caller.** A-022 assumed A-032's filter chain would be the first thing to *read* an access token; `/auth/logout` got there first, because it is the only endpoint the contract does not mark `security: []`. The decoder is therefore proper wiring in `security/jwt/` rather than parsing inside a controller — A-032 consumes this same bean instead of introducing a second one on the same key. `macAlgorithm(HS256)` is pinned explicitly: without it Nimbus honours whatever algorithm the token's own (attacker-controlled) header names. Expiry and issuer are the decoder's validators, not a caller's `Instant` comparison — the same omission that is a small bug here is a total auth bypass in A-032.
  > **Verification has to be real, not a decode.** Accepting an unsigned token would make this an endpoint for logging *other people* out: forge any `jti`, blacklist a stranger's access token. `AuthLogoutIT` asserts a forged token revokes nothing and deletes nothing.
  > **Logout does not revoke the family, and that is the single most important absence here.** Signing out of a laptop must not sign the user out of their phone. Family revocation is A-024's response to *theft*; reusing it for an ordinary sign-out makes the two indistinguishable in the logs and in the user's experience. Tested from both directions — the other device still refreshes, and no tombstone is written.
  > **`discard()`, never `claim()` + `markConsumed()`.** A consumed marker is precisely what turns a later presentation into *detected reuse*, so recording one at logout would mean a stale tab retrying after sign-out revokes the whole family for something that was never theft. `AuthLogoutIT` pins the resulting `type` as `invalid-refresh-token`, not `refresh-token-reuse`.
  > **The access token is mandatory; the refresh cookie is not.** The asymmetry is deliberate: the access token is *how we know whose session to end*, while the cookie merely names a token to delete. The people most in need of a working Sign out are those whose session is already half-broken — cookie expired, already rotated, or withheld by `SameSite=Strict` on that navigation. Refusing them would leave the access token live, which is the one outcome logout must never produce. Idempotent for the same reason: every failure mode here leaves someone logged in who asked not to be.
  > **The blacklist TTL is the token's own remaining life, which is what keeps it self-limiting.** An entry only has to outlive the token it revokes; a second later the token is refused as expired anyway. So the blacklist can never hold more than one access-token-lifetime of logouts however many people sign out, and needs no sweeper. A fixed or generous TTL grows without bound and eventually becomes the thing that pages someone. Keyed on `jti` — already a random UUID and useless to whoever holds it — not on the token, which is a bearer credential and must not sit in a cache that gets snapshotted.
  > **Idle (30 min) and absolute (12 h) resolve an apparent contradiction with A-023's 7-day token.** They are not the same thing: the 7 days is how long the server **remembers** a token, the two bounds are how long it will **honour** one. Remembering for longer is deliberate — a stolen token replayed on day three is refused either way, but only a *remembered* one is refused as **theft**, revoking the family with an alert, rather than shrugged at as an unrecognised value. The visible cost is a browser holding a cookie the server will not accept; harmless, since it is `HttpOnly` and scoped to `/api/v1/auth`, and it buys a theft-detection window six days longer than the session.
  > **`sessionExpiresAt` is stamped at login and inherited by every rotation, exactly like `familyId`.** Recomputing it on rotation would push the 12-hour deadline forward every fifteen minutes, so an active session would never reach it and the bound would exist in the code without ever firing. Conversely `issuedAt` **is** re-stamped, because it doubles as "last activity" for the idle window — that equivalence holds only because rotation mints a fresh record rather than updating one in place, and `StoredRefreshToken#isIdleAt` says so in case that ever changes.
  > **Neither timeout revokes the family.** An idle session and a day-old one are the most ordinary things that happen to a session; treating either as theft would fire the alert constantly and teach everyone to ignore the one signal that matters. Same bucket as A-024's device mismatch.
  > **A refused logout does not clear the cookie**, unlike A-024's refresh refusals. A logout that could not authenticate has ended nothing, and stripping the cookie anyway would let an unauthenticated request half-end a session the caller was never proven to own.
  > **Not enforced yet, and this is the gap to carry forward.** Nothing reads `AccessTokenBlacklist` — the reader is A-032's filter chain, which must call `isRevoked` on every authenticated request. Until then a blacklisted token is poisoned and no route notices, so today logout's real effect is "this session cannot be renewed" plus a correct record that A-032 will act on. Same shape as A-023's device fingerprint, which A-024 then enforced without having to invent it. `AuthLogoutIT` asserts the blacklist directly and says why it cannot yet assert a rejection.
  > **Contract deviation flagged for Debashis** (Stream D owns `contracts/openapi.yaml`, so not edited quietly): `/auth/logout`'s `204` does not document the `Set-Cookie` that clears the refresh cookie, and its `401` does not mention the `invalid-access-token` type. Documented springdoc-side; `ContractConformanceTest` compares path+verb only, so the build is unaffected.
- [x] **A-026** Forced password change on first login — `must_change_password`. **S-03**
  > Backend only — S-03's screen rides with A-030 (`frontend/src/features/auth/` does not exist yet). `PATCH /api/v1/me/password` in a new `MeController` (the contract puts it under `/me`, not `/auth` — `ContractConformanceTest` compares path+verb, so it cannot hang off `AuthController`). `PasswordChangeService` verifies `currentPassword`, rejects a same-password resubmission (`PasswordUnchangedException` — the whole point of a forced change), writes the hash and clears the flag in one statement (`AuthUserRepository#updatePassword`), and revokes the caller's own access token so a stale `mustChangePassword` claim can't lock them out after the fact.
  > **The flag now travels in the JWT**, not just the login body — `AccessTokenIssuer#MUST_CHANGE_PASSWORD_CLAIM`, emitted only when true (absence = not required, the fail-open direction, since every token minted before this task has no such claim). `PasswordChangeGate` is the enforcement decision (three-route allowlist: `/me/password`, `/auth/logout`, `/auth/refresh`) but **nothing calls it yet** — same "hook built, enforced at A-032" shape as A-023's device fingerprint and A-025's blacklist. `PasswordChangeRequiredException` → 403 (not 401, or the frontend's interceptor loops through a login that keeps succeeding) is wired into `AuthExceptionHandler` and ready for the chain to throw it.
  > Extracted `AccessTokenVerifier` out of `LogoutService` (A-025's inline bearer check) since this task needed the identical five lines — one implementation for A-032 to replace instead of two.
  > **Deliberately not done here, and named rather than hidden:** composition rules and no-reuse-of-last-3 (A-028 — needs a `password_history` table that doesn't exist) and rate-limiting `currentPassword` guesses (A-074 — A-021's lockout is intentionally *not* fed from here, or a stolen token could lock the real user out of login). Only the contract's 8–128 length bound is enforced today.
  > 89 unit/slice tests + 14 new `AuthPasswordChangeIT` cases against real MySQL/Redis, all green; full auth suite (234 tests) has no regressions; `ContractConformanceTest` shows `me 1/2` implemented with no undocumented-route or append-only failures.
- [x] **A-027** Forgot/reset password — single-use, 30-min TTL, hashed at rest. **S-02**
  > Backend only — S-02's screen rides with A-030, like S-03 did. `POST /auth/forgot-password` and `POST /auth/reset-password` on `AuthController` (both already in the reviewed contract, both `security: []` — a user who has forgotten their password has nothing to authenticate with). New `password_reset_tokens` table: `token_hash CHAR(64)` and never the token, `expires_at`, and single-use enforced by `used_at` rather than a delete — a deleted row cannot tell "already spent" from "never existed", and the former is what an investigation into "someone else reset my password" needs to see.
  > **Always 202, whatever the address.** Unknown and deactivated accounts are accepted and silently ignored; a 404 for an unknown address turns the endpoint into a staff directory. The rate-limit budget is spent in the controller **before** the lookup, so throttling cannot become the oracle the 202 exists to prevent. Timing is the one channel left open (an unknown address returns after one indexed SELECT) — named rather than hidden, and far smaller than the ~175 ms gulf A-020's decoy hash closes.
  > **"All sessions revoked" is a cutoff, not a family index.** `RefreshTokenStore.revokeSessionsFor` records one instant per user; `RefreshRotationService` refuses any token issued before it. A user→families set would have to be written at login and kept in step with every rotation, and the day it drifts a session that was supposed to be revoked keeps working — for the user who just reset their password *because* they believed they were compromised. Same tombstone reasoning `revokeFamily` already documents. Reaches refresh tokens only: an access token already minted survives its remaining ≤15 min, closing when A-032's chain reads A-025's blacklist.
  > **`429` needed a rate limiter, since none existed anywhere.** Two-dimensional (3/address/15 min, 40/client/30 min) because each catches what the other misses — per-address stops one victim being mail-bombed from many sources, per-client stops one source sweeping many addresses. Not deferred to A-074 like login's: an unlimited "email this address a reset link" endpoint is a mail cannon, and the honest user needs it once, so a limit low enough to stop abuse costs them nothing. Degrades open if Redis is down — refusing account recovery while the cache is unhealthy is the worse failure.
  > **Bug found by its own test:** Jakarta's `@Email` rejects surrounding whitespace, so an address pasted with one trailing space answered 400 before normalisation ran. Trimmed in the record's compact constructor so validation sees the trimmed value.
  > ⚠️ **Not production-ready until D-030, and the reason is in this branch.** `email_log` has no body column and template rendering (D-029/D-030, due 17 Sep) does not exist, so `SmtpMailTransport` still sends body = subject. The reset link therefore rides in the **subject line** — which `email_log.subject VARCHAR(300)` stores permanently, putting a live 30-minute credential in the database in cleartext and contradicting this task's own "hashed at rest". Acceptable while nothing deploys outside `local`; **D-030 is the release gate**. `ForgotPasswordService#resetSubjectCarryingTheLink` carries the delete-me instruction. Flagged for Debashis.
  > **Deliberately not here:** composition rules and no-reuse-of-last-3 (A-028 — still needs a `password_history` table). Only the contract's 8–128 bounds are enforced.
  > 234 auth tests green including `ContractConformanceTest`; 12 new `AuthPasswordResetIT` cases against real MySQL/Redis prove single use, expiry, enumeration-proofing, and that a reset genuinely kills a session established earlier on another device. `SeedManifestTest`/`SeedDataIT` green. Verified end to end against the live stack — mail delivered through the worker to Mailpit with a working link.
- [x] **A-028** Password policy — min 8 with upper/lower/digit/symbol, no reuse of last 3, optional 90-day expiry. *(§10.3)*
  > Closes the gap A-026 and A-027 each deferred and named. Before this, both password-set paths enforced length only — `"aaaaaaaaaaaaaaaa"` was accepted.
  > **The policy is split by what each rule can see, not by taste.** Composition (upper/lower/digit/symbol) is a Bean Validation constraint, `@ValidPassword`, so springdoc emits it and orval generates the matching Zod — one rule enforced in the browser and on the server per D-4. No-reuse cannot be: it needs the account's identity and three database rows, which a constraint validator has no way to reach, so it lives in `PasswordPolicy`. Both paths — `PATCH /me/password` **and** `POST /auth/reset-password` — go through both, because recovering an account and changing a password from inside a session must not differ in what they accept or the weaker one becomes the way in.
  > **Not one four-lookahead regex.** `PasswordComplexityValidator` checks the four classes separately so the message can name the one that is missing — a regex can only ever answer "no". "Symbol" is defined by exclusion (not a letter, digit or whitespace) rather than a fixed ASCII list, which would silently refuse `£`, `€` and every currency mark outside someone's chosen eight; letters are `Character.isUpperCase`, not `[A-Z]`, so a non-ASCII alphabet still counts as letters.
  > **`password_history` is a table, not three columns on `users`.** Depth becomes a `LIMIT` rather than a migration, insertion an `INSERT` rather than a shift-along `UPDATE` that has to move three columns in the right order, and absence an empty result rather than three null checks. The **retired** hash is filed, never the incoming one — filing the new hash would put the live password in the window and quietly reduce the effective depth by one.
  > **The reuse loop deliberately does not short-circuit.** Returning on the first match would let a caller tell "matched the most recent" from "matched the oldest" by how long the refusal took, leaking the shape of someone's password history. Cost is one Argon2id verification per stored hash — ~0.5 s and 192 MB at depth 3 — so the check runs last, after length, composition and (on the change path) `currentPassword`. On the reset path it runs **before** the token is spent, so reaching for an old favourite does not burn the link and force a fresh mail.
  > **The 90-day expiry ships OFF, and that is a recommendation rather than an oversight.** §10.3 calls it optional; NIST SP 800-63B §5.1.1.2 advises against scheduled rotation because people answer a forced change by incrementing a digit — the organisation trades real entropy for the appearance of hygiene plus a quarterly support spike. Built because the blueprint asks and some compliance regimes require it. When on, it rides A-026's `must_change_password` rather than inventing a second forced-change pathway: derived at login from `password_changed_at`, so no write on the hot path, and the gate, the claim and `PATCH /me/password` all work unchanged.
  > **Fixed `SchemaIntegrationIT`, which `develop` was already failing.** A-027 added `password_reset_tokens` without updating the table-count tripwire — its author ran `SeedManifestTest` and `SeedDataIT` but not that class, so it fired on this branch instead. Both tables are now accounted for; the file's own suggestion to assert named tables instead of a count is worth taking, since the failure named neither table.
  > 355 auth + contract tests green; 13 new `PasswordPolicyIT` cases against real MySQL prove composition bites on both endpoints, that a password one change back is refused against real Argon2id hashes, and both window edges — three fillers still refuses, four releases. `SchemaIntegrationIT`, `SeedManifestTest`, `SeedDataIT` green.
  > **Contract note for Debashis** (Stream D owns `contracts/openapi.yaml`): the `Password` schema describes these rules in its `description` but carries no `pattern`, so the generated client does not enforce them. Adding one would close the D-4 loop; server-side is authoritative meanwhile.
- [ ] **A-029** 2FA — 6-digit TOTP, optional per user. **S-04**
- [ ] **A-030** Login screen — centred card, soft indigo gradient, strength meter on reset. **S-01**
- [ ] **A-031** Role-based post-login redirect: Admin/PM → Dashboard · Developer → My Tasks · Support → Ticket Queue · QA/Deployment → Stage Queue.

### The scope guard
- [ ] **A-032** Spring Security filter chain — token valid and unrevoked.
  > **Replaces `ScaffoldSecurityConfig`.** The scaffold ships a permit-all chain because `spring-boot-starter-security` with no `SecurityFilterChain` bean makes Spring's default lock *every* route — the packaged jar returns 401 for its own UI. That placeholder is `@ConditionalOnMissingBean`, so it disappears automatically the moment you define a real chain; you do not need to delete it.
  > **Your chain must permit the static assets** `SpaResourceConfig` serves — `/`, `/index.html`, `/assets/**`, `/favicon.ico` and the SPA's client-side routes. Miss them and the UI fails to load even for authenticated users, which presents as a blank page rather than an auth error.
- [ ] **A-033** Permission model + `@PreAuthorize` — `ticket.read`, `ticket.assign`, `master.write`, etc.
- [ ] **A-034** 🔴 **`ScopeResolver`** producing a JPA `Specification` per role — Admin unrestricted; PM/Support `project_id IN projects`; Developer/QA/Deployment `assigned_to = me`. Composed centrally into every ticket query, never per-controller.
- [ ] **A-035** Out-of-scope IDs return **404, not 403**, on `/tickets/{id}` and every detail route.
- [ ] **A-036** 🔴 **Permission test matrix** — parameterised suite, every role × every route, asserting allow/deny/404. A new route without a matrix entry fails the build.
- [ ] **A-037** ArchUnit rules — no controller reaches a repository directly; no `update`/`delete` method exists on append-only services.

**Exit:** all six roles log in and land correctly; matrix suite passes; a Developer gets 404 on another's ticket.

---

## M2 — Immutability core · weeks 8–9

- [ ] **A-040** Append-only services for the three protected tables — **`insert()` only**, no other method exists.
- [ ] **A-041** Canonical JSON serialiser — fixed key order, fixed timestamp format. Golden-file test, or the verifier cannot reproduce hashes. Lives in `common`.
- [ ] **A-042** 🔴 **Per-ticket hash chain** with `SELECT … FOR UPDATE` on the ticket row before each append. Global chaining forks under concurrency. *(PLAN.md §3.7)*
- [ ] **A-043** Compensating-entry pattern — `is_correction`, `corrects_entry_id`.
- [ ] **A-044** Nightly chain verifier in `worker`, admin alert on break, parallelised per ticket.
- [ ] **A-045** Concurrency test — N parallel appends to one ticket produce a single unbroken chain, no fork.

**Exit:** verifier passes on the seeded corpus; all mutation attempts rejected at the DB; no fork under load.

---

## M6 — Dashboard & reports · weeks 10–16

### Aggregation
- [ ] **A-050** `daily_ticket_stats` and `resource_daily_stats` summary tables.
- [ ] **A-051** 5-minute refresh worker. **Dashboard reads never issue live `COUNT(*)`.**
- [ ] **A-052** `/tickets/{id}/full` aggregated endpoint — one call, not a waterfall of six.
- [ ] **A-053** Cursor pagination + virtualised grid rendering beyond 200 rows.

### Dashboard — S-05
- [ ] **A-054** Shell, role-aware, with project/date/resource filters.
- [ ] **A-055** Widgets 1–6 — KPI cards with sparklines and animated count-up: total, open, closed, critical, delayed, reopened.
- [ ] **A-056** Widgets 7–12 — type donut, daily stacked area, velocity multi-line, resource load bar, priority bar, aging buckets.
- [ ] **A-057** Widgets 13–15 — calendar heatmap, SLA radial gauge, project treemap.
- [ ] **A-058** Widgets 16–19 — stage funnel, rework/ping-pong, avg time per stage (active vs idle), handoff latency. *Depends on Stream C's transitions.*
- [ ] **A-059** Widget 20 — client-wise volume. *Depends on Stream B's client master.*
- [ ] **A-060** **Every card and chart segment deep-links** to a pre-filtered list (`/tickets?status=OPEN&level=CRITICAL&assignee=me`).
- [ ] **A-061** Drill-down modal, slides from the right, CSV export. **S-06**
- [ ] **A-062** Developer dashboard variant — widgets 1–6, 9, 12 scoped to `assignee = me`, plus "My due today / this week".

### Reports — S-27
- [ ] **A-063** Reports hub — card grid, parameterised viewer, filters, chart + table.
- [ ] **A-064** Export engine — Excel, CSV, PDF.
- [ ] **A-065** Scheduled report email (daily/weekly/monthly).
- [ ] **A-066** Reports 1–6: resource scorecard, velocity, effort summary, SLA breach, task type analysis, reopen analysis.
- [ ] **A-067** Reports 7–12: date-wise, project health, aging, workload/capacity, stage funnel, stage cycle time.
- [ ] **A-068** Reports 13–18: rework analysis, deployment report, resource contribution, audit/compliance, client report, email delivery log.
- [ ] **A-069** Resource 360° profile. **S-28**
- [ ] **A-070** "Born critical vs became critical" report — uses `original_level`, the insight managers ask for immediately. *(§6)*

---

## Hardening · weeks 17–18

- [ ] **A-071** Audit Log Viewer — every login, permission change, master change, ticket action. Export only, never editable. **S-16**
- [ ] **A-072** Global search + ticket-ID deep link.
- [ ] **A-073** Performance — load test at 50,000 tickets, index review against real query plans, dashboard first paint under 1.5 s.
- [ ] **A-074** Security — CSRF on cookie routes, strict CSP, security headers, dependency and container scanning.
- [ ] **A-075** Go-live runbook, deployment, TLS, secrets in vault.

---

## Decisions you own

Answer before M2 (PLAN.md §5): **G-4** — does an auto-escalated level revert after closure? *(Recommended: no; keep `original_level` and report both.)*

Answer before M1: **is there an existing employee directory or SSO** that the Resource Master should sync from? If yes, `users` needs an external ID now, not later.
