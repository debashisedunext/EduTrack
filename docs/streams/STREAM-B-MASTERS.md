# Stream B — Masters & Clients · Task Backlog

**Milestones:** M0 (seed + entities) · M3 (all master screens) · then joins C on the ribbon, then M6 reports
**Owner:** Ayush · `ayush.edunext123@gmail.com` · @Ayushedunext
**Branch prefix:** `feat/masters/…`
**Owns:** `backend/api/feature/{masters,clients,imports,workflow}/`, `frontend/src/features/{masters,clients}/`

> **You own two decouplers the whole team depends on.** B-007 (ticket fixture corpus) unblocks D's SLA work and C's ribbon; B-021 (working-hours service) is a hard dependency for every SLA calculation D writes. Ship both early.

> Cross-stream sequencing — who is waiting on you and what to do if you are blocked — is in [`../DEPENDENCIES.md`](../DEPENDENCIES.md).

---

## Sprint 0 — weeks 1–2

*Starts day 3, once Stream A's baseline schema lands.*

- [x] **B-001** Seed: 6 roles + the full permission matrix from blueprint §2. QA and Deployment included — the ribbon cannot be represented without them.
- [x] **B-002** Seed: 11 task types (Change Request, Production Bug, Client Request, Future Release, Internal Bug, Client Bug, Server Issue, Network Issue, Browser Issue, Performance Issue, Other) with icon, colour, default level, default SLA. Plus 4 priorities with colour and escalation flag.
- [x] **B-003** Seed: statuses (New, In Progress, On Hold, Awaiting Info, Rework, Resolved, Closed, Reopened) + the `workflow_transitions` allowed-transition matrix per role.
- [x] **B-004** Seed: 3 workflow templates with their stages — Standard Dev Flow (8 stages), Support Fast-Track (5), Infra Flow (5). *(§4A.9)* — *`V20260807_1700`; 3 templates, 18 stages*
- [x] **B-005** JPA entities + repositories for the full model, built on A's schema. Feature-packaged, not layer-packaged. — *merged in PR #34*
- [x] **B-006** MapStruct base configuration. — *`api/config/BaseMapperConfig.java`; mappers opt in with `@Mapper(config = BaseMapperConfig.class)` and inherit Spring component model, constructor injection, and two compile-time gates: an unmapped target property and a lossy numeric conversion are both build errors. Partial updates ignore null source properties, so a `PATCH` cannot blank a field it never sent. Shared by all four streams — additive, no existing file behaviour changed.*
- [x] **B-007** 🔴 **Ticket fixture corpus** — 200 tickets across 3 projects, varied stages, iterations, cycles, breach states, effort logs and client attribution. *This is what lets D test the SLA scanner and C test the ribbon before either feature exists.* — *`api/feature/fixtures/`, a profile-gated `ApplicationRunner` (`local,fixtures` — never a Flyway migration, per SEED-MANIFEST.md §5). `ReferenceDataFixture` creates the reference data no seed provides — 3 projects (`CRM`/`PAY`/`WEB`, matching D-004's mock so both fixture worlds read as one company), 18 resources across all 6 roles, 8 clients with contacts, 4 org-wide `sla_policies` rows. `SingleTicketFixture` walks each ticket through its real workflow template via `WorkingHoursService` (B-024) — cycles, stage transitions, history, effort logs, sealed exactly like the real write path would. ~14% reworked, ~15% of closed tickets reopened into cycle 2, ~20% deliberately breached (reported 45-65 days back), ~40% client-attributed. `FixtureCorpusIT` (Testcontainers MySQL) proves the shape and idempotency. Hash-chain columns (`prev_hash`/`row_hash`) are left NULL — A-040/A-044 hasn't landed, and inventing an algorithm now risks conflicting with Stream A's; the corpus needs a backfill once it does.*
- [x] **B-008** Seed manifest with fixed load order. One seed file per stream, never a shared file. — *`db/migration/SEED-MANIFEST.md`, held by `SeedManifestTest` (a migration with no manifest row fails the build) and `SeedDataIT` (referential integrity across the string references MySQL does not enforce).*

  > **Building the register found a live defect, fixed in `V20260808_1400`.** `V20260807_1030` renamed the Support role code to `SUPPORT`; `V20260807_1100` (B-003) runs *after* it and re-seeded the old `SUPPORT_DESK` string into 13 `workflow_transitions` rows. `role_code` has no FK, so the rows inserted cleanly and simply matched nobody — the Support Desk role could make **no status transition at all**, including the `RESOLVED → CLOSED` and `CLOSED → REOPENED` moves that G-3 and blueprint §2 reserve for exactly Admin/PM/Support Desk. Undetected because C-014, the first code to read that table, has not landed. B-001's grants were unaffected (that seed ran before the rename and resolves codes to ids through a JOIN).

**Exit:** a migrated DB loads full seed data; entities compile against it; the fixture corpus renders realistic ribbons.

---

## M3 — Master data · weeks 3–9

### Resource & role
- [x] **B-010** Resource list — columns, filters by role/project/manager/status, bulk activate/deactivate, export. **S-07** — *`api/feature/masters/resources/` and `features/masters/resources/`, at `/masters/resources`. Three queries per page whatever the page size — the grid, then projects for its ids, then open-ticket counts for its ids — held by a test that fails on the fifty-first. A real keyset cursor over `(full_name, id)`, not the base64-wrapped offset `CalendarController` uses: CONVENTIONS.md §6 forbids offset paging because a row inserted while somebody is on page 2 shifts one row to page 3 unseen, and the second key is there because two people called Priya Sharma is a normal thing for an organisation to contain. Export is SXSSF, streams every matching row in batches of 500, and neutralises every text cell against spreadsheet formula injection — a department typed `-Ops` is inert in MySQL and a negation in Excel. 112 tests: 93 unit, 19 against real MySQL.*

  > **Bulk deactivate answers 200 with a per-resource outcome, not one verdict.** A selection of forty in which two hold open tickets is the normal case; failing the batch punishes the thirty-eight and succeeding quietly orphans live work. Each row returns `CHANGED`, `UNCHANGED`, `BLOCKED_OPEN_TICKETS` or `NOT_FOUND`, and the blocked check sits *after* the already-in-that-state check — before it, somebody already deactivated but still holding tickets (what a half-finished reassignment leaves behind) would report blocked forever and never clear from the grid. The reassignment wizard itself is B-014; this screen names who it refused and stops.
  >
  > **`?export=` on a list operation is a trap, and `/reports/{key}` and `/audit-logs` are still sitting in it.** The contract idiom looks right and generates a client returning `Blob | UserListResponse` — at which point **two of Stream C's working pages stopped compiling**, because the ticket list's assignee filter and the create form's picker both call `useListUsers` and were suddenly required to narrow a union they have no interest in. Shipped that way for an hour and reverted to `GET /users/export`. Neither of the other two has a consumer yet, which is the only reason nobody has hit it. **Flagged for Streams A and D** rather than changed from here.
  >
  > **B-023's calendar endpoints were unreachable, and this task found it.** `CalendarController` was mapped at `/masters`; nothing in the application declares the `/api/v1` prefix globally, so all nine operations answered on a path the generated client, the MSW handlers and the contract all disagree with. Every calendar call would have 404'd on first contact with a real backend. It survived review and a full unit suite because `CalendarControllerTest` builds the controller with `new` and never asks Spring where it is mounted — a wiring mistake needs a wiring test, and `MasterRoutesTest` is now that test for every controller in the feature.
  >
  > **`FilterDropdown` moved to `components/ui/`**, which its own comment nominated as the trigger for a second caller. Unchanged, plus a Storybook entry. **`components/ui/` is Stream C's path — needs their sign-off.**
- [x] **B-011** Resource create/edit — Personal, Access, Org, Work and Projects sections. **S-08** — *`api/feature/masters/resources/` (`ResourceWriteService`, `ResourceWriteRepository`, `ResourceExceptionHandler`, `TemporaryPasswords`) and `features/masters/resources/`, at `/masters/resources/new` and `/masters/resources/:userId/edit`. One page for both verbs — they are one form, and two components would be the same file twice with one copy always slightly behind. 42 new backend tests (26 unit, 16 against real MySQL) and 35 new frontend ones; the full suites are green at 602 backend unit tests and 288 frontend tests.*

  > **Five of S-08's fields had no column.** `V20260811_1520` adds `date_of_joining`, `avatar_url`, `location`, `weekly_off` and `skills` to `users`; mobile, department, designation, daily capacity and time zone were already there. `weekly_off` is the per-resource override B-023's header said would "land later without changing this shape" — nullable, where **null means inherit the org week and `[]` means no weekly off at all**, which a support rota is a real reason to want. **B-024 does not read it yet**: a resource with an override still has their SLA computed against the org week, and wiring it into the service every SLA figure routes through is a change that should not ride along behind a form. **Flagged for Stream A** — `users` is A-003's table, and although CLAUDE.md only mandates their review for `tickets` and the three append-only tables, the identity table should not change on a Stream B branch unremarked.
  >
  > **`GET /users/{userId}` did not exist, and its absence made `PATCH /users/{userId}` uncallable.** CONVENTIONS.md §5 pairs every `If-Match` write with a detail read that emits the tag; the PATCH declared the precondition and there was nowhere to obtain it. `/projects/{id}` and `/clients/{id}` both have theirs — this was a gap, not a new requirement. Added, with a `428` for a PATCH that arrives without one, because treating a missing precondition as "no conflict" protects only the clients that already opted in.
  >
  > **`UserWriteRequest.projectIds` could not express the Projects section**, which S-08 specifies as "multi-select project assignment **with per-project role**". Replaced by `projects: [{projectId, roleInProject}]` — a breaking change to a request shape no shipped client sent. The role vocabulary is a new `ProjectRoleCode`, **deliberately not `RoleCode`, differing in both directions**: `VIEWER` is a project role and not a global one (a global viewer would mean read-only access to everything), and `ADMIN` is a global role and not a project one (an Admin already sees every project through `ScopeResolver`, so the row would be a grant that changes nothing — and a grant that changes nothing is one somebody later assumes does something). `ck_project_members_role` fixes the same set in the database; B-011 is the column's first writer, so this was the moment to decide it.
  >
  > **B-012 is not done, and the shipped `PATCH` accepts A→B→C→A.** Only self-reference is refused, which is what the DB trigger already caught. Stated in the contract's own description rather than quietly deferred — a contract describing a guarantee the server does not make is worse than no guarantee — and pinned by `ResourceWriteServiceTest.deeperCyclesAreNotYetDetected`, which **documents the hole rather than a guarantee** and inverts when B-012 lands. **The MSW mock already refuses the deeper cycle**, so the frontend will handle the 409 on day one.
  >
  > **B-013 is largely landed as a by-product, and could not not be.** A create that 500s on a duplicate username is not a deliverable, and `password_hash` is `NOT NULL`, so a create cannot happen without generating something. Uniqueness is reported per field and all three at once — an admin fixing a duplicate username should not then discover the duplicate email — and it is checked in the service *and* enforced by the indexes, because the check is the good error message and the index is the thing that is actually true under a race. `TemporaryPasswords` generates 16 characters satisfying §10.3 by construction, omitting `O`/`0`/`l`/`I`/`1` and the shell-hostile symbols: this string is read off a screen and pasted through a chat window, and a failed first login on a fresh account looks exactly like a provisioning bug. It comes back in `meta.temporaryPassword` on a response schema **no read operation returns** — "not populated by the list query" is a property of today's SQL, "not on the type the list returns" is a property of the code.
  >
  > **A MySQL `CHECK` violation is not a `DataIntegrityViolationException`.** Error 3819 arrives under SQL state `HY000` and Spring's translator maps it to `UncategorizedSQLException`, so an unhandled one is a 500. Acceptable here — these constraints sit behind Bean Validation and the service — but worth knowing before anyone adds a `CHECK` that something reachable can trip. Found by `ResourceFormIT` asserting the wrong exception type and being wrong about it.
  >
  > **`FormField` is duplicated from `features/tickets/create/`, on purpose.** A second caller is this codebase's trigger to promote a control to `components/ui/` — which is what B-010 did with `FilterDropdown`, and that move already needs Stream C's sign-off. A second unilateral change to their path in the same week is not one stream's call, so it is duplicated and **nominated for promotion to `components/ui/form-field.tsx`**, kept shape-identical so the merge is a delete rather than a reconciliation. `WeeklyOffPicker` needed no such treatment — it is B-023's, in this stream's own path, and is now reused by its second caller.
- [x] **B-012** 🔴 **Reporting-manager cycle detection** — A→B→A blocked **at any depth**, not just self-reference. The DB `CHECK` only catches self-reference. *`ResourceWriteService.validateManager` walks up `reporting_manager_id` in one recursive CTE (`ResourceWriteRepository.managerChain`) and refuses if the edited resource is on the chain. `deeperCyclesAreNotYetDetected` inverted to `deeperCyclesAreRefused`, as B-011 said it would. 11 new tests: 7 unit, 4 against real MySQL.*

  > **The walk is depth-bounded, and that bound is what makes it terminate.** A recursive CTE over a table that already contains a cycle has no base case — it runs to `cte_max_recursion_depth` and fails the statement, which reaches an admin as a 500 on an edit to somebody else entirely. `MAX_MANAGER_CHAIN` is 64, well under MySQL's 1,000, so the recursion stops at a bound this code chose. **A chain that fills the cap is refused rather than assumed clean**: this service is the only writer that refuses cycles, so the rows it reads may already contain one, and a truncated walk cannot tell "no cycle" from "no cycle *yet*" — the optimistic reading fails open on exactly the data most likely to be broken. `ResourceFormIT.aPreExistingCycleIsSurvivable` builds such a pair with raw SQL (the trigger permits it, because neither row is self-reference) and asserts a refusal rather than a 500.
  >
  > **Self-reference moved from 400 to 409, which is the one client-visible change beyond the new refusal.** Depth 1 and depth 4 are one rule, and giving them two status codes makes a client branch on the implementation. The contract already said 409 and the MSW mock has always returned it — the server was the odd one out. Still field-keyed under `reportingManagerId`, so the form's behaviour is unchanged: the message lands on the picker and the picker takes focus.
  >
  > **The walk replaced the existence check rather than joining it.** An empty chain *is* "no such manager", so a save costs the same number of round trips it did before. Unknown manager stays a 400 — it is a field the caller got wrong, not a shape the organisation cannot hold.
  >
  > **The picker still offers everybody but the edited resource.** Excluding their whole subtree client-side would mean holding the organisation's reporting graph in the browser and keeping it fresh, to pre-empt a rare mistake that now has a clear answer on the field. **Flagged for reconsideration** if the 409 turns out to be common in practice.
- [x] **B-013** Validations — unique username, email and emp code; auto-generated temporary password with force-change-on-first-login. *Landed under B-011, which could not ship a working create without it; this task closed the two gaps that left. §10.3 now has one statement in Java — `feature.auth.PasswordComplexity` — and `TemporaryPasswordsTest` asserts the generator against it instead of against a copy of the rule. The two uniqueness behaviours that existed only as comments are now tested. 8 new tests: 4 unit, 4 against real MySQL.*

  > **The drift this closes is silent, which is why it was worth a task of its own.** §10.3 was written down three times in Java — `PasswordComplexityValidator`, `TemporaryPasswords`' alphabets, and a third time in `TemporaryPasswordsTest`, whose own comment named the duplication and the reason for it. **Nothing re-checks composition at login**, so a policy strengthened in `feature.auth` and a generator left behind would not fail a request, fail a login, or fail a build: the organisation would simply go on issuing temporary passwords its own stated policy rejects, and the discovery would be an auditor's question months later. `PasswordComplexity` holds the four predicates and the length bounds; the test on the other side of the seam is what turns the next divergence into a red build.
  >
  > **`feature/auth` is Stream A's path and this edit is flagged, not quiet.** §10.3 is A-028's rule, and the only alternative was for Stream B to keep a second copy of it — which is the thing being fixed. The footprint is one new file plus a delegation: `PasswordComplexityValidator` keeps the abstain-on-null logic and the message building and calls the extracted predicates, and **`PasswordComplexityValidatorTest` passes unmodified, all 22 of them**, which is the point of leaving it unmodified. `ChangePasswordRequest` and `ResetPasswordRequest` take their `@Size` bounds from the same constants rather than restating `8, 128`; the numbers are identical, so springdoc emits the same schema and the generated client does not change. **Needs Stream A's sign-off.**
  >
  > **The annotation stayed package-private on purpose.** Widening `@ValidPassword` was the obvious move and the wrong one: nothing outside `feature.auth` annotates a field with §10.3 — the two records that do are both there — and exporting a Bean Validation constraint whose only declaration sites are in one package would have been API surface for a caller that does not exist. What another feature needs is to *ask* the policy, not *declare* it. A generator also cannot reuse a `ConstraintValidator` cleanly in any case: `isValid`'s failure branch writes to a `ConstraintValidatorContext` it would have to be handed a null for.
  >
  > **The generator's alphabets are still its own, and still narrower than the policy.** `PasswordComplexity` says what §10.3 permits; `TemporaryPasswords` omits `O`/`0`/`l`/`I`/`1` and the shell-hostile symbols on top of that, because a password read off a screen and pasted through a chat window has constraints a user-chosen one does not. Sharing the *rule* is not the same as sharing the *alphabet*, and collapsing the two would have made a policy change silently widen what gets generated.
  >
  > **Two uniqueness behaviours were reasoned about in comments and asserted nowhere.** `findConflicts` compares with `equalsIgnoreCase` specifically to agree with `utf8mb4_0900_ai_ci`, and nothing created `ITFRM.CASED` against an existing `itfrm.cased` — a case-sensitive check would have passed it to the index, which refuses with a constraint name rather than the field-keyed 409 the form displays on the input. And `FIND_CONFLICTS`' `u.id <> ?` is the only thing stopping **every ordinary edit** reporting the edited resource's own username as taken, since S-08 sends the whole form back on every save. Both now tested against a real container, because a mocked `findConflicts` answers whatever it was told and the claims are about the SQL and the collation.
  >
  > **`ResourceExceptionHandlerTest` was written under B-012 and never committed** — 128 lines sitting uncommitted in the working tree. Carried onto this branch rather than discarded: it asserts the wire shape of the duplicate-field 409, which is this task's contract, and it is the only thing pinning that the two 409s (`manager-cycle` and `duplicate`) stay distinguishable by `type` rather than by prose.
  >
  > **The extraction found a pre-existing edge in the validator, and left it alone.** The four predicates run over `String.chars()` — UTF-16 code *units*, not code points — so a letter above U+FFFF arrives as a surrogate pair, fails `isUpperCase`, and discharges `SYMBOL` instead of `UPPER`. Every alphabet in live use is in the BMP, so nobody is affected today; the scripts this touches are the historic and constructed ones, and emoji land on `SYMBOL` either way. Carried over **unchanged and documented at the method**, because switching to `codePoints()` changes what `@ValidPassword` accepts, and quietly widening auth's rule on a branch whose whole claim is "auth's behaviour did not change" would be the worse of the two bugs. **Flagged for Stream A** as their call.
  >
  > **The frontend's copy of §10.3 stays.** `frontend/src/features/auth/passwordPolicy.ts` is a fourth statement of the rule, in TypeScript, and no amount of Java sharing reaches it. It is Stream A's path, its own comment already names the server as the authority, and the real fix is the `pattern` on the contract's `Password` schema that `ValidPassword`'s javadoc **already flagged to Stream D** — at which point the browser gets the rule from the contract like every other constraint. Left alone rather than half-fixed.
- [x] **B-014** Deactivating a resource with open tickets forces the bulk reassignment wizard. *Coordinate with C on S-24.* — *The masters side of S-24's forcing function: `PATCH /users/{userId}/status` (the route the flow returns through), `features/masters/resources/reassignHandoff.ts` (the handoff contract with C-063), a pre-flight refusal on the grid, and the return leg that finishes the interrupted deactivation. The wizard itself is Stream C's C-063 and none of it is built here. 10 new backend tests (5 unit, 4 against real MySQL, 1 wiring) and 16 new frontend ones; the resources suites are green at 87 frontend tests.*

  > **The blueprint's word is *forces*, and three refusals were pointing at nothing.** `POST /users/bulk-status`, `PATCH /users/{userId}` and the status route all refused to deactivate somebody holding open tickets, correctly, and every one of them ended there — the grid's dialog literally said "the wizard is not built yet, reassign from each ticket", which for somebody holding thirty is not advice anybody follows. A refusal with no exit is an obstacle, and the reliable response to an obstacle is to stop trying, which leaves the organisation with leavers whose accounts are still live. **The deliverable is the exit, not a fourth refusal.**
  >
  > **`PATCH /users/{userId}/status` was in the contract from the first draft, answered by the MSW mock since B-010, described by three javadocs in this package — and no server ever mounted it.** A `PATCH` to that path met `/{userId}`'s sibling mapping and came back 405. Nothing caught it because the operation had no caller: the grid uses the bulk route, the form uses `PATCH /users/{userId}`, and the screen that needed the singular one is the flow this task builds. `MasterRoutesTest` could not have — it asserts where *classes* are mounted and this was a missing *method* — and `ContractConformanceTest` reports unimplemented operations as coverage rather than failing the build, deliberately (D-005), so it was never going to be the alarm. `ResourceControllerTest.SingularStatus.isMountedWhereTheContractSaysItIs` is now the thing that would have. **Worth a look from Streams A and D: the same "declared, mocked, never mounted" gap is invisible for every other operation in the same way.**
  >
  > **The singular route has no decision of its own.** It calls the same `ResourceService.apply` the bulk route does and translates the outcome into a status code. The contract has always claimed the two refuse the same thing for the same reason, and one `apply` is the only way that is still true in a year. **`UNCHANGED` answers 204, not 409** — setting a flag to the value it already holds succeeded, a client retrying after a dropped response has to converge, and the specific row this task exists to unblock is somebody already inactive who still holds tickets, which is what a half-finished reassignment leaves behind and which the reverse ordering would refuse forever.
  >
  > **The refusal now comes *before* the request, because the grid already knew.** `openTicketCount` is a column on the S-07 grid, so firing a bulk deactivate that is certain to be partly refused spends a round trip telling the admin what was on screen the whole time — and makes the refusal read as a failure of the click rather than a fact about the organisation. A selection with nobody blocked still goes straight through with **no dialog at all**; a confirmation step on every bulk action is one nobody reads. The server's guard stays the authority: stale counts and rows selected on an earlier page both bypass the pre-flight and come back through the results dialog, which carries the same links.
  >
  > **`reason` is accepted, validated, and deliberately not persisted — flagged for Stream A.** Both status bodies carry it. `audit_logs` exists and so does `domain.audit.AuditLog`, but **nothing in this repository writes a row to either**, and `actor_id` wants a principal `dev-noauth` does not supply. Introducing the first writer to a shared audit table from a masters branch, with a null actor, would make the audit trail's opening entries the ones nobody can attribute; dropping the field from the contract instead would mean re-adding it later as a breaking change to clients that had learned to omit it. `ResourceControllerTest.reasonIsNotYetRecorded` pins the gap so that wiring it up means changing a test that says why it was not.
  >
  > **`reassignUrl` stays an API path, and the browser route is the frontend's.** The server names `POST /tickets/bulk-reassign`, which nothing can navigate to; `reassignHandoff.ts` owns `/tickets/bulk-reassign?fromUserId=…&returnTo=…`. A server emitting the second would be shipping one web app's routing table to every consumer of the API, including the ones with no router.
  >
  > **The handoff contract, for Stream C (C-063).** Two query parameters, both optional from the wizard's side: `fromUserId` preselects step one — the admin has already said who they are removing, and asking again turns a handoff into a fresh task — and `returnTo` carries an app-relative path home, ours ending `?deactivate=<id>`, which is what makes the return leg finish the job rather than merely land somewhere. **A wizard that ignores both still works**, starting empty and returning nowhere, which is what C gets for free before wiring anything up. `isSafeReturnPath` is exported for the return side: `navigate(searchParams.get('returnTo'))` reads as obviously fine and is an open redirect, and it is written here so it exists to be imported rather than thought of under deadline.
  >
  > **`/tickets/bulk-reassign` is registered in `App.tsx` as a placeholder — Stream C's path, one line, flagged.** Same precedent already set there for the project dashboard, client 360 and resource profile: an unbuilt screen and a broken link look identical to a user and only one of them is worth reporting. C-063 replaces one element.
  >
  > **The return leg is the part that would have been easy to leave out, and the flow is a suggestion without it.** Coming back from the wizard to a grid where the person is still active, with nothing on screen explaining why, is indistinguishable from the feature not working. `ResourceListIT.reassignmentUnblocksTheDeactivation` is the end-to-end claim against real MySQL — refused, tickets moved with the `UPDATE` C-063 will eventually make, same call succeeds. A guard with no exit is a defect, and until that test existed nothing said this one had an exit.
- [x] **B-015** Role & permission master — module × CRUD/approve checkbox matrix. **System roles are non-deletable.** **S-09** — *`api/feature/masters/roles/` (six operations, seven contract paths and schemas), `features/masters/roles/`. **No migration** — A-003 created the three tables and B-001 seeded them, and `domain.identity`'s repositories were written with this screen named in their javadoc. 44 new backend tests (21 unit, 11 controller, 12 against real MySQL) and 35 new frontend ones; the frontend suite is green at 649.*

  > **The matrix is `category × capability`, not the blueprint's `module × CRUD/approve`, and this is the one deliberate deviation.** The seeded vocabulary is eighteen dotted capability codes — `ticket.handoff`, `ticket.force_move`, `history.edit_delete` — not CRUD verbs, and it is already load-bearing: `AuthUserRepository` reads it into the JWT `permissions[]` claim and A-033's `@PreAuthorize` expressions name it. Cutting a new CRUD vocabulary to suit a layout is a cross-stream breaking change wearing a UI costume, and it would need Stream A's sign-off on the token contract to be worth anything. The grid does not exist to be drawn either — the seven code prefixes against the sixteen distinct verbs give a 112-cell grid with 18 cells filled, which is less legible than the sections it would replace, not more. B-001's own seed header already describes S-09 as rendering these codes.
  >
  > **`history.edit_delete` is rendered, disabled, and refused with a 422 — not omitted.** Blueprint §2: "Edit / delete history or ribbon — ❌ (nobody can)". It is seeded with zero grants *so that this screen can show it*; a row that is simply absent reads as a permission somebody forgot, rather than as the guarantee. The refusal is server-side because this `PUT` is **the one reachable UI edge on the append-only rule** CLAUDE.md calls "the guarantee that erodes first" — a disabled checkbox is a courtesy, not enforcement. `RoleService.UNGRANTABLE` is a constant and **not** a `permissions.is_grantable` column, deliberately: a column is one `UPDATE` from unlocking it, and the rule is not "currently ungrantable" but "the append-only guarantee forbids it".
  >
  > **Four refusals, none of them enforced by the schema.** System role undeletable · role in use (409 with `userCount`) · `code` immutable (409) · ungrantable permission (422). The system check runs **before** the in-use check — a system role always has holders, so testing in-use first would say "reassign 6 people" for a role that could never be deleted however many were reassigned, the mirror of B-014's ordering lesson. `users.role_id` is a foreign key *without* a cascade, so without the in-use check the database refuses the delete anyway, as a constraint violation naming a MySQL index rather than a way forward. And `code` is immutable for the `project_code` reason: it is denormalised into every issued JWT, into the `@PreAuthorize` expressions compiled at startup and into `workflow_transitions.role_code`, and changing it orphans all three at once while leaving each looking healthy. `RolePatch` carries a `code` field *only so sending one can be refused* — omit it and Jackson discards it silently, and a caller who believed they renamed the role is told the save succeeded.
  >
  > **`ResourceController`'s hardcoded `Set.of("ADMIN", "PM", …)` is gone**, per its own javadoc: "B-015 owns the role master; when it lands, this reads from it." With it, the first role an Admin added would have 400'd on the resource grid's `?role=` filter and the failure would have looked like a bug in the Role Master. `domain.identity.UserRepository` gained `countByRoleId` — one derived query in **Stream A's package**, flagged here rather than done quietly; it is where every other consumer of `roles` already reads from, and the alternative is a second count somewhere else that can disagree with it.
  >
  > **The mock's `/me` permissions were a different vocabulary entirely** — `ticket.read`, `ticket.write`, `report.read`, `audit.read`, none of them among the eighteen B-001 seeds. Nothing read them, so nothing broke; but the moment S-09 renders the real matrix, `/me` claiming a role holds `ticket.write` while the Role Master shows `ticket.update_progress` is two answers to one question in the same session. It now reads the same store the matrix edits.
  >
  > **A custom role cannot yet be assigned to a resource — flagged, not fixed.** `RoleCode` in the contract is a closed six-value enum typed onto `UserRef`, `UserWriteRequest` and the `listUsers` filter; opening it touches Streams A, C and D and is not this task's call. Until then a custom role is definable and grantable but not selectable, and the create dialog says so rather than letting an admin find out from an empty picker.
  >
  > **Touches `contracts/` — Stream D's directory, same precedent as B-023's nine calendar operations.** Seven paths and eleven schemas added; `check-conventions.py`'s `ROWLESS_403` became a dict so the exemptions it demands a reason for can actually carry one, and `/masters/roles*` is in it: master data is not row-scoped, every role is already public through `listRoles`, so a 403 on the Admin-only writes conceals nothing a 404 would have hidden.

### Project
- [x] **B-016** Project master list/create/edit — code (ticket-ID prefix), name, PM, dates, status, colour tag. **`project_code` is immutable once a ticket exists.** **S-10** — *`api/feature/masters/projects/` (four operations at `/api/v1/projects`, not under `/masters` — five screens already call them there) and `features/masters/projects/`. `V20260813_1420` adds the two S-10 fields the table had no column for. 35 new backend tests (14 unit, 11 controller, 10 against real MySQL) and 30 new frontend ones; the frontend suite is green at 686.*

  > **`Project.isActive` is a boolean and S-10's status has three values, and the boolean was the only one on the wire.** Active / On Hold / Closed cannot be expressed by it, so the master screen's own status column was unrepresentable in the contract that serves it. `status` is added and **`isActive` is kept as a derived field** — five screens filter on it (the project switcher, the ticket list, the create-ticket form, both resource screens) and none of them may break. It derives as **`status <> 'CLOSED'`, deliberately not `status = 'ACTIVE'`**: all five use it to fill a picker, so the obvious reading would mean putting a project on hold silently removed it from the create-ticket form with nothing on screen saying why. Whether On Hold *should* block new tickets is a real product question that belongs on that form where it can carry a message — **flagged for Stream C** rather than decided by a boolean on its way past.
  >
  > **`status` and `isActive` are populated everywhere and deliberately left out of `required`.** `Ticket.project` embeds the whole `Project` schema rather than `ProjectRef` — Stream C's call, and not one to relitigate from a masters branch — so a new required property would land as a new obligation on every ticket payload the tickets feature serves, for a field no ticket consumer reads.
  >
  > **The immutability test is `ticket_seq > 0`, not "a ticket row exists" — and the mock had the weaker one.** The counter records codes *issued*; a ticket created and later hard-deleted still had `CRM-26-00347` sent to a client and quoted in a mail thread, so counting live rows would let a cleanup quietly re-open the prefix for editing, with the orphaning arriving later and looking like something else. It is also already on the row, so the check costs no join. **Resending the same code is always a no-op**, which it has to be: S-10 submits the whole form on every save, and any other reading makes every edit to a live project a 409 — the mirror of the `u.id <> ?` B-013 documents on the resource form.
  >
  > **`PATCH` took `ProjectWriteRequest`, which requires `projectCode`** — a patch that requires the immutable field to be present, so every save on a live project would have carried the code and 409'd. Replaced by a real `ProjectPatchRequest`. It still *has* `projectCode`, where `RolePatchRequest` deliberately has no `code`, and the difference is not an inconsistency between two master screens: a role code is immutable from creation, a project code is freely editable until the first ticket ID is issued.
  >
  > **`GET /projects/{projectId}` did not exist, which made the `PATCH` uncallable** — CONVENTIONS.md §5 pairs every `If-Match` write with a detail read that emits the tag, and there was nowhere to obtain one. Exactly the gap B-011 closed for `GET /users/{userId}`. `ticketsIssued` rides on it so the form can disable the code input and say *why* rather than letting an admin type a replacement and meet the refusal on save; it is in the `ETag` too, so a ticket allocated while the form is open costs a reload — correct, since that is the event that fixes the code.
  >
  > **A second `ProjectRepository` does not build, and the failure names nothing.** Spring derives a bean name from the simple class name, so `api.feature.masters.projects.ProjectRepository` collided with `domain.identity.ProjectRepository` and took out **every `@SpringBootTest` in the module at once** with a `BeanDefinitionOverrideException` that mentions neither the feature nor the task. Renamed `ProjectMasterRepository`. `ResourceRepository` gets away with the short name only because `domain.identity` happens to have no `ResourceRepository`.
  >
  > **`ck_projects_status` lands at the column's first writer.** `status` was an unconstrained `VARCHAR(20)` and `Project`'s javadoc described a *fourth* and *fifth* value — `COMPLETED` and `ARCHIVED` — that the blueprint does not contain and nothing has ever written. Two vocabularies for one column with nothing mapping between them is what B-030 found on `import_batches.status`; here the constraint arrives before anything can write the other two rather than after. `description` and `auto_assign_rule` are added in the same migration: the first is an S-10 field with nowhere to go, the second a contract field the server would otherwise have had to answer with a constant no client could distinguish from a stored value. **`projects` is A-003's table — flagged for Stream A** in the migration header, as B-011 did for `users`.
  >
  > **`allowedTaskTypeIds` came off `ProjectWriteRequest` rather than being accepted and dropped.** It is a join table, not a project column, and it belongs to B-019's Settings tab; taking a list and storing nothing is how a save looks like it worked and did not. Nothing ever sent it. `autoAssignRule` went the other way — stored, because the read schema already promised it.
  >
  > **The manager's role is deliberately not checked.** Restricting it to PM/Admin is the obvious rule and wrong twice over: a support-led project has a legitimate reason to name its Support lead, and a hardcoded role set is exactly what B-015 removed from `ResourceController`. What is checked is that the person exists and is **active** — this is who Stream D's scanners escalate to, and naming somebody who has left sends every escalation to a mailbox nobody reads.
  >
  > **`FormField`/`FieldGroup` now has a third caller and is still not promoted.** B-011 duplicated it from `features/tickets/create/` and nominated it for `components/ui/form-field.tsx`; this imports B-011's copy rather than making a fourth. **`components/ui/` is Stream C's path** and B-010's `FilterDropdown` move is already waiting on their sign-off — a second unilateral change there is not one stream's call. The promotion is now overdue rather than optional.
  >
  > **`RouteAuthorizationTest.aRoleThatHoldsThePermissionIsNotRefused` fails on `develop` and is not this task's.** Verified by stashing the branch and re-running: the handler throws out of `mvc.perform` as a `ServletException` (`Could not open JPA EntityManager`) instead of returning a status, on a test whose whole point is that there is no database. **Flagged for Stream A.**
- [x] **B-017** Team tab — resources + per-project role (PM/Dev/Support/QA/Deploy/Viewer) + allocation %. — *`api/feature/masters/projects/ProjectMember*` (four operations at `/api/v1/projects/{projectId}/members`) and `features/masters/projects/ProjectTeamPage`, at `/masters/projects/:projectId/team`. `V20260813_1810` adds the one S-10 field the table had no column for. 53 new backend tests (36 unit, 17 against real MySQL) and 23 new frontend ones; the frontend suite is green at 709 tests.*

  > **Two of the four operations did not exist, and the missing ones are the half that makes it a screen.** The contract has carried `POST /projects/{id}/members` and `DELETE .../{userId}` since D-001, stub-mocked and never mounted. There was **no `GET`** — the Team tab had nothing to read a roster from — and **no `PATCH`**, so changing an allocation would have meant remove-and-re-add, which deactivates and reactivates the membership row and resets `addedAt`: an edit rendered as a fabricated departure and return. Both added, along with the `{ data }` bodies CONVENTIONS.md §2 requires and `'201': { description: Added. }` did not have.
  >
  > **`projectRole` was typed `RoleCode`, which is wrong in both directions at once.** It contains `ADMIN` — which `ck_project_members_role` refuses, so a request that was well-formed by the contract could only ever have arrived as a **500** — and it lacks `VIEWER`, which S-10 names and this screen has to offer. `ProjectRoleCode` exists for exactly this reason and B-011 has been writing the column through it since it landed; nothing caught the mismatch because the operation had no server and no caller. `ProjectTeamIT.adminIsNotAProjectRole` is the constraint that would have produced the 500, asserted directly. **Same "declared, mocked, never mounted" gap B-014 flagged for Streams A and D** — this is the second instance, and the first where the declaration was also wrong.
  >
  > **The contract's `allocationPct: default: 100` is dropped, and the column is nullable.** Landing it as `NOT NULL DEFAULT 100` writes a claim nobody made onto every row that already exists: B-007's corpus puts several of its 18 resources on all three projects, so they would read **300%**, and B-061's workload report has no way to tell a backfilled 100 from a stated one — a data-entry gap surfaced as a resourcing crisis. `NULL` means "not stated", which is what `NULL` already means one column to the left, for the reason B-011 gave for it there. **Zero is a different fact and both are storable**: "no capacity committed" is a decision, "not stated" is an absence, which is why the mapper reads the column with `getObject` and not `getInt` — `getInt` answers 0 for a SQL NULL and would collapse the two.
  >
  > **This is the second writer of `project_members`, and the two are kept apart by what each statement does *not* name.** B-011's upsert names `role_in_project` and `is_active` and not `allocation_pct`, so a resource-form save preserves what this tab set; B-017's update names `role_in_project` and `allocation_pct` and not `is_active`, so an edit cannot resurrect a membership the resource form removed. Neither is obvious from reading either file, so both are pinned: `aResourceFormSaveDoesNotClearAllocation` reproduces B-011's statement verbatim and fails if anybody widens it, and `patchDoesNotReactivate` is the mirror.
  >
  > **Removal deactivates, which forces the add path to be an upsert.** `uq_project_members` keeps one row per pair forever, so a 409 on re-adding somebody who was removed would make **every removal permanent** — and that reads as a bug in the remove button rather than as a rule. 409 is reserved for somebody already on the team, which is a request with nothing to do. Removing a non-member answers **204**, not 404: it is a setter, and the second half of a double-click must not be an error about a thing that did happen — B-014's `UNCHANGED` call, unchanged.
  >
  > **The patch DTO is a class and every other DTO in the feature is a record, because a record cannot express the semantics.** Jackson binds a record through its canonical constructor, and an *absent* `Optional` creator property is filled with `Optional.empty()` — the same value an explicit JSON null produces. Both collapse into "clear it", so `PATCH {"allocationPct": 60}` would also have wiped the member's project role, and **the response would have looked correct because it echoes what was saved**. A POJO does not have the problem: an absent property means the setter is never called, which is why `ResourceDtos.ResourceWriteRequest` is one. Written as a record first, caught by `ProjectMemberPatchTest` before anything else ran.
  >
  > **The six project-role codes were written down twice in Java and are now `masters/ProjectRoles`**, which B-011's `ResourceDtos.ProjectAssignment` points at too. Nothing re-checks a `@Pattern` against a database `CHECK`, so a seventh code added to one copy and not the other would not fail a request, a save or a build — it would mean one screen accepting a role the other refuses, discovered by somebody asking why. B-013's argument about §10.3 being stated three times, applied one package over. `ProjectRolesTest` is the seam.
  >
  > **Allocations are deliberately not made to sum to 100.** Over-allocation is a real state an organisation gets into and needs to *see*; a server that refused it would mean the true figure could not be written down anywhere. The tab shows the project total, which is honest and free. **The figure that would actually be a warning is a resource's total across *their* projects**, and this screen holds one project's rows — answering it means reading every other team. **Flagged for B-061** rather than approximated from here.
  >
  > **Touches `contracts/` — Stream D's directory, same precedent as B-015's seven paths and B-023's nine.** Two operations added, two corrected, five schemas added; `check-conventions.py` gains two exemptions with their reasons (no pagination on a roster of tens that the tab totals anyway; no `If-Match` on the member patch, where the tag would have to come from a collection read carrying no `ETag` of its own), and `CONVENTIONS.md` records both — adding one without the other is what that file's own header forbids.
  >
  > **`ResourceService.REASSIGN_URL` widened from package-private to public.** This is the second emitter of `OpenTicketsProblem` and the alternative was a second copy of a URL that must agree — the drift `ProjectRoles` exists one package up to prevent. **Flagged**, though both features are Stream B's and both sit under `masters`.
  >
  > **`JdbcClient.param(value, sqlType)` does not exist the way it reads.** `param(int, Object)` is index-then-value, so `param(40, Types.SMALLINT)` binds "parameter 40" and the driver reports "parameter index out of range" — or, for an allocation of **zero**, "invalid JDBC index: needs to start at 1". Neither message names the column or the call. Found on the first run against real MySQL and documented at the method.
  >
  > **`ProjectSwitcher.tsx` carries a one-line B-016 follow-up — Stream A's `app/` directory, flagged.** It called `useListProjects()` unfiltered while its own comment claimed it offered active projects, which was true only while every project was active. B-016 gave projects three states, and the first `CLOSED` one sorts to the top of this list and becomes **the default selection for every user** — landing them on a retired project with nothing saying that was not intended. One line, and the alternative was shipping a switcher whose comment was false.
- [x] **B-018** SLA tab — per task type × level → response hrs, resolution hrs, L1/L2 escalation targets. — *`api/feature/masters/projects/Sla*` (two operations at `/api/v1/projects/{projectId}/sla-policies`) and `features/masters/projects/SlaMatrixPage`, at `/masters/projects/:projectId/sla`. **No migration** — A-007 created `sla_policies` and the columns are the ones blueprint §6 names. 48 new backend tests (27 unit, 13 controller, 8 body-validation) plus 17 against real MySQL, and 36 new frontend ones; the frontend suite is green at 745 tests.*

  > **Both operations were declared, mocked and never mounted, and that is the third instance.** `getSlaPolicies` and `replaceSlaPolicies` have been in the contract and in the generated TypeScript client since D-001 with no server behind either. Nothing failed, because nothing called them. B-023's nine calendar operations and B-014's `PATCH /users/{userId}/status` were the first two; `MasterRoutesTest` now asserts this mount point too. What is different here is that the declaration was also **wrong**, which is B-017's `projectRole: RoleCode` again one screen over.
  >
  > **`l1EscalationUserId` / `l2EscalationUserId` are gone; the columns are `escalate_to_l1` / `escalate_to_l2` and they are flags.** The contract asked for user ids the table has no column for and that Stream D's shipped scanner explicitly does not want: `worker.sla.EscalationPolicies` documents at length that §6 *fixes* who each level means — L1 the assignee's reporting manager, L2 that manager's manager — and `SlaEscalation` derives them from the reporting chain. The matrix only says whether the level applies to this kind of ticket on this project, so a project can decide a Low change request wakes nobody without also having to decide who that nobody would have been. Storing a recipient here would put the reporting chain in two places that can disagree, and correcting the schema instead would have broken D-024 on a masters branch. The mock had been answering a hardcoded `2` and `1` for every cell.
  >
  > **The read is the resolved grid and the write is only the overrides, and the asymmetry is the whole design.** `sla_policies` is layered, so every cell already has a figure a ticket would really be measured against; a response carrying only this project's rows would render as a nearly empty matrix for a project that works perfectly well, an administrator would read "nothing configured" — **and the act of configuring it is the defect**. If the screen sent the resolved grid back, every inherited figure would become a project-level row and the project would silently stop following the org-wide default it was *shown as following*. Nothing looks wrong until somebody changes that default six months later and this project does not move. `source` and `isOverride` are on every cell so the two can be told apart, and `buildOverrides` is the one function that decides what goes on the wire.
  >
  > **A project-level default survives a replace, and the `UPDATE` says so.** The rung between this project's overrides and the org-wide default is a row with `task_type_id IS NULL` — one policy covering every task type at a level. A task type × level grid has no cell for it, so `DEACTIVATE_OVERRIDES` is scoped `AND task_type_id IS NOT NULL`: a replace that dropped it would delete configuration through a screen that never displayed it. B-007's corpus puts one on PAY, so it is exercised rather than hypothetical, in the mock as well as in `SlaMatrixIT`.
  >
  > **Cleared overrides are deactivated, never deleted.** `clients.sla_policy_id` is a foreign key into this table with no cascade, and `PlannedCloseDatePreview.slaPolicyId` puts row ids on the wire — a `DELETE` fails on a constraint naming a MySQL index, or, if somebody "fixes" that with a cascade, silently unsets a client's SLA policy from a project screen. `is_active = 0` is also what the resolution ladder already reads, so a cleared cell falls through to the next rung rather than leaving a hole. Same call B-011 and B-017 made on `project_members`, for a different reason each time.
  >
  > **The §6 ladder is now written down twice, deliberately, with a test between them.** C-012's `PlannedCloseDateService.resolve` walks it one cell at a time through `SlaPolicyRepository`; calling it per cell here is up to five statements × eleven task types × four levels — **two hundred and twenty round trips for one page load**. So `SlaMatrixService.Ladder` indexes three bounded reads and walks the rungs in memory. Nothing about either implementation can be *read* to establish that they agree, and if they drift the SLA tab and the create form quote different numbers at the same client while each screen looks correct on its own. `SlaMatrixIT.theGridAgreesWithThePlannedCloseDate` asserts every cell of a real grid — figures and source name — against C-012's answer, with all five rungs seeded, and `theSourceVocabulariesAreTheSame` is the seam that fails when somebody adds a rung to one enum and not the other.
  >
  > **`master.write`, not `project.manage` — one tab away from three rows that are Admin and PM.** §2 has two separate rows: "Create/edit projects, map resources to project" is ✅ for PM and is the General and Team tabs, and "Master data (task types, **SLA**, workflow, holidays)" is Admin alone. B-001's own description of `master.write` names SLA in it, and B-023 annotates the working calendar the same way. Staffing your own project is project management; setting the response target a client is contractually held to, and deciding whose manager's manager gets woken, is master data. **The obvious "consistency" fix is to widen this row, and it would be widening the wrong one** — so `PermissionMatrix` carries the argument rather than the conclusion.
  >
  > **The `ETag` was missing from the read while the `PUT` required `If-Match`, so the operation was uncallable.** Exactly the gap B-016 closed by adding `GET /projects/{projectId}`. `check-conventions.py` does not catch this class — its §5 detail-read rule fires on paths ending in a path variable and this one ends in a collection segment — and widening the rule would fire on a dozen paginated lists that legitimately have no tag. Left as is, with the pairing recorded in `CONVENTIONS.md` §5 as a rule for humans. The tag is taken over the **resolved** grid, so a change to the org-wide default costs this project's editor a reload: correct, because they were shown inherited figures and are deciding which to override.
  >
  > **`@Valid` on a `List` request body validates the list, not its elements — and the list has no constraints.** Every other write in the feature takes a single DTO where this is unambiguous; this one takes a bare array, and whether the element cascade engages depends on a mechanism (method validation) with different triggers. It does engage here, and the risk was never that validation would be wrong but that it would silently not run: a stored `resolutionHrs` of `0` reads as configured on this screen and behaves as absent everywhere else, because `SlaResolution.hasTarget()` treats a non-positive figure as no target — the ticket drops out of the breach sweep, the pre-breach warning and the delayed KPI with nothing saying so. `SlaPolicyBodyValidationTest` pins it, including that the *second* element is validated and not only the first.
  >
  > **A Save button, where the Team tab beside it saves on change.** B-017 is right to have none — each edit there is one `PATCH` of one field. This is a wholesale replace, so save-on-change would mean forty-four replaces racing each other while somebody types. The consequence is a dirty state, and the thing to get right is what an emptied resolution box means: it removes the override, which is the same request as "inherit" as far as the row is concerned. There is deliberately no separate delete control — two controls for one outcome is how they end up disagreeing.
  >
  > **Touches `contracts/` — Stream D's directory, the same precedent as B-015's seven paths, B-017's two and B-023's nine.** Two operations corrected, one schema corrected, one added, one reused: `SlaSource` already existed from C-012 with the same six values, and a second enum saying the same thing is how two views of one resolution end up describing it differently. **No new exemption in `check-conventions.py`** — the pagination one has been registered since D-002 and this is the operation that makes it real.
  >
  > **`ProjectTabs` is three tabs now, and B-016's rule against stubs held.** Settings (B-019) is still not rendered: a greyed-out tab and a broken one look identical to a user. The SLA tab is a sibling route rather than a nested one for B-017's reason, and a sharper version of it — it needs its own `ETag`, over the matrix rather than over the project, so no shared parent read could ever have served both.
  >
  > **The frontend has no capability gate to hang the Admin-only write on — flagged for Stream A.** `/me` carries `permissions[]` and nothing reads it for gating. This is the first masters screen whose read and write have different roles, so it is the first place the gap is visible rather than theoretical: a PM sees the inputs and meets a `403` in the banner. Refusing to render the inputs from a hardcoded role check is what B-015 removed from `ResourceController` and is not the fix.
- [x] **B-019** Settings tab — allowed task types, mandatory fields, auto-assign rule (round-robin / least-loaded / manual). — *`api/feature/masters/projects/ProjectSettings*` (two operations at `/api/v1/projects/{projectId}/settings`) and `features/masters/projects/ProjectSettingsPage`, at `/masters/projects/:projectId/settings`. `V20260814_1120` creates `project_task_types` and adds `projects.mandatory_fields`. 41 new backend tests (21 unit, 11 controller, 9 body-validation) plus 18 against real MySQL, and 35 new frontend ones; the frontend suite stands at 842 tests. **S-10 is complete — all four tabs.***

  > **An empty allow-list means unrestricted, and getting that backwards would have stopped ticket creation everywhere.** No `project_task_types` rows for a project means every active task type may be raised on it, not none. Every project is in that state, because the table did not exist until the migration ran — so the other reading would have barred every task type on every project the moment it applied. A backfill could not have saved it either: writing eleven rows per project to preserve today's behaviour turns "never configured" into "configured, on 14 Aug, to allow exactly these eleven", and the twelfth type an Admin adds next month is then silently barred organisation-wide by a decision nobody made. The cost an administrator meets is that unticking every box removes the restriction rather than forbidding everything, so the screen says so in a sentence and keeps saying it — eleven empty boxes claim the opposite on their own, and `0 of 11 selected` is the phrasing that reads as the restriction it is not.
  >
  > **`draftFor` is where that rule is won or lost on the frontend.** The server answers `isAllowed: true` for every row when `restrictsTaskTypes` is false, because that is what unrestricted *means* — and seeding the checkbox draft from those flags is the obvious implementation and the defect. The first save would post an allow-list naming every task type that existed that day. The draft reads `restrictsTaskTypes` instead, and the visible consequence is asserted both ways: an untouched screen must report no pending changes.
  >
  > **The read returns retired task types this project still allows, which is not tidiness.** The `PUT` is a wholesale replace assembled from the rows the screen was given, so a retired-but-allowed type that was not rendered would be deleted by the next save through a screen that never displayed it — the deletion-by-omission B-018 guards against for project-level SLA defaults, one master over. They arrive with `isActive: false` and are chipped `retired`, since no new ticket can use them whatever this tab says. The `LEFT JOIN` predicate that keeps them visible is the sort of thing that is right in a mock and wrong in SQL, so it has an integration test.
  >
  > **One writer for `auto_assign_rule` now, and the second was removed rather than documented.** S-10 puts the field on this tab; B-016 rendered it on the General form because this tab did not exist. Leaving it on both would have been worse than a duplicate — `toPatchRequest` sends every field on every save, so a General-tab save would have carried that form's value and silently overwritten whatever Settings had set, every time somebody renamed the project. That is the `project_members` hazard B-011 and B-017 had to pin apart with two named regression tests, avoided instead. The contract still accepts the field on `POST`/`PATCH` (removing it would break for no gain), and `ProjectService` stopped keeping its own `Set<String>` of the three values, deriving them from the enum this task added — one vocabulary for one column, checked against the database's own `CHECK`.
  >
  > **Admin *and PM*, where the SLA tab one route over is Admin alone.** Two tabs of one screen with two capabilities looks like an oversight again and is not: §2 row 2 ("create/edit projects, map resources") is PM ✅ and covers General, Team and Settings; row 5 ("master data — task types, SLA, workflow, holidays") is Admin's and covers SLA. Configuring which task types a project accepts is project configuration; setting the response target a client is contractually held to is master data. The decisive half is that narrowing this to Admin would have **taken a capability away** from PMs, since `auto_assign_rule` has been PM-writable through `PATCH /projects/{projectId}` since B-016.
  >
  > **Mandatory fields are JSON and allowed task types are a join table, and both are sets on a project.** Task type ids are foreign keys into a master an Admin edits, which is the whole argument for normalising; field codes are an application vocabulary with nothing to point at, nothing queries them across projects, and a second join table to serve a checkbox list of ten values is what `users.skills` already declined. `ck_projects_mandatory_fields` constrains **shape and not vocabulary**, unlike `ck_projects_status` — the codes are exactly `TicketCreateRequest`'s optional fields, so pinning them would mean Stream C cannot add a form field without a migration in Stream B's directory. The service refuses an unknown code on the way in and *drops* one on the way out, because a settings read that threw would put the only screen that could repair it behind the failure.
  >
  > **Nothing enforces these settings yet, and that is the one part of the task that lands incomplete.** The tab stores and serves configuration; ticket creation is what has to obey it, and `api/feature/tickets` and `CreateTicketPage` are **Stream C's**. Everything C needs is already on the read — `restrictsTaskTypes` + `taskTypes[].isAllowed` filters the task-type picker and `mandatoryFields` marks the rest — and the gap is stated in the contract, the service javadoc, the screen and both READMEs rather than left to be discovered. **Needs a C task; flagged rather than reached across for.**
  >
  > **Touches `contracts/` — Stream D's directory, the same precedent as B-015's seven paths, B-017's two, B-018's four and B-023's nine.** Two operations and five schemas added, none corrected: unlike the last three masters tasks this route was never "declared, mocked and never mounted", because nothing had declared it. `MasterRoutesTest` asserts the mount point anyway, on the day it was written — a route added with its assertion cannot become the fifth instance. **No new `check-conventions.py` exemption**, and the endpoint slips both §5 and §6 from opposite sides: its `data` is an object rather than an array so the pagination rule is silent, and its path ends in a literal segment so the detail-read `ETag` rule is too. The `ETag` is there anyway because the `PUT` requires one, and `CONVENTIONS.md` §5 now records the pairing as a rule for humans alongside B-018's.
  >
  > **The `ETag` is a 32-bit hash and two states of one resource can collide.** Found the honest way: the controller test's first fixture varied only `restrictsTaskTypes` and the `isAllowed` it implies, and those two booleans move in opposite directions at the same multiplier in a record's `hashCode`, cancelling exactly. Not a fluke of the values — it is what any "restricted, with one type excluded" versus "unrestricted" pair will do, and a stale write against the second would be let through. `ProjectController` and `SlaPolicyController` tag the same way, so it is **recorded in both the controller and the test rather than fixed here**: a stronger tag across all three is a change worth making together, not one screen at a time.

### Simple masters
- [x] **B-020** Task type master — the 11 seeded types, Admin-extensible. **S-11** — *`api/feature/masters/tasktypes/` (four operations at `/api/v1/masters/task-types`) and `features/masters/taskTypes/`, at `/masters/task-types`. **No migration** — A-007 created `task_types` and B-002 seeded it, and every column S-11 needs was already there. 64 new backend tests (51 unit, 13 against real MySQL) and 31 new frontend ones.*

  > **`listTaskTypes` was declared, mocked and never mounted, and that is the fourth instance.** It has been in the contract and in the generated TypeScript client since D-001 with no server behind it, after B-023's nine calendar operations, B-014's `PATCH /users/{userId}/status` and B-018's two SLA operations. What is different here is that **three shipped screens already call it** — the ticket list, the ticket detail page and the create form — so all three would have 404'd on first contact with a real backend rather than merely being uncallable. `MasterRoutesTest` now asserts this mount point too.
  >
  > **There is no `DELETE`, and B-019 wrote that decision down before this screen existed.** `tickets.task_type_id`, `sla_policies.task_type_id` and `project_task_types.task_type_id` are all foreign keys *without* cascades; B-019's migration says at the constraint that "B-020's master deactivates rather than deletes for exactly this class of reason." A delete would fail as a violation naming a MySQL index, and "fixing" that with a cascade would silently rewrite what a historical ticket says it was raised against. `TaskTypeMasterIT.foreignKeysAreRestrictive` asserts the claim against `information_schema` rather than leaving it in a comment, and `TaskTypeControllerTest.thereIsNoDelete` fails if anybody adds the mapping — a `DELETE` added later would compile and pass every other test in the file.
  >
  > **`code` is now on the contract's `TaskType`, and the reason is a live defect one stream over.** `features/tickets/create/ticketForm.ts` decides which types make the Client field mandatory (§4B.2) by **matching on the display name** — and B-020 is the screen that makes a name editable. Its own comment already says "matching a display string means a rename in the Task Type master silently disables the rule", and until now there was no stable key to move it to. There is one now, and it is immutable. **Flagged for Stream C**: `CLIENT_REQUIRING_TASK_TYPES` should hold codes, not names. Not changed from here — it is their path, and the duplicate-`name` refusal below closes the wider hole in the meantime.
  >
  > **A duplicate `name` is refused, and no index says so.** Only `uq_task_types_code` exists. Two types rendering the same label are indistinguishable in every picker in the product, and this screen is the only thing that can create the collision — so the rule is the service's, case-insensitively, and `TaskTypeMasterIT` asserts it agrees with `utf8mb4_0900_ai_ci` the way B-013 asserted the resource form's did.
  >
  > **`defaultLevel` is checked twice, and the second check names B-021.** First against the **priority master**, and it must be active — a hardcoded four is exactly what B-015 removed from `ResourceController`, and B-021's whole point is that an Admin can add a level. Then against the four values the contract's `Level` enum can carry, because a fifth priority stored here would serialise into a response **the generated client's own zod schema rejects** — a screen that breaks on read because of a save made on a different screen. Opening `Level` touches `tickets.level` in three streams and is B-021's call; until then the refusal says which of the two rules it is.
  >
  > **`TaskTypePatch` is a POJO and every other DTO in the feature is a record — B-017's lesson, one screen over.** `icon` and `defaultSlaHrs` are genuinely clearable, so absent and explicitly-null must differ; Jackson fills an *absent* `Optional` creator property on a record with `Optional.empty()`, the same value an explicit null produces. Both collapse into "clear it", so `PATCH {"name": "…"}` would have wiped the type's icon and its default SLA **while echoing back a response that looked correct**. `TaskTypePatchTest` pins both directions and `TaskTypeListPage.test.tsx` asserts the same thing from the browser end.
  >
  > **No migration, and the `CHECK` was considered and rejected.** B-016 landed `ck_projects_status` at the column's first writer and the same argument seemed to apply here — but a `CHECK` on `default_level` would fix the four levels in the schema, which is precisely what B-021 exists to unfix. `colour` is guarded by Bean Validation and by the contract's pattern, and B-011 already documented that a MySQL `CHECK` violation surfaces as an uncategorised 500 rather than a field-keyed 400. So `task_types` is untouched and this needed no Stream A review.
  >
  > **The palette is the frontend's and the shape is the server's.** CLAUDE.md forbids a colour that is not a §12.1 token; the server can only check `#RRGGBB`, because it has no palette to check against. So the create dialog offers the eight-colour colour-blind-safe chart palette as swatches — the same eight B-002 cycled through — and `taskTypeForm.test.ts` fails if that list drifts, since nothing else would catch it.
  >
  > **The mock gained a retired task type, and that found a real disagreement.** `Fax Request` is inactive, as `Transport` is in the module fixture: an endpoint that deliberately returns inactive rows cannot be exercised by a fixture in which every row is active. Adding it immediately showed that the mock's SLA grid was built from **all** task types while `SlaMatrixService` builds it from the active ones — so the mock would have rendered a row for a retired type that the real server does not return. Fixed in the mock, and it is the consequence this screen has to state out loud: retiring a type removes a row from every project's SLA tab.
  >
  > **`TaskType`'s new properties are populated everywhere and deliberately not `required`** — B-016's call on `Project.status`, and it was not theoretical: marking them required regenerated `TaskType` with non-optional fields and immediately broke `ticketForm.test.ts`'s fixtures, which construct one. A required property is an obligation on every consumer that builds the type, for a field no ticket consumer reads.
  >
  > **`RouteAuthorizationTest.aRoleThatHoldsThePermissionIsNotRefused` still fails on `develop` and is still not this task's.** Verified again by stashing the branch: the same `Could not open JPA EntityManager` B-016 flagged for Stream A on a test whose whole point is that there is no database. **Still flagged.**
  >
  > **The frontend suite has an intermittent failure under parallel load, and it is pre-existing.** `TicketListPage`'s project-filter test and `RolePermissionsPage`'s matrix tests fail perhaps one run in three, never the same one twice, and always pass when their file is run alone. **Measured rather than assumed**: three runs of the same nine files with this branch stashed flake at the same rate on `develop`, so it is the environment and the 1 s default `asyncUtilTimeout`, not this change. B-020's own tests raise that timeout locally for the assertions that follow a mutation — `configure({ asyncUtilTimeout })` globally would fix the symptom everywhere and hide a genuinely hanging query when one appears, so it is deliberately not done. **Worth a task**: the fix is a `waitFor` in the affected tests, and it is Streams B and C's own files.
- [x] **B-021** Priority master — Low/Medium/High/Critical + colour + default SLA hours + escalation flag. Drives C's priority dropdown. **S-12** — *`api/feature/masters/priorities/` (four operations at `/api/v1/masters/priorities`) and `features/masters/priorities/`, at `/masters/priorities`. **No migration** — A-007 created `priorities` and B-002 seeded the four, and every column S-12 needs was already there. 81 new backend tests (62 unit, 19 against real MySQL) and 33 new frontend ones.*

  > **`listPriorities` had no controller at all, and this is the fifth instance after B-023, B-014, B-018 and B-020** — the second in a row where shipped screens already call the route. `CreateTicketPage` builds its `LevelPicker` from it and `TicketListPage` builds its level filter from it; both would have 404'd on first contact with a real backend. `MasterRoutesTest` pins the mount point.
  >
  > **S-12's "Admin can add further levels without a release" is the one part of this task that lands incomplete, and it is stated rather than quietly dropped.** `Level` is a closed four-value enum typing `Ticket.level`, `Ticket.originalLevel`, `TaskType.defaultLevel`, `SlaPolicyWrite.level`, `SlaPolicyCell.level`, `ChangeTicketPriorityBody.level` and two query parameters. A fifth code would serialise into a response the generated client's own zod schema rejects, and Stream C's `LevelPicker` and `columns.tsx` key their chip variants off `Record<Level, …>` maps a fifth key leaves `undefined`. B-020's `normaliseLevel` refused the mirror case and named B-021 as the task that would decide; the decision is to refuse with a message naming what must change, and to say so on the screen with the button disabled rather than offer a form that always fails. **Needs a coordinated A/C/D task; flagged rather than reached across for.** Everything else S-12 lists — name, colour, default SLA hours, escalation flag, order, retire, reactivate — is fully editable.
  >
  > **Nothing has a foreign key to `priorities`, so "no DELETE" is a stronger rule here than on the sibling screen.** A task type is pointed at by three FKs and a delete at least fails loudly; `tickets.level`, `task_types.default_level` and `sla_policies.level` are all `VARCHAR` columns holding the code, so a delete here would *succeed* and leave every historical ticket rendering a level nothing resolves. The same absence is why **every usage count keys on the code, never on the id** — a join written against `priorities.id` would compile, run, and return zero for every level, and no mocked-repository test could tell. That is what the integration suite needs a real MySQL for.
  >
  > **`is_escalation_trigger` is a pointer §6 depends on, and until this task nothing in the codebase read or wrote it.** A-007 declared it a bare `TINYINT(1) NOT NULL DEFAULT 0` and documented it as "the level the SLA engine escalates TO on breach"; zero flags silently switches off one of blueprint §1's three headline behaviours, and two make the target whatever order D's scanner happens to return. `PriorityService` makes it single-writer — setting it clears the others, clearing the last one is refused — and the screen renders it checked-and-disabled on the level that holds it, because a checkbox whose only outcome is a 409 should not be operable. **The MSW mock had flagged two levels since D-001.**
  >
  > **A retire is refused on task types and permitted on everything else, which is not the call B-020 made.** Tickets keep the level and go on rendering it; SLA policy rows survive but stop resolving, because `SlaMatrixService` builds its column axis from the active levels. Task types are different in kind: `TaskTypeService.normaliseLevel` refuses a retired level as a `defaultLevel`, so retiring would leave those types failing validation on their next save, on a screen whose admin cannot see what caused it. One screen must not be able to put another into a state it cannot get out of.
  >
  > **An ordering bug, found and closed before merge.** `{"isActive": false, "autoEscalates": true}` is one request that passes both guards when each reads the entity's stored state — the escalation check sees the level still active, the retire check sees it not yet flagged, and the end state is a retired escalation target. `update` now derives the post-patch active state once, before anything is written, rather than ordering two writes carefully and trusting nobody reorders them. Named regression test, and the MSW handler mirrors the derivation.
  >
  > **The mock's four level colours were wrong and are corrected.** §12.1 states the level chips exactly — `#10B981 / #3B82F6 / #F59E0B / #EF4444`, which is what B-002 seeds — and the handler returned `#84CC16 / #F59E0B / #9A3412 / #BE185D`. Nothing caught it because `LevelPicker` renders the frozen `level-*` tokens rather than the hex, so the wrong values reached a screen and were never displayed. The rows also moved out of a frozen literal in the handler and into `db.ts`, because a `PATCH` needs something to write to.
  >
  > **`listPriorities` stays active-only by default, departing from B-020's and B-064's "return retired rows, let pickers filter".** Not a preference: `CreateTicketPage` filters task types by `isActive` and then maps priorities straight into `LevelPicker` unfiltered, because until now the endpoint could not return a retired one. Widening the default would put a retired level into the create form and the ticket list filter, both Stream C's files. `?includeInactive=true` serves the grid, and the two lists are kept apart in the cache by their query keys. **Worth a C task to add the filter**, after which the default can widen.
  >
  > **Touches `contracts/` — Stream D's directory, the same precedent as B-015's seven paths, B-017's two, B-018's four, B-020's and B-023's nine.** Three operations, one parameter and three schemas added, and `Priority` widened with `name`, `seq`, `isActive` and the three counts — additively, so C's two `p.level` readers are untouched. Two `check-conventions.py` `ROWLESS_403` exemptions with their reason, and CONVENTIONS.md §5 gains the `PATCH` row.
- [x] **B-022** Notification template master — event, channel, subject, HTML body, merge tags, per-event on/off, per-role recipients. Drives D's mail engine. **S-15** — *`V20260815_1100__notification_template_recipients.sql`; `api/feature/masters/notificationtemplates/` (five operations at `/api/v1/masters/notification-templates`) and `features/masters/notificationTemplates/`, at `/masters/notification-templates`. `MergeTag` and `NotificationRecipient` added to `domain/notifications/`. 62 new backend tests (47 unit, 15 against real MySQL) and 32 new frontend ones.*

  > **The table existed, and nothing had ever reached it.** `notification_templates` came with A-007, the entity and repository with B-005, and `email_log.template_id` has pointed at it since `V20260805_1530` — with no controller, no contract operation, no caller anywhere, and **no rows**. That is a different shape from the five "declared, mocked, never mounted" gaps `MasterRoutesTest` records (B-023, B-014, B-018, B-020, B-021): this was modelled and never reached. Same outcome — every mail sent today builds its subject in Java and enqueues with `templateId = null`. The migration seeds one row per (event, channel) pair §11 ticks, for all 27 events, so D-010 has something to render.
  >
  > **The channel vocabulary is `IN_APP|EMAIL|PUSH`, and A-007's `POPUP|BELL|EMAIL` column comment is superseded.** That comment predates D-042. Everything that runs keys on `NotificationChannel` — preferences, the mandatory-mail check, push subscriptions — and S-15's own line in §7.4 says "in-app / email / push". A template stored as `POPUP` would never be found by a renderer looking up `IN_APP`, and the miss is silent: the send falls back to a hand-built string and nobody learns that the master an Admin has been editing was never read. **The bell is not a channel** (D-042's own argument): it renders the same title and body the toast does, so §11's popup and bell columns are one template. Zero rows existed, so this is picking the right vocabulary before the first one lands, not changing one. **Flagged for Stream D.**
  >
  > **The rule this screen exists in order not to break.** "Per-event on/off" taken unqualified hands an Admin one click that silences, org-wide, the mail D-036 spent a whole method making unmutable per-user. So switching off an `EMAIL` template is refused with `409` when the event is an assignment, escalation or status request — exactly `NotificationEvent.isMandatoryMail()`, stated over the *category* so a new escalation event is covered the moment it is declared. Mail only: §7.7 gives the guarantee to mail, and the `IN_APP` template for the same event stays switchable. **The recipient list is deliberately not guarded** — removing `ASSIGNEE` would silence it just as effectively, and is still allowed, because §11's "To" column is a default rather than a law and an org routing assignment mail through a shared desk address is doing something legitimate. Asserted as a hole so removing it later is a decision.
  >
  > **Two events in "mandatory" categories send no mail at all, and the second one was found by asserting the rule exhaustively.** Because `isMandatoryMail()` is stated over the category, it says true for a few events whose §11 row has a dash in the Email column. D-055 already recorded `STATUS_REQUEST_ANSWERED` and called it moot. Running the assertion over the whole enum turned up `TICKET_REASSIGNED_AWAY` — an `ASSIGNMENT`, because being taken off a ticket does change who is responsible, and §11 gives it a bell entry and no mail, which is right: the person is being told work has *left* them, not arrived. Seeding a mail for either, to satisfy a category rule, would be inventing one the blueprint says not to send. The IT asserts both directions, so a third is a failure and a decision rather than a silent omission.
  >
  > **Recipients are positions relative to a ticket, not roles**, so `recipients` is a closed `NotificationRecipient` vocabulary rather than a join onto `roles`. §4B.6 says "per-role recipient list" and taking that literally builds the wrong table: of the ten things §11 names, two look like role codes and eight — assignee, stage owner, previous assignee, reporter, watchers, mentioned user, client contact, support desk — are resolved per send. Even the two that share a spelling are joins: `PROJECT_MANAGER` means the PM *of this ticket's project*.
  >
  > **Merge tags are validated on save, because nothing else would catch a typo.** §4B.6 promises an Admin can edit these "without a code release", which also means without a developer reviewing it — so `{{ticketId}}` for `{{ticket_id}}` saves cleanly, prints literal braces in a client-facing mail, and the first person to notice is the client. `MergeTag` is the catalogue, an unknown tag is a `400` naming the ones that exist, and the IT runs the same check across all fifty seeded rows because the migration is data and nothing else would.
  >
  > **Every read is Admin, which no other master in this stream does.** Task types, levels, roles and the calendar open theirs to all six on §2 row 3's argument — every role may raise a ticket, so a role that could not read those masters could not raise one. Nothing a non-Admin sees is built from this route, and the content is not neutral: the seeded rows include the mail sent to a **client contact**, the escalation naming the Reporting Manager, and A-044's chain-verification alarm. §2 gives the audit log to Admin alone on that reasoning. Reasoned rather than read off, and recorded in `ROWLESS_403`.
  >
  > **Touches `contracts/` — Stream D's directory, the same precedent as B-015's seven paths, B-017's two, B-018's four, and B-020's, B-021's and B-023's.** Five operations, one parameter and nine schemas added, three `ROWLESS_403` exemptions with their reasons, one `NO_PAGINATION` exemption (bounded by two enums, the way `/me/notification-preferences` is bounded by one), and CONVENTIONS.md §5 gains the `PATCH` row. **`MergeTag` and `NotificationRecipient` land in `domain/notifications/`, Stream D's package** — they must, because the renderer lives in `worker`, which cannot see `api`; flagged for sign-off on A-044's precedent in that same file.
  >
  > **A stale rule found in D's mock, corrected in passing.** `isMandatoryMail` in `mocks/handlers/rest.ts` covered `ASSIGNMENT` and `ESCALATION` only. The Java version has covered `STATUS_REQUEST` since D-055 found it in §4B.6's table, so the mock rendered the status-request email preference as unlocked while the server locks it. Nothing failed because no test asserted that row. Corrected here rather than left, because B-022's handlers need the same rule and two versions of it in one file is how the next one drifts. **Flagged for Stream D.**
- [x] **B-064** 🔴 **Module master read endpoint** — `GET /api/v1/masters/modules` over the `product_modules` table C-065 creates and seeds (Student, Admission, Fees, Examination, Attendance, Library, Inventory, Parent App). Read-only, **every row in `seq` order including deactivated ones**, each carrying `isActive` — same as `/masters/task-types`. A picker offers only the active ones; a grid still has to render the name of a module some old ticket was raised against, and filtering them out here leaves that cell blank. The contract shape is D-060's, and its mock has a retired module referenced by one seeded ticket so the distinction is exercised. **Blocks C-068.** Added 11 Aug 2026 with blueprint revision 1.3 — the eight rows are reference data, and a master served out of the tickets feature is how the masters pattern stops meaning anything. No admin CRUD screen: the client asked for a fixed list, and the table exists so that changing it later is a row rather than a release. If a Module Master screen is wanted, it is a new task on the S-11/S-12 pattern, not a change to this one. — *No migration: C-065's `V20260819_1336` created and seeded `product_modules`, so this mounts a route over a table that already exists — nothing touching schema, nothing needing Stream A's review. `domain/masters/ProductModule` + `ProductModuleRepository`; `api/feature/masters/modules/` (one operation at `GET /api/v1/masters/modules`). No contract change — D-060 declared it and it is already exempt from §6's cursor rule in `check-conventions.py`. No frontend change — the client was generated, the mock has served it since D-060, and three of Stream C's screens already call it. 19 new backend tests (11 unit, 8 against real MySQL) plus the module's six rows in `PermissionMatrix`.*

  > **The seventh "declared, mocked, never mounted" — and the first that would have failed silently.** `listModules` has been in the contract, in the MSW mock and in the generated TypeScript client since D-060 with no controller anywhere in the backend, after B-023's nine calendar operations, B-014's `PATCH /users/{userId}/status`, B-018's two SLA operations, B-020's `listTaskTypes`, B-021's `listPriorities` and B-025's six client operations. This one had the most built on top of it: `CreateTicketPage`'s module picker (C-068), S-20's "Where it happened" group and its inline editor (C-069), and S-17's module filter and grid column (C-070) all shipped green against the mock. The other six 404'd loudly on first contact with a real backend; this one degrades quietly — `moduleName()` returns `undefined`, the cell renders an em dash, and every ticket in the product reads as **"no module was recorded"**, which is a state a ticket can genuinely be in. A route nobody mounted would have looked like data nobody filled in. `MasterRoutesTest` pins the mount point and says so.
  >
  > **The backlog says "Blocks C-068" and C-068 shipped first, which is the finding rather than a scheduling slip.** Stream C could build against the mock and had no reason to wait; nothing in the toolchain distinguishes "the mock answers this" from "a server answers this", so the dependency went unnoticed in both directions. It is the same gap the six before it went through, and the reason `MasterRoutesTest` exists at all.
  >
  > **No `includeInactive`, and the repository carries no active-only query either.** The route's two consumers need opposite things from one response — a picker offers live rows, a grid renders the name of whichever module a ticket was actually raised against. Filter server-side and the second gets a blank cell, which reads as missing data rather than as a retirement. That matches `listTaskTypes` and departs from `listPriorities`, whose departure B-021 argued from its consumers being unable to filter; this master's consumers already do, in `whereItHappened.ts`. `ProductModuleRepository` deliberately declares only the two queries this feature uses, and `ModuleServiceTest` asserts that over the interface — a derived query needs no body, so a `findByIsActiveTrue…` added later "for convenience" would arrive with nothing to review and put an active-only list one autocomplete away.
  >
  > **The entity is `ProductModule`, not `Module`.** `java.lang.Module` is implicitly imported into every compilation unit in the language; an entity called `Module` would shadow it here and be shadowed by it elsewhere, and the resulting error names a JPMS type nobody in this codebase has ever referenced. The class matches the table rather than the screen label.
  >
  > **Read-only, and there is no detail read either.** The sibling masters carry a `GET /{id}` solely to emit the `ETag` their `PATCH` requires as `If-Match` (CONVENTIONS.md §5) — B-011 added one to serve a write, not as a pattern to copy. With no write there is nothing for it to serve. `ModuleControllerTest` asserts the absence of a write mapping rather than leaving it to review, because a `POST` bolted on later would arrive with no ETag, no precondition and no `ticketCount` to make a retire decision informed. An admin screen is a new task on the S-11/S-12 pattern, as the entry above says.
  >
  > **All six roles read it**, on §2 row 3's argument rather than convenience: every role may raise a ticket, §7.5 puts the Module field on the create form, so a role that could not list modules could not fill it in. The contract declares only 200 and 401, so no 403 is reachable and there is no `ROWLESS_403` entry to add.
  >
  > **The write-side rule stays in Stream C and is not duplicated.** `feature/tickets/ModuleGuard` (C-067) refuses a deactivated module on a ticket write with a 400 keyed on `moduleId` — the right home, since the rule is about the write rather than the read. It reaches `product_modules` through a raw `JdbcClient` rather than through `ProductModuleRepository`; **noted for Stream C rather than changed**, because `feature/tickets` is C's directory and the consolidation is C's call.
  >
  > **What the IT proves that the seed cannot.** All eight seeded rows are active, so every assertion about the seed would pass just as well against an implementation that filtered retired rows out — the mock has carried a deactivated `Transport` since D-060 for exactly this reason, and MySQL needed one inserted. The suite also asserts `seq` ties breaking on `id` (there is no unique index, so two rows sharing a `seq` are legal and would otherwise swap places between reads) and that `fk_tickets_module` still restricts deletes, read out of `information_schema` rather than assumed from the migration: an `ON DELETE SET NULL` added later would break nothing in Java and quietly turn every historical module reference into the same blank cell this task exists to prevent.

### Working calendar
- [x] **B-023** Working calendar & holiday master — org holidays, weekly off pattern, per-resource leave. **S-14** — *`V20260808_1630__working_calendar.sql` + `WorkingCalendar`; `api/feature/masters/Calendar*`; `features/masters/calendar/`. 9 new contract operations. **B-024 now has everything it needs.***

  > **Days are ISO-8601 — Mon=1 … Sun=7 — everywhere.** The contract described `weeklyOff` as "ISO day numbers" while constraining it to `0–6`, and the mock sent `[0, 6]`: JavaScript's `Date.getDay()` convention wearing an ISO label. Read literally by a backend using `DayOfWeek`, `0` is not a day and **Sunday becomes a working day** — every SLA spanning a weekend short by a day, in the one calculation CLAUDE.md says must have a single implementation. A third numbering was also live in `docs/plan/calendar.json` (`[0,1,2,3,4]`, Python's `date.weekday()`), unread but documented as the shared source B-024 should use. All three are now ISO, and `ck_working_calendar_weekly_off` makes the database refuse a `0`.
  >
  > **Two defects the tests caught before merge.** The working day was first stored as `TIME`/`LocalTime`; both `api` and `worker` set `hibernate.jdbc.time_zone: UTC`, so it round-tripped shifted by the JVM offset — 09:30 written, 15:00 read. Now `SMALLINT` minutes from midnight, which nothing can convert. And MapStruct's default collection strategy mutates what the getter returns, but `getWeeklyOff()` derives a fresh `EnumSet` each call — so `PUT` answered 200, echoed the new week back, and saved nothing. Both have named regression tests.
- [x] **B-024** 🔴 **Working-hours calculation service** — `workingHoursBetween(start, end)` and `addWorkingHours(start, n)`, honouring weekends, holidays and resource leave. **Every SLA, duration and utilisation figure in the system routes through this.** D is blocked on it. Blueprint §5 calls it the most commonly missed requirement. — *`domain/masters/WorkingHoursService.java`, in `domain` rather than `api.feature.masters` so D's `worker` module and C's `api.feature.tickets` can both call it without reaching across a stream boundary. Both methods take an optional `projectId` (project holidays, unioned with the org calendar per B-023) and an optional `userId` (that resource's approved leave, including half-days). Walks calendar days in the working calendar's own zone — never UTC-naively, per the round-trip bug B-023 already hit once — and resolves a recurring holiday against every year the walk touches via `HolidayRepository.findAllOrgWideOrForProject`, a new query added because the existing date-windowed one cannot see a recurring holiday stored in a prior year. 21 unit tests (fixture calendar, no Docker) plus 8 integration tests against real MySQL, including the canonical blueprint §5 case — a Friday-18:00 ticket with a 4-hour SLA lands Monday morning, not Saturday.*

### Client master
- [x] **B-025** Client list — columns, filters, row-expand to show contacts inline, bulk activate/deactivate. **S-32** — *`api/feature/clients/` — controller, service, `ClientQueryRepository`, DTOs and problem handler; `features/clients/` — the grid, its filters, the row-expand and the bulk bar. No migration: `clients`, `client_contacts` and `client_projects` came with the baseline and B-005 mapped them. First adopter of A-053's cursor kernel.*

  > **The sixth "declared, mocked, never mounted" operation set, and the largest.** All six client operations have been in the contract, in the MSW mock and in the generated TypeScript client since D-001 with **no controller anywhere in the backend** — after B-023's nine calendar operations, B-014's `PATCH /users/{userId}/status`, B-018's two SLA operations, B-020's `listTaskTypes` and B-021's `listPriorities`. Nothing failed because nothing has run against a real backend. `ClientRoutesTest` pins the mount point the way `MasterRoutesTest` does for the masters package, which cannot cover this one: the contract serves clients at the top level, so `feature/clients` is a sibling package rather than a subpackage.
  >
  > **Two S-32 columns were unrenderable and two filters did not exist.** Blueprint line 946 asks for Projects and Last Ticket Date and the `Client` schema carried neither; it asks to filter by support plan and account manager and `listClients` took neither. Both added additively — no existing consumer's fields move — along with `PATCH /clients/bulk-status`, because S-32's bulk action over fifty rows should not be fifty requests with a partial-failure story. One unknown id fails the whole batch and writes nothing, and the problem document names **every** missing id rather than the first, so a stale selection is fixed in one round.
  >
  > **The bulk path is a literal where a variable could have matched.** `/clients/bulk-status` and `/clients/{clientId}` are both two segments. Spring's `PathPatternParser` ranks the literal higher — the same ranking `/tickets/new` and `/masters/resources/new` already depend on in `App.tsx` — and since `clientId` binds as a `long`, the failure mode if a router ever preferred the variable is a loud 400 rather than a silent mis-dispatch. `ClientRoutesTest` asserts the path cannot parse as an id.
  >
  > **This screen warns where S-07 refuses, and that is the blueprint's sentence rather than an inconsistency.** The resource grid blocks a deactivation that would orphan open tickets; §4B.2 says deactivating a client blocks *new* tickets and **never hides historical ones**, so nothing is orphaned and there is nothing to fix first. The warning carries the count; enforcing the block is B-029's, on the ticket create path. `openTicketCount` is on every row rather than only in the dialog, the call B-015 made with `userCount` and B-020 with `ticketCount`.
  >
  > **Four queries per page, never four per row.** The page is read first and the aggregates fetched for exactly the ids it returned — joining three collections into the page query multiplies rows by their product. "Open" is `statuses.is_terminal = 0` joined rather than a hardcoded `'CLOSED'`, so a second terminal status added through S-13 keeps counting correctly. The keyset is `(name, id)`: `client_code` is unique and the name is not, and after a bulk import duplicate names are ordinary — the IT walks 23 clients at a limit of 5 and asserts each is visited exactly once.
  >
  > **Touches `contracts/` — Stream D's directory, the same precedent as B-015's seven paths, B-017's two, B-018's four, and B-020's and B-023's nine.** One operation, two query parameters and two `Client` fields added, one new schema, one `ROWLESS_403` exemption and one `NO_IF_MATCH` exemption with their reasons, and CONVENTIONS.md §5 gains the bulk row. **Also touches `frontend/src/mocks/` and `App.tsx`** — `db.ts` gains `clientProjects`, which nothing modelled, seeded so Acme is on two projects and Bluewave on none: a fixture where every client has exactly one cannot tell a screen that handles the multi and empty cases from one that has never met either.
- [x] **B-026** Client create/edit across four tabs: Identity · Commercial · Contacts · Projects & SLA. **S-33** — *`api/feature/clients/` gains `ClientWriteService`, `ClientWriteRepository`, `ClientStatus`, `ClientSupportPlan` and three operations (`GET /clients/{clientId}`, `POST /clients`, `PATCH /clients/{clientId}`); `features/clients/` gains the four-tab form at `/masters/clients/new` and `/masters/clients/:clientId/edit`. `V20260816_1030` adds the four §4B.2 fields the table had no column for, plus `ck_clients_status`. 54 new backend tests (29 unit, 8 controller, 7 against real MySQL, 10 vocabulary) and 40 new frontend ones.*

  > **`GET /clients/{clientId}` did not exist, which made the `PATCH` uncallable.** CONVENTIONS.md §5 pairs every `If-Match` write with a detail read that emits the tag, and `updateClient` has declared the precondition since D-001 with nowhere to satisfy it. Exactly the gap B-011 closed for `GET /users/{userId}` and B-016 for `GET /projects/{projectId}`, and B-025's own README predicted it. `ClientDetail` is a **separate schema** rather than more fields on `Client`: the list inlines one `Client` per client per page, and twenty-five more fields would be weight paid for by the grid, by §4B.2's ticket-form dropdown and by `Client360Response`, none of which render them. The contract's `clientCode` was `maxLength: 50` against a `VARCHAR(20)` column and `name` was `200` against `VARCHAR(150)` — it was accepting values MySQL would refuse, and nothing had found it because no server implemented the operation.
  >
  > **Adding §4B.2's third status changed what `isActive` means, and getting that backwards would have emptied the ticket form's client dropdown.** B-025 derived it as `status = 'ACTIVE'`, exact while the column carried two values. §4B.2 puts a client dropdown on the ticket create form **filtered on that boolean**, so under the narrow reading every Prospect would have vanished from that form the moment the field shipped — silently, with nothing on screen saying why, and looking exactly like a dropdown that had lost its data. It derives as **`status <> 'INACTIVE'`**, which is B-016's call on `Project.isActive` one master over. The half that would have been easy to miss is the consequence for the status setter: a Prospect is *already* active by that projection, so `isActive: true` against one is a **no-op** — writing `ACTIVE` anyway would let S-32's bulk Activate turn a shortlist of prospects into contracted clients, a commercial fact changed by a checkbox with nothing recording it. `ClientStatus.activatedFrom` is the single statement of the rule so the singular and bulk setters cannot drift, and `status` is now on the **list** row as well as the detail, because once a prospect and a contracted client are both active the grid's chip cannot be drawn from a boolean.
  >
  > **A round-trip test found a live one-day shift on every `LocalDate` read through JPA, and it is not this task's to fix everywhere.** `contract_start = '2025-04-01'` written through JPA came back as **2025-03-31** — measured with the JVM at UTC, the connection at UTC, the MySQL session at `+00:00` and the raw column holding the right value throughout, so it is not a time-zone misconfiguration to be argued about. On one connection, one row: `getObject(LocalDate.class)` → `2025-04-01`; `getDate(…)` → `2025-03-31`. `hibernate.jdbc.time_zone: UTC` puts Hibernate on the second path. The write is unaffected, which is what makes it invisible — nothing looks wrong until somebody compares a contract end date on screen with the contract. Same shape as the `TIME` defect B-023 hit (09:30 written, 15:00 read) and the same resolution: take the conversion out rather than configure around it. B-026 reads its two columns through `JdbcClient`. **`Holiday.holidayDate` has the identical bug and is flagged, not fixed** — an org holiday a day out means `WorkingHoursService` treats the wrong day as non-working and every SLA crossing it is wrong. That is B-023's follow-up and **Stream A's property**; changing it is one line and a re-run of every temporal test in the repository, which is not a change to make on a masters branch on the way past. `Project.startDate`/`endDate` escape only because B-016 happened to write and read them through `JdbcClient` already.
  >
  > **Five tests in `ClientMasterIT` could never have passed, and the suite hid it.** They took a project id from `allProjectIds()`, whose assertion message read "the fixture corpus seeds projects" — and nothing does: B-007's corpus is a profile-gated `ApplicationRunner` (`local,fixtures`), no migration inserts a `projects` row, and no test in the module activates either profile. So the helper's own assertion failed before the test it served could say anything, in a file whose other eighteen always passed. Repaired rather than reported, since it is the same stream's file: a test that needs a project now makes one and cleans it up, which also stops each of them depending on what another suite left behind.
  >
  > **One shape for `POST` and `PATCH`, where the project master needed two.** B-016 had to introduce a separate `ProjectPatchRequest` because requiring an immutable `projectCode` on a patch would have made every edit to a live project a 409. **No client field is immutable**, so there is nothing to keep off the patch shape. `client_code` stays editable with one consequence stated at the field: it is B-035's upsert key, so changing it means the next import carrying the *old* code creates a second client rather than updating this one. And the body is a **record**, where B-017's and B-020's patches had to be POJOs — those needed absent and explicitly-null to differ; this one is the whole representation, so "the form did not send a short name" and "the admin emptied the box" are the same event.
  >
  > **`projectIds` is the one field where absent and empty must differ, and it is the one B-035 will get wrong.** Absent leaves the `client_projects` mapping alone; an empty array unmaps the client from everything. The import writes client rows and never touches associations, so a null read as "unmap" would have every import silently detach every client it updated from every project — surfacing much later as a ticket form whose client dropdown has gone empty. Stated in the contract and pinned by tests on both sides rather than left to be inferred from a null check. The replace is a delete-then-insert, which B-017 and B-018 both argued themselves *out* of: `project_members` and `sla_policies` rows carry facts of their own — an allocation, an escalation flag, an `added_at` somebody reads as a start date — so removing and re-adding fabricates a departure and a return. `client_projects` holds nothing but the association and its default flag, which is exactly what is being replaced; A-006's own comment says its `ON DELETE CASCADE` is "safe here and only here" for the same reason.
  >
  > **The tabs are state, not routes, which is the opposite of `ProjectTabs` and for the reason that made `ProjectTabs` right.** S-10's four tabs each own a different resource with a different `ETag` — no shared parent read could have served both the project and its SLA matrix. S-33's four are **one resource behind one Save button**, so routes would mean either four reads of the same client or losing every unsaved edit on a tab change, and a form that discards what you typed when you check something on another tab is worse than one long page. Two consequences are pinned by tests: the panels are **`hidden`, never unmounted**, because react-hook-form unregisters an input that leaves the DOM and this form sends every field on every save — the bug would stay invisible until somebody edited an address and found their notes cleared; and a **server error opens the tab its field is on**, taking the first bad field in *schema* order rather than the order the server serialised, or a 400 naming `timezone` while the admin is on Commercial marks an input nobody can see and the save reads as silent.
  >
  > **The Contacts tab reads and does not write, and the SLA field is read-only — both deliberate, both stated on screen.** Add/edit/remove is B-027's whole task, and `POST /clients/{clientId}/contacts` is a **seventh** "declared, mocked, never mounted" operation waiting for it. What B-026 owes that task is the tab and the one rule an administrator needs from this screen: **a client without a primary contact is not selectable on a ticket** (B-028), reported from `hasPrimaryContact` and *not* enforced here — the save is what creates the client the contact must hang off, so refusing it would make the rule impossible to satisfy. And **nothing reads `clients.sla_policy_id`**: C-012's ladder resolves org → project → task type and never consults it, so a picker here would be a control whose only effect is to write a number nobody looks at. The stored value is shown, preserved, and the tab points at B-018's matrix. **Flagged for C-012** rather than faked.
  >
  > **The MSW mock and the server disagreed about support-plan casing, and only `npm run dev` would have shown it.** The fixture and every server write store `PREMIUM`; the mock stored `Premium`. Nothing caught it because the grid rendered the stored string raw and B-025's filter matches case-insensitively — but S-33's `<Select>` binds to the codes, so it would have shown nothing selected against the mock and been perfectly correct against a real backend, which is the worst way round for a bug to sit. Mock corrected, the grid now labels the code rather than shouting it, and the list filter's values are the stored codes so the two screens share one vocabulary. `db.ts` also gains a **PROSPECT client with no primary contact** — a fixture in which every client is ACTIVE and contactable cannot tell a screen that handles the third state and B-028's warning from one that has never met either.
  >
  > **Touches `contracts/` — Stream D's directory, the same precedent as B-015's seven paths, B-017's two, B-018's four, and B-020's, B-021's, B-022's and B-025's.** One operation added, two corrected, four schemas added and two `maxLength` defects fixed; two `ROWLESS_403` exemptions with their reasons, and CONVENTIONS.md §5's `PATCH /clients/{id}` row now records the detail read that makes it callable. **Also touches `App.tsx`** — two routes, Stream A's file, the same one-line precedent B-014 set.
- [x] **B-027** `client_contacts` child grid — add/edit/remove, primary flag, notification opt-in, portal access. — *`api/feature/clients/` gains `ClientContactService` and `ClientContactWriteRepository`; three operations mounted at `/clients/{clientId}/contacts` and `/clients/{clientId}/contacts/{contactId}`, plus `?includeInactive=` on the existing read. `features/clients/` gains `ContactEditorDialog`, `contactForm.ts` and `contactQueries.ts`, and the Contacts tab becomes a grid. **No migration** — `client_contacts` came with the baseline with every column §4B.2 names, `designation` among them. 39 new backend tests (15 service, 7 controller, 2 wiring, 6 problem-document, 9 against real MySQL) and 35 new frontend ones.*

  > **The seventh "declared, mocked, never mounted" operation, and the first where the *missing* verbs were the deliverable.** `createClientContact` has been in the contract, the MSW mock and the generated client since D-001 with no server behind it, after B-023's nine calendar operations, B-014's `PATCH /users/{userId}/status`, B-018's two SLA operations, B-020's `listTaskTypes`, B-021's `listPriorities` and B-025's six client operations. What is different is that a `POST` alone is not a child grid: **without a `PATCH` an edit is remove-and-re-add**, which deactivates the row a historical ticket points at and issues a new id — a corrected phone number rendered as a departure and an arrival. B-017 added `PATCH /projects/{id}/members/{userId}` for the identical reason, where the missing verb would have reset `added_at`. Both new verbs land with their `ClientRoutesTest` assertions written the same day, so neither can become the eighth instance.
  >
  > **Removal deactivates, and `information_schema` is asked rather than trusted.** `tickets.client_contact_id` references `client_contacts` **without** a cascade: a real `DELETE` fails as a constraint violation naming a MySQL index, and "fixing" that with a cascade would rewrite who a historical ticket says reported it. `removalDeactivatesBecauseTheForeignKeyIsRestrictive` asserts the `DELETE_RULE` directly, the way B-020 asserted the task-type foreign keys, because the claim is about the schema and a comment cannot fail. `is_primary` is cleared in the same statement — `primaryContacts` filters on `is_active = 1` and `demoteOtherPrimaries` does not, so a removed contact keeping its flag gives the grid and the promotion path two answers to "who is the primary" that disagree.
  >
  > **`?includeInactive=` is the whole design, and the default is the half that matters.** The grid sends `true` and every picker leaves it off — B-021's split on `listPriorities`, one master over. Getting it backwards fails in opposite directions: the wrong default offers a departed contact on every new ticket, and the wrong grid read makes somebody vanish with no way to tell "removed" from "never existed" and no way to see the address is still spoken for. Adding the parameter shifted `useListClientContacts`'s generated signature, which is what surfaced the **live defect one stream over**: `TicketDetailPage` resolves the reporter's name from this list, so the moment B-027 removed anybody the "Reported by" line on their historical tickets would have gone blank — reading as missing data rather than as a departure. It now sends `includeInactive: true` and `CreateTicketPage` deliberately does not. **Two one-argument edits in Stream C's files, flagged.**
  >
  > **Losing the last primary is allowed, which is not the call B-021 made on the mirror case.** B-021 refused to clear the last `is_escalation_trigger` and was right: a level with no escalation target silently switches off one of §1's headline behaviours with nothing on any screen wrong. A client with no primary contact is different in kind — it is the state **every client is created in**, B-028 reports it on the ticket create path where a caller can act on it, and after somebody leaves it is simply true. And it could not have been refused consistently in any case: the `DELETE` produces the same state and *cannot* be refused, because a contact who cannot be removed until somebody else is promoted reads as a broken button. One rule with two answers is worse than a permissive rule stated clearly, so the removal dialog names the consequence before the click instead.
  >
  > **A duplicate email is refused within the client and nowhere else, and nothing in the schema says so.** `ix_client_contacts_email` is deliberately **not** unique — the same address under two clients is a consultant retained by both, and D-039 resolves inbound mail by taking the set and disambiguating on `website_domain`. So unlike B-013's username check, which has an index behind it, the service here is the *only* thing refusing it, which is why the claim is asserted against a real container in both directions. It matches case-insensitively to agree with `utf8mb4_0900_ai_ci` and reads through the index rather than wrapping the column in `UPPER()`. **A removal frees the address**, which is the ordinary case rather than an edge: somebody leaves and their replacement inherits the mailbox.
  >
  > **Three contract defects, all found by implementing the operation.** `ContactWriteRequest.name` was `maxLength: 150` against a `VARCHAR(120)` — the contract accepting values MySQL refuses, which is exactly what B-026 found on `clientCode` and `name`. `Contact` carried no `designation`, a column present since the baseline, so the one field telling a desk whether they are speaking to the IT Director or a helpdesk operator was unreadable. And `Contact.email` was non-nullable against a nullable column: **required on the write and nullable on the read** is the correct shape, not an inconsistency — B-035's import will write rows from spreadsheets that omit an address, and a non-null read type makes the generated client's own zod reject a row the database permits.
  >
  > **No `If-Match` on the contact `PATCH`, and the exemption carries the interesting half.** The tag would have to come from `listClientContacts`, a collection with no `ETag` of its own — `PATCH /projects/{id}/members/{userId}`'s argument unchanged. What is worth writing down is that **the parent has one and every contact write moves it**: `contactCount` and `hasPrimaryContact` are inside `ClientDetail`'s `hashCode`. So `contactQueries.ts` invalidates the client as well as the list, and without that the admin's next Save on the Identity tab comes back `412` — "somebody else saved this client while you were editing", about a change they made themselves on this screen seconds earlier. Pinned by an IT that asserts the hash actually moves.
  >
  > **`ClientExceptionHandler` can now emit two different 409s under one `type`, so it finally has a test.** A duplicate client code and a duplicate contact email are both `duplicate`, distinguishable only by which key is in `errors` — and CONVENTIONS.md §3 says a client is expected to branch on the status and the `type`. `ClientExceptionHandlerTest` is the seam, written for the reason `ResourceExceptionHandlerTest` was under B-013: nothing about reading the handler establishes that the two stay tellable apart, and a client that switched on the `detail` sentence instead would break the first time somebody reworded a message. It also pins the shape B-026 shipped untested — that a duplicate code *mixed with another failure* is a 400, and that every message is a string **array** rather than a bare string, which the form's `messages[0]` would otherwise index character by character and mark the field with the letter "C".
  >
  > **`ClientService.contactsOf` was deleted rather than left beside the new reader.** B-025 put it there because there was nothing else for it to live on; keeping it would mean two classes deciding what a client's contacts are, and they would disagree the first time one honoured `includeInactive` and the other did not — the drift `ProjectRoles` (B-017) and `PasswordComplexity` (B-013) exist to prevent, in the same feature this time.
  >
  > **Every button on the tab is `type="button"`, and that is a defect avoided rather than a style note.** The panel is inside `ClientFormPage`'s `<form>` and a button's default type is `submit`, so an "Add contact" without the attribute **saves the client and navigates away** — right-looking for one click and wrong for the other. The editor is a real `<form>` and is safe for a different reason: radix portals `ModalContent` to `document.body`, so it is a sibling rather than a descendant. `ClientFormPage.test.tsx` holds the assertion, because the tab's own test renders it alone and structurally cannot catch it.
  >
  > **`develop` did not compile, and it is not this task's.** A-055 landed `features/dashboard/` with `u.fullName` on a `User` that has never had the field (it is `displayName`; `full_name` is the *column*) and two unused `React` imports under `noUnusedLocals`. `npm run build` — and therefore `make verify`, and therefore the integration gate — was red for **every** branch, not just this one. Verified by stashing. Fixed here in three lines because nothing could otherwise be verified at all, and **flagged for Stream A**: `features/dashboard/` is their path, and the reason it reached `develop` is worth their attention more than the fix is.
  >
  > **Touches `contracts/` — Stream D's directory, the same precedent as B-015's seven paths, B-017's two, B-018's four, and B-020's, B-021's, B-022's, B-025's and B-026's.** Two operations added, one corrected, one parameter added, two schemas corrected; one `NO_IF_MATCH` exemption and two `ROWLESS_403` exemptions with their reasons, and CONVENTIONS.md §5 records the pairing. **Also touches `frontend/src/mocks/`** — the fixture gains a removed contact at Acme and a designation on the rest, because a fixture in which every contact is live cannot exercise the parameter that separates the grid from the picker.
- [x] **B-028** Validation: unique client code, valid emails, **at least one primary contact before the client is selectable on a ticket**. — *`domain/validation/EmailFormat` and `domain/clients/ClientCodeFormat` — one statement each, in `domain` because `feature/imports` and `feature/clients` both reach it and neither should reach the other. `feature/clients` loses two `@Email` annotations and gains the checks in `ClientWriteService` and `ClientContactService`; `Client` gains `hasPrimaryContact`; `ClientQueryRepository`'s `isActive` predicate is corrected. `features/tickets/create` renders an unmet client greyed and unselectable via one additive prop on `SearchableDropdown`. **No migration.** 14 new backend tests plus 3 against real MySQL, and 30 new frontend ones.*

  > **Two of the three rules already existed and disagreed with themselves, which is the whole task.** "Unique client code" and "valid emails" read like B-026 and B-027 had done them. They had — twice each, differently. B-030's importer required a dotted TLD and refused hyphens in a client code; the S-33 form accepted `accounts@acme` and issued `ACME-IN`. Both columns are read by both, and `client_code` is **B-035's upsert key** — so the form was issuing codes the import would later reject, and a rejected row is not a message somebody reads and fixes but a client the import silently declines to update. `FieldValidators`' own javadoc says it exists so that two places do not answer "is this a valid email?" differently "by the second release"; the second place had been there since B-026. The strict readings won on both, because every one of these columns exists to be *sent to* or *matched against*, and a hyphen breaks nothing (unlike `project_code`, this is not a ticket-ID prefix).
  >
  > **`@Email` is gone rather than tightened, and the removal is what fixes the four-tab form.** Bean Validation refuses the whole body at the binding layer, so an email failure could never join the collected error map S-33 depends on — an admin with a bad address on Identity and a departed account manager on Commercial saved twice to learn twice. It also ran the pattern against the *untrimmed* value while `apply` stored the trimmed one, so an address pasted out of a spreadsheet cell was refused where the identical address typed by hand was accepted. And on the contact editor it conflated two answers: `@Email` treats blank as valid, so "you did not fill this in" and "what you typed is malformed" were split across two annotations that had to agree.
  >
  > **`ContactValidationException.isDuplicate()` was `errors.containsKey("email")`, and that had to change before the email rule could land.** Exact while a duplicate was the only way that field could fail — B-028 gives it a second, and under the inference `"sara@acme" is not a well-formed email address` would have rendered **409 Conflict**. CONVENTIONS.md §3 says a client branches on the status, so the editor would have reported "already in use" about an address no other contact holds. The thrower states it now. `ClientValidationException.isDuplicateCodeOnly` still reads the map and still gets away with it, because `clientCode` has exactly one way to fail — noted rather than changed, since changing it would be a fix to nothing.
  >
  > **`?isActive=true` was dropping every prospect, on the one call the ticket form makes.** `ClientQueryRepository.page` filtered `status = 'ACTIVE'` while the row it returned projected `status <> 'INACTIVE'` — B-026 widened the projection when it added `PROSPECT`, argued the case for it at length on `ClientStatus`, and left the predicate alone. So a prospect came back from `GET /clients` saying `isActive: true` and did not come back from `GET /clients?isActive=true` at all. Nothing failed because the two had never been run against each other: `absentIsActiveReturnsEveryone` covers ACTIVE and INACTIVE, where the two predicates are identical, and **the MSW mock filtered the wide way with a comment claiming "it is the derivation the server uses"**. It was not — the same mock-versus-server shape as B-026's support-plan casing and the same worst-way-round, correct under `npm run dev` and wrong against MySQL. `ClientService.ACTIVE` is deleted rather than left, because the wrong comparison should not be one autocomplete away.
  >
  > **The gate is shown and refused, never filtered out.** `hasPrimaryContact` goes on the **list** row and not only on `ClientDetail`, because the list row is what §4B.2's dropdown renders — off the `primaryContacts` map `attachAggregates` already fetches, so no extra query. Deriving it from `primaryContact` at each call site was the alternative and is worse than it looks: that field is `@JsonInclude(NON_NULL)`, so a client without one omits the key and a picker reading the object gets `undefined`, one `??` away from offering everybody. Removing the client from the dropdown instead of disabling it was rejected for the reason `ClientStatus` already gives about prospects: a client that is simply absent is indistinguishable from a dropdown that has lost its data, and the person raising the ticket is usually the person who can go and fix the master.
  >
  > **The server-side backstop is owed and cannot be paid here.** Enforcement belongs on `POST /tickets`, which **has no controller** — C-013's own notes say the ticket-create service does not exist and `feature/tickets/` holds a code generator, an SLA preview and attachments. Writing the rule into a service with no caller would be dead code; writing the endpoint would be Stream C's task. So the obligation is stated in `createTicket`'s contract description — a 400 keyed on `clientId`, **not** a 404, because the client is legitimately visible and it is the combination being refused — beside B-029's mirror rule, and **flagged for Stream C**.
  >
  > **A fixture in which every mapped client had a primary contact could not have caught any of this.** `CLIENT_PROJECTS` gains one row — Kestrel, the PROSPECT with no contacts, on project 1 — and it exercises both findings at once on the one screen the rule is about: it must be *offered* (a prospect is active) and must not be *selectable* (no primary contact). The mock's `primaryContact` was also matching on `isPrimary` alone where the server requires `is_active` too, so a *removed* primary kept the client selectable; B-027 seeded a removed contact precisely so that distinction could be tested, and nothing had.
  >
  > **Touches `contracts/` — Stream D's directory, the same precedent as B-015's seven paths, B-017's two, B-018's four, and B-020's, B-021's, B-022's, B-025's, B-026's and B-027's.** One schema field added, one parameter documented, one operation description gains the rule it owes. **Also touches `components/ui/searchable-dropdown.tsx` and `features/tickets/create/` — Stream C's**, one additive prop with a Storybook story and a five-case test, and one call site; every existing caller renders identically. The precedent is C-010's own four ARIA props on the same component. `frontend/src/lib/email.ts` is new and shared — the browser's copy of a rule that has to exist twice, asserted against the same corpus as `EmailFormatTest` so neither side can drift alone.
- [x] **B-029** Deactivating a client with open tickets warns and blocks new tickets, but never hides historical ones. — *`features/clients/` gains `ticketEligibility.ts` (both new-ticket gates, one statement) and `DeactivationWarningDialog.tsx` (lifted out of `ClientBulkStatusBar`, now with S-33 as a second caller); `ClientListPage` remembers every row it has seen; `ClientFormPage` warns before a Status → Inactive save. **No backend production change and no migration** — the server was already right about the third clause and had nothing saying so, which is what the new ITs are. 3 new ITs against real MySQL and 44 new frontend tests.*

  > **Three clauses in one sentence, and only the middle one had been read as the task.** Blueprint line 523 says deactivating "prompts a warning and blocks new ticket creation against it, but never hides the historical tickets". B-025 shipped the warning and B-025/C-013 got the block for free from `?isActive=true`, so the work left looked like enforcement — and enforcement is the one part that cannot be built, because `POST /tickets` has no controller. What was actually outstanding was **a hole in each of the other two**, both of which had been reachable for three tasks.
  >
  > **The third clause was broken on S-15, which is the screen the sentence is about.** `TicketListPage`'s client filter read `useListClients({ isActive: true })`. Deactivate a client and their name left the filter, so every ticket they had ever raised became unreachable through the one control that reaches them — the second half of line 523 breaking the third. Nothing failed loudly; a dropdown quietly stopped offering somebody. Identical in shape to B-027's `includeInactive` defect on the *detail* page of the same feature, and the fix is the mirror of it: **the create form still sends the parameter and must.** Same operation, opposite answers, and the question that separates them is "new or historical", not "active or not" — now written on the parameter in the contract, because two more callers will face the same choice.
  >
  > **The warning was on one of the two paths that deactivate a client.** S-33's Identity tab reaches `INACTIVE` in two clicks and saved without a word, on a page that had *already loaded* `openTicketCount` and never showed it. So `DeactivationWarningDialog` moved out of `ClientBulkStatusBar` rather than being copied: one consequence described twice is how the two start disagreeing, which is `ProjectRoles` (B-017), `PasswordComplexity` (B-013) and — B-028 found — `FieldValidators` itself. **And the bulk warning was reading the current page**, a limitation B-025 documented and left: this grid adds to a selection *across* pages on purpose, so a client ticked three pages ago was deactivated unwarned. It now reads every row the grid has seen, which needs no round trip because the rows were already fetched.
  >
  > **Both new-ticket gates now come from one function, and the second one is what forced it.** B-028 wrote `hasPrimaryContact` inline on the S-19 dropdown, correctly, while it was the only gate. `ticketEligibility.ts` holds both, fails closed on an absent field (B-028's whole argument about `primaryContact` being `NON_NULL` applies to any permissive default), and reports deactivation *before* a missing contact — telling somebody to go and add a contact to a client the business has closed sends them to fix the wrong thing. It also backs a **submit-time refusal**, which the dropdown structurally cannot: `getOptionDisabled` guards a click, and Save & Create Another keeps a `clientId` across an arbitrary number of tickets, so a selection routinely outlives the deactivation that invalidated it. With no server behind `POST /tickets`, that function is the only enforcement in the system — stated out loud rather than left implied by a greyed option. **Flagged for Stream C**, along with the two one-argument edits in their files, the precedent B-027 and B-028 both set.
  >
  > **The backend was right and nothing said so, which is not the same as being tested.** `openTicketCounts`, `lastTicketDates` and `TicketListSpecs` never look at `clients.status` — correct *by omission*, and an omission is exactly what a later change takes away without noticing. One `AND c.status = 'ACTIVE'` added to the count query by somebody reasonably assuming a grid should not total closed accounts breaks the clause in total silence. The three new ITs assert it **through** the deactivation rather than against an already-inactive fixture: the same figures before and after, the `tickets` rows untouched (which is also what makes S-32's Activate a real undo), and all three readings of `?isActive=` at once — the last of which is what would have caught B-028's defect a task earlier.
  >
  > **Two fixture findings, and the second is why the first went unnoticed for three tasks.** No ticket in the mock had ever belonged to a deactivated client: the generator assigns `clientId: i % 3 === 0 ? null : i % 3`, which only produces 1 and 2, both ACTIVE. So a screen that hides a deactivated client's history and one that never hides anything were indistinguishable — which is how S-15 kept sending `?isActive=true`. Oldco now has three tickets, assigned to Ravi because he is `currentUserId` and a Developer, so `scopedTickets` would otherwise make them invisible to every default test. And **`mulberry32` was seeded once at module load and never rewound**, so each `resetDb()` continued the stream: ticket N's level and assignee depended on how many tests had run before it. Adding one test to `TicketListPage.test.tsx` made a C-016 assertion fail three tests later in another `describe`, on a row that no longer existed — both passing alone, the failure naming neither cause. Rewound in `createDb()`, three lines, and **flagged for Stream D**; it makes the existing comment on that generator true.
  >
  > **Touches `contracts/` — Stream D's directory, the same precedent as B-015's seven paths, B-017's two, B-018's four, and B-020's, B-021's, B-022's, B-025's, B-026's, B-027's and B-028's.** One parameter description gains the rule that separates a picker from a historical view; no schema, operation or response changes, so the generated client is unmoved. **Also touches `features/tickets/{list,create}/` — Stream C's** — one argument removed, one added, one submit guard — **and `frontend/src/mocks/`.**

### Excel import — S-34
- [x] **B-030** 🔴 **Import engine as a schema registry** — built once, registered twice (clients, resources). Blueprint §4B.3: "build it once, register two schemas." — *`api/feature/imports/` — the SPI (`ImportSchemaDefinition`), the registry, the validation kernel, header auto-match and the staging seam; `schemas/ClientImportSchema` is the first registration and is a list of columns plus two methods. Adding an importable entity is adding one `@Component`: no route, no migration, no registry edit. B-031…B-037 are steps that plug into this; B-038 is a file. 83 unit tests plus 9 against real MySQL.*

  > **The engine's guarantees are enforced, not documented.** `ImportValidationEngine` holds no repository and no path to `upsert()`, so blueprint §4B.3's "nothing is written yet" cannot be eroded by a convenient `save()` later, and `ImportEngineIsolationTest` fails the build if the dependency appears. The same test fails any reference to `domain.clients` from the engine — the first half of a second implementation, which CLAUDE.md's stop rule otherwise relies on somebody remembering on a Thursday afternoon.
  >
  > **Two contract-vs-schema conflicts, found by reading them against each other before either had a consumer.** `ImportBatchResponse.batchId` was `format: uuid` while `import_batches.id` is a `BIGINT AUTO_INCREMENT` like every other id in the system — contract corrected to `int64`, client regenerated. And `import_batches.status` carried a private `PENDING|VALIDATING|COMMITTING|DONE` that no caller could observe while the contract promised `QUEUED|RUNNING|COMPLETED|FAILED`: a row at its own default serialised as a value the generated TypeScript union does not contain, so the frontend's zod schema would have rejected the response the backend just produced. The contract won; `V20260810_2010` adds a `CHECK` so they cannot diverge again. Two of the old states were unreachable anyway — the dry run writes nothing, so no row exists until commit.
  >
  > **`develop` was briefly red under this branch, for a reason unrelated to B-030 — and Stream A fixed it independently.** `SchemaIntegrationIT.schemaHasExpectedShape` expected 44 tables; A-027 added `password_reset_tokens` in PR #70 without bumping it. This branch carried the 44→45 repair for a few hours, then A-028 landed the same fix (to 46, with `password_history`) and B-030 dropped its version on the next rebase. Recorded because the near-collision is the argument the comment on that line already makes: a table count is a tripwire nobody can read, it makes every new table a cross-stream edit, and two branches nearly repaired it twice. Asserting named tables would do the same job without the coupling.
  >
  > **ArchUnit is currently a no-op on this project, and this would have been its first use.** It is a declared test dependency, but 1.3.0's bundled ASM stops at class file major 68 and PLAN.md §D-10 compiles at `release 25`, which is 69. It does not fail on a class it cannot read — it logs and skips, so all four rules passed having examined nothing. The tripwire was rewritten to scan source (and to fail if the scan finds no files). **Flagged for Stream A:** the real fix is an `archunit.version` bump in `backend/pom.xml`, and until then any ArchUnit test anyone adds is a green tick that proves nothing.
- [x] **B-031** Step 1 — template download. Apache POI **SXSSF**, with data-validation dropdowns on Status and Support Plan, and one filled example row. — *`ImportTemplateWriter` renders any registration, not the client master: headings, dropdowns and the example row are all `ImportField` declarations B-030 had already made for this task. The first route in `feature/imports/`, so `ImportController` and the advice B-030 deferred both land here, plus S-34's page at `/masters/clients/import` with step 1 working and the other four named. 18 backend tests plus the permission matrix's six roles, 8 frontend.*

  > **The header row is exactly `ImportField#header()`, and that is the whole task's load-bearing decision.** Marking required columns with an asterisk was the first draft and it breaks the *next* step: `HeaderMatcher` matches an uploaded file's headings against that string, so the template this product hands the user would fail to auto-match when they upload it back, and B-033 would present twenty columns for manual mapping — a defect that appears one task later, in somebody else's screen, with nothing pointing back here. Required-ness moved to an **Instructions** sheet, which also carries the two rules a user cannot guess: that Client Code silently *updates* rather than duplicating, and that a blank cell does not clear a stored value. `ImportTemplateWriterTest` asserts the round trip — template → `HeaderMatcher` → nothing missing — rather than asserting the strings, so it keeps holding if the matcher gets smarter.
  >
  > **A dropdown Excel cannot carry now fails the request instead of quietly not being there.** The explicit-list constraint stops at 255 characters. The tempting fallback — write the sheet without that column's dropdown — is the worse outcome by some distance: the column looks like free text, the user types their own spelling, and the rejection arrives at step 4 pointing at a cell the template never constrained. It throws, naming the field and what such a schema actually needs (a hidden lookup sheet and a formula constraint). Nothing registered today is close to the limit, and a test says so, so this fires for a schema somebody adds rather than for one that exists.
  >
  > **Every cell is text-formatted, which is not cosmetic.** Excel's general format turns a client code of `00123` into 123 and eats the leading zero of a postal code, and the row is then rejected at step 4 for a value the user never typed, in a file they can see nothing wrong with.
  >
  > **`master.write`, with the counter-argument recorded rather than dismissed.** This template contains no organisation data at all — column headings, enum values already public through the client grid's own filters, and a fictional example row — so a looser rule would leak nothing. It is Admin's anyway because the file's only use is a screen §7.4 puts inside the Admin-only master data module, and a route whose permission is looser than its screen is how a screen acquires a second entrance. That needed a `403` on an operation the contract had declared with only `200` and `401`, plus a `ROWLESS_403` entry: a blank template is not a row, so there is no existence for the 404 rule to protect.
  >
  > **`/imports/users/template` answers 404, and the test that says so is written to be deleted.** The contract's `ImportSchema` enum declares `users`; B-038 has not registered it. The honest answer today is "no such template", not a stub — and the assertion fails the day that one `@Component` lands, which is the right amount of ceremony for "the second schema now exists".
  >
  > **Touches `contracts/` — Stream D's directory, the precedent B-015, B-017, B-018, B-020…B-029 all set** (the operation gains `403` and `404`, one description paragraph; no schema or response body changes, so the generated client moves only in its error union) — **and `security/permission/PermissionMatrix.java`, Stream A's**, which a new route structurally cannot avoid: `PermissionMatrixTest.everyRoutedEndpointHasAMatrixEntry` fails the build without it, which is exactly what CLAUDE.md's "permission-matrix entries for all six roles" is enforced by. **Also `App.tsx` and `features/clients/ClientListPage.tsx`** — the latter to replace the disabled Import button with the `Link` its own comment asked for.
- [x] **B-032** Step 2 — upload, max 5 MB / 5,000 rows, **event-driven SAX parse** (the DOM reader would load the whole workbook per concurrent import). Sheet selector for multi-sheet workbooks. — *`SheetReader` with two implementations, `XSSFReader` + `XSSFSheetXMLHandler` streaming for `.xlsx` and sixty lines of RFC 4180 for `.csv`; the route, the caps and the staging sit above both and mention neither. S-34's step 2 with a drop zone, the sheet selector and a columns-found summary. 42 backend tests plus the six roles, 21 frontend.*

  > **Nothing is buffered whole, and that is a second decision after "use SAX".** The wanted sheet is parsed from the iterator's own stream rather than read into an array first. XML compresses by an order of magnitude or more, so a workbook comfortably under the 5 MB byte limit can hold gigabytes of sheet XML — buffering it turns a file that passed every declared check into an out-of-memory error, and the byte cap cannot see it coming. **The row cap throws from inside the content handler**, so a million-row sheet is refused after 5,001 rows instead of after all of them. And there is a third cap the blueprint does not name: 16,384 columns × 5,000 rows is eighty million cells out of a small file, and the row cap only fires once each row has been built.
  >
  > **`.xls` is refused, which is a deviation from §4B.3's own list and is stated rather than quietly dropped.** This task line says "event-driven SAX parse" and SAX is an XML API: an `.xls` is a binary OLE container of BIFF records with no XML in it, so the only reader for it is `HSSFWorkbook` — the whole-workbook reader this step exists to avoid. The refusal is a 415 naming the fix (Save As `.xlsx`), the picker does not offer it, and adding it properly is one more `SheetReader` and nothing else. Flagged to the developer rather than decided silently.
  >
  > **Dates come back ISO whatever the cell is formatted as, and this is the assertion most worth having.** A date typed into Excel is formatted `dd/MM/yyyy` across most of the world. Left alone it reaches step 4 as `01/04/2026`, `FieldValidators.isoDate()` rejects it, and every row of the file is refused — over a value the user never typed, in cells that look perfectly correct in their own spreadsheet, with nothing to distinguish their file from the template's example row. Only the date *format* is intercepted; a number stays a number.
  >
  > **`StagedUpload.rows` gained a row number, because position stopped being one.** B-030 held rows as a bare `List<Map<String, String>>`, leaving the dry run to compute the number as `index + 2`. That is right only while every row between the header and this one survived parsing, and blank rows do not — Excel leaves trailing blanks constantly and a deleted bad row leaves a gap. The failure would be step 4 saying "row 41: Client Code is required" about a row the user can see is fine, which is worse than saying nothing: they check, find the cell filled in, and stop believing the preview.
  >
  > **Duplicate headings are suffixed rather than one column disappearing.** Rows are keyed by heading, so a workbook with two columns called `Email` has two values competing for one key and the loser is gone — not in the mapping screen, not reported anywhere. The second becomes `Email (2)`.
  >
  > **The sheet selector re-posts the file, and `?replaces=` is what makes that affordable.** The alternative — a "re-read what you staged" route — needs the original bytes of every open upload held for the staging TTL, on a store whose own javadoc calls itself a stopgap, to save re-sending a file the browser is still holding. B-030 predicted B-032 would replace `InMemoryImportStagingStore`; it did not, because object storage is a feature of its own (client, bucket, lifecycle, credentials in four environments) and folding it in would have made a step-2 task about infrastructure. What B-032 did instead was avoid making the store carry more. Recorded in the package README so the unmet prediction is visible rather than forgotten.
  >
  > **A multipart defect Stream C had already found, and now two features carry the same workaround.** Orval pins `Content-Type: multipart/form-data` on the generated call and `api/http.ts` spreads caller headers after the branch that deliberately omits one for a `FormData` body, so a body arrives with no `boundary` and Spring's `@RequestPart` answers 400 or 500 for every upload. `uploadTicketAttachment.ts` documents it and works around it; this does the same, with a regression test. **The one-line fix is in `api/http.ts` — Stream D's** — and the second instance is the argument for making it.
  >
  > **A `MaxUploadSizeExceededException` handler was written, found unreachable, and deleted.** The container refuses a body over `spring.servlet.multipart.max-file-size` while resolving the multipart request, before a handler has been chosen, so controller-scoped advice is matched against a handler that does not exist yet. Left in, it would have read as coverage and provided none; Spring's own problem-details handler (A-020) answers those, with the framework's wording rather than ours. Stated in the controller javadoc instead.
  >
  > **Touches `contracts/` — Stream D's directory, the precedent B-015, B-017, B-018 and B-020…B-031 all set** (the upload operation gains two query parameters and four declared responses, and `ImportUploadResponse` gains `fileName` and `sheet`; the client regenerates) — **and `security/permission/PermissionMatrix.java` and `PermissionMatrixTest.java`, Stream A's.** The second of those is not just a matrix row: the route declares `consumes = multipart/form-data`, so the harness's JSON fixture request answers 415 during handler mapping — *before* `@PreAuthorize` — and all six of its cases would have passed while asserting nothing about authorisation. The same silent-green failure `everyRouteWithARequiredBodyCarriesAFixture` exists to catch for `@RequestBody`, which cannot see a `@RequestPart`. **Also `frontend/src/mocks/`.**
  >
  > **`frontend/src/api/generated/` moved in two files nobody edited.** B-029 changed a parameter description in the contract without regenerating, so the committed client was stale; regenerating for this task picked it up. Comment-only, and it is what the staleness check exists to notice.
- [x] **B-033** Step 3 — column mapping, auto-matched by header, manual override per column, saveable presets. Unmapped required columns block Next. — *`MappingStep` renders one row per column the registration declares, read from a new `GET /imports/{schema}/fields`; the mapping itself never goes to the server, because `/validate` and `/commit` already carry it. Presets are three routes over `import_mapping_presets`, upserting on `(schema, name)`. `columnMapping.ts` holds the rules as pure functions. 25 backend tests plus 6 against real MySQL and the six roles on four routes, 25 frontend.*

  > **Step 3 needed no route for the mapping, and that is the finding rather than a shortcut.** The mapping is chosen in the browser out of what step 2 returned and travels with step 4's `/validate` body and step 5's `/commit` body — which the contract has said since B-030. A parked server copy would be a fifth piece of wizard state to keep in step with the other four, and the dry run needs the mapping in its own request regardless because the user can go back and change it. What was genuinely missing was *our* half of the mapping.
  >
  > **`GET /imports/{schema}/fields` exists so the field list stays declared once.** §4B.3's rule for this step is "unmapped required columns block the Next button", and `required` is derivable from nothing step 2 returns — the upload response describes the user's file. Without this route the screen would carry a copy of `ImportField`'s declarations in TypeScript, which is a second declaration of the client master and the exact thing B-030's registry exists to prevent; B-038 would then have needed a third. A *projection* of `ImportField` rather than the record serialised, because `validators()` is a list of lambdas with no wire representation and serialising a schema definition whole is how an internal type becomes a public contract by accident. Not folded into the upload response, though that would have saved a route: it is a property of the schema rather than of the file, the sheet selector re-posts the upload, and a client wanting to say what the import accepts before a file is chosen could not.
  >
  > **Presets are a table, not `localStorage`, and the cheaper option was rejected on what the feature is for.** §4B.3 says "reused for the *next* import" — next month, quite possibly from a reimaged laptop and by a different Admin. A preset records how another system's export is shaped: organisation knowledge, not a display preference. Org-wide rather than per user for the same reason; `created_by` is recorded, is on no key, and an unidentifiable caller saves with no attribution rather than being refused, because failing a legitimate save to protect a column nothing reads is the wrong trade. **The table-count tripwire that made this expensive is gone** — A-029 replaced `COUNT(*) == 46` with a named set asserted by `.contains`, so a new table is no longer a cross-stream edit to Stream A's file. The name was added anyway, which is the discipline that comment asks for.
  >
  > **Saving is an upsert on `(schema, name)`, so the route answers 200 and declares no `Idempotency-Key`** — honestly rather than by omission. That is what Save means to somebody who has just corrected one column; the alternative is five presets called *CRM export* in a picker that cannot tell them apart. Case-insensitive through the table's collation for the same reason. The read-back is by `(schema_key, name)` and not by `LAST_INSERT_ID()`, which reports 0 for an update that changed nothing — so re-saving a preset whose mapping was already correct would have answered with a preset id of zero.
  >
  > **A preset naming an undeclared field is refused with a 422, not trimmed, and this is the decision most worth keeping.** A preset is applied weeks after it is saved, against a file nobody is looking at today. Dropping the unknown key silently leaves it *looking* complete in the picker, applying cleanly, and the column it was meant to map simply not imported — which surfaces as four hundred clients created with no support email and nothing on screen having said so. The source column is **not** checked and cannot be: a preset exists to be applied to a *different* file, so "does this column exist" has no answer at save time. That question is asked on the client, against the file in hand.
  >
  > **Applying a preset is a function because it can fail partially, and both failure modes are real.** Carrying over an entry whose column this file lacks is the worse one: the mapping would name a heading that is in no dropdown on screen, so the row would render unmapped while the required-field check counted it as mapped — and Next would be enabled over a column that is not mapped at all. Dropping it silently is the other: a user who picks *CRM export* and watches three of eleven fields fill in cannot tell a preset that was always partial from one whose columns have been renamed. So the dropped entries come back with the mapping and the screen names them.
  >
  > **One row per *our* field, not per *their* column.** §4B.3 says "a manual override dropdown per column" and the direction has to be the mapping's own. Rendered the other way round — a row per column in the file with a dropdown of our fields — the screen cannot show a required column that is absent from the file entirely, because there is no row for a column that is not there. That is the one case this step exists to catch.
  >
  > **`@Transactional` was written on the preset service and removed once what it bought was examined.** `list` and `delete` are one statement each; `save` is two, and the race a transaction looks like it closes — another Admin re-saving that name in between — is not closed by one, because under `READ COMMITTED` the other session's commit is visible to the second statement either way and both statements address the row for `(schema_key, name)`. What it *did* do was open a JPA `EntityManager` per call, which put a live database between the routes and any test of them. Four route tests gained, nothing lost.
  >
  > **Two contract-vs-code conflicts, found the way B-030 found its two — by reading them against each other.** The field-type enum was drafted `TEXT|EMAIL|DATE|NUMBER|BOOLEAN|ENUM` and `ImportFieldType` is `TEXT|EMAIL|DATE|ENUM|INTEGER`: the generated TypeScript union would have rejected a response the backend produced for every numeric column. Code won. And **the mock's `suggestedMapping` was keyed `domain` and `isActive`, which `ClientImportSchema` does not declare** — harmless while step 2 only counted the entries, but step 3 looks the mapping up by field name, so two columns would have shown as matched in the summary and unmapped in the table.
  >
  > **Touches `contracts/` — Stream D's directory, the precedent B-015, B-017, B-018 and B-020…B-032 all set** (four operations, eight schemas, two `check-conventions.py` exemption lists; the client regenerates) — **and `security/permission/PermissionMatrix.java`, Stream A's**, which four new routes structurally cannot avoid. **Also `frontend/src/mocks/` and a migration in `backend/domain/`**, which touches none of the append-only tables. `features/clients/README.md` lost a paragraph that was in it twice.
- [x] **B-034** 🔴 **Step 4 — dry-run validation preview.** Nothing is written. Per-row status: will create / will update / duplicate in file / rejected + reason. Summary counts. — *B-030 had already built the verdicts; what was missing was a route to reach them and the refusals that keep a caller from reaching them with a request whose answer would be useless. `ImportValidationService` resolves the schema, finds the staged upload, checks both halves of the mapping and maps the rows; the engine was untouched except where the blueprint asked for more. `ValidationStep` is the summary, the four filter tabs and §4B.3's own table. 28 backend tests plus 2 against real MySQL and the six roles on the new route, 40 frontend.*

  > **`ImportSchemaDefinition.findExisting` went from `Set<String>` to `Map<String, Map<String, String>>`, and that is the task's real decision.** §4B.3's step-4 table reads `♻ Will update │ Name, phone` — it names *which fields would change* — and existence alone cannot answer that. Without it "38 will update" is a verdict nobody can act on: a spreadsheet correcting six phone numbers and one overwriting every address in the account produce the same word, and the user is being asked to approve one of them. B-030 had settled `reason = null` for an update and the contract documented it, so this is a correction to a decision rather than a gap. The comparison stays in the engine and stays schema-agnostic — only the current values need a registration. An empty map is still permitted and still means "it exists, I cannot say what is in it", so B-038 pays nothing for this.
  >
  > **Three rules inside that comparison, each a way it could be confidently wrong.** *Only fields the row carries are compared*, because those are the only fields a commit writes — reporting the rest would promise an erasure that will not happen, which is the most alarming thing this message could get wrong. *The natural key is excluded*, since it is how the row was matched and differs only in case, which the upsert leaves alone. *A row that matches what is stored says `No change`* rather than nothing — silence there reads as a change nobody could name.
  >
  > **`ClientImportSchema.currentValues` is written directly against `upsert`, one `put` per `set`.** A column added to one and not the other is a field the preview silently never reports as changing, and there is no test that can notice a field nobody thought of. Adjacency is the only thing that makes it visible in review, so the two lists are adjacent and the javadoc says why. The `ClientImportUpsertIT` cases are there for the half a unit test cannot check — a stub schema agrees with itself by construction.
  >
  > **Four refusals, split by remedy rather than by cause.** All four are 422 and all four mean "the request refers to something absent", so one `import-failed` would have been defensible on the status — and would have made the screen parse English to choose between the only three things it can offer. Instead: `import-upload-unavailable` sends the user to step 2, `import-incomplete-mapping` and `import-unknown-column` to step 3, and an undeclared target field reuses **B-033's own `import-unknown-field`**, because to a caller "this import has no such column" is one condition whichever request surfaced it.
  >
  > **An expired upload and a mismatched sheet are deliberately one condition.** They read as two; the remedy for both is the same action, and two types would let the screen write two sentences that both end "upload the file again". Expired and never-existed are indistinguishable for a duller reason — the store purges on access, so by the time the lookup misses there is nothing left to say which it was.
  >
  > **An incomplete mapping is refused rather than previewed, and this is the one that would have shipped wrong.** Running it works: with `name` unmapped every row comes back rejected reading "Name required", which points at the spreadsheet when the fault is one dropdown on the previous step — the user goes and checks, finds the cell filled in, and stops believing the preview. With the *natural key* unmapped it is worse than noise: nothing can be matched, so the preview would promise to create four hundred clients that already exist. B-033 enforces the same rule in the browser; that one explains the button, this one makes the answer correct for any caller, B-035's commit included.
  >
  > **An unknown source column is refused rather than read as blank.** `ImportMapping` reads by heading, so a heading the sheet lacks yields nothing and the field is simply absent — the commit runs and the Support Email column the user carefully mapped was never read, with nothing on screen having said so. The realistic cause is a preset applied to a renamed export, which `ImportMappingPresetService` explains it *cannot* check at save time; this is where that question first has an answer, so this is where it is asked.
  >
  > **The dry run does not consume the upload.** Read the preview, go back to step 3, change one column, run it again — that is the ordinary path through this screen, and a route that discarded the staging entry would answer "your file expired" to the second attempt. The screen's mirror of that is dropping the preview whenever the mapping or the file changes: a stale preview is specific, authoritative and about a run that will not happen.
  >
  > **A route test with a real 200 and no database, which is not a contrivance.** The engine only probes for rows that survived content validation, so a file whose every row is rejected reaches a genuine 200 through the no-connection context — and that is also the response an Admin gets from a file full of mistakes, which is the case this screen exists for. The create-and-update verdicts stay in `ClientImportUpsertIT` where a real collation can disagree with them.
  >
  > **`domain/clients/ClientRepository` lost `findClientCodesIn` and gained `findByClientCodeIn`.** The projection of one column cannot answer the changed-field question, so it was replaced rather than joined by a second query — one caller, and two ways to ask the same thing is how they drift. It costs entities for the matched subset instead of strings: bounded by the 5,000-row cap, the same order of memory as the staged sheet already in heap, read inside `@Transactional(readOnly = true)` with only strings escaping.
  >
  > **Touches `contracts/` — Stream D's directory, the precedent B-015, B-017, B-018 and B-020…B-033 all set** (one operation gains five declared responses and a description; `ImportRowVerdict` and `ImportPreviewResponse.data` gain `required` lists, so the generated TypeScript stops making every field optional; one `check-conventions.py` exemption) — **and `security/permission/PermissionMatrix.java`, Stream A's**, which a new route structurally cannot avoid. **Also `frontend/src/mocks/`**, where the step-4 fixture was rebuilt: it answered `willCreate: 120` beside four hand-written rows, so the screen showed "All 4" from `rows.length` and tabs summing to 128 from the counts — two contradictory numbers on the one screen whose whole job is being believable. It now derives its counts from its rows, the way the real `ImportPreview` does.
- [x] **B-035** Step 5 — commit as a background job with progress bar. **Upsert on client code — update, never duplicate.** — *`ImportCommitService` resolves and refuses, re-runs the dry run, opens the batch, releases the staging entry and hands an immutable row list to a private pool; `ImportCommitRunner` walks it one transaction at a time. `ImportBatchController` is the progress poll, ETag'd over the counters. `CommitStep` is the bar, the counts and the two terminal states. 23 backend tests plus 9 against real MySQL and the six roles on two new routes, 28 frontend.*

  > **The commit body carries no verdicts, and could have.** It takes the same `uploadId` and `mapping` step 4 took and re-derives the judgements server-side — one pass over rows already in heap and one existence probe. Sending the approved preview would have been the obvious saving and a mistake of the kind that is invisible until it is exploited: the rows written would be whatever the caller said they were, and §4B.3's "nothing is written until you have seen the preview" would be a convention the browser observes rather than a property of the system.

  > **B-034's four refusals moved into `ImportRequestResolver` rather than being copied.** `/commit` has to refuse the same requests in the same order with the same `type`s — an incomplete mapping is not a different condition for arriving one step later, and a screen that handled it at step 4 must not need a second branch. Two copies agree on the day they are written, and this is the copy where disagreeing writes to the client master. `ImportValidationServiceTest` is unchanged by the move, which is the point: its assertions are about what a caller sees.

  > **Two new refusals, and they are two because the remedies are opposite.** `import-nothing-to-commit` means no row is writable and the user has to go back to their spreadsheet; `import-rejected-rows-present` means most of the file is fine and they asked for all-or-nothing. One shared type would put an "import the valid rows only" offer on a screen where there are none. **A run that would write nothing is refused rather than accepted and completed instantly** — `import_batches` is B-037's audit trail, and a row claiming a file was imported when nothing was is a false entry in the record that exists to make bad imports traceable.

  > **`skipRejected: false` is all-or-nothing, not "write them anyway".** The permissive reading is not an operation this feature can perform: a row the engine rejected has no valid value to write. The strict one is a real requirement — §4B.3 offers "import valid rows only" as a *choice*, and a choice needs an opposite.

  > **The staging entry is released before the response, not after the run.** The job outlives the thirty-minute TTL, so it holds its own row list and never looks the upload up again. The visible consequence is that committing the same `uploadId` twice answers `import-upload-unavailable` — which is the honest refusal for the request that would otherwise write the file twice, and is why the route needs no idempotency handling despite the contract declaring the parameter.

  > **One row, one transaction, and a row that fails at write time costs one row.** A file of five hundred where row 314 breaks a constraint no validator declares must not lose the other 499. The cost is that an interrupted run leaves half the file imported — correct here rather than a compromise, because the operation is an upsert on the natural key, so re-running finishes the job instead of duplicating what landed. Counters flush every fifty rows: per row would double the feature's write load so that a bar polled every two seconds could be accurate to a row nobody can read.

  > **Saturation is a 503, where `AttachmentScanConfig` uses caller-runs.** That is the one place this diverges from the precedent one feature over, and deliberately: a scan is seconds, a commit is five thousand upserts, and running it on the request thread holds open the connection whose response tells the browser which batch to poll. The batch is marked `FAILED` rather than deleted — a refused attempt that left no trace is indistinguishable from an attempt nobody made.

  > **`processed` is derived, not a column.** `created + updated + rejected`, and there is no `processed_rows` in the baseline DDL — so no migration. A fourth number holding the sum of three is a fourth number that can disagree with them.

  > **Touches `contracts/` — Stream D's directory, the precedent B-015, B-017, B-018 and B-020…B-034 all set** (two operations gain declared responses and descriptions; `ImportBatchResponse.data` gains a `required` list and two fields, so the generated TypeScript stops making every counter optional and a progress bar reading `processed ?? 0` stops being possible; two `check-conventions.py` exemptions) — **and `security/permission/PermissionMatrix.java`, Stream A's**, which two new routes structurally cannot avoid. **Also `frontend/src/mocks/`**, where the step-5 fixture had the same class of defect B-034 found in step 4's: `batchId` was a UUID string against a contract that has said `int64` since B-030, and the two handlers disagreed with each other — commit answered `rejected: 0` and the poll answered `rejected: 3` for the same run, then answered COMPLETED on its first call, so the progress bar could never be seen moving. It now holds a real batch and advances it a row per poll.
- [x] **B-036** Error report generation — `.xlsx` with an appended Reason column, so users fix and re-upload only the rejected rows. — *`ImportErrorReportWriter` renders any registration; `ImportErrorReportService` decides when a report exists and who may read it; `ImportReportStore` + `S3ImportReportStore` put it in the bucket PLAN.md §2.2 named for it. `ImportCommitRunner` generates it at the end of the walk and `ImportBatchService.finish` stamps the key on the same write that sets the status. One new route, and the step-5 button that has been disabled since B-035 now works. 27 backend tests plus 2 more against real MySQL and the six roles on the new route, 5 frontend.*

  > **It has to be written during the run, and that decided the whole shape of the task.** The rejected rows are never persisted: B-035 releases the staging entry before the job starts and re-derives the preview rather than storing it, so once `ImportCommitRunner` has walked its list there is nothing anywhere to build a report from. "Generate it on download" was not an option to weigh. So the commit keeps the non-writable verdicts instead of counting them — `unwritten`, whose `size()` is the `int` it replaced — and the report covers **every row the run did not write**: the dry run's rejections, the in-file duplicates, and the rows a database constraint refused after the preview had approved them. All three are rows the user's file contained and the client master did not receive, which is the only distinction that matters to somebody fixing a spreadsheet. The write-time reason is deliberately not the JDBC message — that string carries a constraint name, a table name and sometimes the SQL, on its way into a file that gets emailed around.

  > **The report is written before the status turns terminal, never after, and the ETag had to grow to match.** A client stops polling the instant it reads `COMPLETED`, so a key stamped in a later transaction is a report the screen that wanted it has already given up asking for — the button would stay disabled on a run that has one. `finish` now carries the key, so one write settles the status, the counters and the report together; `ImportDtos.Batch.etag` gained `errorReportUrl` for the same reason, because a validator that does not cover every field of the representation is a `304` that withholds a change. The mock server had the identical defect and was fixed the same way.

  > **The report is emitted in the *template's* shape, not the upload's, which is what makes the blueprint's promise literal.** The columns are `ImportField#header()` in template order plus a leading `Row` and a trailing `Reason`, so `HeaderMatcher` matches every one of them on the way back in and the corrected rows need no remapping and *cannot* be remapped wrongly. Echoing the user's own headings would look more faithful and would land them on step 3 with a mapping to rebuild — the step this file exists to save them. The cost is stated rather than hidden: columns they never mapped are absent because they were never read, and `Row` is what bridges it. Pinned as a round trip, not as string equality. **One gap worth knowing before somebody files it as a bug:** a file where *every* row is rejected is refused at `/commit` as `import-nothing-to-commit`, so it has no batch and no report — which follows from that refusal being right, since a batch claiming to have imported a file it never touched is a false entry in B-037's audit trail.

  > **Touches `feature/tickets/attachments/` — Stream C's directory — and it is the move that package asked for in writing.** `AttachmentStorageProperties`' own javadoc said PLAN.md §2.2 lists three users of the bucket and that "when the second one arrives this record should move out of this package". B-036 is the second. The `S3Client` and `S3Presigner` are now `api/storage/ObjectStorageConfig`'s and `AttachmentStorageConfig` keeps only the bean that is about attachments; C's 174 attachment tests are unchanged and green. The alternative — a second `S3Client` in `feature/imports` — does not work, because two beans of one type make C's own by-type injection ambiguous. **Also touches `contracts/` (one new operation, `errorReportUrl` and the ETag note rewritten, one `check-conventions.py` exemption)** and **`security/permission/PermissionMatrix.java`, Stream A's**, which a new route structurally cannot avoid.

  > **`errorReportUrl` is a route, not a signed URL — the one place this diverges from §4B.4 on purpose.** Attachments are served as short-lived presigned URLs, which is right for a 50 MB video read repeatedly. An error report is small, read once, and a verbatim extract of the client master; a signed URL is a bearer credential that outlives the screen that minted it, in a browser history, a chat paste and a proxy log. Proxying it costs a few hundred kilobytes and buys `master.write` checked at the moment the bytes are read — `ImportReportStore` has no method that can produce a public address at all. And a storage failure costs the report, never the import: by then four hundred clients are already written, and marking that batch `FAILED` would report the wrong thing to everyone who reads it. **`S3ImportReportStore` itself is the one class no test reaches** — this project has no MinIO container, C-025's attachment tests mock storage for the same reason — flagged in the package README rather than left looking covered.
- [x] **B-037** `import_batches` traceability — every import identified, a bad one reversible as a set. — *Two routes: `GET /import-batches` is the history panel, `POST /import-batches/{batchId}/reverse` is the set. `ImportSchemaDefinition` grows a seventh method so B-038 gets reversal without writing any of it; `ImportReversalService` holds the refusals, `ClientImportSchema.reverse` holds the deletes, `ClientImportReferences` holds the one query that decides what must be kept. One migration, four columns. 30 backend tests plus 8 against real MySQL and the six roles on two new routes, 25 frontend.*

  > **Both halves of the blueprint's promise were unbuilt, and the rails for them were already there.** §4B.3 closes with *"every import writes an `import_batch` row so a bad import can be identified and reversed as a set"*, and §17 names it the mitigation for "Client Excel import silently corrupts the master". B-035 wrote the row and stamped `clients.import_batch_id`; `ImportBatchRepository.findByEntityOrderByCreatedAtDesc` and `ClientRepository.findByImportBatchId` had both been sitting there since B-030 and B-035 with javadoc naming this task and **zero callers**. Until this landed a batch id was known only to the browser tab that started the run — so "identified" was true of the database and of nothing a person could reach.

  > **Reversal is on the SPI, not in the engine, and that is the task's one structural decision.** Everything that is true of any registration — which requests are refused, in what order, what the batch row records — stayed in `ImportReversalService`. What only a registration can know — that a client is deleted, that its contacts go with it, that a ticket keeps it — went behind `ImportSchemaDefinition.reverse`. B-038 registers resources and gets reversal for free, which is the same rule the other six methods exist for.

  > **`reversed_at` is four columns, deliberately not a fifth `status`.** `REVERSED` was the obvious shape and it destroys the only record of the outcome: `ImportBatchStatus` is explicit that COMPLETED "is not a synonym for no rejections", so overwriting it collapses "completed with 6 rejections, later reversed" and "failed at row 314, later reversed" into one word — and leaves B-036's `error_report_key` belonging to a run whose status no longer explains why it has one. `retained_rows` is stored beside `reversed_rows` because neither is derivable afterwards: once the clients are gone the `import_batch_id` pointing at the run is gone with them, so an unreversed batch and a fully reversed one both COUNT zero.

  > **A reversal deletes what the run created and cannot restore what it updated, and the API says so rather than letting a count imply it.** `import_batch_id` is stamped on insert only — B-035's decision, made for exactly this — so a spreadsheet that created 12 clients and corrected 400 phone numbers reverses to 12 deleted, not to 412 clients removed from the master. There is no before image anywhere, so the other 400 could not be restored even if they were in scope. The response carries `updatedRowsNotReverted` and the confirmation dialog states it **before** the button, because somebody who reads it afterwards has already pressed it believing something else.

  > **A client that has since been ticketed is kept, not destroyed, and a partial reversal is an outcome rather than a failure.** `fk_tickets_client` is RESTRICT; the two ways to force the count to zero are failing the whole reversal because one client got used, or deleting a ticket's client. `client_contacts` is RESTRICT too and goes the other way — a contact is wholly owned by its client and means nothing without one, where a ticket is independent work that merely names it. That line is the whole of what `ClientImportReferences` decides, and it is asserted against a real MySQL because every claim in it is a claim about a foreign key and all of them are true against a mock.

  > **Three refusal types, because the remedies differ.** `import-batch-not-finished` clears itself and the screen should wait; `import-batch-already-reversed` never clears and the screen should stop offering the button; `import-schema-unavailable` needs an operator. The second is the one that protects the record: a second reversal would delete nothing, so succeeding quietly is tempting — and would overwrite `reversed_at` and both counters with its own zeroes, a false entry in the one table that exists to make bad imports traceable. `reversible` is computed server-side for the same reason `errorReportUrl` is: those two rules are the service's, and a copy in TypeScript is a second statement of them on a screen whose job is deleting rows.

  > **`POST`, not `DELETE`, and `master.write`, not a capability of its own.** The resource at the path is the batch, and the batch is emphatically not deleted — it is the audit trail and survives with four more columns filled in. And although this is **the only route in the product that deletes rows from the client master** (B-029 deactivates; everything else preserves), a separate capability would mean an Admin who can write 412 clients in one action cannot take those same 412 back.

  > **The engine isolation guard caught two things, and both were right to catch.** `ImportBatchUserNames` reads `domain.identity` to name an importer — engine behaviour by the guard's own reasoning, since `imported_by` is a column on the engine's own row and no registration would resolve it differently, so it is a *per-file* exemption with a liveness test rather than a package-wide hole. And `registrationsAreIndependent` flagged `ClientImportReferences` for naming `ClientImportSchema` in its javadoc; that rule now uses `implements ImportSchemaDefinition` as its definition of a registration, agreeing with `registrationsAreWhereTheyAreExpected` two tests below it, which is what it should have said all along.

  > **Two defects the tests found rather than review.** `ImportBatchService.history` called `Map.of(...).get(null)` for a run with no importer — an immutable map throws on a null key, so one `dev-noauth` import would have 500'd the whole history panel; found by the IT against a real database. And the reversal's refusal notice rendered *behind* the open confirmation dialog, which is `aria-hidden` from everything under it — an alert no screen reader could reach and nobody could act on without first dismissing the thing covering it; found by the panel's own test looking for `role="alert"`. The dialog now closes on a refusal.

  > **Touches `contracts/` — Stream D's directory, the precedent B-015…B-036 all set** (`ImportBatchResponse.data` is extracted as a named `ImportBatch` schema now that three operations return it, plus seven fields, two new operations and two response schemas; two `check-conventions.py` `ROWLESS_403` entries and one `NO_PAGINATION` entry) — **and `security/permission/PermissionMatrix.java`, Stream A's**, which two new routes structurally cannot avoid. **Also `frontend/src/mocks/`**, where `importBatches` stops being seeded empty: that was right for the progress bar, whose runs must be created by pressing Import, and exactly wrong for a panel whose point is showing runs from before this session — an empty history could only be seen by importing first, and a reversal could not be exercised at all.
- [x] **B-038** Resource bulk import — the second registration, not a second build. — *`ResourceImportSchema` is one `@Component`: fifteen field declarations, `findExisting`, `upsert`, `reverse`. `ResourceImportRepository` holds its SQL. One migration, one column. The frontend wizard moves out of `features/clients/`, becomes `ImportWizardPage(config)` and is registered twice. 57 backend unit tests plus 21 against real MySQL, 162 frontend.*

  > **The engine did not change, and that is the whole deliverable.** No route, no branch, no edit to `ImportSchemaRegistry`, nothing under `feature/imports` outside `schemas/`. The contract already declared `users` in its `ImportSchema` enum and `RESOURCE` on `ImportBatch.entity` — D-001 wrote both — so no operation, schema or permission-matrix row was added either. What proved it is that **seven assertions across six test classes existed whose only claim was that `/imports/users/*` answered 404**, each carrying a note to delete itself the day the second `@Component` landed. All seven failed on the first run after it did, and nothing else in the suite moved.

  > **Repointed at `widgets` rather than deleted, against what those notes asked for.** They said "delete this when B-038 lands", and the reason they gave was that `users` was the live case — not that the rule had stopped mattering. An unregistered path segment must still answer 404 rather than 400, 406 or an empty success, and after B-038 nothing else pins that; `ImportTemplateControllerTest`'s also asserts the problem document's content type, which is exactly where a `produces` an-.xlsx handler could turn a 404 into a 406 the frontend cannot read. Deleting six live assertions to honour the letter of a note would have been the more obedient reading and the worse one.

  > **`employeeCode` is deliberately *not* `FieldValidators.alphanumeric()`, despite that method's javadoc naming this task as its expected user** — "B-038's resource registration has fields — an employee code — where letters and digits really is the constraint". It does not: S-08 accepts any non-blank string up to 20 characters and `EDU-0142` is a code organisations issue. This is B-028's defect exactly, and it costs more here than it did there. Because this is the upsert key, a refused value is not a message somebody reads and fixes — it is an employee the import silently declines to update inside a run that reports success. A prediction written into a javadoc is not evidence about the column.

  > **Reporting manager is the omission worth arguing about, and it is refused for a sharper reason than the client registration's account manager.** That one was excluded because a spreadsheet can only carry a person's name; an *employee code* identifies a manager exactly, so the column would resolve reliably. It is still absent because B-012's cycle rule is one of this stream's red rules and holds **at any depth** — A→B→C→A, which the database trigger never caught. A file can name a manager three rows below the person reporting to them, so honouring the column means ordering the whole file topologically and re-checking the chain as each row lands: a whole-file operation the row-at-a-time SPI has no hook for, where the failure mode is not a rejected row but a cycle written into the reporting line. Set on S-08 afterwards, where the check already works.

  > **Role is a static `ENUM` of six codes, and the third choice was the right one.** Reading `roles` at request time is more correct in principle — S-11 can create a role — and cannot be had: `ImportSchemaRegistry` calls `fields()` during its own construction to refuse a malformed registration at boot, and three `@SpringBootTest`s on this path run with Flyway excluded and no database. Resolving at write time instead would fail every row *after* the preview approved them. So the vocabulary is declared, and pinned by a test that reads the `@Pattern` off `ResourceWriteRequest` by reflection rather than restating it — a third copy is how a vocabulary drifts, and the direction it drifts is always "somebody added a role to the form".

  > **The password is generated, hashed and thrown away, and the two cheaper options were both refused.** `users.password_hash` is NOT NULL, five thousand temporary passwords cannot come back through a progress bar, and `/auth/forgot-password` already exists — so an imported account gets an Argon2id hash of a random string nobody ever sees, plus `must_change_password`. **That costs roughly a tenth of a second per created row** at §10.3's parameters, which is stated in the class comment rather than discovered by whoever runs a 5,000-row onboarding. Hashing once per batch is cheap and puts a shared credential in the identity table. An unusable placeholder is cheaper still and arguably *safer* — nothing could match it — but gives `password_hash` a second meaning on Stream A's table that every reader would have to learn. Neither is a change to make on a masters branch; an IT asserts two imported accounts do not share a hash, so a later "optimisation" fails rather than lands.

  > **Reversal is where resources stop resembling clients.** `clients` has two inbound foreign keys worth reasoning about; `users` has around forty, because a person is referenced by every ticket they touched, every history row they wrote and every comment they left. Enumerating them would be a list that goes stale the first time another stream adds a column, and a stale list fails in the worst direction — the missing check reads as "nothing references them". So the pre-checks cover only the three cases that both happen and can be put in a sentence somebody can act on (tickets, direct reports, managed projects), and the `DataIntegrityViolationException` catch is what makes it complete. B-037's two-layer shape with the weights reversed: there the pre-check answered almost everything, here it carries the head and the backstop carries the tail.

  > **What goes with the account and what keeps it alive is the only thing `reverse` really decides.** A membership, a reset token, a password-history row, a TOTP recovery code, a leave record and a summary row are facts about nobody but this person and go with them — all six are RESTRICT, so without a statement each a reversal would retain every person who had been put on a project, which is most of a joiner list. `user_roles`, `notifications`, `notification_preferences`, `push_subscriptions` and `chat_participants` are ON DELETE CASCADE and are deliberately *absent* from that list rather than forgotten. A ticket, a comment, an effort log or a project they manage is independent work, and the two ways to make the delete succeed are destroying it or destroying its author.

  > **The dry run cannot catch a duplicate email, and the API says so rather than letting it surprise somebody.** `users` has three unique indexes and only one can be the natural key, so a file carrying two employee codes with one email address passes step 4 and the second row is refused by `uq_users_email` at write time — counted rejected, carried into B-036's error report like any other. Widening the SPI to "declare your other unique constraints" would be a generalisation built for one caller.

  > **The frontend wizard moved out of `features/clients/`.** It was `ClientImportPage` naming clients in twenty-four places; it is now `ImportWizardPage(config)` under `features/imports/`, with `ClientImportPage` and `ResourceImportPage` as two ten-line registrations and two routes. The nouns are configured rather than genericised into "record" on purpose: the sentence this screen is judged on is *"nothing has been written — no client has changed"*, and "no record has changed" is not the reassurance somebody about to import four hundred rows is asking for. `importHistory.ts`'s sentence builders take the nouns as an argument, with a test in the *other* registration's words — the way a threaded-through parameter silently fails is being accepted and then not used.

  > **Touches `contracts/` — Stream D's directory, the precedent B-015…B-037 all set** — but only to correct six descriptions that said `users` was unregistered "until B-038", which stopped being true. No operation, schema, parameter or enum changed; the generated client was regenerated for the two files that carry the changed prose. **Also `frontend/src/mocks/`**, where the handlers become schema-aware through one `IMPORT_REGISTRATIONS` table rather than a branch per route, and a third `import_batches` row is seeded under `RESOURCE` — with only CLIENT runs the panel's `entity` filter cannot be told from a broken one, since both render the same empty resource history. **`PermissionMatrix` is untouched**, which is the point: schema is a path segment, so B-038 registered an entity without adding a route.

  > **`users.import_batch_id` is flagged for Stream A on B-011's precedent (V20260811_1520).** Not `tickets` and not one of the three append-only tables, so CLAUDE.md asks for no review — but the identity table is close enough to the security core that it should not change on a Stream B branch unremarked. One nullable column, one index, one RESTRICT foreign key; a rollback is one DROP. "Stamped on insert only" is held by `ResourceImportSchema.upsert` and its tests rather than by DDL: nothing in the schema can express it, and a trigger refusing an UPDATE of one column on `users` is an immutability guard Stream A should own if it is ever wanted.

### Workflow templates
- [x] **B-039** Status/stage/workflow master tab 1 — statuses, categories, allowed-transition matrix per role. **S-13** — *`api/feature/masters/statuses/` is the first code to serve either `statuses` or `workflow_transitions`; one migration adds the category §7.4 asks for; `features/masters/statuses/` is the S-13 shell with tab 1 live and B-040/B-041 disabled beside it. Six new routes, all six roles on each. 35 backend unit tests plus 20 against real MySQL, 44 frontend.*

  > **`category` cannot be derived from `is_open` and `is_terminal`, which is why it is a column.** NEW and REOPENED are To-do while ON_HOLD, AWAITING_INFO and REWORK are In progress — five statuses carrying `is_open = 1, is_terminal = 0`, identical on both columns and three categories apart. Deriving it would hard-code the eight seeded codes in Java, putting the master's own vocabulary back into the application S-13 exists to take it out of. **RESOLVED is the row that keeps the column honest**: `DONE` while `is_open` stays 1, because the category describes the *work* and `is_open` describes the *ticket record*. The obvious fourth guard — "DONE implies not open" — would refuse a seeded row, so it does not exist in the service, the form or the mock, and a test in each says so.

  > **Retiring a status is not local, and it is the one thing in this task that would have shipped wrong.** The gate Stream C consults reads the *transition* row's `is_active` and never looks at the status — so retiring ON_HOLD without touching the matrix leaves `IN_PROGRESS → ON_HOLD` live, and the engine goes on moving tickets into a status the master says is gone. Nothing fails; the two disagree, and it surfaces on a ticket page weeks later. Fixing it in C's gate was the smaller diff and is not this stream's file. So the retire **refuses while any ticket is still in the status** — those tickets would be stranded with no move offered anywhere, the same "one screen must not put another into a state it cannot get out of" rule that makes `taskTypeCount` block a level retire — and otherwise deactivates every transition on **both ends** in the same transaction, returning the count so the dialog states it before the click. **Reactivating does not bring them back**: a restore would have to guess which rows this retire cleared versus which were cleared deliberately, and on a whitelist a wrong guess grants a move nobody approved.

  > **G-3 stays data, and refusing to hard-code it is the decision.** B-003's seed header said changing "may a Developer close a ticket?" should be "a seed edit, not a deploy"; S-13 makes it a screen edit. The obvious move was to lock `RESOLVED → CLOSED` for Developer/QA/Deployment in the service — and that would put back into code the one decision `workflow_transitions` exists to keep out of it, leaving an organisation whose sign-off process differs from ours unable to say so without a release. The grid **flags** the four governance-settled moves with their reason and lets an Admin change them; `StatusTransitionServiceTest` asserts a Developer close is *accepted*. What is refused unconditionally is the one edit with a global consequence: **at least one on-create row must survive**, or no role can raise a ticket on any screen and this is the only screen that could undo it.

  > **A ninth status is refused with 400, the same call B-021 made on `Level`.** `StatusCode` is a closed eight-value enum typing `Ticket.status` and two query parameters, so a ninth code would serialise into a response the generated client's own zod rejects — a ticket list breaking on read because of what was saved on a master screen. One difference worth recording: S-12 *promises* "Admin can add further levels without a release" and cannot deliver it, where **S-13 makes no such promise**, so here the closed set is a constraint rather than an unkept one.

  > **`listStatusTransitions` emits an `ETag`, and it is the only collection read in the contract that does.** Every other collection hands its precondition to a single-row route because it is a view over rows edited one at a time; this one *is* the unit of edit and has no per-cell verb. Without the tag the `PUT` would have needed a `NO_IF_MATCH` exemption, and a whole-matrix replace is exactly the write where a lost update is worst — the loser's cells vanish with nothing to indicate they were ever there. The tag covers the whole matrix even on a role-filtered read, or two Admins editing different columns would each save over the other with both preconditions passing. The replace is an **upsert**: a row already present keeps its id, a row absent is **deactivated rather than deleted**, which is B-017's and B-018's argument applied to a table whose `requiresReason`/`requiresEffort` are facts somebody authored — and is what lets the read tell a *cleared* cell from a never-configured one.

  > **One finding recorded rather than fixed.** MySQL answers a violated `CHECK` with 3819 and a missing `NOT NULL` default with 1364, and Spring's MySQL error-code map contains neither — both arrive as `UncategorizedSQLException`, which is *not* a `DataIntegrityViolationException`. Any service catching the latter to turn a constraint breach into a 409 will not catch these two. Nothing in B-039 depends on it, since both rules are enforced in the service before the database is reached; adding vendor codes to the translator is application-wide and Stream A's. `StatusMasterIT.categoryCheckIsEnforced` carries the note.

  > **Touches `contracts/` — Stream D's directory, the precedent B-015…B-037 all set** (a `StatusCategory` schema, a `StatusId` parameter, six operations, ten response and request schemas, two `NO_PAGINATION` and three `ROWLESS_403` entries in `check-conventions.py`) — **and `security/permission/PermissionMatrix.java`, Stream A's**, which five new routes structurally cannot avoid. **Also `db/migration/`** for the category column: `statuses` is A-007's table and none of the four protected ones, so CLAUDE.md asks for no Stream A review.

- [x] **B-040** Tab 2 — stages: sequence, code, display name, owner role, icon, stage SLA hours, optional flag, allowed return targets. Drag to reorder. — *`api/feature/masters/stages/` is the first code ever to **write** `workflow_stages`; no migration, so no Stream A review. Five new routes plus `listWorkflowTemplates` finally served, all six roles on each. 62 backend unit tests plus 15 against real MySQL, 51 frontend.*

  > **A stage belongs to a template, and §7.4 reads as though it does not.** The blueprint describes tab 2 as one flat list of "the ribbon stages" and tab 3 as templates "built by picking stages" — which implies a catalogue that templates draw from. There is no such table: `workflow_stages.template_id` is `NOT NULL` behind a cascading foreign key, so `DEV` on Standard Dev Flow and `DEV` on Support Fast-Track are two independent rows sharing a code. CLAUDE.md settles it — PLAN.md wins on implementation and A-005 is what it produced — so the tab carries a **template selector** and every route is scoped beneath one. The alternative reading meant a new table, a migration touching the ribbon, and two answers to what a stage is.

  > **Renaming a code in use is the widest edit in the product and it fails silently, which is why it is refused.** `ticket_stage_transitions.to_stage` stores the code as plain text with no foreign key, and `StageSlaRepository` joins `ws.stage_code = tr.to_stage`. A rename does two things at once and **neither errors**: every historical ribbon segment stops resolving to a stage, and the §4A.7 "stuck in stage" scan stops matching those rows and never alerts on them again. The second is the one that matters — a silent gap in an escalation scanner is not a bug anyone reports, it is an alert that does not arrive. Frozen the moment either usage count is above zero; allowed on a stage nothing has entered, which is the only provably safe case and the one that matters, being a typo caught the same afternoon. `isCodeEditable` is computed server-side so the form does not restate the rule.

  > **The reorder is two passes, and that is MySQL's requirement rather than a style.** `uq_workflow_stages_seq (template_id, seq)` is a unique key and InnoDB enforces it **per row** — there are no deferred constraints in MySQL 8.4 — so writing the final values straight out collides the instant two stages swap, and the transaction rolls back on an operation the Admin experiences as dragging one row up. Pass one parks above the occupied range, pass two writes 10, 20, 30 …, both in one transaction. **No mock can catch this**: against a mocked repository the one-pass version passes every assertion, which is what `StageMasterIT`'s swap and full-reversal cases exist for.

  > **`canReturnTo` is a backward target, so a reorder can invalidate one that was correct when it was authored.** §4A.1's loop-back table is entirely backward moves; a forward "return" is an ordinary advance with a reason attached. Drag `DEV` past `QA` while `QA → DEV` exists and the reorder is refused with every offending pair named, on the server and — before the request — on the screen. The seed obeys the rule on all three templates and the IT asserts that rather than trusting it: B-004 wrote those arrays by hand before any code validated them, and a seed that disagreed would refuse an Admin's first drag for a state they did not create.

  > **No delete, and the absence is the design rather than a gap.** §7.4 says stages in use are deprecated and never deleted; the flag and its guard are **B-042**. The narrower version — delete only where both counts are zero — was drawn and rejected: it is safe in itself, and it is also a delete route on the ribbon's definition table existing before the rule that protects that table does. Asserted absent in `StageControllerTest`, and there is no `DELETE` row in the permission matrix because there is no route. **Nor does it refuse to edit a template with live tickets**: A-005's "versioned by copy, never edited in place" is kept by B-043's designer cloning, there is no version column to clone into yet, and a tab refusing every template with an open ticket would refuse all three seeded ones. `openTicketCount` warns with a number instead.

  > **`listWorkflowTemplates` was declared, mocked and never mounted — the sixth such case, and the first where the shape had drifted too.** `version`, `projectId` and `taskTypeId` name no column and no mapping table, so they were removed rather than served as a hard-coded `1` and two nulls, and the two query parameters filtering on them went with them: a filter that silently ignores its argument is worse than one that does not exist. The mapping is **B-041**'s, with the table to store it in.

  > **The inline `stages` array nearly went with them, and a shipped screen is why it did not.** B-040's drafting removed it from `WorkflowTemplate` as redundant beside `listStages` — and `TicketListPage` has built S-25's stage filter from it since C-013, deduplicating by `stageCode` across every template. Removing it would have emptied a live filter to tidy a response. Caught by `tsc`, not by review. So two stage shapes ship on purpose: `WorkflowStage` is the ribbon as a **vocabulary** (no identity, because a filter matches on a code several templates share), `Stage` is the ribbon as **rows to edit** — and serving the latter inline would have put four fields with no meaning to a filter, and two grouped `COUNT`s per template behind them, into a response every ticket list reads.

  > **Two findings recorded rather than fixed.** `NotificationTemplateDtos.TemplateView` and this task's `TemplateView` both became schema `TemplateView` in springdoc, where the second silently replaces the first — `GET /masters/workflow-templates` served notification-template fields until `ContractConformanceTest` caught it, and the same collision existed in the contract's own `parameters` as a duplicate `TemplateId` key, which `yaml.safe_load` accepts silently and orval refuses. Both are renamed here; **the general problem is that neither springdoc nor YAML warns**, and the next duplicate simple name will do the same thing. Separately, `db.stages` (Stream C's ribbon fixture) uses `DEVELOPMENT`/`VERIFICATION` where the database seeds `DEV`/`VERIFY`, so the mock and the real backend disagree about the ribbon's vocabulary; B-040's rows follow the database, and reconciling the older fixture means editing `ReopenDialog.test.tsx` and Stream C's mock fixtures, which are theirs.

  > **Touches `contracts/` — Stream D's directory, the precedent B-015…B-039 all set** (a `Stage` schema and five request/response schemas, two parameters, five new operations, one existing operation corrected, one `NO_PAGINATION` and three `ROWLESS_403` entries in `check-conventions.py`) — **and `security/permission/PermissionMatrix.java`, Stream A's**, which five new routes structurally cannot avoid. **Also `frontend/src/mocks/`** for the template and stage rows, and one assertion in B-039's `StatusMasterPage.test.tsx`, which pinned tab 2 as disabled and was true for exactly as long as it should have been.

- [x] **B-041** Tab 3 — templates built by picking stages, mapped to project × task type, with a **live ribbon preview** rendering as the Admin edits. — *`api/feature/masters/templates/` and `features/masters/templates/`; one migration creates the mapping table §4A.9 has always needed. Seven routes, one of them declared since D-001 and served by nobody, all six roles on each. 47 backend unit tests plus 12 against real MySQL, 43 frontend.*

  > **The mapping had no table, and the contract's shape said it did not need one.** `WorkflowTemplateWriteRequest` has carried scalar `projectId` and `taskTypeId` since D-001, which reads as though a template belongs to one project × task-type pair — and §4A.9 refutes that in the paragraph that introduces it, mapping Standard Dev Flow to Production Bug, Change Request *and* Future Release. Two scalars cannot hold three pairs. B-040 removed both from the *response* rather than serve two nulls and named this task as the one that owed them somewhere to live; they leave the *request* here for the same reason, and the mapping becomes its own resource. `stages` left the request too, on a smaller argument: `createStage` already holds the `canReturnTo` direction check, the code-uniqueness check and the `seq` spacing, and a second way in would be a second copy of all three.

  > 🔴 **The obvious version of the mapping table is silently broken, and this is the sentence to find before anyone simplifies it.** `UNIQUE (project_id, task_type_id)` reads as "one template per pair" and does not enforce it: MySQL treats every NULL inside a unique index as distinct, so `(5, NULL)` inserts twice, under two different templates, and neither insert fails. The resolver then has two answers for the same rung and returns whichever the optimiser reached first — a ticket raised on Monday and one raised on Tuesday getting different ribbons, with nothing anywhere recording that the configuration was ambiguous. Two `STORED` generated columns collapsing NULL to 0, with the unique key over those, restore the constraint. `TemplateMasterIT` writes through `JdbcTemplate` rather than through the service to prove it: the service checks for the collision itself, so going through it would prove the service's guard and say nothing about the schema. **Against mocks the broken schema passes every test in the package.**

  > **Both columns are nullable and NULL means "any", which is what makes the ladder rows instead of code.** Exact pair → this project/any task type → this task type/any project → an explicit catch-all → `is_default` → nothing. Without the two wildcard forms an organisation running one flow across a project would need one row per task type and a twelfth the day somebody adds a task type. **Project beats task type at equal specificity** — a project is the narrower population, so "everything on this engagement follows that flow" outranks "this kind of work usually follows this one". The opposite precedence is defensible and would route real tickets differently, which is exactly why it is written down and tested rather than left to whichever `ORDER BY` was typed first. And `rung` is served alongside the answer because a template id alone cannot tell *an Admin wrote this rule* from *nothing matched* — a pair silently falling through to the default is the one failure mode §4A.9's configuration has no other way to surface.

  > **Nothing sets `tickets.workflow_template_id`, and this task does not change that.** The column has existed since A-004 and the foreign key since A-005; the only code in the repository that ever writes it is `SingleTicketFixture`, which is why `RibbonAssembler` and `NoNextStageException` both carry a documented "ticket with no template" path and why C-013's stage filter is built from the template list rather than from the ticket. **Wiring the resolver into ticket creation is Stream C's** — `TicketService` is `feature/tickets/`, and CLAUDE.md asks for that owner's sign-off rather than a quiet edit. So B-041 ships the table, the ladder and a route that returns the answer, and the call site follows. `TemplateResolver` is `public` for exactly that reason and is the only public class in the package.

  > **"In use" means two different things, and folding them into one rule would be wrong in both directions.** Deactivation is refused by `mappingCount` — by what the template is *for* — because switching off a template three rules route to means the next ticket on any of those pairs resolves to a template the master says is out of service; history is irrelevant, and a template with ten thousand closed tickets and no live rule *should* be retired. Deletion is refused by `ticketCount` — by history alone — because the delete cascades `workflow_stages` and every historical ribbon segment resolves its display name, icon and owner role through those. That is B-042's argument one table up with **one difference worth recording**: `tickets.workflow_template_id` is a *real* foreign key, unlike the stage codes B-042 had to defend by hand, so the database would refuse this delete on its own. The service check exists so the refusal arrives as a sentence with a number in it, and so the screen can decline to offer the button rather than discovering the rule by pressing it.

  > **Exactly one default, and it can only be moved.** `is_default` is the ladder's last rung, so two of them means a ticket's ribbon depends on row order and none of them means every unmapped pair routes nowhere. The database asserts neither — a plain `TINYINT` with an index — so it is held in the service: setting a new default clears the old in the same transaction, and clearing the current default without naming a replacement is refused. B-039's *"at least one on-create row must survive"* on a different table, for the identical reason: this is the only screen that could undo it. The clear is **one statement rather than a read-modify-write**, which is not a micro-optimisation — the read-modify-write version is a lost update waiting to happen, with two Admins promoting two different templates each clearing what the other had just set. And the `PATCH` applies `isActive` **before** `isDefault`, or one request switching a template off and handing the default away would walk straight through a rule neither field could break alone.

  > **The preview reuses B-050's ribbon rather than drawing its own, and every history field is absent rather than zero.** `buildPreviewRibbon` turns the stage list into the `Ribbon` shape `RibbonStrip` already draws; a second renderer would drift from §4A.3, and the first time it did the preview would stop being a preview. A `0` in `effortHrs` or `durationMins` renders as a real measurement of zero — a claim about a ticket that does not exist — and `enteredAt` is worse, since a date would make the preview look like a journey somebody could click into. **No segment is `CURRENT`**, which is the decision most likely to be revisited: `RibbonStrip` hangs its contextual handoff button off the current index, so a preview with one would be inviting a handoff on a template, and `canAdvance: false` suppresses the button but not the "you are here" ring. A deprecated stage renders `SKIPPED` rather than a seventh state invented for this screen, which would put a shape on the ribbon no ticket page can produce.

  > **The springdoc name collision B-040 recorded would have happened a third time, and was avoided rather than discovered.** `NotificationTemplateDtos` already owns `TemplateView`, `TemplateResponse` and `TemplateListResponse`, and the first draft of this package used all three — springdoc would have let the second silently replace the first, exactly as it did on B-040, with `ContractConformanceTest` as the only thing that would have caught it. The records are named `WorkflowTemplateDetail`, `TemplateMapping`, `TemplateResolution` for that reason alone. **The same collision exists in TypeScript and bites harder**, because there it is a compile error rather than a silent overwrite: `templateEtag` in `mocks/handlers/rest.ts` was already the notification master's.

  > **Two tests were replaced rather than deleted**, B-042's precedent. `StatusMasterPage.test.tsx`'s *"shows tab 3 disabled, naming the task that will build it"* and `StagesTab.test.tsx`'s *"tab 3 is still disabled and still names its task"* each asserted the absence this task ends, and each named it. What stands in their place is that all three tabs are reachable and none is a stub, and — on tab 2 — that leaving it does not lose the template you were editing. "Three tabs exist" would assert less than nothing. **One unrelated flake was fixed rather than left**: `mocks.test.ts`'s full-size PNG case builds a 1200×800 image in jsdom and had been sitting a few hundred milliseconds under the 5 s default, failing about one run in three; six new handlers in `rest.ts` tip it over more often but are not what makes it slow, and a test that fails on a coin flip teaches everyone to re-run CI instead of reading it.

  > **Touches `contracts/` — Stream D's directory, the precedent B-015…B-042 all set** (three path items, six new operations, one existing operation reshaped, nine schemas, one `NO_PAGINATION` and three `ROWLESS_403` entries in `check-conventions.py`) — **and `security/permission/PermissionMatrix.java`, Stream A's**, which seven new routes structurally cannot avoid. **Also `db/migration/`**: `workflow_template_mappings` is a new table reading `workflow_templates`, `projects` and `task_types`, none of the four protected ones, so CLAUDE.md asks for no Stream A review — the same call B-039 and B-042 made. **And `frontend/src/mocks/`**, where the B-041 stub handler B-040 left in place finally writes a real row.

  > **One consequence recorded rather than fixed, because it is not this stream's file.** `fk_workflow_template_mappings_project` is `RESTRICT` rather than `CASCADE`, deliberately — a mapping is not a fact about the project but a routing rule an Admin authored, and cascading it away with the project would silently re-route every task type that rule covered to the default. Neither the project nor the task-type master offers a hard delete today, so this is insurance rather than a live path; what is worth a look on whichever task adds one is that the refusal will now name a table that master has never heard of.
- [x] **B-042** 🔴 **Stages in use may be deprecated, never deleted** — deletion breaks every historical ribbon. Live tickets keep the template version they started on. — *One migration adds `workflow_stages.is_deprecated` and `deprecated_at`; two new routes on B-040's controller, all six roles accounted for. 21 new backend unit tests plus 4 against real MySQL, 11 frontend.*

  > **The obvious guard is exactly wrong, and it is the first thing to get right.** A retire route naturally reads as "refuse while tickets are standing in it" — and §7.4's clause is *"stages used by live tickets can only be deprecated"*, so that guard would refuse the only case the word exists to describe. Deprecating with open tickets is **allowed**: they keep rendering their segment and keep their ordinary way out of it, and the dialog states the number rather than blocking on it. What deprecation stops is new *entry*.

  > **What is refused instead are the two states with no way back out.** The template's **last live stage** — a workflow with nothing live routes no ticket at all and no screen would notice, which is B-039's status-retire argument on a different table. And a **live stage that still returns to it**: `can_return_to` is a whitelist of moves the transition service will honour, so an arrow into a retired stage is an entry into a stage nothing may enter. That is B-040's forward-return refusal seen from the other side, so it reuses the same problem `type` and the same `pairs` property — the screen highlights both ends of each pair on the ribbon it is already drawing and never needs to know which operation produced the list.

  > **A delete exists now, and it refuses most of the stages it can be pointed at.** B-040 shipped tab 2 with *no* removal at all rather than a delete this task would have had to take away, and drew and rejected the narrow "delete only where both counts are zero" on the ground that a delete route on the ribbon's definition table should not exist before the rule protecting that table does. Both land here. The surviving case is the complement of §7.4's clause — nothing has entered it, nothing stands in it, nothing live returns to it, not the last live stage — the typo caught the same afternoon. Everything else is 409 `stage-in-use` carrying both counts **and** `canDeprecate: true`, because an Admin told "no" with no alternative concludes the row cannot be got rid of at all.

  > **Nothing in the database would have refused any of it, which is why the rule is a service and not a constraint.** A-005 made `ticket_stage_transitions.to_stage` and `tickets.current_stage` plain `VARCHAR` holding the code with no foreign key onto `workflow_stages`, so a delete cascades nothing, fails nowhere, and leaves every historical ribbon segment pointing at a definition that is gone while the §4A.7 stuck-in-stage scan quietly stops matching them. Two silent consequences, the same pair B-040 froze `stage_code` against.

  > **`If-Match` on the `DELETE`, none on the setter, and the asymmetry is the argument.** The delete's entire guard is that both usage counts are zero — and both are inside the tag `getStage` emits, so a ticket entering the stage while the confirmation dialog sits open moves the tag and the request is refused with 412 rather than performed on evidence that stopped being true. The deprecation route is an idempotent setter naming a state rather than a delta, so two Admins racing produce whichever state was asked for last; `NO_IF_MATCH`, beside `/users/{userId}/status`. It is a separate route rather than a field on the `PATCH` for the reason that patch's own convention forces: null means "leave it alone" there, so a boolean would carry three wire states for a column with two, and the one write with a consequence for live tickets would arrive indistinguishable from a display-name edit.

  > **`isDeprecated` finally does something on a branch that shipped in C-013.** `TicketListPage` has skipped deprecated codes when building S-25's stage filter since then, against a field hard-coded `false` from D-001 — written, covered and unreachable for two months. Serving the column turns it on. `StageMasterIT` asserts B-004's seed is entirely live, because a retired row arriving by migration would shrink that filter with nothing on any screen to explain it.

  > **Two tests were replaced rather than deleted.** `StageControllerTest.noDeleteExists` and `StagesTab.test.tsx`'s *"there is no delete"* each asserted the absence B-040 shipped on purpose and each named this task as the one that would end it. What stands in their place is the shape of the delete and its precondition, and which control appears on which row — "a delete button exists" would assert less than nothing.

  > **Touches `contracts/` — Stream D's directory, the precedent B-015…B-040 all set** (two operations, three fields on `Stage`, the real flag on the inline `WorkflowStage`, one request schema, one `NO_IF_MATCH` and one `ROWLESS_403` entry in `check-conventions.py`) — **and `security/permission/PermissionMatrix.java`, Stream A's**, which two new routes structurally cannot avoid. **Also `db/migration/`**: `workflow_stages` is A-005's table and none of the four protected ones, so CLAUDE.md asks for no Stream A review, the same call B-039 made.

  > **Two consequences recorded rather than fixed, because neither is this stream's file.** `ReopenService` (C) validates a restart stage by existence alone, so a reopen can now restart a ticket into a deprecated stage — whether that should be refused is C's call, and it may well be right to allow it on a cycle that ran on the old ribbon. And Stream D's stage-SLA scanner must go on matching deprecated codes for history, since those rows are exactly what §4A.7's scan is for; what is worth a look there is whether a *new* stuck-in-stage alert should be raised against a retired stage.
- [x] **B-043** Workflow template designer — drag stages onto a canvas, set owner role and SLA, draw return paths, preview, map. **S-30** — *`features/masters/designer/` on its own route, the one `App.tsx` reserved at B-039. **No endpoint, no migration, no contract change and no `PermissionMatrix` row** — every write is a route B-040, B-041 and B-042 already shipped. 31 model tests plus 26 against the mock server.*

  > **Every rule S-30 needs was already written, and that is what makes this a screen rather than a subsystem.** Tab 2 holds the stage editor and its reorder; tab 3 holds the preview and the routing rules. What did not exist is the thing §7.4 names — **one surface** where the flow is laid out spatially, the arrows are arrows and the ribbon redraws under your hands, instead of three panels on two tabs each holding a third of the answer. So `stageForm`, `stageQueries`, `templateQueries`, `previewRibbon` and `RibbonStrip` are all imported unchanged, and the only new logic is `canvasModel.ts`: the palette, the drop arithmetic, the arc layout and the refusals.

  > **The order is staged and everything else writes when you finish it, which is the arrangement worth arguing about.** A canvas invites the opposite, and a designer holding every edit until one Save would be a batch of creates, patches and a reorder **across three route families with no rollback between them** — half a flow saved and no way to say which half. The append-only core cannot hand this screen a transaction and a bulk "save the whole flow" route would be pretending otherwise. Only the reorder is staged, because that route is a whole-set `PUT` under one `If-Match` and it is what tab 2 already does. **One seam is named rather than hidden**: a node dropped in the middle is created *last*, since the create route appends and a caller-chosen `seq` collides with `uq_workflow_stages_seq`, so the canvas moves it to the drop position as a staged move and the live region says "save the flow to keep it there".

  > **"Drag stages onto a canvas" needs something to drag, and there is no stage catalogue.** `workflow_stages.template_id` is `NOT NULL`, so `DEV` on Standard Dev Flow and `DEV` on Support Fast-Track are two independent rows — B-040 recorded it and B-041 resolved the same phrase by reading "picking" as cloning a whole template, which is right for standing up a new flow and useless for adding one stage to an existing one. What does exist is a **vocabulary**: `WorkflowTemplate.stages`, the inline array B-040 fought to keep on the list response, whose union is the set of stage names the organisation has agreed on. Dropping one **creates a new row here** and does not link back, so every card names the templates the code came from — an Admin who thought they were editing a shared definition would be wrong. Codes already on the canvas are excluded, because `uq_workflow_stages_code` is per template and a second `DEV` is a 409 the palette can see coming.

  > **Two silent bugs the tests found, both about `seq`.** *The preview did not follow the drag*: `buildPreviewRibbon` and `previewChain` both sort by `seq` — correctly, for B-041's caller, which hands them what the server returned — and a staged reorder is an array in a new order whose rows carry the *old* `seq`, so the canvas showed the dragged flow and the preview underneath showed the saved one. That is the single thing §7.4 words as a requirement, and it would have looked right in every screenshot taken before the first drag; `resequence` renumbers on B-004's 10/20/30 spacing, which is what the reorder route will write. *The return-target picker offered forward stages for one round trip*: `returnTargetOptions` reads a `null` position as "appended last, so everything qualifies", which is right for a stage being created and wrong for an existing one whose detail read has not landed — ticking one in that window sent a patch the server refuses. The list row already carries the same `position`, so it is the fallback rather than `null`.

  > **Narrowest-first is the whole of the arc layout's correctness.** Widest-first is the natural way to write the interval greedy and it draws a containing arc on the inner lane with the nested one dipping *underneath* it — a loop-back crossing its own container twice, which reads as two arrows tangled rather than one inside the other. Nesting on the page has to match nesting in the flow. Deterministic matters more than optimal: an arc that changed depth when an unrelated one was added would make the canvas appear to move on its own. **A broken arc is laid out rather than filtered** — a staged reorder can leave an arrow pointing forwards, and dropping it from the layout would make it vanish at the moment it became a problem, leaving an Admin reading a Save button that names a pair they can no longer see.

  > **The SVG is `aria-hidden` and the inspector is the accessible copy, not a lesser path.** Every arc is also a row of text in the *Returns to* list, and a path announced as a path is a second, worse reading of the same fact — the picture is for the eye, the list is editable and the picture is not. A canvas is the archetype of the control that ships pointer-only, so every node carries Move left / Move right, the palette carries Add, every arrow can be drawn from a checkbox, and `WorkflowDesignerPage.test.tsx` drives all of it through those rather than through synthetic drag events.

  > **`MappingPanel` was moved rather than rebuilt.** S-30's sentence ends *"then map it to project × task type"*, so the designer needs tab 3's panel; a second one would be a second copy of `duplicatePairKeys`, of the order-insensitive dirty check and of the resolution checker's reading of §4A.9's ladder — B-041's own README argues that case one level down about the stage editor. `templates/MappingPanel.tsx` is a pure move out of `TemplatesTab.tsx` and both screens render it.

  > **Touches only this stream's paths — the first task in this run of the backlog that does.** No `contracts/` (Stream D's), no `PermissionMatrix.java` (Stream A's) and no `db/migration/`, because every write is an existing route with all six roles already decided: create a stage, patch one, replace the order, replace the mappings, create a template with `copyStagesFromTemplateId`. The one file outside `features/masters/` is `App.tsx`'s route table, which is where the note reserving this route has sat since B-039.

**Exit:** an Admin can stand up a complete tenant — resources, projects, clients (bulk-imported), calendar, workflow templates — without touching the database.

---

## Weeks 10–11 — join Stream C on the ribbon

C owns the ribbon's service layer; you take the UI. Tasks assigned from `STREAM-C-TICKETS.md`, typically:

- [x] **B-050** Ribbon segment component — 6 states (completed, current, pending, reworked, skipped, blocked), 5 data points each. — *`frontend/src/components/ribbon/` (`RibbonSegment.tsx`, `segmentState.ts`, `useElapsedMins.ts`, stories, README), plus two formatters moved into `lib/`. 44 new frontend tests. No backend, contract, migration or mock change — `RibbonSegment`, `SegmentState` and `buildRibbon` were all already shipped by D-004 and C-042.*

  > **Touches `components/ribbon/` — Stream C's directory in TEAM-PLAN.md §6 and in `.github/CODEOWNERS`**, which is what this whole week is: §6's timeline row for weeks 10–11 reads "**joins C** — ribbon UI, segment states, compact variant". Divyansh is requested as reviewer automatically, since the CODEOWNERS rule for this path has been sitting there unmatched — the file's own header names `components/ribbon/` as one of five "right paths for unwritten work" that start protecting it on the commit that creates the directory. This is that commit. Also touches `features/tickets/journey/journeyFormat.ts` (C's, under the 19 Aug carve-out to D) — a move and a re-export, no behaviour change, flagged rather than made quietly.

  > **The current segment's timer is wall-clock, and every other duration on the ribbon is not.** `durationMins` is *working* minutes computed server-side against the calendar, and it is `null` for the stage a ticket is sitting in — which is the only stage a running timer is for. The browser has no working calendar (B-024 is server-side), so the timer counts clock time and the tile labels it **`elapsed`** where sealed segments read **`in stage`**. A ticket handed to Deployment at 18:00 on Friday shows `62h elapsed` on Monday against the ~8h the server records when it seals, and printing both under one caption is exactly the confusion CLAUDE.md's working-calendar rule exists to prevent. The component prefers a server duration wherever one exists, so if the ribbon endpoint ever serves a live working-minutes figure for the open hop this branch simply stops being reached.

  > **Skipped and blocked reuse the pending tokens, and no token was requested.** `styles/tokens.css` ships `--ribbon-done|current|pending|reworked|breached` and nothing for either state. That file is Stream C's and frozen after Sprint 0 — "others request, never add" — and neither needs a colour it does not have: §4A.3 describes skipped as a dashed outline with a struck-through label and blocked as grey with a pause icon, so the distinction is shape and word, which is where the blueprint put it. `--ribbon-breached` stays unused: an SLA breach is a fact about a stage's clock, is absent from `SegmentState`, and `RibbonSegment` carries no flag to render it from — when D's scanner surfaces one it is an overlay on a state, not a seventh state.

  > **Three gaps recorded rather than approximated, each another stream's.** `BLOCKED` has no hold-reason field — §4A.3 wants one on hover, the contract carries `skipReason` and `handoffNote` and nothing else, and `buildRibbon` never emits the state at all, so it is unexercised outside these unit tests (**Stream D**, one field). A skip records no authoriser, though §4A.3 asks for "who authorised it" beside the reason (**C-047**, which writes the skip). And the template's `icon` is not rendered: it is an arbitrary lucide name from B-043's designer, resolving one costs the whole icon set or a dynamic import per segment, and no screen here resolves one — §4A.3's own diagram puts a *state* icon in that position, which is six static imports.

  > **Two things the compiler could not see, and one of them is worth remembering.** `export { ROLE_LABEL } from '@/lib/roleLabel'` in `resources/columns.tsx` type-checks and lints clean and then throws at render: a bare re-export forwards a name **without binding it locally**, and the Role column two hundred lines down reads it. 21 `ResourceListPage` tests caught it while `tsc --noEmit` and `eslint --max-warnings 0` both passed over it. Then the specifier form `export { ROLE_LABEL }` fixed the crash and produced two *new* `react-refresh/only-export-components` warnings on exports that had not changed — the plugin cannot see through an export specifier, so it treats the file as component-exporting and flags everything else in it, which fails CI's `--max-warnings 0`. Both went away by deleting the re-export altogether: `ResourceFormPage` and `ResourceListPage` now import `ROLE_LABEL` from `@/lib/roleLabel` directly, which is one less indirection than before the move. `journeyFormat.ts` keeps its re-export — it exports no components, so neither problem applies, and its call sites genuinely are unchanged.
- [x] **B-051** Compact dot variant for the ticket list. — *`components/ribbon/` (`RibbonDots.tsx`, `compactDots.ts`, stories, tests), `features/masters/templates/routeToTemplate.ts`, and the `journey` column in `features/tickets/list/`. 42 new frontend tests. No backend, contract, migration or `PermissionMatrix` change — both reads it needs are already `everyRole`.*

  > **The column S-17 shipped without, and the clause that unblocked it.** `features/tickets/list/README.md` recorded the compact ribbon as deliberately unbuilt by C-014: a grid mixing projects "cannot resolve one row's dot count without resolving that row's own template, and doing that per row is the per-ticket-detail waterfall S-20 deliberately collapsed". It named two exits, one being **"a stage-order lookup keyed by project and task type that does not require fetching every template"** — which B-041 made answerable three weeks later. So this is a task closed by reading the note the earlier task left rather than by a contract change: no route, no migration, no `PermissionMatrix` row, and the two reads it makes (`GET /masters/workflow-templates` and `.../{id}/mappings`) were already permitted to all six roles.

  > **`.../resolution` per pair was built first and measured worse, and that measurement is the design.** The route answers one project × task type, which is exactly right for S-13 tab 3 where a person picks one. On a grid it is not: the fixture draws `taskTypeId` from `int(1, 11)` across three projects, so a page of 25 rows is up to 25 distinct pairs, and **one MSW round trip costs about half a second in the test rig** — running this directory's seven suites together went from one flaky failure to six, all of them C-014's documented Radix-popover timing flake tipping over under the load. What ships is bounded by the *master* instead: the template list this screen already fetches for its stage filter, plus one `mappings` call per template — three in the fixture, and a number an Admin curates by hand. It does not grow with rows, with paging, or with how many projects a reader can see. The cost is `routeToTemplate.ts`, a **second implementation of §4A.9's ladder** beside `TemplateResolver.java` and the mock's; what makes it affordable is that the server ships its own ranking key (`TemplateMapping.specificity` is documented as "what the resolver breaks ties on") and that a test asserts the client agrees with `.../resolution` pair for pair against the fixture, so a drift fails the build rather than drawing the wrong ribbon.

  > **Three facts the row payload does not carry, and none is guessed.** `docs/prototype/index.html` paints a *completed* dot amber and hardcodes its index at 2 — it knows the one walkthrough it draws. `TicketSummary` carries `iterationNo` and no transitions, so amber here marks the stage the ticket is in **now**, on the prototype's own sentence for the colour ("amber means it has been sent back"), which is a claim about the ticket rather than about a particular earlier stage. `SKIPPED` and `BLOCKED` are the two of `SegmentState`'s six that cannot be produced at all, both being facts in `ticket_stage_transitions`. And a `currentStageCode` absent from the resolved template — a template re-cut since the ticket started, since **nothing versions a template yet** (B-042) — renders an em dash: eight hollow dots would claim the ticket has not started, and an index would claim where it is.

  > **Shape carries the state; colour repeats it.** A 7px dot has no room for `RibbonSegment`'s icon and word, and `tokens.css`'s ribbon block requires a second channel — so completed is a filled circle, current a larger circle inside a ring, pending a hollow outline, and reworked a **diamond**, four marks that stay apart in greyscale and on a printed screenshot. Pinned as a behaviour, so a later tidy-up that collapses them to four colours fails. No token was requested; `--ribbon-done|current|pending|reworked` are all four. The strip is one `role="img"` with one sentence rather than eight focusable marks — 200 stops per page for a keyboard reader, in a cell with nothing to activate — and §S-17's "hovering a dot names the stage and its owner" is a native `title`, deliberately not the Radix tooltip C-052 put on the tile: right for eight tiles on a detail page, a different question for 200 in a grid.

  > **The column holds until the whole routing table has arrived, and that is the one defect the integration test caught.** `routeToTemplate` falls through to the default template when no rule matches, which is right and is what the server does — but *"no rule matched"* and *"the rules have not arrived yet"* are indistinguishable from a half-loaded table, so drawing dots on first paint put **every** row on Standard Dev Flow's eight stages for a frame. A wrong ribbon on most of them, and a claim nothing could back. The hook now renders nothing until every `mappings` read has *succeeded* — `isSuccess` rather than `!isPending`, so a failed read holds the column as well instead of quietly falling back to the default. Either way the cell is an em dash: a missing column, never a broken grid.

  > **⚠ For Stream C — the mock speaks two stage vocabularies, and it is not only this column.** `db.stages`, the flat ribbon fixture the mock's ticket generator draws `currentStageCode` from, says `DEVELOPMENT`, `DEPLOYMENT` and `VERIFICATION`; the workflow template master says `DEV`, `DEPLOY` and `VERIFY`, following the database's own `V20260807_1700` seed. B-040 found this and recorded it at the top of `mocks/db.ts`, leaving it because reconciling means renaming codes Stream C's reopen fixture and `ReopenDialog.test.tsx` assert on. Against the real backend both halves agree and the Journey column resolves normally; against the mock most rows read an em dash, which is `buildCompactDots` behaving correctly on an inconsistent fixture. **S-17's Stage filter has the same problem and has had it since C-013** — its options come from the template stages, so filtering the mock to "Development" matches no row at all. One rename in `STAGES` and `OPEN_STAGES` closes both. Flagged rather than done across the boundary; `TicketJourneyColumn.test.tsx` writes the template vocabulary onto the rows it is about, because pinning the divergence would pin a bug.

  > **Touches `features/tickets/list/` — Stream C's — as weeks 10–11 intend.** TEAM-PLAN.md §6's timeline row reads "**joins C** — ribbon UI, segment states, compact variant", and B-050 recorded the same arrangement for `components/ribbon/`. The edits in C's files are one column definition, one field on `ColumnRenderContext`, one hook call, and moving `renderContext` below the list query so it can see the rows. Flagged rather than made quietly, on the precedent B-027 through B-029 set in this same file. `templateQueries.ts` also gained `mappingsQueryOptions` beside its existing hook — Stream B's own file — so the grid and S-13 tab 3 read **one** cache entry and an Admin editing a routing rule sees the ticket list re-route without a reload.
- [x] **B-052** Ribbon accessibility — keyboard navigation, ARIA label per segment reading stage, owner, state and effort. — *`components/ribbon/` (`rovingFocus.ts`, `RibbonStrip.tsx`, `RibbonSegment.tsx`, stories, tests, README). 29 new frontend tests. No backend, contract, migration, `PermissionMatrix` or token change, and the first of B-050…B-052 to touch no file outside this one directory.*

  > **Half of §4A.3's closing bullet was already shipped, and the backlog line does not say so.** *"Fully keyboard-navigable; each segment has an ARIA label reading the stage, owner, state and effort"* — the label half is B-050's `segmentAriaLabel`, which emits stage, owner, state, **duration** and effort plus the loop count and the skip reason, with all seven pinned in `segmentState.test.ts`; B-050's own docstring hands the keyboard half here by name. So this task is the keyboard, and re-deriving the label would have been a second vocabulary for the same four facts. `RibbonDots` needed nothing either: B-051 gave the grid cell one `role="img"` sentence and a native `title` per dot, and argued there why 200 focusable marks in a grid is the thing that name exists to avoid.

  > **Eight segments are one tab stop, and the alternative is not "slightly more presses".** S-20 stacks History, Effort, Chat and the roll-up grid **below** the ribbon, so eight tab stops is eight presses standing between a keyboard reader and everything they came for — sixteen on a ticket that has looped. `useRovingFocus` makes the strip a composite widget in the APG sense: `←`/`→` between segments, `Home`/`End` to the ends, both wrapping. The keys are `TicketDetailTabs`' and the rich-text toolbar's deliberately, both of which already ship — a third set of arrow-key semantics on the same screen would *be* the accessibility problem rather than the fix. `ArrowUp`/`ArrowDown` are left to the page: the ribbon is a row, and swallowing the vertical arrows traps a reader inside a widget they cannot scroll past.

  > **The tab stop starts on the current segment, and stops chasing it the moment a reader moves it.** Landing on an Intake finished four days ago and making somebody arrow forward to find out where the ticket *is* would be a working keyboard interface answering the wrong question. It is a preference and not a controlled value because the strip re-renders on every `stage.changed` frame D-058 pushes, and a live handoff moving `CURRENT` must not yank the tab stop away from someone reading with it.

  > **Focus does not select, which is the one place this differs from `TicketDetailTabs`.** Those tabs select on focus and record why: every panel is already loaded from the one `/full` call, so moving costs nothing. Selecting here is not free — C-052 filters History, Effort and (once `D-047` lands) Chat down to one stage and iteration, so arrowing across eight stages to *read* the ribbon would refilter three panels eight times, and a keyboard user could not inspect the journey without changing what sits under it. Activation is explicit: `Enter` or `Space`.

  > **Every tile joins the roving, including the ones with nothing to activate.** A strip rendered without `onSelectSegment` draws `<div role="group">` tiles that *nothing could focus* before this task — so C-052's tooltip, the only place a segment's entered, exited, note and idle-vs-active figures are written down, was pointer-only on all three callers that render one: a sealed past cycle, S-13 tab 3's live preview and S-30's designer preview. They take a `tabIndex` now and are still not buttons, because there is still nothing to press.

  > **The defect the task found: `actionSlot` was rendering inside the tile's own `<button>`.** §4A.3 puts *Hand off to QA →* on the current segment, and B-050 read that literally enough to render it among the tile's children — a `<button>` inside a `<button>`, which the HTML spec forbids, which browsers recover from differently, and which **no keyboard can reach**, because the outer control consumes Enter and Space before the inner one sees them. On S-20 that is live today for anyone the server says `canAdvance` for. The card is now a `<div>` holding two siblings, the trigger and the action; the focus ring moved to the card with `has-[:focus-visible]`, the pattern `CommentBox` already uses, so it still outlines the whole tile.

  > **A performance bug found by a flake, worth not reintroducing.** The first cut built `ref` and `onFocus` closures fresh on every render, so React detached and reattached all eight nodes each time — and each tile's ref is composed by Radix's `Slot` with `TooltipTrigger`'s own, which is the Popper **anchor**. `TicketDetailPage.test.tsx` then began timing out under parallel load in a *different* test each run while its single-file time was unchanged (18.8s against 19.1s on the base branch), which is what eight tooltips being torn down and re-anchored on every selection, focus and realtime frame looks like from the outside. `itemProps` caches both callbacks per index; `develop`'s combined run of `tickets/detail`, `masters/templates`, `masters/designer` and `tickets/list` is green twice over since. If a later change makes those closures depend on anything but the index, the flake comes straight back.

  > **Touches `components/ribbon/` and nothing else** — Stream C's directory, under the weeks 10–11 "**joins C**" arrangement B-050 and B-051 recorded in this same file. Unlike either of them there is no second stream's file in the diff at all: no `contracts/`, no `PermissionMatrix.java`, no `db/migration/`, no `styles/tokens.css`, and no edit in `features/tickets/`. Divyansh is still requested as reviewer by the CODEOWNERS rule on the path.
- [x] **B-053** Readability at 8 stages on a laptop — horizontal scroll with the current segment auto-centred, collapsed "…" grouping for completed stages beyond the first three. *(§17 risk mitigation)* — *`components/ribbon/` (`collapsedGroup.ts`, `CollapsedGroupTile.tsx`, `RibbonStrip.tsx`, stories, tests, README), plus one assertion in Stream C's `features/tickets/detail/TicketDetailPage.test.tsx`. No backend, contract, migration, `PermissionMatrix` or token change.*

  > **The one file outside `components/ribbon/`, and why it had to change.** `TicketDetailPage.test.tsx`'s own "renders the real ribbon strip" test asserted 7 list items for the §14 walkthrough ticket's sealed cycle 2 — true before this task and false after it, because that cycle's own fixture (`Deployment`/`Verification`/`Sign-off` all `COMPLETED`, no `CURRENT` since the real current stage code is `CLOSED`, which `buildRibbon` never renders) is exactly the shape this task collapses: 4 individual tiles plus one 3-segment group. The test now asserts 5 items, that `Sign-off` is not directly visible until the group is expanded, and that expanding it (the same control this task ships) makes it so — proving the real page wires B-053 rather than re-asserting the pre-B-053 count. Flagged rather than changed quietly, on the same precedent B-027 through B-029 and B-050's own note on `features/tickets/journey/journeyFormat.ts` set in this file.

  > **Only `COMPLETED` collapses, and a skip or hold in the middle splits the run rather than papering over it.** `segmentState.ts`'s own header asked B-053 to read the same vocabulary B-051's dots do rather than invent a fourth one, and `REWORKED`/`SKIPPED`/`BLOCKED` each carry something a completed tile does not — a loop badge, a reason, a hold — so none of the three ever joins a collapsible run. `collapsedGroup.test.ts` pins the split explicitly: a skipped stage between two completed runs produces two groups, not one that quietly skips over it.

  > **Collapsing hides a tile, not a segment.** B-052 spent itself making every stage on the strip reachable, including the read-only ones — a group that dropped a stage from the DOM to shrink the strip would undo exactly that for whichever stage a reader most wants to click on to filter History and Effort below. So `buildRibbonRows` never removes a segment from `ribbon.segments`; `expandedGroups`, one `Set<string>` of run keys kept in `RibbonStrip` and nowhere else, is the only thing deciding whether a run renders as one tile or as itself. The group's own tile is the toggle either way — collapsed it reads "N completed stages collapsed", expanded it reads "Collapse N completed stages" and the real segments render right after it — one control, not a pair to keep in sync.

  > **The group is always a button, which is the one place it differs from a segment tile.** `RibbonSegment` renders `<div role="group">` when the caller wires no `onSelect`, because a sealed cycle is read-only for advancing it. Folding a group is a display decision, not a selection, so `CollapsedGroupTile` takes no `onSelect` at all and stays a real button — and reachable — on every ribbon, sealed or not. It joins `useRovingFocus`'s count the same as any tile; the hook already tolerated a segment list that can shrink or grow under a live `stage.changed` frame, which is exactly the tolerance a group's own expand and collapse needed.

  > **Auto-centring keys on the current stage, never on the roving tab stop.** The two had to stay independent: arrowing across the strip to read it, the same way expanding a group to look inside it, must not drag the scroll position along, for the same reason B-052's own tab stop does not chase a live handoff once a reader has moved it. So the one `React.useEffect` that calls `scrollIntoView({ inline: 'center', block: 'nearest' })` depends only on the current segment's own stage code and iteration — never on `selectedSegment`, never on which groups are open. `prefers-reduced-motion` picks `smooth` versus `auto`, checked the same way `useCountUp.ts` already does, and the call itself is the same optional-chained `scrollIntoView` `HandoffDialog.tsx`'s own auto-scroll already uses — neither pattern is new here, only their combination on this strip is.

  > **Touches `components/ribbon/` and nothing else** — Stream C's directory, under the weeks 10–11 "**joins C**" arrangement B-050 through B-052 recorded in this same file, and the last of the four. No `contracts/`, no `PermissionMatrix.java`, no `db/migration/`, no `styles/tokens.css`, no edit in `features/tickets/`. Divyansh is still requested as reviewer by the CODEOWNERS rule on the path.

---

## Weeks 12–14 — M6 reports

Split with Stream A. Typically yours:

- [x] **B-060** Client report — volume, open vs closed, SLA compliance, avg resolution, satisfaction per client; drills into the client 360 view.

  > **Four of §7.8's five figures ship, and the fifth is recorded as absent rather than approximated.** There is no CSAT, rating or feedback column in the schema, in `contracts/openapi.yaml`, or in any of the forty-odd migrations — blueprint §17 item **19**, *"CSAT / feedback on closure — a 1–5 rating drives the client-satisfaction report"*, is a **phase 2–3** item nobody has built. So the runner declares no satisfaction column and the catalogue description says why, which is the same call B-039 made about a category nothing could derive. Three near-misses were available and each is worse: a column of em dashes reads as "we asked and they did not answer" — a claim about the clients rather than about the schema; reopen rate relabelled puts a plausible number under the wrong word, since reopens measure our quality and not whether the client was content; and SLA compliance standing in collapses two of the five into one and makes the report agree with itself. This is the report §7.8 describes as *shaped to be sent to a client*, so an invented figure costs more here than anywhere else in the product. One migration closes it whenever CSAT lands, and `ReportRunnersIT` asserts the absence by name so that task finds a failing test pointing at what it now makes possible.

  > **The runner contract could not carry the filter the report is built around.** A-063's `ReportRunner.run(scope, from, to, projectIds)` had nowhere for `clientId` to travel, so B-060 added `ReportFilters(clientId, taskTypeId, level)` beside the scope. **The other two were already dead and it did not show**: the contract has declared `?taskTypeId=` and `?level=` since A-066, `TicketReportRepository` takes both on three queries, and every caller passed a hardcoded `null` because `ReportController` accepted neither — so `sla-breach` declared a Level filter and an SLA breach filtered by level was the same report. Wiring `clientId` while leaving those is the same bug with one fewer excuse, so `sla-breach` and `effort-summary` now read theirs. **Filters stay separate from `ReportScope`**: a filter narrows what was asked for and may be ignored, a scope narrows what is allowed and may not, and folding `resourceId` into the bag is how the one value with a security consequence gets read directly by the next runner. They are also in the ETag — two clients differ only by `?clientId=`, and a validator ignoring it hands the second caller a 304 over the first client's rows.

  > **The drill-in named an entity, not a path.** The generic column/row shape had no notion of a destination and `reportKey === 'client-report'` in the viewer would be the second copy of the server's vocabulary this feature already refuses to keep. A column now declares `linkTo` + `linkIdKey`; routes stay in `entityLinks.ts`, because a server spelling `/clients/42` is a router in a second language that nothing keeps in step — the first symptom being report links 404ing after a rename every other screen survived. The id is carried in the row with **no column of its own**, so it never reaches the table or `?export=`, which iterates columns: an internal id is not a figure to put in a spreadsheet sent to a client. S-32 itself is still a `ScreenPlaceholder`, which is C-019's deliberate arrangement for all three unbuilt 360 screens.

  > **Three populations in one row, which do not reconcile and are not meant to.** `raised` is flow bounded by the window, `closed` and both SLA halves are flow over closures in it, and `openNow` is **stock** read at request time and deliberately unbounded — the backlog a client is unhappy about is mostly older than any window they would think to ask about. The SLA denominator is closures that carried a `planned_close_date`, not all closures, so a client whose work was never date-committed reads as an em dash rather than 0% or 100%; A-057's migration makes the same split for the dashboard gauge. `is_client_raised` is **not** consulted despite §4B.7 calling it the flag that drives client-wise reporting: a client's experience covers every ticket about their work, including the ones the desk raised for them.

  > **Touches `feature/reports/` — Stream A's directory — by this backlog's own M6 split**, `contracts/openapi.yaml` (D's: one schema, two column properties), and A's `ReportRunnersIT`, where the client fixture is *attached to the existing tickets* rather than seeded with its own, so six other reports' assertions are untouched. No migration and no new route, so no Stream A schema review and no `PermissionMatrix` entry.
- [x] **B-061** Resource performance scorecard and workload/capacity report. — *No new runner: `ResourceScorecardRunner` (A-066) and `WorkloadCapacityRunner` (A-067) both existed and both were `available`. What was missing was the half §7.8 names and the half B-017 flagged — and, underneath them, a filter five reports drew and none applied. 12 new backend tests (5 unit, 9 against real MySQL) and 7 new frontend ones.*

  > **`?resourceId=` was accepted, resolved, and thrown away.** `ReportService` computed `scope.resourceSubject(resourceId)` and handed it to the ETag and to `meta.appliedScope` — never to the runner. Every runner re-derived it as **`scope.resourceSubject(null)`**, which answers null for anybody who is not a delivery role, so the Resource control did nothing for the Admins and PMs it exists for on all five reports that declare it: scorecard, velocity, effort summary, reopen analysis and workload. `workload-capacity` was worse — `ReportRepository.workload` had no parameter to honour it with at all. **The failure state was not an inert control but a lying one**: the viewer printed *"showing one resource, across all projects"* over unnarrowed rows, and the ETag varied by a value the body did not depend on, so two people asked for from one URL could share a validator. This is B-060's `?taskTypeId=`/`?level=` finding one seam deeper, and the same shape §7.8's filter list exists to prevent.
  >
  > **The fix is a sixth parameter on `ReportRunner.run`, not a fourth field on `ReportFilters`.** `resourceSubject` sits beside `projectIds` because they are the two *resolved narrowings* — both computed by `ReportScope` from the request, both already intersected with what the caller may see. `ReportFilters` is deliberately not where it goes, and that record's own javadoc says why: a filter narrows what was asked for and may be ignored, a scope narrows what is allowed and may not, and folding in the one value with a security consequence is how the next runner reads it directly and quietly reopens what §2 withholds. Making an inert parameter live is exactly the change that can leak, so `ReportRunnersIT.ResourceFilter.aDeliveryRoleCannotAimIt` pins the overrule on the *figure* rather than the row count — one row is what both the correct and the leaking answer return.
  >
  > **Each regression test filters to the person who is *not* there.** Asserting that naming somebody returns their rows passes against the bug, because filtering to the only person present is indistinguishable from not filtering — which is precisely how this survived A-066 and A-067 with tests on every other column.
  >
  > **Trend arrows are a column type, not a renderer branch.** §7.8 ends the scorecard's line with "Trend arrows" and the column was typed `NUMBER`, so the cell read `-3` and the bar chart plotted a signed delta as a series beside an SLA percentage and a cycle time in hours. `TREND` is the one `ColumnType` that is not a data type — it says what the number *means*. A `key === 'trend'` branch in `ReportTable` would have rendered the arrow and left the chart wrong, and would have been the second copy of the server's vocabulary this feature already refuses to keep. **The direction is not coloured**: the same type will carry a reopen-rate trend, where up is bad, and a renderer deciding good from the sign would be wrong on half its uses. Exports write the raw number — a spreadsheet cell holding an arrow glyph cannot be sorted or summed, which is what an export is for.
  >
  > **Allocation across all projects — the figure B-017 could not compute from the Team tab and flagged here by name.** Three columns from `project_members`: projects, allocated, allocation stated on. The total spans **every active membership, including projects the caller cannot see**, and that is not a widening: `resource_daily_stats` is keyed by user and has no project column, so the load columns beside it have always shown a person's whole load. Narrowing only the allocation would reproduce the exact bug B-017 named — a PM owning one of somebody's three projects reads 50% and concludes there is room. What crosses the boundary is a percentage and a count, never a project name.
  >
  > **The total is published as a floor rather than dressed up as a total.** `allocation_pct` is nullable and means "not stated" — B-017 refused the contract's `default: 100` because a backfill would read 300% for every fixture resource on three projects. So `SUM` covers only the memberships that stated a figure, `SUM` over an all-null set is SQL NULL and stays NULL (`getBigDecimal`, never `getInt`, which answers 0 and would collapse "not stated" into "committed to nothing"), and **the count of projects that actually stated one is published beside it** so a reader seeing 150% across 3 projects can tell whether two more are unaccounted for. Treating unstated as 100 invents a resourcing crisis out of a data-entry gap; treating it as 0 asserts a commitment nobody made; suppressing the row withholds load figures that are fine. Naming the limit on the card is the call `client-report` made about the figure it does not have.
  >
  > **`workload-capacity` moves from `stacked-bar` to `bar`, which is a correctness fix rather than a preference.** Stacking asserts the series partition a total, and these never did — an open ticket is also counted under critical and under delayed, so the bar's height was already double-counting before this task added a percentage and two counts that would have stacked on top of it. **Not the whole fix and recorded as such**: eight series on one axis is still hard to read, and that is A-067's chart to redesign.
  >
  > **Touches `feature/reports/` — Stream A's directory — by this backlog's own M6 split, the precedent B-060 set.** Also `contracts/openapi.yaml` (D's: one enum value) and three exporters, where `TREND` had to be admitted to the numeric arm or it would have been written to Excel as text. No migration, no new route, so no Stream A schema review and no `PermissionMatrix` entry.
  >
  > **`edutrack-domain` in the local `.m2` was stale and the API module resolves the jar, not the freshly built classes** — every `@SpringBootTest` in `feature/reports` failed to load its context with `Failed to introspect Class LoginAudit`, naming a class that compiles fine on this branch. Fixed by `./mvnw -pl domain -am -DskipTests install` before running any IT with `-pl api`. Not this branch's, and it names a class B-061 does not touch.
- [ ] **B-062** Export engine integration for all report types.
- [x] **B-063** Timesheet view — stage-aware, a resource's week across all tickets. — *`GET /users/{userId}/timesheet` and `/timesheet` · `feature/reports/Timesheet{Controller,Service,Repository,Dtos}`, `features/masters/timesheet/TimesheetPage`. 16 unit and 18 real-MySQL backend tests, 9 frontend ones. No migration.*

  > **The approval step §21 also asks for is not here, and could not have been.** "An approval step for the manager" cannot be a flag on `ticket_effort_logs` — the table is append-only, hash-chained, and a trigger refuses every mutation but sealing a stage transition. A separate approvals table is a migration, Stream A's review, and a **policy nobody has decided**: the backdating window and who signs off are still an open question in this stream's own decisions list, and `feature/tickets/effort/README.md` already records it as *"raised rather than made"*. Building the flow here would have settled it by accident, in the file least likely to be read by whoever eventually decides. **Raised as its own task**; the view it would approve exists now, which is the part that was blocking nothing else.

  > **Built in `feature/reports/` because the visibility rule already lives there.** "Whose timesheet may I open" is not a new question — it is blueprint §2's *View team member history* row, written down once as `Profile360Repository.isVisibleTo`: Admin anyone, PM their reportees **or** their projects, Support their projects, the three delivery roles nobody but themselves. That method is package-private. Building this under `feature/masters/` — this stream's own directory, and the comfortable choice — would have meant **a second copy of a row-level access rule**, which is precisely the arrangement B-062 spent a whole task dismantling three files away. A refusal answers 404, never 403, so ids cannot be walked to learn who exists.

  > **Stage-aware is the row key, not a column.** Ticket **and** stage **and** iteration **and** cycle. Six hours on one ticket reads very differently when four of them are a second pass through QA, and a grid keyed on ticket alone cannot say so. All four are needed for PLAN.md §3.4's reason: without `cycleNo` a reopened ticket re-entering the same stage at the same iteration folds cycle 1's hours into cycle 2's line.

  > **Capacity is `WorkingHoursService`'s answer, one call per day, never seven times eight.** Weekly offs, org holidays and that resource's own approved leave are already in it, and a half day is already half — the IT asserts the seeded calendar's **nine** hours rather than a round eight, so an implementation that computed a working day instead of asking for one fails. **No `projectId` is passed**: a week spans projects, and one project's holiday calendar would answer for a person it does not describe. Utilisation is null rather than 0% when the week offered nothing — somebody on leave all week makes no claim about how they spent it.

  > **Corrections stay visible and signed.** A reversal nets out of the totals and marks its row rather than being filtered away; a timesheet that hid them would not add up to the same week the ticket's own Effort tab shows, and the two are read side by side in exactly the argument this screen exists to settle.

  > **A test that only passes on a UTC laptop was found and closed.** `TimesheetIT` seeds a holiday on a Wednesday and asserts Wednesday has no capacity — and it was proving it about **Tuesday**. `EduTrackApplication`'s static block sets the JVM zone to UTC (its own comment explains why: a `LocalDate` read back east of Greenwich comes back a day early), but in a `@SpringBootTest` that block runs *after* the `@Container` field has already initialised the MySQL driver, so the driver and the application disagree by the offset. A static block above the container pins the zone first. `WorkingHoursServiceIT` never needed one — it is a `@DataJpaTest`, so nothing changes the zone underneath it. CI runs on UTC hosts where none of this can appear, which is what made it worth writing down.

  > **Touches `feature/reports/` — Stream A's directory — by this backlog's own M6 split**, the precedent B-060, B-061 and B-062 set, plus one `PermissionMatrix` row (`everyRole`; who may open whose week is a row question, not a capability one). Also `contracts/openapi.yaml` and `mocks/handlers/rest.ts` (D's: one path, four schemas, one handler — the mock-coverage test refuses a contract operation with no handler, so the route brings its handler with it). **No migration**: `ix_effort_user_date (user_id, work_date)` has existed since the A-006 baseline, so no Stream A schema review. **The row carries `ticketId` as the code and nothing else** — `TicketId` is a string everywhere else in this contract, and a numeric id beside it would have made the timesheet the one payload where a ticket has two names.

- [x] **B-065** 🟡 **Timesheet approval** — §21's second half, split out of B-063 rather than dropped. A manager marks a resource's week reviewed, and the review is a **new row in a new table**, never a flag on `ticket_effort_logs`. — *`V20260822_1200__timesheet_approvals.sql` (Stream A review flagged, table sits beside the protected three); `feature/reports/` gains `TimesheetApprovalRepository`, `TimesheetApprovalService`, `TimesheetExceptionHandler` and one route on `TimesheetController`, on `ResourceWriteRepository`'s read/write split. `Profile360Repository` gains `isDirectManagerOf`. One `PermissionMatrix` row (`adminAndPm`). 8 new backend unit tests plus 6 against real MySQL, 5 frontend.*

  > **The decision, answered 22 Aug 2026: 7-day backdating window (unenforced — a separate, unbuilt task in Stream C's `feature/tickets/effort/`), sign-off by Admin or the resource's own *direct* reporting manager, one review per resource per week.** Only the second and third points shape this table; the backdating figure belongs to `EffortLogRequest.workDate`, not to this task, and building that enforcement here would have been touching Stream C's file uninvited.

  > **Direct manager, not `isVisibleTo`'s wider reading.** `GET .../timesheet` admits a PM sharing a project with the subject as well as their direct reportee — right for "may this PM look", wrong for "may this PM sign off": approving is a manager's act, not a colleague's. `Profile360Repository.isDirectManagerOf` is deliberately narrower, and deliberately not a transitive walk up the chain either, on `isVisibleTo`'s own precedent for leaving that a separate, undecided widening.

  > **Two refusals, two status codes, on CONVENTIONS.md §7.** `hasAnyRole('ADMIN','PM')` on the controller is the one **403** this feature answers — rowless, because whether a Developer may ever approve *any* week does not depend on which one is named, `BulkReassignController`'s identical shape. Underneath it, whether *this* PM may approve *this* resource's week is a row question and is a **404** instead, `GET .../timesheet`'s own precedent — a 403 there would concede which user ids exist to a caller who is not their manager.

  > **One review per week, enforced twice.** `uq_timesheet_approvals_week` is the authority; `TimesheetApprovalService` only gives the race a name — a losing insert is re-read and reported as a 409 naming who got there first, never a silent second row.
- [x] **B-062** Export engine integration for all report types.

  > **The engine was written once and wired once, and two more surfaces grew their own.** A-064's `ReportExporter` was built so "a report added by A-066 is exportable the day it lands without touching this package", and it was. What nobody checked was the rest of the product: `AuditExportService` (A-071) reused the writers and rewrote the delivery, and `ResourceExportWriter` (B-010, this stream's own file) rewrote both — 262 lines carrying a **third copy of the spreadsheet formula-injection guard**, plus its own SXSSF window, byte-order mark and RFC-4180 quoting. Three copies of a security control is the arrangement where fixing it once leaves two live and nothing fails to say so. The two copies had already drifted: **Open Tickets was a number in the xlsx and a string in the CSV**, because one path called `setCellValue(int)` and the other `String.valueOf`, and nothing declared that the two files should agree.

  > **B-010's reason for writing its own was real, and it was the engine's signature.** `write(…, List<Map<String, Object>> rows)` needed every row in the heap before the first byte — while the same class's javadoc explained at length that nothing larger than SXSSF's window is ever resident. Both could not be true: the streaming was of the *output* only. `/users/export` walks a keyset cursor in batches of 500, so it could not call the engine, so it wrote one. **`ExportRows` is the fix and the whole task rests on it** — a push source matching the shape `streamAll(filter, sink)` already has, with a `knownCount()` that answers `null` rather than a guess, because a cursor cannot count without a second `COUNT(*)` over the filter the export exists to run once.

  > **The PDF is the one exporter that buffers, and it now plots what it prints.** It truncates at 100 rows and used to draw its chart from *every* row, so on a long report the picture summarised data the document did not contain — a reader checking a peak against the table could not find it and had nothing on the page to explain why. Both read the same truncated page now, and the total is counted while draining rather than asked for up front. Holding 100 rows is bounded whatever the query returns, which is the property the other two formats were already careful about.

  > **`ExportDelivery` is the delivery half, extracted rather than invented.** Filename, headers, `no-store`, and the rethrow that aborts rather than delivering a silently short spreadsheet — all of it was correct inside a package-private service whose one method took a `ReportService.Rendered`, so a feature with rows and columns but no *report* could not reach it. The seam is now a file's worth of columns and rows, which is all any of the three callers ever had. One thing changed on the way: **the filename is stamped UTC**, where the report path read the server's zone and the resource path already read UTC — the same export naming itself differently depending on which host answered.

  > **The resource export gains a provenance block, and that is the point rather than the cost.** The file now leads with its title and the filters that produced it, so a directory narrowed to one project and one manager is no longer a file indistinguishable from the whole organisation — the reader it is forwarded to was taking 14 rows for the headcount. Ids rather than names for project and manager: resolving them is two more queries on a path whose whole point is one streaming pass, and "project 7" is recoverable where a missing line is not. **PDF is still refused on `/users/export`** although the engine writes one — the contract declares two formats, and routing through a wider engine must not quietly widen what a route answers.

  > **301 lines of `ResourceExportWriterTest` were ported, not deleted with their subject.** The BOM, the quoting, the CRLF endings, the SXSSF spill past the window and the formula-injection cases all now run against the real exporters, driven exactly as `ResourceController` drives them. The guarantee people rely on is not "`CsvReportExporter` escapes a leading equals" — it is "*the resource export* cannot run a formula in somebody's spreadsheet", and only a test through this path says that. A future refactor routing it somewhere unguarded fails there.

  > **`ReportExportContractTest` is what was actually missing for "all report types".** Nothing anywhere asserted A-064's central claim. Every catalogue descriptor — built and unbuilt, because five of the eighteen are somebody's next task — is now exported in all three formats: a title Excel refuses as a sheet name, a key that would break a shell script, a `ColumnType` no exporter handles. Mutation-checked: making one numeric type write as text turns it red. B-061 had to find two switch statements by reading in order to add `TREND`; this is the test that would have pointed at them.

  > **Touches `feature/reports/` — Stream A's directory — by this backlog's own M6 split**, the same standing this stream wrote B-060 under. **`feature/audit/` is Stream A's and is not in that split**, so it takes the one-line change the signature forces and nothing else: its duplicated delivery block is recorded in a comment there and **owed to Stream A**, not fixed quietly from outside the package. No migration, no new route, **no contract change and no client regeneration** — every route and every offered format is exactly what it was, which is the strongest thing that can be said about a consolidation. **A clients-list export (§S-13's `Export` action) is still absent** and stays that way: it is a screen feature rather than a report type, and building it as a fourth writer is what this task exists to prevent.
- [ ] **B-063** Timesheet view — stage-aware, a resource's week across all tickets.

---

### Client 360

- [ ] **B-066** **Client 360 — the placeholder S-20 links to, and it has never had a task.** `CLIENT_ROUTE` renders `ScreenPlaceholder title="Client 360"`. Same origin as A-077: S-20 makes every entity in the ticket summary panel a link, and `TicketDetailPage`'s comment assigns this destination to Stream B — *"the client 360 to Stream B… Each owner replaces one element."* Stream A's sibling (A-069 / S-28, resource 360) shipped; this one and the project dashboard did not, and neither had a task until now. Raised by Debashis on the project lead's instruction after auditing the router's placeholders; scope and shape are yours. The client master (S-33/B-029) is the maintenance screen — this is the read-across view a PM opens from a ticket.


### The two screens the sidebar promises and nothing builds

- [ ] **B-067** **Masters index — the sidebar's Masters entry lands on a placeholder.** `/masters` renders `ScreenPlaceholder title="Masters"` with a hand-made list of links to the masters that do exist. Its comment says "the masters index arrives with the rest of M3" — M3 has largely landed and no task names it, so the stand-in has quietly become the screen. Scope is small: the index over the masters that exist, permission-filtered so a role that cannot open a master does not see a dead card. Raised by Debashis on the project lead's instruction after auditing the router's placeholders.

- [ ] **B-068** 🟡 **Org settings screen — or the decision that the API is enough.** `/settings` renders `ScreenPlaceholder title="Settings"`. This is [`DEPENDENCIES.md`](../DEPENDENCIES.md) row 24, still open: `PUT /attachments/limits` exists and is `master.write`, §11's sidebar has a Settings entry, and **no S-number specifies an org-wide settings screen** — so today an Admin sets the caps by asking somebody rather than from a page. The register records the owner as uncertain ("masters' surface by shape, C's by feature — Ayush?"). **Raised so it stops being invisible, not to settle it.** If the answer is that the API is enough, close this with that reason and the placeholder should say so rather than promising a screen.


## Decisions you own

Answer during M3 (PLAN.md §5, blueprint §16):

- Can a client contact be created inline from the ticket form? *(Recommended: yes — or the desk picks the wrong existing contact to avoid the detour.)*
- Does the client Excel import update existing records or only insert? *(Recommended: upsert on client code, with the dry run showing exactly what changes.)*
- Who may edit Planned Close Date after assignment? *(Recommended: PM/Admin only, reason mandatory, logged.)*
- Effort backdating window? *(Recommended: 7 days, then manager approval.)*
