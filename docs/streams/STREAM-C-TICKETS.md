# Stream C — Tickets & Ribbon · Task Backlog

**Milestones:** M0 (design system) · M4 (tickets + the Workflow Ribbon)
**Owner:** Divyansh · `divyansh.edunext@gmail.com` · @Divyanshedunext
**Branch prefix:** `feat/tickets/…`
**Owns:** `backend/api/feature/{tickets,transitions}/`, `frontend/src/features/tickets/`, `frontend/src/components/{ui,ribbon}/`, `frontend/src/styles/tokens.css`

> **You carry the largest slice — roughly 40% of the product surface and the hardest UI in it.** Stream B joins you from week 10 to take the ribbon's frontend. You also own the shared component library, which all three other streams consume: changes there are additive only, and Storybook is the contract.

> Cross-stream sequencing — who is waiting on you and what to do if you are blocked — is in [`../DEPENDENCIES.md`](../DEPENDENCIES.md).

---

## Sprint 0 — weeks 1–2

*Depends on nothing. Start day 1.*

- [x] **C-001** Vite + React 18 + TypeScript scaffold, TanStack Query, Zustand, React Hook Form.
- [x] **C-002** 🔴 **Design tokens** from blueprint §12.1 → `tokens.css` + `tailwind.config.ts`. All 12 colour tokens, level chips, the colour-blind-safe chart palette, ribbon segment states. Type scale (Inter / Plus Jakarta Sans, base 14/20), 4px spacing scale, radius 12/8/999, the two shadow levels. **Frozen after Sprint 0** — other streams request tokens rather than adding them.
- [x] **C-003** Shared component library — button, input, select, searchable dropdown, chip, table, modal, slide-over, toast, skeleton loader, empty state, avatar stack.
- [x] **C-004** Storybook, with every shared component documented. If it isn't in Storybook, it isn't shared.
- [x] **C-005** App shell — collapsible 240px sidebar, top bar with global search, project switcher, notification bell, chat badge, avatar menu. Toast layer bottom-right.
- [x] **C-006** Command palette on `Ctrl+K` for jump-to-ticket.

**Exit:** `npm run storybook` renders the library in correct tokens; `npm run dev` serves the shell against D's MSW mocks with no backend running.

---

## M4 — Tickets · weeks 3–14

### Create & list
- [x] **C-010** Create ticket — all field groups from blueprint §7.5: Identity, Core, People, Effort, Extra. **S-19** — *`frontend/src/features/tickets/create/`, route `/tickets/new`. Length bounds and the request body are imported from the generated Zod and the mapper's output is parsed against `createTicketBody`, so the form cannot drift from the contract without a test failing — same guard C-011 put on `TicketCode`. `plannedCloseDate` is **omitted**, never nulled, when blank: omitting means "compute it from the SLA policy", while `null` means "no planned close date" and silently drops the ticket out of every delay calculation. One idempotency key per logical ticket, minted on mount and rotated only after a ticket exists — the generated `useCreateTicket` is unused because orval drops header parameters, so it cannot send the header at all. Level is a chip radio group rather than a dropdown (four options, all colours visible, no popup) sourced from the priority master and rendered in the frozen `level-*` tokens. 26 tests; folder README carries every decision and hand-off.*
  - ⚠ **Open for Stream D — `contracts/openapi.yaml`.** `TicketCreateRequest` has no field for four things §7.5 asks for — **sub-type/category**, **environment** (Prod/UAT/Dev, "critical for bug triage"), **linked tickets**, **tags** — so they are not rendered rather than rendered and silently dropped. Two more render read-only at their server default and cannot be overridden: **date reported** (backdating is Admin/PM per §7.5) and **reported by** (Support Desk raising on a client contact's behalf). Smaller: D-004's mock ignores `GET /clients?projectId=`, so the client list looks unfiltered under `npm run dev` though it is filtered against the real API; and §7.5 caps the title at 200 characters where the contract says 300 — the form uses the contract's, since refusing at 200 rejects input the API accepts.
  - ⚠ **Open for Stream B — Task Type master.** §4B.2 calls the "requires a client" rule *configurable per task type*, but `TaskType` has no `requiresClient` flag. The rule matches on the master's **name**, so a rename silently disables it. `ticketForm.test.ts` pins that failure mode; move it onto a flag when one exists.
  - `components/ui/searchable-dropdown.tsx` gained four optional ARIA props (`id`, `aria-labelledby`, `aria-describedby`, `aria-invalid`) forwarded to the trigger — additive, every existing call site renders identically, Storybook story `LabelledWithError`. Without `id` the control reaches a screen reader unnamed, which fails AA.
- [x] **C-011** 🔴 **Ticket ID generation** — `UPDATE projects SET ticket_seq = LAST_INSERT_ID(ticket_seq + 1)` then `SELECT LAST_INSERT_ID()`. **Never `COUNT(*)`** — it breaks silently under concurrency. Format `{CODE}-{YY}-{00001}`. *(PLAN.md §3.2)* — *`api/feature/tickets/`: `TicketCode` (format), `TicketSequenceRepository` (the idiom), `TicketCodeGenerator` (`@Transactional(MANDATORY)` — `LAST_INSERT_ID()` is connection-local, so both statements must share one connection). Verified 2026-08-08 against MySQL 8.4.6 with a real Flyway run: 64 concurrent allocations across 8 threads gave 64 distinct, gapless numbers. Mutation-checked — swapping the idiom for a read-then-write race still passes 6 of the 7 integration tests and is caught only by `concurrentAllocationsAreAllDistinct` (11 distinct where 64 were issued), which is exactly why that test exists.*
  - ⚠ **Needs Stream D sign-off — `contracts/openapi.yaml` is Debashis's file.** `TicketId` was `^[A-Z][A-Z0-9]{1,9}-\d{2}-\d{5}$` — *exactly* five digits — and that pattern types the `ticketId` **path parameter**, so it was already generated into the Zod guarding attachments, comments, journey, ribbon and client-360. The counter never resets, so past 99,999 on one project the ticket would be created and stored correctly and then be unreachable on every route addressing it. Widened to `\d{5,}` and the client regenerated (7 files, pattern-only diff; `tsc --noEmit` and `check-conventions.py` both clean). `TicketCodeTest.everyEmittedCodeMatchesTheContract` now reads the pattern **out of the contract** rather than copying it, so the two cannot drift again — verified by narrowing it back, which fails the test naming the exact rejected codes. `frontend/src/mocks/mocks.test.ts` also held a hand-copied `\d{5}` while claiming to assert "the contract format"; it now imports the generated `getTicketDetailPathTicketIdRegExp` — the path pattern, because an unaddressable new ticket is the failure being guarded against. Also Stream D's file.
  - ⚠ **Open for Stream B:** PLAN.md §3.2 also requires `projects.project_code` to be rejected for edit once the project has any ticket — historical ticket codes stop resolving otherwise. That belongs to the Project Master (S-10), not this task.
- [x] **C-012** SLA policy resolution → auto-computed Planned Close Date, **previewed inline before the user commits**. — *`api/feature/tickets/{SlaResolution,PlannedCloseDateService,PlannedCloseDateController,PlannedCloseDateDtos,TicketExceptionHandler,UnknownLevelException}`, `frontend/src/features/tickets/sla/`, `GET /tickets/planned-close-date`. **The service resolves and hands off; it walks no calendar itself** — `WorkingHoursService` (B-024) does, and its javadoc already named this task as its caller. Same reason the preview is a server round trip rather than a calculation in the form: a browser-side estimate would be a second implementation of that class and one that *cannot* agree with it, because the browser cannot see leave for a resource the user has no permission to read — and it would disagree quietly, by a weekend, with only one of the two numbers ever on screen. **The ladder runs five rungs, not §6's three**: project×type×level → project×level → org default → priority master default → task type default → `NONE`. Rungs 4 and 5 are this task's addition and are argued in the folder README — `SlaPolicyRepository`'s javadoc stops at rung 3 with "no SLA, leave it alone", which is right for a scanner and wrong for a date, because a project whose matrix is unconfigured (every project on day one) would give every ticket `planned_close_date IS NULL` and silently drop it out of the breach sweep, the pre-breach warning, the delayed KPI and Due Today. Rung 4 precedes rung 5 because the first three are keyed by level and the priority default is the last figure that still varies with it; a type default first hands Critical and Low the same date, which is exactly what §4B.1's dropdown exists to remove — and it looks like a working feature, because a date still appears. **A non-positive `resolutionHrs` is no target, not "due on arrival"** — `addWorkingHours` returns the start unchanged for it, so honouring a zero gives a planned close date already in the past and an immediate breach on a ticket nobody has read. `source` is carried onto the wire so the date is explicable and so whoever edits S-13's matrix can see which row applied. Unknown project is **404 saying nothing more** (out-of-scope arrives here as "does not exist" once A-034 lands); unknown level is **400 and does echo the value** — a level code is not a row id, and a bad level falling off the ladder would look identical on screen to a correctly spelled one nobody configured. 33 new tests; `tsc -b`, `eslint --max-warnings 0`, `npm run build`, redocly and `check-conventions.py` all clean; the C-012 tests green across 10 repeated runs.*
  - ⚠ **Needs Stream D sign-off — `contracts/openapi.yaml`.** `previewPlannedCloseDate` plus `PlannedCloseDatePreview`, `PlannedCloseDateResponse` and `SlaSource`. Additive only, client regenerated and verified byte-identical against the spec. Same precedent C-011 and C-015 set for a blocking additive contract edit with a flagged sign-off rather than a synchronous ask.
  - ⚠ **Also Stream D — `frontend/src/mocks/`, and one of these was a live defect.** `POST /tickets` stamped `now + defaultSlaHrs` in **wall-clock** milliseconds, so the date the preview showed and the date the ticket carried differed by every weekend and holiday between them; it now uses the same resolution and walk. `GET /projects/{id}/sla-policies` derived the whole matrix from `taskTypes.defaultSlaHrs`, giving **every level identical hours** — a preview that never moves when the level changes is precisely this feature failing invisibly; each cell is now resolved. `db.ts` gains a real `slaPolicies` table seeded so all five rungs are reachable under `npm run dev`, and `handlers/sla.ts` is new.
  - ⚠ **`SlaPolicyWrite.taskTypeId` is required**, so `GET /projects/{id}/sla-policies` cannot express the layered `(project, null, level)` and org-wide rows the table actually stores. S-13's editor needs either a nullable `taskTypeId` or a separate representation for the defaults — resolving each cell is right for reading and cannot round-trip a write.
  - ⚠ **`TicketCreateRequest` carries no `slaPolicyId`**, so a ticket does not record which policy it was created under. The contract's own note on `replaceSlaPolicies` says "tickets carry the policy they were created under", and today nothing does. Not blocking here — the preview is stateless — but C-038's reopen and the breach report both want it.
  - ⚠ **The backend was not compiled or tested.** Only JDK 8 is installed on the machine this was written on, and javac 8 fails on *existing* files (`ChatParticipantId`, `EmailSuppressions`) before reaching any of this — records and text blocks. CI's Temurin 25 is the first thing to execute it. The frontend, contract and mock halves are fully verified.
  - **Deleted `frontend/src/__repro.test.tsx`** — Stream A's debugging leftover from A-030: `console.log`, a hardcoded 1500 ms sleep, **no `expect` anywhere**, and two network-waiting `findBy*` calls on the default 1000 ms. It provided no coverage and could only produce false alarms, and it failed in **5 of 5** loaded full-suite runs — more than every other file combined. The same default-timeout defect C-019 fixed in `App.test.tsx` after that fix had already landed. Removed at Divyansh's direction and flagged here and in the PR body rather than done quietly.
  - ⚠ **For everyone — the suite is at its limit on a 4-core machine.** Unconstrained, `vitest` spawns a worker per core and 37 jsdom + React + MSW files starve each other; failures then land on whichever file is unluckiest, including `mocks/coverage.test.ts`, which is fully deterministic and *cannot* fail for a real reason. Nothing reproduces in isolation and `--maxWorkers=2` is green every time. GitHub's standard runners are also 4-core, so CI is exposed to the same starvation — worth capping the pool in `vite.config.ts` before it starts costing people re-runs.
- [x] **C-013** Actions: Save & Assign · Save as Draft · Save & Create Another. On save → ID generated → notification → `CREATED` history row → email. — *`frontend/src/features/tickets/create/`. Validation is chosen **per attempt**: the clicked button sets a ref the resolver reads, and the ref falls back to `assign` once the attempt settles, so a draft attempt cannot leave the form permanently lenient (`applies the draft rules per attempt rather than latching them on` pins it). A draft relaxes exactly the three rules that are the blueprint's and not the contract's — mandatory description, mandatory effort, §4B.2's client rule — because `TicketCreateRequest.required` is `[projectId, title, taskTypeId, level]` and relaxing further just earns a 400. It still rejects a malformed entry: `4h` fails on every path. Blank optionals are omitted, never sent empty — `estimatedHrs: 0` is a genuine zero-hour estimate, a different claim from "not estimated yet". **Save & Create Another rotates the idempotency key**, which is the sharp bug here: it is the only path reaching the form twice without a remount, and a carried-over key replays the first response for 24 h, so the user is shown the first ID again and the second ticket is never created — the mock does not implement replay, so only asserting the two keys **differ** catches it. It keeps the batch context (project, client, contact, task type, level, assignee, watchers), clears what describes one ticket, and refocuses the title from an effect rather than inline, because `reset` drops React Hook Form's ref registry and a same-tick `setFocus` silently no-ops. **Save & Assign does not require an assignee** — §7.5 leaves Assigned To unstarred and C-015 ships an Unassigned saved view, so the action bar states which way it will go instead. 39 tests in the folder, all green; lint, `tsc`, `build` and `check-conventions.py` clean, and the three actions driven end-to-end in real Chrome against the MSW mocks.*
  - ⚠ **For every stream — a fixed bottom action bar collides with the toast viewport.** `ToastViewport` is `fixed bottom-0 right-0 z-[100]`, which is exactly where a right-aligned action bar puts its primary button, so a success toast physically covers it and clicks land on the toast. It only bites when the screen *keeps* the user after the action — every navigating path is gone before the toast matters — which is why C-010 never hit it and Save & Create Another does. Fixed locally by confirming inline in the bar's live region; moving the viewport would change every screen in all four streams, so it is not a one-branch change. If a second screen hits this, that is the point to raise it as a shared fix. Neither this nor the sibling layout bug (four buttons plus the hint overflowed the bar and pushed **Save & Assign** onto a second row, off it) was findable in jsdom — both needed a real browser.
  - ⚠ 🔴 **Open for Stream D — a draft is not distinguishable from a ticket.** `saveAsDraft` is on `TicketCreateRequest`, but **`StatusCode` has no `DRAFT`** and `TicketResponse` carries no draft flag, so the field is write-only with no observable effect: a saved draft shows in every list and saved view, is handed a planned close date and an SLA clock it will breach, and notifies whoever is in Assigned To. D-004's mock encodes the gap literally — `status: body.saveAsDraft ? 'NEW' : 'NEW'` in `mocks/handlers/tickets.ts`. The form sends the right request and needs no change; what is missing is somewhere to record draft-ness, suppression of the notification, email and SLA clock while it is one, and exclusion from the default views. Smaller: `taskTypeId` and `level` are `required`, so a draft cannot be saved from a title alone — a reasonable floor the form is built to, but if §7.5 means to accept less, both need to become optional when `saveAsDraft` is true.
  - ⚠ **The rest of the save chain is server-side and spans three owners.** ID generation is C-011 and landed. The `CREATED` history row belongs to a ticket-create service that does not exist yet — `backend/api/…/feature/tickets/` holds only the code generator — and the 🔔 notification and the email are Stream D's engines. The MSW mock already inserts the `CREATED` row, so the frontend is exercised against the intended behaviour.
- [x] **C-014** Ticket list — filters (project, client, type, level, stage, status, assignee, dates), sticky header, density toggle, column chooser. **S-17** — *`frontend/src/features/tickets/list/`. Filter state lives in the URL, not component state — `TopBar`'s global search (C-005) already navigates here as `/tickets?q=…`, so every filter follows the same rule for the same reason a saved view (C-015) will want it: a filtered grid is a link a manager can paste into chat. Cursor pagination (`?cursor=`, never offset — CONVENTIONS.md) has no page numbers to jump to, so Previous/Next are backed by a stack of visited cursors instead of the wireframe's `‹ 1 2 3 ›`; `meta.totalCount` is optional per its own doc comment ("never computed live over tickets") and the footer degrades to `Rows 12–36` without it rather than assuming it is always there. Level options come from the priority master in its order, same rule C-013's `LevelPicker` follows; Stage options are deduped out of `GET /masters/workflow-templates` since there is no dedicated stages endpoint. Density and column visibility persist to `localStorage` — per-user display preference, a different concern from C-015's named server-side saved views. 9 tests; `tsc -b`, `eslint` and the full suite clean across five repeated runs.*
  - ⚠ **The compact ribbon column is deliberately not here.** Not named in this task's own backlog line, and building it needs more than the list fetches: a template's stage sequence is keyed by `projectId` **and** `taskTypeId` (`GET /masters/workflow-templates`), so a grid mixing tickets from several projects cannot resolve one row's dot count without resolving that row's own template — the same kind of per-row waterfall S-20 collapsed into one aggregated call for the detail page. Today's mock only ever seeds one template, so the gap is invisible until a second one exists. Needs a contract answer before it can be built — a per-ticket ribbon summary folded into `TicketListResponse`, or a cheap stage-order lookup keyed by project and task type — see the folder README.
  - ⚠ **Worth knowing for the other streams — chaining two Radix popover picks in a test is a real, load-only flake.** `FilterDropdown` and `SearchableDropdown` both restore focus to their trigger a frame after closing (`CreateTicketPage.test.tsx` already flags this for `SearchableDropdown`). Picking one filter and immediately opening a *different* filter's popover races that restoration: if the second popover is already open when focus jumps back to the first trigger, Radix reads it as focus leaving the second popover and dismisses it before the click lands. Only reproduces under real CPU contention — running the full suite, not this file alone — which took several repeated full runs to catch. Retrying "reopen, then click" as one `waitFor` unit recovers; retrying only the click, against a popover no longer there, does not. Full detail and the fix in the folder README.
- [x] **C-015** Saved views — My Open, Due Today, Overdue, Unassigned, Reopened, Closed This Month. — *`frontend/src/features/tickets/list/`. Six fixed, built-in presets over C-014's URL-based filter state — no `SavedView` schema exists and the blueprint gives zero elaboration on filter criteria, so nothing user-creatable was invented. `useTicketListFilters` gained `applyFilters(next, opts)` to set several keys atomically in one URL update (a saved view needs several keys to land together, not clobber each other via N sequential `setFilter` calls) plus `isDelayed`/`reopenedOnly`/`unassigned`/`excludeClosed`/`closedFrom`/`closedTo`, serialized the same boolean-or-absent way as the contract's existing `isDelayed`/`isClientRaised` — never a literal `'false'` in the URL. `SavedViewsMenu.tsx` replaces the old disabled stub; the active view (if any) is computed by comparing the current URL-backed filters against each recipe key-by-key, never stored separately, so it cannot drift from what picking a view, editing a chip, pasting a link, or Reset actually produced. `isDelayed` and `reopenedOnly` were already full round-trips through the contract and mock — C-014 just never put a control in front of them. 23 new tests (10 `SavedViewsMenu.test.tsx`, 6 `useTicketListFilters.test.tsx`, 3 added to `TicketListPage.test.tsx`, 4 added to `mocks/mocks.test.ts`); `tsc -b`, `eslint` and `npm run build` clean; full suite clean across five repeated runs bar one intermittent failure in Stream D's `useNotificationStream.test.tsx` (passes standalone every time; only flakes under full-suite CPU contention) — untouched by this change and outside `frontend/src/features/tickets/`.*
  - ⚠ **Needs Stream D sign-off — `contracts/openapi.yaml` is Debashis's file.** `GET /tickets` had no way to express three of the six recipes: "assignee is null" (`assigneeId` is equality-to-a-specific-user only), "not closed" (`status` is single-value equality), or a date range on `actualCloseDate` (`dueFrom`/`dueTo` filter `plannedCloseDate`). Added three additive, optional params in the same style as the existing `isDelayed`/`isClientRaised`/`reopenedOnly`/`dueFrom`/`dueTo`: `unassigned` (boolean), `excludeClosed` (boolean), `closedFrom`/`closedTo` (date, mirroring `dueFrom`/`dueTo` but against `actualCloseDate`). Client regenerated via `npm run api:generate` — only `listTicketsParams.ts` and `tickets.zod.ts` changed, pattern-only diffs. Same precedent C-011 set for a blocking, in-session contract edit with a flagged sign-off rather than a synchronous ask.
  - Also fixed in the same change, not new scope: `frontend/src/mocks/handlers/tickets.ts`'s `GET /tickets` handler declared `dueFrom`/`dueTo` since C-014 but never read them — the filter looked wired end-to-end and silently was not. "Due Today" depends on it actually working, so it is fixed alongside the three new params, plus filtering for `unassigned`/`excludeClosed`/`closedFrom`/`closedTo`. Covered by new tests in `mocks/mocks.test.ts` deliberately using different days for the due-date and closed-date ranges, so a handler that swapped the two date fields would still fail.
  - No Storybook entry — `SavedViewsMenu` is ticket-list-specific (hardcodes the six view names and `TicketListFilters`'s shape), not a shared-library control; reasoning recorded in the folder README next to `ColumnChooserMenu`/`DensityToggle`, which carry the same exemption.
- [x] **C-016** Row colour cues — soft amber left border on delayed, soft red on critical.
- [ ] **C-017** Bulk select → reassign / change level / close (PM & Admin only).
- [x] **C-018** My Tasks — hard-scoped to `assigned_to = me`, grouped Due Today / Overdue / This Week / Later, inline Quick Update on every row, optional Kanban toggle. **S-18**

### Where it happened — module, screen, feature, steps

Added 11 Aug 2026 at the client's request; blueprint revision 1.3 (§7.5, §8.2) and PLAN.md §3.9 carry the specification. Four fields that turn "which module generates the most concerns" into a query, and give a developer opening a bug something to reproduce from. **Attachments were part of the same request and need nothing new — §4B.4 already specifies them and C-023 – C-028 already build them.**

- [ ] **C-065** `product_modules` master + the four columns on `tickets` — table (`code`, `name`, `seq`, `is_active`) seeded with Student, Admission, Fees, Examination, Attendance, Library, Inventory, Parent App in a Stream C seed file (TEAM-PLAN §7.3); `module_id` FK, `screen_name VARCHAR(120)`, `feature VARCHAR(120)`, `steps_to_generate MEDIUMTEXT`, all nullable, plus `ix_tickets_module`. **Needs Stream A's review before merge** — it ALTERs `tickets` (TEAM-PLAN §7.1). Timestamp-versioned; the eight rows are seed data, never a Java enum.
- [x] **C-066** Shared rich-text editor in `components/ui/` + Storybook — the first one in the codebase; description is a plain textarea today and C-029's comment box needs the same control, so it is built once here. Client-side sanitisation over PLAN.md §3.9's allow-list, `dangerouslySetInnerHTML` only ever over sanitiser output, keyboard-accessible toolbar with ARIA labels. — *`components/ui/rich-text.ts` (allow-list, sanitiser, plain-text projection, toolbar presets, prose classes), `rich-text-editor.tsx`, `rich-text-view.tsx`, one story file, 94 tests. **The sanitiser is the deliverable and the toolbar sits on top of it**, which is why `contentEditable` + `execCommand` rather than TipTap or Lexical: both are document-model editors bought to produce fourteen tags we already specify, and adopting one means a second source of truth beside the contract. `execCommand` emits `<b>`, `<i>` and `<strike>` — **none of which are on §3.9's list**, so without the alias map the toolbar would appear to work and silently lose every bold word at rest; that map had to exist anyway for content pasted out of Word. Paste is intercepted and sanitised **before** insertion, because once markup is in the DOM its styles have applied. §3.9's `data:image/*` is **narrowed to raster types** — SVG is a scriptable document format, not a screenshot format — and the check is repeated in an `afterSanitizeAttributes` hook because DOMPurify has a second branch that waves any `data:` URI through on `img` regardless of `ALLOWED_URI_REGEXP`. Empty is `isRichTextEmpty`, not `.trim()`: an emptied contentEditable leaves `<p><br></p>` behind, which is 13 characters of nothing and passes a naive required check. Toolbar is one tab stop on the APG roving-tabindex pattern — thirteen stops would put twelve presses between the label and the text on every rich-text field on the product. A repo-wide test fails the build on any `dangerouslySetInnerHTML` outside `RichTextView`. `tsc -b`, `eslint --max-warnings 0`, `storybook build` and the full 327-test suite clean.*
  - **The description is migrated with it** — S-19's textarea and S-20's `<p>` both go through the editor and `RichTextView` now, at Divyansh's direction after the component landed. A shared control with no consumer is a control nobody has proved, and the round trip is where the interesting failures are: the mapper sanitises on the way out as well as in, and the required check is `isRichTextEmpty` rather than `.min(1)`, because a focused-then-abandoned editor holds `<p><br></p>` — 13 characters of nothing that a length check reads as present. Binding is react-hook-form's `Controller`; `register()` cannot bind a contentEditable, which has no `input` event carrying a `value`. Two new tests cover the whole path: formatting typed on the form arrives on the wire as sanitised HTML, and a stored `<script>` renders clean on the detail page. **Steps to Generate (C-068) and the comment box (C-029) still take it, unchanged, on the compact or full bar.**
  - ⚠ **`ensureRichText` is a temporary, and dated.** Every description in the database is plain text until C-067 lands the write path and backfills. Handed straight to `RichTextView`, HTML collapses its line breaks — so a two-paragraph description written last week silently becomes one, which is an unannounced edit to somebody's bug report. The shim gives markup-free values their paragraphs back and leaves everything else alone; it detects markup by **parsing**, not by looking for `<`, because a description reading `fails when (a < b)` has no element in it and a regexp guessing at tags mangles exactly that. **Delete it with the backfill** — three tests pin the behaviour and will tell whoever removes it what they are removing.
  - ⚠ **For Stream A and C-067 — two §3.9 questions this raised.** (1) **`img` has no `alt` on the allow-list**, so there is no author-supplied alternative text and the client marks every image decorative (`alt=""`) to stop a screen reader reading out several thousand characters of base64. That is the correct fallback, not a fix: an image carrying the whole bug report is content, and content needs a description. Whether authors may write `alt` is a §3.9 change, not a component change. (2) **The `data:image/svg+xml` narrowing** above is a deviation from §3.9's literal `data:image/*` — narrowing is the safe direction, but the server sanitiser should narrow the same way or the two lists disagree and the client's copy stops being a faithful preview of what will be stored.
  - ⚠ **New dependency — `dompurify` ^3.4.13** (29 kB minified, no transitive dependencies, ships its own types). Hand-rolling the sanitiser was the alternative and was rejected: mXSS through parse/serialise round trips is exactly the class of bug a hand-rolled allow-list gets wrong, and DOMPurify's own re-parse loop is the mitigation. Its hooks are global to the instance, so **other streams must import `sanitizeRichText` rather than calling `DOMPurify.sanitize` directly**, or the two configurations quietly become one.
- [ ] **C-067** Backend wiring for all four fields — create, PATCH, read, validation, sanitisation, history; Bean Validation bounds so springdoc emits them into the contract; **server-side HTML sanitisation on the write path** (§3.9 — the client's copy is advice, this one is the guarantee); one `FIELD_CHANGED` history row per changed field, same as any other. Module resolved against the master, inactive modules rejected on write but still readable on old tickets.
- [ ] **C-068** Create form S-19 — the new "Where it happened" group: module dropdown from `GET /masters/modules`, screen name, feature, steps in C-066's editor. **Built against D-060's mock, not against B-064** — DEPENDENCIES §6's first rule, and the reason the mock server exists at all; B-064 is what it integrates with later, not what it waits for. **Module required on Save & Assign for bug-type task types only**, waived on Save as Draft — same shape as the existing description rule, and the reasoning is in §7.5: forcing a module on a change request teaches people to pick the first item in the list.
- [ ] **C-069** Detail page S-20 shows all four, inline-editable — Module, Screen and Feature in the summary panel under Type; Steps to Generate below the description where the person about to reproduce it is already looking. Editable by the roles that may edit the description, each change writing its own `FIELD_CHANGED` row.
- [ ] **C-070** List S-17 gains a Module filter — and a Module column **off by default** in the column chooser (the grid is at its width budget). Filter state in the URL like every other C-014 filter, so a filtered grid stays a pasteable link.

### Detail page — S-20
- [x] **C-019** Detail shell + summary panel — every entity a link (assignee → profile, project → dashboard, client → 360 view, linked ticket, cycle → its effort logs). — *`frontend/src/features/tickets/detail/`, route `/tickets/:ticketId` (the `TicketDetailPlaceholder` C-006 landed on is deleted). One aggregated `GET /tickets/{id}/full` plus two masters lookups for the bare `taskTypeId` and `clientContactId` — nothing else on the page fetches, because the waterfall is what makes a detail page feel slow, not the payload size. **Cycle and tab live in the URL**, same rule C-014 set for the list; `?cycle=` re-fetches rather than filtering client-side, because an earlier cycle is a different sealed journey, not a subset of this one. **The cycle link carries `cycle` as well as `tab`** — setting only `tab=effort` would show the current cycle's hours under an earlier cycle's label, which is PLAN.md §3.4's cross-cycle contamination arriving through the front door; the mirror image is guarded too, with totals summed from `cycles[]` rather than from the payload's cycle-scoped `effortLogs`, which would make the grand total shrink when a reader selected cycle 1. Both have a test. The action bar renders **only** what `availableActions` allows: `handoff`/`rework`/`skip-stage` are ribbon-segment actions per §4A and are deliberately absent rather than duplicated, `Close` and `Reopen` render disabled with a title naming C-040/C-039 — hiding them would misreport permissions the server just granted — and Quick Update is live, reusing C-018's S-21 panel unchanged. Out-of-scope is **404 and only 404**: the test asserts the copy never contains "permission", "access", "not allowed" or "forbidden", so a copy edit cannot reintroduce the existence leak. Tab strip is hand-rolled to the APG pattern rather than adding `@radix-ui/react-tabs` for one screen; 30 tests; `tsc -b`, `eslint --max-warnings 0` and the full suite clean.*
  - ⚠ **Open for Stream D — `contracts/openapi.yaml`.** Two S-20 summary fields have nowhere to come from. **Linked tickets** is in this task's own backlog line and in the wireframe, and `Ticket` has no field for it — the same gap C-010 flagged on the create form, and C-064 needs it too; the row is **not rendered** rather than shown as a permanent em dash, because a dash reads as "no links" when the truth is "the API cannot say". **Assigned by** is likewise absent, so it is derived from the most recent history entry that changed `assigneeId` — exact for any reassignment, `null` for a ticket still with its original assignee, and it falls back to an em dash rather than guessing at `reportedBy`, which is a different person for anything the desk raises on a client's behalf.
  - ⚠ **Also Stream D — `frontend/src/mocks/`: watchers were accepted and dropped.** `watcherIds` has been on `TicketCreateRequest` and `TicketPatchRequest` since D-001 and `/full` returns `watchers`, but `POST /tickets` never stored the field and `/full` answered `watchers: []` unconditionally — so C-010's watcher picker looked wired end to end and silently was not. Fixed alongside, the same way C-015 fixed `dueFrom`/`dueTo`: `watcherIds` on the mock `Ticket`, kept on create, mapped through `userRef` in `/full`, and the walkthrough ticket seeded with Meera and Anil per the wireframe.
  - ⚠ **For every stream — the mock's richest ticket is invisible to the mock's default user.** `CRM-26-00347` is the §14 walkthrough fixture and this stream's exit criterion, and it is assigned to **Meera**; the default signed-in user is **Ravi**, a Developer, whose scope is `assigned_to = me`. Under `npm run dev` it 404s, and all three seeded notifications deep-linking to it land on "Ticket not found". That is the scope guard working correctly on data not built for it. Left alone — changing either the assignee or the default user moves ground under every other stream's tests — but worth knowing before anyone spends an hour on it.
  - **Two red tests on `develop`, both fixed here; neither is a regression from C-019** — verified by running the full suite with this branch stashed. `App.test.tsx`'s `/me` assertion was the only network-waiting `waitFor` in the repo still on its **default 1000 ms**; raised to 4000 to match every other such assertion.
  - ⚠ **Needs Stream D sign-off — `features/notifications/useNotificationStream.test.tsx`.** `Dismiss › closes the toast without marking it read` looks like the same timeout flake and **is not**, which is the part worth recording: raising its timeout to 4000 failed three runs out of three, because it waits longer for a toast that is *correctly* there. `resetDb()` re-seeds three undelivered `TICKET_HANDED_OFF` rows, so every `renderStream()` replays them as toasts (D-046) — and only the D-046 block drained that queue, in its own `beforeEach`. `Dismiss` dismisses the pushed toast and then asserts *nothing* is showing, which held only while the replay had not landed; under a full run's contention it lands inside the window and the leftover toast is a seeded row, so the failure reads as "dismiss did not close the toast" and points at an innocent handler. Drain moved to the file-level `beforeEach` — every block wants it, and D-046's tests still `queue()` afterwards. Full suite green three runs in a row. Changed without prior sign-off at Divyansh's direction; flagged here and in the PR body rather than done quietly.
- [ ] **C-020** Priority dropdown — colour chips, pre-filled from task type default, recomputes and previews PCD on change. **Mandatory reason once assigned.** Inline-editable from the summary panel. Writes `LEVEL_CHANGED`; **`original_level` never overwritten**. *(§4B.1)*
- [ ] **C-021** Client + client-contact dependent dropdowns, type-ahead over name/code/domain, filtered to clients mapped to the project, inline "+ Add contact". Auto-fills SLA, account manager as watcher, client timezone. *(§4B.2)*
- [ ] **C-022** Client-raised flag driving client-wise reports, CSAT and the client-visible default on comments.

### Attachments — §4B.4
- [x] **C-023** Upload surfaces: create form, detail, comment box, handoff dialog, quick update. — *`components/ui/{attachments.ts,attachment-picker.tsx}` + one story file, `features/tickets/attachments/`, and three surfaces wired. 116 tests. **The picker is presentational and does not upload** — nothing in `components/ui/` imports from `@/api`, and a picker that knew how to would pull the generated client into Stream A's and Stream B's bundles the first time either used it; `useTicketAttachments` is the half that fetches. It has two modes because the surfaces genuinely differ: detail and quick update have a ticket ID and upload the moment a file is chosen, while **the create form has none and `TicketCreateRequest` carries no `attachmentIds`**, so files stage and `flush(newTicketId)` runs after the 201. `flush` returns per-file outcomes rather than throwing — by then the ticket exists, and reporting "the ticket was not created" would send the user looking for one that does. Uploads are sequential, not `Promise.all`: the 50 MB and 20-file caps can only be enforced against a settled total. Validation carries running totals across a whole drop, because twelve 5 MB files are each under both caps and the twelve of them are 10 MB over the ticket's. `accept` is extensions, never MIME types — a `.log` reaches the browser with an empty `type` on every platform, and greying it out blocks the file support agents attach most; drag-and-drop bypasses `accept` anyway, which is why the guard is code. Two of the five listed surfaces — **the comment box (C-029) and the handoff dialog (C-052) — do not exist yet**; both already carry `attachmentIds` on the wire and the `compact` variant was built for §4B.5's inline `[📎]`, so they adopt it unchanged. `tsc -b`, `eslint --max-warnings 0` and the full `features/tickets` suite clean.*
  - ⚠ 🔴 **For Stream D — the generated `uploadAttachment` cannot work against a real server.** Orval emits `headers: { 'Content-Type': 'multipart/form-data' }`, and `api/http.ts` spreads caller headers **after** the branch that deliberately omits a content type for a `FormData` body — so the generated header wins, no `boundary` is ever generated, and a real Spring `@RequestPart` returns 400 or 500 for **every** upload. Stream D's own `api/http.test.ts` already asserts the intent ("does not set Content-Type on FormData — the browser must add the boundary") against a mocked fetch, so the header silently defeats a documented guarantee. The fix is one line in `api/http.ts`, which is Stream D's file (D-003); `api/generated/` cannot be the fix because it is generated. `features/tickets/attachments/uploadTicketAttachment.ts` sends the same request without the header, imports its types from the generated code, and **should be deleted in favour of `useUploadAttachment` the day `http.ts` is fixed.**
  - ⚠ **For Stream D / shared test infrastructure — no multipart upload can be exercised under vitest.** jsdom supplies `FormData`/`Blob`/`File`, Node supplies `fetch`/`Request`, and Node refuses to serialise a foreign `FormData` — every upload leaves as the literal `[object FormData]` under `Content-Type: text/plain`, and any handler calling `request.formData()` dies inside undici pointing at the request builder. It is not bridgeable from a test file: undici brand-checks its own `Blob`/`File` and rejects both `node:buffer`'s and the one off `Response.blob()`. It belongs in `test/setup.ts`, which already carries `makeAbortSignalsCrossRealmSafe` for the identical jsdom-versus-Node realm problem. **Consequence: the multipart round trip is unverified in CI** — the hook test overrides the handler and mirrors its contract, and `uploadTicketAttachment.test.ts` pins the header behaviour against a mocked fetch. Stream B's import wizard has the same problem waiting for it.
  - ⚠ **For Stream D — `contracts/openapi.yaml` and the mock.** `TicketCreateRequest` has no `attachmentIds` where `QuickUpdateRequest`, `HandoffRequest` and `CommentWriteRequest` all do, so a create-with-attachments is two round trips and is not atomic. There is no settings endpoint for §4B.4's "all configurable" limits, so the defaults live in `attachments.ts`. The mock enforces the 10 MB per-file cap and nothing else — no 415, no per-ticket 50 MB or 20-file cap, `commentId` accepted and ignored, and `commentDto` hard-codes `attachments: []` so a comment's files will not round-trip for C-029. `attachmentDto` also builds `/mock-files/…` URLs no handler serves; nothing renders them today, but C-026's gallery will 404 on every image.
  - ⚠ **For Stream A and the blueprint — §4B.4 gives mp4 two different size limits.** The type row allows "video (mp4, up to 50 MB)"; the limits row says "10 MB per file", and the contract and mock both enforce a flat 10 MB. The client mirrors the API at 10 MB — allowing 50 MB would let a user watch a 40 MB upload run and then be refused by the server. If 50 MB for video is meant, the contract and the server have to say so first.
  - Changed outside this folder, minimally and by design: `quickUpdateForm.ts`'s `toQuickUpdateRequest` gained an **optional** third parameter (`attachmentIds`, omitted when empty, same send-if-touched rule as every other field), so its 2-argument call sites and tests are untouched; `CreateTicketPage.tsx` lost its `Attachments · C-023` placeholder chip; `TicketDetailPage.tsx` gained one child component. The detail strip lives in `detail/TicketAttachmentsSection.tsx` rather than inline because the page early-returns for its loading and 404 states and a hook cannot be called after a conditional return. **The picker's capacity line is suppressed when it is disabled** — on a sealed cycle "1 of 20 files · 50 MB" describes a budget the reader cannot spend, and it also stops a second `role="status"` landing on a page whose sealed-cycle banner is already one.
- [x] **C-024** 🔴 **Clipboard paste** alongside drag-drop and file picker. The blueprint is right that paste decides whether support agents actually use it. — *`components/ui/{attachments.ts,use-attachment-paste.ts,attachment-picker.tsx}` + one story, and the create form's description editor wired. 50 new tests. **Every browser hands a Snipping Tool capture over as `image.png`** — Safari has used `Image (1).png`, some paths supply no name at all — so C-023's duplicate check, which is right about everything else, refuses the *second* capture a user pastes: paste works once and then appears broken, which is the hardest kind of failure to report. Clipboard-invented names are renamed `screenshot-2026-08-12-143005.png` from the local clock, **disambiguated against what is already attached** (`-2`, `-3`, …) — which is why naming lives in the picker and not in the paste listener, since only the picker knows what is on the ticket. The OS clipboard holds one image, so attaching several screenshots is necessarily *paste, paste, paste*, and a user doing that beats a one-second stamp; a collision was previously refused by the duplicate check, so the second capture vanished behind "already attached" and paste appeared to work once and then break. **A clipboard name must never cause a rejection.** The cost is that a genuine double `Ctrl+V` attaches the same image twice instead of being refused — the right side to err on, because a duplicate is visible and one click to remove while a silently dropped screenshot is indistinguishable from a broken feature. Real names — a drop, a browse, a file-manager copy — are untouched and keep the duplicate check, where a repeated name means something. Several images in one paste (a multi-select, a run of spreadsheet cells) fall out of the same mechanism rather than a special case. The rename allocates a new `File` over the same blob rather than shadowing `name`, because **Chrome's `FormData` reads a file's name from an internal slot, not the JS property** — a shadowed name would upload under the old one, working everywhere except where it matters. Extensions are derived from the MIME type and never defaulted: guessing `png` for an unknown type would name a file something it is not and C-025's sniffing would then refuse what this side had just accepted, so `image/tiff` keeps `.tiff` and falls off the allow-list honestly. **The listener is on `document`** — a paste fires at whatever has focus and nothing ever focuses a drop zone — which makes it global, so registrations are a stack and only the top handles the event: ticket detail's picker and the quick update slide-over over it would otherwise upload one paste twice, against two different requests. It stands down for a rich-text editor (C-066 intercepts paste itself and the event bubbles here afterwards) and for an ordinary text paste into an `input`/`textarea`, since copying an image from a web page brings an `<img>` tag along as `text/html` and a file's presence is not evidence the user meant to attach it — but it **does** take an image-only clipboard from a text field, because pasting a screenshot into a plain input otherwise does nothing at all. A paste runs through the same `accept` as a drop, so no route reaches `onAdd` having skipped the caps, and the editor's files get there through the picker's new `addFiles` ref handle for that reason. Announced in the live region, which is now mounted whenever the control is live rather than appearing with its first message — a paste has no visible action behind it, and several screen readers only watch regions already in the tree; still absent while disabled, so a sealed cycle does not gain a second `role="status"`. `tsc -b`, `eslint --max-warnings 0`, `npm run build`, `storybook build` and the full 633-test suite all clean. All three guards mutation-checked — disabling the contentEditable skip, the rename, or the collision step each turns tests red, including the page-level ones.*
  - ⚠ **For everyone — an orphaned dev server was the flakiness, not the suite.** Chasing intermittent failures here turned up a `vite` process from **12 Aug on port 5174**, still running two days later and spinning through an hour of CPU on a 4-core machine. With it alive the full run took 301 s and failed 7 tests; with it and the live dev server stopped, 121 s and 633 passed. The failing set was different every run and included suites this branch never touches (`working week`, `resource leave`, `S-17 Ticket List`), which is the tell. Worth checking for a stray `vite` before believing a red run — it costs a round trip to blame the wrong change, and this machine is the one the C-023 note already flagged as at its limit.
  - **No contract change and no mock change** — the first ticket task in a while that needed neither. Paste produces `File[]` and hands them to C-023's existing upload path; nothing new goes on the wire.
  - ⚠ **For Stream D — this unblocks D-053's file/image share.** [`overrides.json`](../plan/overrides.json) records it as blocked on C-024 and C-025 with the note that a second upload pipeline in chat would put two answers next to each other for "is this file safe". `useAttachmentPaste` and the clipboard rules in `attachments.ts` are in `components/ui/` and take no ticket concepts, so chat's composer can adopt both unchanged — the remaining half is C-025's storage and scanning, and the `ticket_attachments.ticket_id NOT NULL` table shape still needs Divyansh's sign-off before a DM can hold a row at all.
  - ⚠ **`AttachmentPicker` is now a `forwardRef` and takes two new props** (`enablePaste`, plus the `addFiles` handle). Additive — every existing call site renders identically, and paste is on by default because §4B.4's adoption argument is the whole point of the task. Storybook story `ClipboardPaste` is the contract; it needs a real browser to mean anything, which Storybook is.
  - ⚠ **A test trap worth knowing before it costs someone an hour.** The rename reconstructs the `File`, and jsdom computes `size` from content — so a fixture using `Object.defineProperty(file, 'size', …)`, which is how every attachment test in this codebase builds a large file, silently becomes one byte once it is renamed. A paste test asserting the 10 MB cap against an oversized *screenshot* passes for the wrong reason. Nothing is wrong in a browser, where the blob really is that long. Both the test file and the folder README say so at the point of use.
- [ ] **C-025** Security — extension allow-list **and** MIME sniffing, AV scan before the file becomes visible, EXIF stripped, S3/MinIO keys `tickets/{id}/{uuid}`, short-lived signed URLs, never a public bucket. The allow-list is §4B.4's, which as of revision 1.3 includes the legacy binary Office formats (`.doc`, `.xls`) because clients still send them — and which is precisely why the extension alone is never the test.
- [ ] **C-026** Thumbnails, gallery strip, lightbox with zoom and next/previous.
- [ ] **C-027** Limits — 10 MB/file, 50 MB/ticket, 20 files/ticket, all configurable.
- [ ] **C-028** Delete within 15 minutes by the uploader; after that a soft delete leaving a tombstone. `is_client_visible` flag per attachment.

### Comments — §4B.5
- [ ] **C-029** Rich-text comment box under the description, always visible above the tabs. Ctrl/Cmd+Enter to post.
- [ ] **C-030** `@mention` type-ahead over project members, firing notification + email.
- [ ] **C-031** Visibility toggle — **default internal, always.** Client-visible shown in a different colour before posting.
- [ ] **C-032** Stamping — author, role, **stage and iteration at time of writing**, timestamp.
- [ ] **C-033** 5-minute edit window, then locked with an "edited" marker and the original preserved. Deletion leaves a tombstone. **No role, including Admin, can silently rewrite a comment.**
- [ ] **C-034** 🔴 **Interleave comments into the History tab** — field changes, handoffs, attachments and comments in one chronological stream, not two lists to reconcile.

### Effort & quick update
- [ ] **C-035** Effort logging, append-only, **auto-stamped with current stage and iteration** so it lands in the right journey row with no user action.
- [ ] **C-036** Quick Update slide-over — status, effort + date, work note, % complete, revised ETA with reason, attach. Two clicks, no reload, optimistic UI, closes with a toast. **S-21**
  - ⚠ **Built ahead of schedule as part of C-018**, which needed the panel for its own "inline Quick Update on every row" — `frontend/src/features/tickets/quick-update/`. Status, effort + date, work note, % complete and revised ETA-with-reason are all there, closes with a toast, two clicks. Two things remain open: **📎 Attach** (needs C-023's attachment picker, which doesn't exist yet — see the `my-tasks/README.md` note) and **optimistic UI** (deliberately skipped — every other mutation in this codebase, create included, waits for the server round trip rather than patching the cache by hand; revisit only if this specific panel's latency turns out to matter more than the others).
- [x] **C-037** Quick Update must **not** expose: ticket ID, reported by, assigned by, date reported, cycle history, the ribbon, prior effort logs, level (unless PM), project.

### Cycles & closure
- [ ] **C-038** 🔴 **Reopen transaction** — seal cycle N (`is_sealed`), insert cycle N+1 with fresh start/PCD/assignee, `reopen_count++`, clear `actual_close_date`, write history. All in one transaction. **Cycle 1's effort is never touched.** *(§4.1)*
- [ ] **C-039** Reopen dialog — mandatory reason, restart stage (defaults Triage), new assignee, new PCD, revised estimate. Warning banner that cycle N and its ribbon will be sealed. **S-22**
- [ ] **C-040** Close/resolve dialog — resolution summary, root cause category, actual close date, final effort confirmation, optional client verification request. **S-23**
- [ ] **C-041** Materialised `total_effort_hrs`, refreshed on every effort insert. Per-cycle and grand totals.

### The Workflow Ribbon — §4A
- [ ] **C-042** Transition service — writes append-only `ticket_stage_transitions` rows. Action codes: `FORWARD`, `REWORK`, `DEPLOY_FAILED`, `VERIFY_FAILED`, `SIGNOFF_REJECTED`, `CLARIFICATION`, `SKIP`, `OVERRIDE`.
- [ ] **C-043** 🔴 **The golden rule** — only the *current stage owner* (plus PM and Admin) may advance a ticket. A Developer cannot push a ticket into Deployment while it sits with QA. Enforced server-side.
- [ ] **C-044** Handoff dialog — next stage (pre-filled from template), assign-to filtered to the receiving role's project members **with current open load shown**, handoff note, **mandatory effort confirmation for the stage being left**, attachments. **S-29**
- [ ] **C-045** On submit: seal the current row (`exited_at`, `duration_mins` in working minutes), insert the next, notify the receiving owner, advance the ribbon live over WebSocket. *Needs D's STOMP topics.*
- [ ] **C-046** Backward moves — reason mandatory, defect list on QA fail, `iteration_no` increments for every subsequent row in that cycle.
- [ ] **C-047** Skip a stage — PM/Admin only, reason mandatory, segment renders struck-through with the reason on hover.
- [ ] **C-048** Force-move (`OVERRIDE`) — PM/Admin, logged as an override.
- [ ] **C-049** Reassignment *within* a stage does **not** create a new segment — writes `STAGE_REASSIGNED` and splits effort attribution so both resources appear in the roll-up.
- [ ] **C-050** Unassigned receiving role → ticket falls to a project-level queue, PM alerted after 2 hours.
- [ ] **C-051** 🔴 **Ribbon component** — 6 segment states, each showing stage name + icon, owner avatar + name, time in stage, effort logged, loop-back badge.
- [ ] **C-052** Interactions — click a segment filters History/Effort/Chat below to that stage and iteration; hover tooltip with entered/exited/owner/note/effort/idle-vs-active; current segment carries the inline contextual action button, hidden for everyone else.
- [ ] **C-053** Cycle selector above the ribbon; selecting cycle 1 renders that cycle's completed journey read-only. **Each cycle has its own ribbon.**
- [ ] **C-054** `Cycle 2 · Iteration 3` chips — two independent counters, easy to get wrong. Iteration = pushed backwards within a cycle; cycle = reopened after closure.

### Journey tab — §4A.4
- [ ] **C-055** Roll-up grid — iteration, stage, resource, role, in, out, duration, effort, per hop.
- [ ] **C-056** 🔴 **Active vs idle split** — `idle = duration − Σ effort in that stage+iteration`. A stage with 2 days duration and 2 hours effort is a queue problem, not a capacity problem. This single insight justifies the whole feature.
- [ ] **C-057** Per-resource roll-up + cycle total + all-cycles total.
- [ ] **C-058** Roll-up query — **must join effort logs on `cycle_no` as well as stage and iteration**, or cycle 1's effort double-counts into cycle 2 after a reopen. *(PLAN.md §3.4 — a defect in the blueprint's own query.)*

### Remaining tabs & screens
- [ ] **C-059** History tab — cycle-grouped, expandable to every field change and handoff. **No edit or delete icon exists for anyone**, and the API rejects it even if the DOM is manipulated.
- [ ] **C-060** Attachments tab — gallery, filterable by client-visible, grouped by cycle and stage.
- [ ] **C-061** Effort tab — every log line, sum per cycle + grand total.
- [ ] **C-062** Stage Queue / team inbox — "Waiting in QA", "Waiting in Deployment", sorted by time-in-stage descending. The landing page for QA and Deployment. **S-31**
- [ ] **C-063** Bulk reassignment wizard — source resource → tickets → target → reason → confirm. Each move writes its own history entry. **S-24**
- [ ] **C-064** Ticket linking — blocks / is blocked by / duplicate of / relates to.

**Exit:** blueprint §14 walkthrough A runs end to end — 8 stages, a QA rework, a reopen into cycle 2, and a Journey grid reconciling to 38.0 h across 5 resources and 3 iterations.

---

## Decisions you own

Answer before M4 (PLAN.md §5):

- **G-1** Is effort mandatory at handoff — blocking or warn-only? *(Recommended: blocking. Without it the per-resource roll-up is fiction within a month.)*
- **G-2** Does a rework loop reset the Planned Close Date? *(Recommended: no. The original date stands; rework is what `iteration_no` measures.)*
- **G-3** Can a Developer close a ticket, or only mark Resolved? *(Recommended: Resolved only — closure belongs to the Sign-off stage owner.)*
- Can a ticket skip QA, and who authorises it? *(Recommended: PM/Admin only, reason mandatory, never for Production Bug.)*
- Should comments default to internal or client-visible? *(Recommended: internal, always — an accidental leak costs far more than an extra click.)*
