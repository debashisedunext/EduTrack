/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * EduTrack API
 * The agreed contract between all four streams. Every endpoint in blueprint §13.

**This file is hand-authored and under review (D-001).** It is the design
contract, not yet the generated one. From D-003 onwards `springdoc` emits the
real spec from the Java DTOs — Bean Validation annotations are the single
source of truth for validation rules — and D-005's CI check fails the build
when the committed TypeScript client drifts from it. Until then this file is
what the frontend generates against and what the backend must implement.

## Conventions that apply to every operation

- **Prefix** — every path is served under `/api/v1`.
- **Envelope** — success responses are `{ data, meta? }`. Errors are
  `{ error }` carrying an RFC 9457 problem object. Nothing returns a bare
  array or a bare scalar; adding a field later must never be a breaking change.
- **Errors** — `application/problem+json`, RFC 9457. `type` is a stable URI
  the frontend may switch on; `detail` is for humans and may change.
- **Idempotency** — every POST that creates something accepts
  `Idempotency-Key`. Replaying a key within 24 h returns the original response
  rather than creating a second row.
- **ETag** — detail reads return `ETag`; send `If-None-Match` to get `304`,
  and `If-Match` on the corresponding `PATCH` to avoid a lost update.
- **Pagination** — `?cursor=&limit=`, never offset. Offset paging over a table
  that is being written to skips and repeats rows.

`contracts/CONVENTIONS.md` is the normative version (D-002), including the
endpoints that deliberately opt out of `If-Match` or pagination and why.

## Two rules the API shape itself enforces

**Row scoping returns 404, not 403.** Out-of-scope IDs are indistinguishable
from non-existent ones. A `403` on `/tickets/{id}` would confirm the ticket
exists, which is an existence leak. Scoping is applied server-side by
`ScopeResolver` on every ticket query — never by a frontend filter.

**History and effort logs are append-only.** `GET` and `POST` exist;
`PUT`, `PATCH` and `DELETE` deliberately do not, on `/history`,
`/effort-logs`, and stage transitions. This is not an oversight to be fixed
later — the routes must never be added. A correction is a new compensating
entry (`isCorrection`, `correctsEntryId`), like an accounting reversal, and
the database rejects mutation independently via triggers and grants.

 * OpenAPI spec version: 1.0.0-draft
 */
import {
  useMutation,
  useQuery
} from '@tanstack/react-query';
import type {
  DataTag,
  DefinedInitialDataOptions,
  DefinedUseQueryResult,
  MutationFunction,
  QueryClient,
  QueryFunction,
  QueryKey,
  UndefinedInitialDataOptions,
  UseMutationOptions,
  UseMutationResult,
  UseQueryOptions,
  UseQueryResult
} from '@tanstack/react-query';

import type {
  CommentListResponse,
  CommentResponse,
  CommentWriteRequest,
  EditCommentBody,
  ListCommentsParams,
  NotFoundResponse,
  Problem,
  ValidationFailedResponse
} from '.././model';

import { http } from '../../http';




/**
 * @summary List comments
 */
export const listComments = (
    ticketId: string,
    params?: ListCommentsParams,
 signal?: AbortSignal
) => {
      
      
      return http<CommentListResponse>(
      {url: `/tickets/${ticketId}/comments`, method: 'GET',
        params, signal
    },
      );
    }
  



export const getListCommentsQueryKey = (ticketId?: string,
    params?: ListCommentsParams,) => {
    return [
    `/tickets/${ticketId}/comments`, ...(params ? [params]: [])
    ] as const;
    }

    
export const getListCommentsQueryOptions = <TData = Awaited<ReturnType<typeof listComments>>, TError = NotFoundResponse>(ticketId: string,
    params?: ListCommentsParams, options?: { query?:Partial<UseQueryOptions<Awaited<ReturnType<typeof listComments>>, TError, TData>>, }
) => {

const {query: queryOptions} = options ?? {};

  const queryKey =  queryOptions?.queryKey ?? getListCommentsQueryKey(ticketId,params);

  

    const queryFn: QueryFunction<Awaited<ReturnType<typeof listComments>>> = ({ signal }) => listComments(ticketId,params, signal);

      

      

   return  { queryKey, queryFn, enabled: !!(ticketId), ...queryOptions} as UseQueryOptions<Awaited<ReturnType<typeof listComments>>, TError, TData> & { queryKey: DataTag<QueryKey, TData, TError> }
}

export type ListCommentsQueryResult = NonNullable<Awaited<ReturnType<typeof listComments>>>
export type ListCommentsQueryError = NotFoundResponse


export function useListComments<TData = Awaited<ReturnType<typeof listComments>>, TError = NotFoundResponse>(
 ticketId: string,
    params: undefined |  ListCommentsParams, options: { query:Partial<UseQueryOptions<Awaited<ReturnType<typeof listComments>>, TError, TData>> & Pick<
        DefinedInitialDataOptions<
          Awaited<ReturnType<typeof listComments>>,
          TError,
          Awaited<ReturnType<typeof listComments>>
        > , 'initialData'
      >, }
 , queryClient?: QueryClient
  ):  DefinedUseQueryResult<TData, TError> & { queryKey: DataTag<QueryKey, TData, TError> }
export function useListComments<TData = Awaited<ReturnType<typeof listComments>>, TError = NotFoundResponse>(
 ticketId: string,
    params?: ListCommentsParams, options?: { query?:Partial<UseQueryOptions<Awaited<ReturnType<typeof listComments>>, TError, TData>> & Pick<
        UndefinedInitialDataOptions<
          Awaited<ReturnType<typeof listComments>>,
          TError,
          Awaited<ReturnType<typeof listComments>>
        > , 'initialData'
      >, }
 , queryClient?: QueryClient
  ):  UseQueryResult<TData, TError> & { queryKey: DataTag<QueryKey, TData, TError> }
export function useListComments<TData = Awaited<ReturnType<typeof listComments>>, TError = NotFoundResponse>(
 ticketId: string,
    params?: ListCommentsParams, options?: { query?:Partial<UseQueryOptions<Awaited<ReturnType<typeof listComments>>, TError, TData>>, }
 , queryClient?: QueryClient
  ):  UseQueryResult<TData, TError> & { queryKey: DataTag<QueryKey, TData, TError> }
/**
 * @summary List comments
 */

export function useListComments<TData = Awaited<ReturnType<typeof listComments>>, TError = NotFoundResponse>(
 ticketId: string,
    params?: ListCommentsParams, options?: { query?:Partial<UseQueryOptions<Awaited<ReturnType<typeof listComments>>, TError, TData>>, }
 , queryClient?: QueryClient 
 ):  UseQueryResult<TData, TError> & { queryKey: DataTag<QueryKey, TData, TError> } {

  const queryOptions = getListCommentsQueryOptions(ticketId,params,options)

  const query = useQuery(queryOptions, queryClient) as  UseQueryResult<TData, TError> & { queryKey: DataTag<QueryKey, TData, TError> };

  query.queryKey = queryOptions.queryKey ;

  return query;
}




/**
 * **`isClientVisible` defaults to false, always.** An accidental leak to a
client costs far more than an extra click (decision recorded in §16).

Stamped server-side with the author's role and the stage and iteration at
time of writing, so the comment stays meaningful when read months later.
`@mentions` fire a notification and an email.

 * @summary Post a comment
 */
export const createComment = (
    ticketId: string,
    commentWriteRequest: CommentWriteRequest,
 signal?: AbortSignal
) => {
      
      
      return http<CommentResponse>(
      {url: `/tickets/${ticketId}/comments`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: commentWriteRequest, signal
    },
      );
    }
  


export const getCreateCommentMutationOptions = <TError = ValidationFailedResponse | NotFoundResponse,
    TContext = unknown>(options?: { mutation?:UseMutationOptions<Awaited<ReturnType<typeof createComment>>, TError,{ticketId: string;data: CommentWriteRequest}, TContext>, }
): UseMutationOptions<Awaited<ReturnType<typeof createComment>>, TError,{ticketId: string;data: CommentWriteRequest}, TContext> => {

const mutationKey = ['createComment'];
const {mutation: mutationOptions} = options ?
      options.mutation && 'mutationKey' in options.mutation && options.mutation.mutationKey ?
      options
      : {...options, mutation: {...options.mutation, mutationKey}}
      : {mutation: { mutationKey, }};

      


      const mutationFn: MutationFunction<Awaited<ReturnType<typeof createComment>>, {ticketId: string;data: CommentWriteRequest}> = (props) => {
          const {ticketId,data} = props ?? {};

          return  createComment(ticketId,data,)
        }

        


  return  { mutationFn, ...mutationOptions }}

    export type CreateCommentMutationResult = NonNullable<Awaited<ReturnType<typeof createComment>>>
    export type CreateCommentMutationBody = CommentWriteRequest
    export type CreateCommentMutationError = ValidationFailedResponse | NotFoundResponse

    /**
 * @summary Post a comment
 */
export const useCreateComment = <TError = ValidationFailedResponse | NotFoundResponse,
    TContext = unknown>(options?: { mutation?:UseMutationOptions<Awaited<ReturnType<typeof createComment>>, TError,{ticketId: string;data: CommentWriteRequest}, TContext>, }
 , queryClient?: QueryClient): UseMutationResult<
        Awaited<ReturnType<typeof createComment>>,
        TError,
        {ticketId: string;data: CommentWriteRequest},
        TContext
      > => {

      const mutationOptions = getCreateCommentMutationOptions(options);

      return useMutation(mutationOptions, queryClient);
    }
    /**
 * Author only, within five minutes of posting. After that the comment is
locked and this returns `422`.

An edited comment keeps `originalBody` and renders an "edited" marker.
**No role, including Admin, can silently rewrite a comment** — that is what
keeps the thread admissible as evidence.

 * @summary Edit within the five-minute window
 */
export const editComment = (
    ticketId: string,
    commentId: number,
    editCommentBody: EditCommentBody,
 ) => {
      
      
      return http<CommentResponse>(
      {url: `/tickets/${ticketId}/comments/${commentId}`, method: 'PATCH',
      headers: {'Content-Type': 'application/json', },
      data: editCommentBody
    },
      );
    }
  


export const getEditCommentMutationOptions = <TError = NotFoundResponse | Problem,
    TContext = unknown>(options?: { mutation?:UseMutationOptions<Awaited<ReturnType<typeof editComment>>, TError,{ticketId: string;commentId: number;data: EditCommentBody}, TContext>, }
): UseMutationOptions<Awaited<ReturnType<typeof editComment>>, TError,{ticketId: string;commentId: number;data: EditCommentBody}, TContext> => {

const mutationKey = ['editComment'];
const {mutation: mutationOptions} = options ?
      options.mutation && 'mutationKey' in options.mutation && options.mutation.mutationKey ?
      options
      : {...options, mutation: {...options.mutation, mutationKey}}
      : {mutation: { mutationKey, }};

      


      const mutationFn: MutationFunction<Awaited<ReturnType<typeof editComment>>, {ticketId: string;commentId: number;data: EditCommentBody}> = (props) => {
          const {ticketId,commentId,data} = props ?? {};

          return  editComment(ticketId,commentId,data,)
        }

        


  return  { mutationFn, ...mutationOptions }}

    export type EditCommentMutationResult = NonNullable<Awaited<ReturnType<typeof editComment>>>
    export type EditCommentMutationBody = EditCommentBody
    export type EditCommentMutationError = NotFoundResponse | Problem

    /**
 * @summary Edit within the five-minute window
 */
export const useEditComment = <TError = NotFoundResponse | Problem,
    TContext = unknown>(options?: { mutation?:UseMutationOptions<Awaited<ReturnType<typeof editComment>>, TError,{ticketId: string;commentId: number;data: EditCommentBody}, TContext>, }
 , queryClient?: QueryClient): UseMutationResult<
        Awaited<ReturnType<typeof editComment>>,
        TError,
        {ticketId: string;commentId: number;data: EditCommentBody},
        TContext
      > => {

      const mutationOptions = getEditCommentMutationOptions(options);

      return useMutation(mutationOptions, queryClient);
    }
    /**
 * The row survives with `isDeleted`; the body is cleared. Nothing vanishes.
 * @summary Delete a comment, leaving a tombstone
 */
export const deleteComment = (
    ticketId: string,
    commentId: number,
 ) => {
      
      
      return http<void>(
      {url: `/tickets/${ticketId}/comments/${commentId}`, method: 'DELETE'
    },
      );
    }
  


export const getDeleteCommentMutationOptions = <TError = NotFoundResponse,
    TContext = unknown>(options?: { mutation?:UseMutationOptions<Awaited<ReturnType<typeof deleteComment>>, TError,{ticketId: string;commentId: number}, TContext>, }
): UseMutationOptions<Awaited<ReturnType<typeof deleteComment>>, TError,{ticketId: string;commentId: number}, TContext> => {

const mutationKey = ['deleteComment'];
const {mutation: mutationOptions} = options ?
      options.mutation && 'mutationKey' in options.mutation && options.mutation.mutationKey ?
      options
      : {...options, mutation: {...options.mutation, mutationKey}}
      : {mutation: { mutationKey, }};

      


      const mutationFn: MutationFunction<Awaited<ReturnType<typeof deleteComment>>, {ticketId: string;commentId: number}> = (props) => {
          const {ticketId,commentId} = props ?? {};

          return  deleteComment(ticketId,commentId,)
        }

        


  return  { mutationFn, ...mutationOptions }}

    export type DeleteCommentMutationResult = NonNullable<Awaited<ReturnType<typeof deleteComment>>>
    
    export type DeleteCommentMutationError = NotFoundResponse

    /**
 * @summary Delete a comment, leaving a tombstone
 */
export const useDeleteComment = <TError = NotFoundResponse,
    TContext = unknown>(options?: { mutation?:UseMutationOptions<Awaited<ReturnType<typeof deleteComment>>, TError,{ticketId: string;commentId: number}, TContext>, }
 , queryClient?: QueryClient): UseMutationResult<
        Awaited<ReturnType<typeof deleteComment>>,
        TError,
        {ticketId: string;commentId: number},
        TContext
      > => {

      const mutationOptions = getDeleteCommentMutationOptions(options);

      return useMutation(mutationOptions, queryClient);
    }
    