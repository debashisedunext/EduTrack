/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * EduTrack API
 * The agreed contract between all four streams. Every endpoint in blueprint §13.

**This file is hand-authored and under review (D-001).** It is the design
contract, not yet the generated one. From D-003 onwards `springdoc` emits the
real spec from the Java DTOs — Bean Validation annotations are the single
source of truth for validation rules — and D-005's CI check fails the build
when the committed TypeScript client drifts from it. Until then this file is
what the frontend generates against and what the backend must implement.

## Conventions that apply to every operation

- **Prefix** — every path is served under `/api/v1`.
- **Envelope** — success responses are `{ data, meta? }`. Errors are
  `{ error }` carrying an RFC 9457 problem object. Nothing returns a bare
  array or a bare scalar; adding a field later must never be a breaking change.
- **Errors** — `application/problem+json`, RFC 9457. `type` is a stable URI
  the frontend may switch on; `detail` is for humans and may change.
- **Idempotency** — every POST that creates something accepts
  `Idempotency-Key`. Replaying a key within 24 h returns the original response
  rather than creating a second row.
- **ETag** — detail reads return `ETag`; send `If-None-Match` to get `304`,
  and `If-Match` on the corresponding `PATCH` to avoid a lost update.
- **Pagination** — `?cursor=&limit=`, never offset. Offset paging over a table
  that is being written to skips and repeats rows.

`contracts/CONVENTIONS.md` is the normative version (D-002), including the
endpoints that deliberately opt out of `If-Match` or pagination and why.

## Two rules the API shape itself enforces

**Row scoping returns 404, not 403.** Out-of-scope IDs are indistinguishable
from non-existent ones. A `403` on `/tickets/{id}` would confirm the ticket
exists, which is an existence leak. Scoping is applied server-side by
`ScopeResolver` on every ticket query — never by a frontend filter.

**History and effort logs are append-only.** `GET` and `POST` exist;
`PUT`, `PATCH` and `DELETE` deliberately do not, on `/history`,
`/effort-logs`, and stage transitions. This is not an oversight to be fixed
later — the routes must never be added. A correction is a new compensating
entry (`isCorrection`, `correctsEntryId`), like an accounting reversal, and
the database rejects mutation independently via triggers and grants.

 * OpenAPI spec version: 1.0.0-draft
 */
import {
  useMutation,
  useQuery
} from '@tanstack/react-query';
import type {
  DataTag,
  DefinedInitialDataOptions,
  DefinedUseQueryResult,
  MutationFunction,
  QueryClient,
  QueryFunction,
  QueryKey,
  UndefinedInitialDataOptions,
  UseMutationOptions,
  UseMutationResult,
  UseQueryOptions,
  UseQueryResult
} from '@tanstack/react-query';

import type {
  ListObNotificationsParams,
  ObModuleGatedResponse,
  ObNotificationListResponse,
  Problem,
  UnauthorizedResponse
} from '.././model';

import { http } from '../../http';




/**
 * **The bell popover and the full page are the same endpoint.** "The last
eight" is a `limit`, not a second route — two would mean two places to
keep the tab filter and the badge honest.

**This is not `GET /notifications`.** The two centres share neither a
store, a tab vocabulary nor an event catalogue: onboarding entries live
in `ob_notifications`, are keyed on the module's own
`ObNotificationEvent`, and group under All / Assignments / Escalations /
Reminders. A `module` parameter on S-26's route would have been the only
thing the two had in common, and would have had to fan out to two
tables underneath. Plan §1.2's separability, followed through.

**Staff only, by construction.** An entry is addressed to a `users` row.
§9's portal screens have no notification centre, so an `IN_APP` message
queued for a client contact is refused at delivery with a reason rather
than written where nobody can read it.

Entries are ordered by id, never `created_at`: one gate opening
notifies four owners in the same microsecond, and a cursor over a
non-unique key either repeats a row or skips one.

 * @summary Onboarding notification centre (OB-13)
 */
export const listObNotifications = (
    params?: ListObNotificationsParams,
 signal?: AbortSignal
) => {
      
      
      return http<ObNotificationListResponse>(
      {url: `/onboarding/notifications`, method: 'GET',
        params, signal
    },
      );
    }
  



export const getListObNotificationsQueryKey = (params?: ListObNotificationsParams,) => {
    return [
    `/onboarding/notifications`, ...(params ? [params]: [])
    ] as const;
    }

    
export const getListObNotificationsQueryOptions = <TData = Awaited<ReturnType<typeof listObNotifications>>, TError = Problem | UnauthorizedResponse | ObModuleGatedResponse>(params?: ListObNotificationsParams, options?: { query?:Partial<UseQueryOptions<Awaited<ReturnType<typeof listObNotifications>>, TError, TData>>, }
) => {

const {query: queryOptions} = options ?? {};

  const queryKey =  queryOptions?.queryKey ?? getListObNotificationsQueryKey(params);

  

    const queryFn: QueryFunction<Awaited<ReturnType<typeof listObNotifications>>> = ({ signal }) => listObNotifications(params, signal);

      

      

   return  { queryKey, queryFn, ...queryOptions} as UseQueryOptions<Awaited<ReturnType<typeof listObNotifications>>, TError, TData> & { queryKey: DataTag<QueryKey, TData, TError> }
}

export type ListObNotificationsQueryResult = NonNullable<Awaited<ReturnType<typeof listObNotifications>>>
export type ListObNotificationsQueryError = Problem | UnauthorizedResponse | ObModuleGatedResponse


export function useListObNotifications<TData = Awaited<ReturnType<typeof listObNotifications>>, TError = Problem | UnauthorizedResponse | ObModuleGatedResponse>(
 params: undefined |  ListObNotificationsParams, options: { query:Partial<UseQueryOptions<Awaited<ReturnType<typeof listObNotifications>>, TError, TData>> & Pick<
        DefinedInitialDataOptions<
          Awaited<ReturnType<typeof listObNotifications>>,
          TError,
          Awaited<ReturnType<typeof listObNotifications>>
        > , 'initialData'
      >, }
 , queryClient?: QueryClient
  ):  DefinedUseQueryResult<TData, TError> & { queryKey: DataTag<QueryKey, TData, TError> }
export function useListObNotifications<TData = Awaited<ReturnType<typeof listObNotifications>>, TError = Problem | UnauthorizedResponse | ObModuleGatedResponse>(
 params?: ListObNotificationsParams, options?: { query?:Partial<UseQueryOptions<Awaited<ReturnType<typeof listObNotifications>>, TError, TData>> & Pick<
        UndefinedInitialDataOptions<
          Awaited<ReturnType<typeof listObNotifications>>,
          TError,
          Awaited<ReturnType<typeof listObNotifications>>
        > , 'initialData'
      >, }
 , queryClient?: QueryClient
  ):  UseQueryResult<TData, TError> & { queryKey: DataTag<QueryKey, TData, TError> }
export function useListObNotifications<TData = Awaited<ReturnType<typeof listObNotifications>>, TError = Problem | UnauthorizedResponse | ObModuleGatedResponse>(
 params?: ListObNotificationsParams, options?: { query?:Partial<UseQueryOptions<Awaited<ReturnType<typeof listObNotifications>>, TError, TData>>, }
 , queryClient?: QueryClient
  ):  UseQueryResult<TData, TError> & { queryKey: DataTag<QueryKey, TData, TError> }
/**
 * @summary Onboarding notification centre (OB-13)
 */

export function useListObNotifications<TData = Awaited<ReturnType<typeof listObNotifications>>, TError = Problem | UnauthorizedResponse | ObModuleGatedResponse>(
 params?: ListObNotificationsParams, options?: { query?:Partial<UseQueryOptions<Awaited<ReturnType<typeof listObNotifications>>, TError, TData>>, }
 , queryClient?: QueryClient 
 ):  UseQueryResult<TData, TError> & { queryKey: DataTag<QueryKey, TData, TError> } {

  const queryOptions = getListObNotificationsQueryOptions(params,options)

  const query = useQuery(queryOptions, queryClient) as  UseQueryResult<TData, TError> & { queryKey: DataTag<QueryKey, TData, TError> };

  query.queryKey = queryOptions.queryKey ;

  return query;
}




/**
 * Takes no `tab`, deliberately: a "mark all read" that left some unread is a lie the badge contradicts a second later.

 * @summary Mark every onboarding notification as read
 */
export const markAllObNotificationsRead = (
    
 ) => {
      
      
      return http<void>(
      {url: `/onboarding/notifications/read-all`, method: 'PATCH'
    },
      );
    }
  


export const getMarkAllObNotificationsReadMutationOptions = <TError = UnauthorizedResponse | ObModuleGatedResponse,
    TContext = unknown>(options?: { mutation?:UseMutationOptions<Awaited<ReturnType<typeof markAllObNotificationsRead>>, TError,void, TContext>, }
): UseMutationOptions<Awaited<ReturnType<typeof markAllObNotificationsRead>>, TError,void, TContext> => {

const mutationKey = ['markAllObNotificationsRead'];
const {mutation: mutationOptions} = options ?
      options.mutation && 'mutationKey' in options.mutation && options.mutation.mutationKey ?
      options
      : {...options, mutation: {...options.mutation, mutationKey}}
      : {mutation: { mutationKey, }};

      


      const mutationFn: MutationFunction<Awaited<ReturnType<typeof markAllObNotificationsRead>>, void> = () => {
          

          return  markAllObNotificationsRead()
        }

        


  return  { mutationFn, ...mutationOptions }}

    export type MarkAllObNotificationsReadMutationResult = NonNullable<Awaited<ReturnType<typeof markAllObNotificationsRead>>>
    
    export type MarkAllObNotificationsReadMutationError = UnauthorizedResponse | ObModuleGatedResponse

    /**
 * @summary Mark every onboarding notification as read
 */
export const useMarkAllObNotificationsRead = <TError = UnauthorizedResponse | ObModuleGatedResponse,
    TContext = unknown>(options?: { mutation?:UseMutationOptions<Awaited<ReturnType<typeof markAllObNotificationsRead>>, TError,void, TContext>, }
 , queryClient?: QueryClient): UseMutationResult<
        Awaited<ReturnType<typeof markAllObNotificationsRead>>,
        TError,
        void,
        TContext
      > => {

      const mutationOptions = getMarkAllObNotificationsReadMutationOptions(options);

      return useMutation(mutationOptions, queryClient);
    }
    /**
 * Already-read answers `204` alongside just-marked: the caller asked for a state and it holds either way. Re-reading does not restamp `read_at`, or "when did you see this" becomes "when did you last open the list".

 * @summary Mark one onboarding notification as read
 */
export const markObNotificationRead = (
    notificationId: number,
 ) => {
      
      
      return http<void>(
      {url: `/onboarding/notifications/${notificationId}/read`, method: 'PATCH'
    },
      );
    }
  


export const getMarkObNotificationReadMutationOptions = <TError = UnauthorizedResponse | ObModuleGatedResponse,
    TContext = unknown>(options?: { mutation?:UseMutationOptions<Awaited<ReturnType<typeof markObNotificationRead>>, TError,{notificationId: number}, TContext>, }
): UseMutationOptions<Awaited<ReturnType<typeof markObNotificationRead>>, TError,{notificationId: number}, TContext> => {

const mutationKey = ['markObNotificationRead'];
const {mutation: mutationOptions} = options ?
      options.mutation && 'mutationKey' in options.mutation && options.mutation.mutationKey ?
      options
      : {...options, mutation: {...options.mutation, mutationKey}}
      : {mutation: { mutationKey, }};

      


      const mutationFn: MutationFunction<Awaited<ReturnType<typeof markObNotificationRead>>, {notificationId: number}> = (props) => {
          const {notificationId} = props ?? {};

          return  markObNotificationRead(notificationId,)
        }

        


  return  { mutationFn, ...mutationOptions }}

    export type MarkObNotificationReadMutationResult = NonNullable<Awaited<ReturnType<typeof markObNotificationRead>>>
    
    export type MarkObNotificationReadMutationError = UnauthorizedResponse | ObModuleGatedResponse

    /**
 * @summary Mark one onboarding notification as read
 */
export const useMarkObNotificationRead = <TError = UnauthorizedResponse | ObModuleGatedResponse,
    TContext = unknown>(options?: { mutation?:UseMutationOptions<Awaited<ReturnType<typeof markObNotificationRead>>, TError,{notificationId: number}, TContext>, }
 , queryClient?: QueryClient): UseMutationResult<
        Awaited<ReturnType<typeof markObNotificationRead>>,
        TError,
        {notificationId: number},
        TContext
      > => {

      const mutationOptions = getMarkObNotificationReadMutationOptions(options);

      return useMutation(mutationOptions, queryClient);
    }
    