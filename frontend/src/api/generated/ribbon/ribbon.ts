/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * EduTrack API
 * The agreed contract between all four streams. Every endpoint in blueprint §13.

**This file is hand-authored and under review (D-001).** It is the design
contract, not yet the generated one. From D-003 onwards `springdoc` emits the
real spec from the Java DTOs — Bean Validation annotations are the single
source of truth for validation rules — and D-005's CI check fails the build
when the committed TypeScript client drifts from it. Until then this file is
what the frontend generates against and what the backend must implement.

## Conventions that apply to every operation

- **Prefix** — every path is served under `/api/v1`.
- **Envelope** — success responses are `{ data, meta? }`. Errors are
  `{ error }` carrying an RFC 9457 problem object. Nothing returns a bare
  array or a bare scalar; adding a field later must never be a breaking change.
- **Errors** — `application/problem+json`, RFC 9457. `type` is a stable URI
  the frontend may switch on; `detail` is for humans and may change.
- **Idempotency** — every POST that creates something accepts
  `Idempotency-Key`. Replaying a key within 24 h returns the original response
  rather than creating a second row.
- **ETag** — detail reads return `ETag`; send `If-None-Match` to get `304`,
  and `If-Match` on the corresponding `PATCH` to avoid a lost update.
- **Pagination** — `?cursor=&limit=`, never offset. Offset paging over a table
  that is being written to skips and repeats rows.

`contracts/CONVENTIONS.md` is the normative version (D-002), including the
endpoints that deliberately opt out of `If-Match` or pagination and why.

## Two rules the API shape itself enforces

**Row scoping returns 404, not 403.** Out-of-scope IDs are indistinguishable
from non-existent ones. A `403` on `/tickets/{id}` would confirm the ticket
exists, which is an existence leak. Scoping is applied server-side by
`ScopeResolver` on every ticket query — never by a frontend filter.

**History and effort logs are append-only.** `GET` and `POST` exist;
`PUT`, `PATCH` and `DELETE` deliberately do not, on `/history`,
`/effort-logs`, and stage transitions. This is not an oversight to be fixed
later — the routes must never be added. A correction is a new compensating
entry (`isCorrection`, `correctsEntryId`), like an accounting reversal, and
the database rejects mutation independently via triggers and grants.

 * OpenAPI spec version: 1.0.0-draft
 */
import {
  useMutation,
  useQuery
} from '@tanstack/react-query';
import type {
  DataTag,
  DefinedInitialDataOptions,
  DefinedUseQueryResult,
  MutationFunction,
  QueryClient,
  QueryFunction,
  QueryKey,
  UndefinedInitialDataOptions,
  UseMutationOptions,
  UseMutationResult,
  UseQueryOptions,
  UseQueryResult
} from '@tanstack/react-query';

import type {
  ForceMoveRequest,
  GetRibbonParams,
  GetStageQueueParams,
  HandoffRequest,
  NotFoundResponse,
  ReworkRequest,
  RibbonResponse,
  SkipStageBody,
  StageQueueResponse,
  UnauthorizedResponse,
  UnprocessableTransitionResponse,
  ValidationFailedResponse
} from '.././model';

import { http } from '../../http';




/**
 * One ribbon per cycle. `?cycle=1` renders that completed journey read-only.

 * @summary Ribbon segments for a cycle
 */
export const getRibbon = (
    ticketId: string,
    params?: GetRibbonParams,
 signal?: AbortSignal
) => {
      
      
      return http<RibbonResponse>(
      {url: `/tickets/${ticketId}/ribbon`, method: 'GET',
        params, signal
    },
      );
    }
  



export const getGetRibbonQueryKey = (ticketId?: string,
    params?: GetRibbonParams,) => {
    return [
    `/tickets/${ticketId}/ribbon`, ...(params ? [params]: [])
    ] as const;
    }

    
export const getGetRibbonQueryOptions = <TData = Awaited<ReturnType<typeof getRibbon>>, TError = NotFoundResponse>(ticketId: string,
    params?: GetRibbonParams, options?: { query?:Partial<UseQueryOptions<Awaited<ReturnType<typeof getRibbon>>, TError, TData>>, }
) => {

const {query: queryOptions} = options ?? {};

  const queryKey =  queryOptions?.queryKey ?? getGetRibbonQueryKey(ticketId,params);

  

    const queryFn: QueryFunction<Awaited<ReturnType<typeof getRibbon>>> = ({ signal }) => getRibbon(ticketId,params, signal);

      

      

   return  { queryKey, queryFn, enabled: !!(ticketId), ...queryOptions} as UseQueryOptions<Awaited<ReturnType<typeof getRibbon>>, TError, TData> & { queryKey: DataTag<QueryKey, TData, TError> }
}

export type GetRibbonQueryResult = NonNullable<Awaited<ReturnType<typeof getRibbon>>>
export type GetRibbonQueryError = NotFoundResponse


export function useGetRibbon<TData = Awaited<ReturnType<typeof getRibbon>>, TError = NotFoundResponse>(
 ticketId: string,
    params: undefined |  GetRibbonParams, options: { query:Partial<UseQueryOptions<Awaited<ReturnType<typeof getRibbon>>, TError, TData>> & Pick<
        DefinedInitialDataOptions<
          Awaited<ReturnType<typeof getRibbon>>,
          TError,
          Awaited<ReturnType<typeof getRibbon>>
        > , 'initialData'
      >, }
 , queryClient?: QueryClient
  ):  DefinedUseQueryResult<TData, TError> & { queryKey: DataTag<QueryKey, TData, TError> }
export function useGetRibbon<TData = Awaited<ReturnType<typeof getRibbon>>, TError = NotFoundResponse>(
 ticketId: string,
    params?: GetRibbonParams, options?: { query?:Partial<UseQueryOptions<Awaited<ReturnType<typeof getRibbon>>, TError, TData>> & Pick<
        UndefinedInitialDataOptions<
          Awaited<ReturnType<typeof getRibbon>>,
          TError,
          Awaited<ReturnType<typeof getRibbon>>
        > , 'initialData'
      >, }
 , queryClient?: QueryClient
  ):  UseQueryResult<TData, TError> & { queryKey: DataTag<QueryKey, TData, TError> }
export function useGetRibbon<TData = Awaited<ReturnType<typeof getRibbon>>, TError = NotFoundResponse>(
 ticketId: string,
    params?: GetRibbonParams, options?: { query?:Partial<UseQueryOptions<Awaited<ReturnType<typeof getRibbon>>, TError, TData>>, }
 , queryClient?: QueryClient
  ):  UseQueryResult<TData, TError> & { queryKey: DataTag<QueryKey, TData, TError> }
/**
 * @summary Ribbon segments for a cycle
 */

export function useGetRibbon<TData = Awaited<ReturnType<typeof getRibbon>>, TError = NotFoundResponse>(
 ticketId: string,
    params?: GetRibbonParams, options?: { query?:Partial<UseQueryOptions<Awaited<ReturnType<typeof getRibbon>>, TError, TData>>, }
 , queryClient?: QueryClient 
 ):  UseQueryResult<TData, TError> & { queryKey: DataTag<QueryKey, TData, TError> } {

  const queryOptions = getGetRibbonQueryOptions(ticketId,params,options)

  const query = useQuery(queryOptions, queryClient) as  UseQueryResult<TData, TError> & { queryKey: DataTag<QueryKey, TData, TError> };

  query.queryKey = queryOptions.queryKey ;

  return query;
}




/**
 * **Only the current stage owner — plus PM and Admin — may advance a
ticket.** A developer cannot push a ticket into Deployment while it sits
with QA. Enforced server-side; a client that offers the button anyway gets
`422`.

One transaction seals the current transition (`exitedAt`, `durationMins`
**in working minutes**, not wall-clock), inserts the next, notifies the
receiving owner, and pushes `stage.changed` to `/topic/ticket.{id}` so
anyone viewing sees it land.

`effortHours` is mandatory when the project requires effort confirmation
at handoff (governance decision G-1, default blocking). Without it the
per-resource roll-up is fiction within a month.

 * @summary Advance to the next stage (S-29)
 */
export const handoffTicket = (
    ticketId: string,
    handoffRequest: HandoffRequest,
 signal?: AbortSignal
) => {
      
      
      return http<RibbonResponse>(
      {url: `/tickets/${ticketId}/handoff`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: handoffRequest, signal
    },
      );
    }
  


export const getHandoffTicketMutationOptions = <TError = ValidationFailedResponse | NotFoundResponse | UnprocessableTransitionResponse,
    TContext = unknown>(options?: { mutation?:UseMutationOptions<Awaited<ReturnType<typeof handoffTicket>>, TError,{ticketId: string;data: HandoffRequest}, TContext>, }
): UseMutationOptions<Awaited<ReturnType<typeof handoffTicket>>, TError,{ticketId: string;data: HandoffRequest}, TContext> => {

const mutationKey = ['handoffTicket'];
const {mutation: mutationOptions} = options ?
      options.mutation && 'mutationKey' in options.mutation && options.mutation.mutationKey ?
      options
      : {...options, mutation: {...options.mutation, mutationKey}}
      : {mutation: { mutationKey, }};

      


      const mutationFn: MutationFunction<Awaited<ReturnType<typeof handoffTicket>>, {ticketId: string;data: HandoffRequest}> = (props) => {
          const {ticketId,data} = props ?? {};

          return  handoffTicket(ticketId,data,)
        }

        


  return  { mutationFn, ...mutationOptions }}

    export type HandoffTicketMutationResult = NonNullable<Awaited<ReturnType<typeof handoffTicket>>>
    export type HandoffTicketMutationBody = HandoffRequest
    export type HandoffTicketMutationError = ValidationFailedResponse | NotFoundResponse | UnprocessableTransitionResponse

    /**
 * @summary Advance to the next stage (S-29)
 */
export const useHandoffTicket = <TError = ValidationFailedResponse | NotFoundResponse | UnprocessableTransitionResponse,
    TContext = unknown>(options?: { mutation?:UseMutationOptions<Awaited<ReturnType<typeof handoffTicket>>, TError,{ticketId: string;data: HandoffRequest}, TContext>, }
 , queryClient?: QueryClient): UseMutationResult<
        Awaited<ReturnType<typeof handoffTicket>>,
        TError,
        {ticketId: string;data: HandoffRequest},
        TContext
      > => {

      const mutationOptions = getHandoffTicketMutationOptions(options);

      return useMutation(mutationOptions, queryClient);
    }
    /**
 * `reason` is mandatory; a defect list is expected on a QA failure.

**`iterationNo` increments for every subsequent transition in this
cycle** — that is what makes rework measurable and what the ping-pong flag
counts at `iterationNo >= 3`. It does **not** touch `cycleNo`, and by
default it does not move the planned close date (decision G-2): the
original commitment stands, and rework is what `iterationNo` measures.

 * @summary Send the ticket backwards
 */
export const reworkTicket = (
    ticketId: string,
    reworkRequest: ReworkRequest,
 signal?: AbortSignal
) => {
      
      
      return http<RibbonResponse>(
      {url: `/tickets/${ticketId}/rework`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: reworkRequest, signal
    },
      );
    }
  


export const getReworkTicketMutationOptions = <TError = NotFoundResponse | UnprocessableTransitionResponse,
    TContext = unknown>(options?: { mutation?:UseMutationOptions<Awaited<ReturnType<typeof reworkTicket>>, TError,{ticketId: string;data: ReworkRequest}, TContext>, }
): UseMutationOptions<Awaited<ReturnType<typeof reworkTicket>>, TError,{ticketId: string;data: ReworkRequest}, TContext> => {

const mutationKey = ['reworkTicket'];
const {mutation: mutationOptions} = options ?
      options.mutation && 'mutationKey' in options.mutation && options.mutation.mutationKey ?
      options
      : {...options, mutation: {...options.mutation, mutationKey}}
      : {mutation: { mutationKey, }};

      


      const mutationFn: MutationFunction<Awaited<ReturnType<typeof reworkTicket>>, {ticketId: string;data: ReworkRequest}> = (props) => {
          const {ticketId,data} = props ?? {};

          return  reworkTicket(ticketId,data,)
        }

        


  return  { mutationFn, ...mutationOptions }}

    export type ReworkTicketMutationResult = NonNullable<Awaited<ReturnType<typeof reworkTicket>>>
    export type ReworkTicketMutationBody = ReworkRequest
    export type ReworkTicketMutationError = NotFoundResponse | UnprocessableTransitionResponse

    /**
 * @summary Send the ticket backwards
 */
export const useReworkTicket = <TError = NotFoundResponse | UnprocessableTransitionResponse,
    TContext = unknown>(options?: { mutation?:UseMutationOptions<Awaited<ReturnType<typeof reworkTicket>>, TError,{ticketId: string;data: ReworkRequest}, TContext>, }
 , queryClient?: QueryClient): UseMutationResult<
        Awaited<ReturnType<typeof reworkTicket>>,
        TError,
        {ticketId: string;data: ReworkRequest},
        TContext
      > => {

      const mutationOptions = getReworkTicketMutationOptions(options);

      return useMutation(mutationOptions, queryClient);
    }
    /**
 * `reason` mandatory. The segment renders struck-through with the reason on
hover, rather than disappearing — a stage that silently vanishes from the
ribbon makes the history unreadable later.

 * @summary Skip an optional stage — PM and Admin only
 */
export const skipStage = (
    ticketId: string,
    skipStageBody: SkipStageBody,
 signal?: AbortSignal
) => {
      
      
      return http<RibbonResponse>(
      {url: `/tickets/${ticketId}/skip-stage`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: skipStageBody, signal
    },
      );
    }
  


export const getSkipStageMutationOptions = <TError = NotFoundResponse | UnprocessableTransitionResponse,
    TContext = unknown>(options?: { mutation?:UseMutationOptions<Awaited<ReturnType<typeof skipStage>>, TError,{ticketId: string;data: SkipStageBody}, TContext>, }
): UseMutationOptions<Awaited<ReturnType<typeof skipStage>>, TError,{ticketId: string;data: SkipStageBody}, TContext> => {

const mutationKey = ['skipStage'];
const {mutation: mutationOptions} = options ?
      options.mutation && 'mutationKey' in options.mutation && options.mutation.mutationKey ?
      options
      : {...options, mutation: {...options.mutation, mutationKey}}
      : {mutation: { mutationKey, }};

      


      const mutationFn: MutationFunction<Awaited<ReturnType<typeof skipStage>>, {ticketId: string;data: SkipStageBody}> = (props) => {
          const {ticketId,data} = props ?? {};

          return  skipStage(ticketId,data,)
        }

        


  return  { mutationFn, ...mutationOptions }}

    export type SkipStageMutationResult = NonNullable<Awaited<ReturnType<typeof skipStage>>>
    export type SkipStageMutationBody = SkipStageBody
    export type SkipStageMutationError = NotFoundResponse | UnprocessableTransitionResponse

    /**
 * @summary Skip an optional stage — PM and Admin only
 */
export const useSkipStage = <TError = NotFoundResponse | UnprocessableTransitionResponse,
    TContext = unknown>(options?: { mutation?:UseMutationOptions<Awaited<ReturnType<typeof skipStage>>, TError,{ticketId: string;data: SkipStageBody}, TContext>, }
 , queryClient?: QueryClient): UseMutationResult<
        Awaited<ReturnType<typeof skipStage>>,
        TError,
        {ticketId: string;data: SkipStageBody},
        TContext
      > => {

      const mutationOptions = getSkipStageMutationOptions(options);

      return useMutation(mutationOptions, queryClient);
    }
    /**
 * **Any stage to any stage** — forward, backward, or sideways — logged as
`OVERRIDE` rather than as whichever direction it happens to move. Unlike
`rework`/`skip`, there is no default destination: `toStageCode` is
always required.

Gated on `ticket.force_move` alone (Admin and PM hold it, nobody else
— blueprint §2's "Force-move ribbon backwards" row); the golden rule's
current-stage-owner check is not the point of this route; the
capability already is. `reason` is mandatory so the override is
self-explaining in the ribbon's history the way a rework's is.

 * @summary Force-move to any stage — PM and Admin only (C-048)
 */
export const forceMoveTicket = (
    ticketId: string,
    forceMoveRequest: ForceMoveRequest,
 signal?: AbortSignal
) => {
      
      
      return http<RibbonResponse>(
      {url: `/tickets/${ticketId}/force-move`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: forceMoveRequest, signal
    },
      );
    }
  


export const getForceMoveTicketMutationOptions = <TError = ValidationFailedResponse | NotFoundResponse | UnprocessableTransitionResponse,
    TContext = unknown>(options?: { mutation?:UseMutationOptions<Awaited<ReturnType<typeof forceMoveTicket>>, TError,{ticketId: string;data: ForceMoveRequest}, TContext>, }
): UseMutationOptions<Awaited<ReturnType<typeof forceMoveTicket>>, TError,{ticketId: string;data: ForceMoveRequest}, TContext> => {

const mutationKey = ['forceMoveTicket'];
const {mutation: mutationOptions} = options ?
      options.mutation && 'mutationKey' in options.mutation && options.mutation.mutationKey ?
      options
      : {...options, mutation: {...options.mutation, mutationKey}}
      : {mutation: { mutationKey, }};

      


      const mutationFn: MutationFunction<Awaited<ReturnType<typeof forceMoveTicket>>, {ticketId: string;data: ForceMoveRequest}> = (props) => {
          const {ticketId,data} = props ?? {};

          return  forceMoveTicket(ticketId,data,)
        }

        


  return  { mutationFn, ...mutationOptions }}

    export type ForceMoveTicketMutationResult = NonNullable<Awaited<ReturnType<typeof forceMoveTicket>>>
    export type ForceMoveTicketMutationBody = ForceMoveRequest
    export type ForceMoveTicketMutationError = ValidationFailedResponse | NotFoundResponse | UnprocessableTransitionResponse

    /**
 * @summary Force-move to any stage — PM and Admin only (C-048)
 */
export const useForceMoveTicket = <TError = ValidationFailedResponse | NotFoundResponse | UnprocessableTransitionResponse,
    TContext = unknown>(options?: { mutation?:UseMutationOptions<Awaited<ReturnType<typeof forceMoveTicket>>, TError,{ticketId: string;data: ForceMoveRequest}, TContext>, }
 , queryClient?: QueryClient): UseMutationResult<
        Awaited<ReturnType<typeof forceMoveTicket>>,
        TError,
        {ticketId: string;data: ForceMoveRequest},
        TContext
      > => {

      const mutationOptions = getForceMoveTicketMutationOptions(options);

      return useMutation(mutationOptions, queryClient);
    }
    /**
 * "Waiting in QA", "Waiting in Deployment" — the landing page for QA and
Deployment roles. Sorted by time-in-stage descending, so the ticket
rotting longest is first.

 * @summary Team inbox for a stage (S-31)
 */
export const getStageQueue = (
    params: GetStageQueueParams,
 signal?: AbortSignal
) => {
      
      
      return http<StageQueueResponse>(
      {url: `/stages/queue`, method: 'GET',
        params, signal
    },
      );
    }
  



export const getGetStageQueueQueryKey = (params?: GetStageQueueParams,) => {
    return [
    `/stages/queue`, ...(params ? [params]: [])
    ] as const;
    }

    
export const getGetStageQueueQueryOptions = <TData = Awaited<ReturnType<typeof getStageQueue>>, TError = UnauthorizedResponse>(params: GetStageQueueParams, options?: { query?:Partial<UseQueryOptions<Awaited<ReturnType<typeof getStageQueue>>, TError, TData>>, }
) => {

const {query: queryOptions} = options ?? {};

  const queryKey =  queryOptions?.queryKey ?? getGetStageQueueQueryKey(params);

  

    const queryFn: QueryFunction<Awaited<ReturnType<typeof getStageQueue>>> = ({ signal }) => getStageQueue(params, signal);

      

      

   return  { queryKey, queryFn, ...queryOptions} as UseQueryOptions<Awaited<ReturnType<typeof getStageQueue>>, TError, TData> & { queryKey: DataTag<QueryKey, TData, TError> }
}

export type GetStageQueueQueryResult = NonNullable<Awaited<ReturnType<typeof getStageQueue>>>
export type GetStageQueueQueryError = UnauthorizedResponse


export function useGetStageQueue<TData = Awaited<ReturnType<typeof getStageQueue>>, TError = UnauthorizedResponse>(
 params: GetStageQueueParams, options: { query:Partial<UseQueryOptions<Awaited<ReturnType<typeof getStageQueue>>, TError, TData>> & Pick<
        DefinedInitialDataOptions<
          Awaited<ReturnType<typeof getStageQueue>>,
          TError,
          Awaited<ReturnType<typeof getStageQueue>>
        > , 'initialData'
      >, }
 , queryClient?: QueryClient
  ):  DefinedUseQueryResult<TData, TError> & { queryKey: DataTag<QueryKey, TData, TError> }
export function useGetStageQueue<TData = Awaited<ReturnType<typeof getStageQueue>>, TError = UnauthorizedResponse>(
 params: GetStageQueueParams, options?: { query?:Partial<UseQueryOptions<Awaited<ReturnType<typeof getStageQueue>>, TError, TData>> & Pick<
        UndefinedInitialDataOptions<
          Awaited<ReturnType<typeof getStageQueue>>,
          TError,
          Awaited<ReturnType<typeof getStageQueue>>
        > , 'initialData'
      >, }
 , queryClient?: QueryClient
  ):  UseQueryResult<TData, TError> & { queryKey: DataTag<QueryKey, TData, TError> }
export function useGetStageQueue<TData = Awaited<ReturnType<typeof getStageQueue>>, TError = UnauthorizedResponse>(
 params: GetStageQueueParams, options?: { query?:Partial<UseQueryOptions<Awaited<ReturnType<typeof getStageQueue>>, TError, TData>>, }
 , queryClient?: QueryClient
  ):  UseQueryResult<TData, TError> & { queryKey: DataTag<QueryKey, TData, TError> }
/**
 * @summary Team inbox for a stage (S-31)
 */

export function useGetStageQueue<TData = Awaited<ReturnType<typeof getStageQueue>>, TError = UnauthorizedResponse>(
 params: GetStageQueueParams, options?: { query?:Partial<UseQueryOptions<Awaited<ReturnType<typeof getStageQueue>>, TError, TData>>, }
 , queryClient?: QueryClient 
 ):  UseQueryResult<TData, TError> & { queryKey: DataTag<QueryKey, TData, TError> } {

  const queryOptions = getGetStageQueueQueryOptions(params,options)

  const query = useQuery(queryOptions, queryClient) as  UseQueryResult<TData, TError> & { queryKey: DataTag<QueryKey, TData, TError> };

  query.queryKey = queryOptions.queryKey ;

  return query;
}




