/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * EduTrack API
 * The agreed contract between all four streams. Every endpoint in blueprint §13.

**This file is hand-authored and under review (D-001).** It is the design
contract, not yet the generated one. From D-003 onwards `springdoc` emits the
real spec from the Java DTOs — Bean Validation annotations are the single
source of truth for validation rules — and D-005's CI check fails the build
when the committed TypeScript client drifts from it. Until then this file is
what the frontend generates against and what the backend must implement.

## Conventions that apply to every operation

- **Prefix** — every path is served under `/api/v1`.
- **Envelope** — success responses are `{ data, meta? }`. Errors are
  `{ error }` carrying an RFC 9457 problem object. Nothing returns a bare
  array or a bare scalar; adding a field later must never be a breaking change.
- **Errors** — `application/problem+json`, RFC 9457. `type` is a stable URI
  the frontend may switch on; `detail` is for humans and may change.
- **Idempotency** — every POST that creates something accepts
  `Idempotency-Key`. Replaying a key within 24 h returns the original response
  rather than creating a second row.
- **ETag** — detail reads return `ETag`; send `If-None-Match` to get `304`,
  and `If-Match` on the corresponding `PATCH` to avoid a lost update.
- **Pagination** — `?cursor=&limit=`, never offset. Offset paging over a table
  that is being written to skips and repeats rows.

`contracts/CONVENTIONS.md` is the normative version (D-002), including the
endpoints that deliberately opt out of `If-Match` or pagination and why.

## Two rules the API shape itself enforces

**Row scoping returns 404, not 403.** Out-of-scope IDs are indistinguishable
from non-existent ones. A `403` on `/tickets/{id}` would confirm the ticket
exists, which is an existence leak. Scoping is applied server-side by
`ScopeResolver` on every ticket query — never by a frontend filter.

**History and effort logs are append-only.** `GET` and `POST` exist;
`PUT`, `PATCH` and `DELETE` deliberately do not, on `/history`,
`/effort-logs`, and stage transitions. This is not an oversight to be fixed
later — the routes must never be added. A correction is a new compensating
entry (`isCorrection`, `correctsEntryId`), like an accounting reversal, and
the database rejects mutation independently via triggers and grants.

 * OpenAPI spec version: 1.0.0-draft
 */
import {
  useMutation
} from '@tanstack/react-query';
import type {
  MutationFunction,
  QueryClient,
  UseMutationOptions,
  UseMutationResult
} from '@tanstack/react-query';

import type {
  BounceEmailWebhookBody,
  InboundEmailWebhookBody,
  UnauthorizedResponse
} from '.././model';

import { http } from '../../http';




/**
 * Unauthenticated but **signature-verified**; an unverified signature is a
`401`, never a silently-accepted comment. Quoted text is stripped before
the body becomes a comment. Email-to-ticket creation is phase 6.

 * @summary Inbound mail — reply becomes a comment
 */
export const inboundEmailWebhook = (
    inboundEmailWebhookBody: InboundEmailWebhookBody,
 signal?: AbortSignal
) => {
      
      
      return http<void>(
      {url: `/webhooks/email/inbound`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: inboundEmailWebhookBody, signal
    },
      );
    }
  


export const getInboundEmailWebhookMutationOptions = <TError = UnauthorizedResponse,
    TContext = unknown>(options?: { mutation?:UseMutationOptions<Awaited<ReturnType<typeof inboundEmailWebhook>>, TError,{data: InboundEmailWebhookBody}, TContext>, }
): UseMutationOptions<Awaited<ReturnType<typeof inboundEmailWebhook>>, TError,{data: InboundEmailWebhookBody}, TContext> => {

const mutationKey = ['inboundEmailWebhook'];
const {mutation: mutationOptions} = options ?
      options.mutation && 'mutationKey' in options.mutation && options.mutation.mutationKey ?
      options
      : {...options, mutation: {...options.mutation, mutationKey}}
      : {mutation: { mutationKey, }};

      


      const mutationFn: MutationFunction<Awaited<ReturnType<typeof inboundEmailWebhook>>, {data: InboundEmailWebhookBody}> = (props) => {
          const {data} = props ?? {};

          return  inboundEmailWebhook(data,)
        }

        


  return  { mutationFn, ...mutationOptions }}

    export type InboundEmailWebhookMutationResult = NonNullable<Awaited<ReturnType<typeof inboundEmailWebhook>>>
    export type InboundEmailWebhookMutationBody = InboundEmailWebhookBody
    export type InboundEmailWebhookMutationError = UnauthorizedResponse

    /**
 * @summary Inbound mail — reply becomes a comment
 */
export const useInboundEmailWebhook = <TError = UnauthorizedResponse,
    TContext = unknown>(options?: { mutation?:UseMutationOptions<Awaited<ReturnType<typeof inboundEmailWebhook>>, TError,{data: InboundEmailWebhookBody}, TContext>, }
 , queryClient?: QueryClient): UseMutationResult<
        Awaited<ReturnType<typeof inboundEmailWebhook>>,
        TError,
        {data: InboundEmailWebhookBody},
        TContext
      > => {

      const mutationOptions = getInboundEmailWebhookMutationOptions(options);

      return useMutation(mutationOptions, queryClient);
    }
    /**
 * Marks the address invalid and alerts Admin.
 * @summary Bounce and complaint notifications
 */
export const bounceEmailWebhook = (
    bounceEmailWebhookBody: BounceEmailWebhookBody,
 signal?: AbortSignal
) => {
      
      
      return http<void>(
      {url: `/webhooks/email/bounce`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: bounceEmailWebhookBody, signal
    },
      );
    }
  


export const getBounceEmailWebhookMutationOptions = <TError = UnauthorizedResponse,
    TContext = unknown>(options?: { mutation?:UseMutationOptions<Awaited<ReturnType<typeof bounceEmailWebhook>>, TError,{data: BounceEmailWebhookBody}, TContext>, }
): UseMutationOptions<Awaited<ReturnType<typeof bounceEmailWebhook>>, TError,{data: BounceEmailWebhookBody}, TContext> => {

const mutationKey = ['bounceEmailWebhook'];
const {mutation: mutationOptions} = options ?
      options.mutation && 'mutationKey' in options.mutation && options.mutation.mutationKey ?
      options
      : {...options, mutation: {...options.mutation, mutationKey}}
      : {mutation: { mutationKey, }};

      


      const mutationFn: MutationFunction<Awaited<ReturnType<typeof bounceEmailWebhook>>, {data: BounceEmailWebhookBody}> = (props) => {
          const {data} = props ?? {};

          return  bounceEmailWebhook(data,)
        }

        


  return  { mutationFn, ...mutationOptions }}

    export type BounceEmailWebhookMutationResult = NonNullable<Awaited<ReturnType<typeof bounceEmailWebhook>>>
    export type BounceEmailWebhookMutationBody = BounceEmailWebhookBody
    export type BounceEmailWebhookMutationError = UnauthorizedResponse

    /**
 * @summary Bounce and complaint notifications
 */
export const useBounceEmailWebhook = <TError = UnauthorizedResponse,
    TContext = unknown>(options?: { mutation?:UseMutationOptions<Awaited<ReturnType<typeof bounceEmailWebhook>>, TError,{data: BounceEmailWebhookBody}, TContext>, }
 , queryClient?: QueryClient): UseMutationResult<
        Awaited<ReturnType<typeof bounceEmailWebhook>>,
        TError,
        {data: BounceEmailWebhookBody},
        TContext
      > => {

      const mutationOptions = getBounceEmailWebhookMutationOptions(options);

      return useMutation(mutationOptions, queryClient);
    }
    